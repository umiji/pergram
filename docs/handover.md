# セッション引き継ぎ記録

**書けるのはオーケストレーターだけ。** 新しいセッションはここを最初に読む。
台帳（`docs/task-list-pergram.csv` と `docs/tasks/T-XXX.md`）が正であり、
この文書は**そこから読み取れない文脈**だけを持つ。

---

## 最終更新: 2026-09-12（T-072 の所要時間の実測と、組織改善の対策決定）

**このセッションは分析だけを行った。コード・規約・雛形は1文字も変えていない。**
作業ブランチ `org/process-weight` を切ってある（差分は自動生成の2ファイルのみ）。

### 発端

PO が T-072（waitlist を1行にまとめる）の所要時間を問題視した。
「ただ DB のインサートロジックを変えるだけなのに1時間」。
別の AI による分析が添えられていたが、**その見積り（25〜35ターン）は実測 750 と20倍ずれており、
そこから逆算した「TDD の儀式がオーバーヘッド」という結論は根拠を持たない。**

### 実測で確定したこと（推測ではない）

**1. 時間の正体 — 1往復 ≈ 18秒。ツールの実行時間は主因ではない**

| 量 | 実測 |
|---|---|
| T-072 の総消費 | 120.3M トークン / 台帳上の返答回数 750 |
| **担当5体の実 API 往復** | **339**（記録上610レコードは、1応答をテキストとツールの2件に分けて書いているため） |
| ツール呼び出し | 362（**うち Bash 265**） |
| **全362回のツール実行時間の合計** | **1,340秒＝22分** |
| **1往復あたり** | **約18秒**（うちツール実行約3秒、思考と生成が約15秒） |
| ツールが実時間に占める割合 | **16.5%（テスト①）/ 19.3%（実装②）** ← 途切れず走った2体で測定 |

**2. 往復の使われ方 — 「探して読む」が最大の塊**

| 用途 | 往復 | 換算時間 |
|---|---|---|
| **探す・読む・git確認**（grep 75 / cat・sed 25 / git 35 ほか） | **約140** | **約42分** |
| テスト実行 56回（実装①だけで27回） | 56 | 約17分（実行は5.5分、**思考が11分**） |
| 実際に書く（Edit 55 / Write 21 / 書き出し5） | 81 | 約24分 |

> **「探して読む」42分が、PO の言う「1時間」のほぼ全部である。**
> 原因は、担当エージェントが会話履歴を持たず、**毎回ゼロから同じコードベースを探し直す**こと。
> T-072 では9回起動したので、9回それをやった。

**3. 並列のツール呼び出しが一度も無い。** 1応答あたりのツール数の分布が全エージェントで
`{0回, 1回}` のみ。**2回以上がゼロ。** 独立した読み取りをまとめれば往復が直接減る。

**4. `Read` ツールの使用は5体で14回だけ。** 残りは `grep` と `cat` で探している。
**ファイルの場所を知らないまま始まっている証拠。**

**5. 直接通信の4経路が1本も使われていない。** T-072 の `## 連絡してよい相手` は「無し」。
全受け渡しが「ファイルに書く → 終了 → オーケストレーターが読む → 新しいエージェントを起こす
→ そいつが全部読み直す」で回った。テスト担当自身が「レビューとの直接のやりとりは無い。
タスクファイルに commit されていた記録を読んだだけ」と書き残している。

**6. タスクファイルが指示と議事録の兼用になっている。** `docs/tasks/T-072.md` は1,047行で、
**指示は先頭135行（13%）のみ。残り912行は決定ログ・作業ログ・証拠＝履歴。**
後から起きるエージェントほど読む量が増える。

**7. 中断待ちが長い。** テスト②・レビュー・実装①の実時間が各2,058分（約34時間）。
9日→10日にまたがって中断・再開されたためで、**作業時間ではなく待たされていた時間**である。

**8. 製品コード258行に対し、テスト1,399行・文書1,367行**（`git diff --stat a5f07e5 b0fe4a7`）。

### 実測で否定されたこと（次の担当が同じ結論へ戻らないために）

- ❌ **「TDD の儀式（Red-Green・決定ログ・証拠採取）が主因」** — 儀式は往復の少数派。
  加えて Red-Green は実際に空振りテストを1件検出しており、
  **既存テストの期待値を反転する判断と「テストを緩めて通す」禁止事項とを識別する唯一の手段**である
- ❌ **「差し戻しの2往復が無駄」** — D-1 / D-2 はどちらも実バグ（回答の消失・二重計上の復活）で、
  レビューとテストが**独立に同じ経路へ到達**して確定した
- ❌ **「設計エージェントが動いていないから遅い」（オーケストレーターが当初立てた仮説）** —
  T-072 の設計（1行にまとめて `id` を捨てる）は正しく、一度も覆っていない。
  **設計の不在は T-070→T-071→T-072 の3周（238M）を説明するが、T-072 単体の750ターンは説明しない。
  2つは別問題として扱うこと**
- ❌ **「変更範囲の拡張（S-4）がエージェントの再起動を強いる」** — T-072 の `worker/schema.sql` の件は、
  報告1往復のあと**同じエージェントのまま続行**している

### 次のセッションが最初にやること — 対策5本を折り込む

**PO 承認済み（2026-09-12）。折込先は pergram 先行**（`org-` は配布物だが、PO が承知のうえで先行適用を選択）。
🔒 **変更をパッチとして退避し、`docs/org-improvements.md` へ `適用範囲: 配布物 /
状態: 適用済み（このリポジトリ先行）` で記録すること。** 配布物が配り直された時点で消えるため、
消えたことに気づけて再適用できる形にしておく。過去に2ファイル分が黙って消えた事故がある。

| # | 対策 | 削減対象 | 直す場所 |
|---|---|---|---|
| **1** | **`## 参照すべき成果物` を必須にし、ファイルと行番号で入口を書く。**未記入なら割り当て前に検査で止める | 探す・読む **約140往復＝42分** | `.claude/templates/T-XXX.md` / `.claude/scripts/org-check.py` |
| **2** | **開発中は関連テストだけ実行。一式は完了報告の直前に1回** | テスト **56往復＝17分** | `.claude/agents/org-implementation.md` / `org-test.md` |
| **3** | **差し戻しは同じエージェントを `SendMessage` で再開する。枝番（`-b`）を付けて起こし直さない** | 起動9回 → 6〜7回。1回ぶん丸ごと（テスト②は21.8M / 133往復） | `.claude/skills/org-orchestrate/SKILL.md`「差し戻すとき」 |
| **4** | **`## 連絡してよい相手` を実際に書く**（レビュー⇄実装、テスト⇄実装） | オーケストレーター経由の往復 | `.claude/templates/T-XXX.md` / `org-orchestrate` |
| **5** | **独立した確認は1往復にまとめる**（並列のツール呼び出し） | 並列率 0% → 往復が直接減る | 担当エージェント定義4種 |

**5本すべてが「探す時間・往復数を削る」に向いており、レビューとテストの関門には一切触れない。**

### 次のセッションで検討すること（PO 指示）

**(a) この5本が開発品質にどう影響するか。**
オーケストレーターの現時点の見立てを置いておく（次セッションで検証すること）。

- 対策1・3・4・5 は**検証の量を1つも減らさない**ので、品質リスクはほぼ無いと見ている
- ⚠️ **対策2だけはリスクがある。** 関連テストだけ回すと、
  **変更が無関係のテストを壊したときに発見が遅れる。**
  緩和は「完了報告の直前に必ず一式を1回」だが、これは規約に明記しないと守られない
- ⚠️ **対策1の副作用**: 入口を書くのはオーケストレーターなので、
  **オーケストレーターの負荷が増える。** 組織全体のトークンの63%が既にオーケストレーターである
  （1.363B / 2.152B、うち「タスク外」が802.8M＝37%）。**ここが詰まると全体が詰まる**

**(b) 軽いモニタ手法を入れるべきか。オーケストレーターの意見を以下に書く。**

### 測定についてのオーケストレーターの意見（PO 依頼により記録）

**結論: 測定点を新しく足す必要はほぼ無い。足すべきは「既にある記録を読む集計スクリプト1本」である。**

**理由。今回の分析に使ったデータは、すべて既に存在していた。**
`~/.claude/projects/<プロジェクト>/<セッションID>/subagents/*.jsonl` に
**全担当エージェントの全ターンが、タイムスタンプ付きで残っている。**
計測のための追加ログは1行も要らなかった。**つまり実行時のオーバーヘッドはゼロにできる。**

**提案する集計スクリプト（`.claude/scripts/org-turns.py` 相当。タスク完了時に1回走らせる）**

| 出す数字 | なぜ要るか | 今回の値（T-072） |
|---|---|---|
| 担当別の API 往復数 | 時間はここに比例する（1往復≈18秒） | 339 |
| ツール分類別の回数（探す / 読む / テスト / 書く / git） | **最大の塊を特定できる** | 探す・読む140 |
| **探す・読む比率** | **最重要の1指標。**これが高いタスクは指示の入口が書けていない | **53%** |
| 並列のツール呼び出し率 | 0% なら往復がそのまま無駄になっている | **0%** |
| テスト一式の実行回数 | 回数がそのまま往復 | 56（実装①で27） |
| 起動回数（冷えた起動 / 再開の別） | 冷えた起動1回＝探し直し1回ぶん | 9回・再開は1体のみ |
| **同じファイルを何回読んだか（再読率）** | **今回測っていない。安く測れて、対策1の効果が直接出る** | 未測定 |

**新しく測るべき点は1つだけ、「再読率」である。** 既存の記録から計算できるので、
**これも実行時の負荷はゼロ。**

**測らなくてよいもの（測ると重くなる割に判断が変わらない）**

- モデルの思考時間と生成時間の分離 — 分けても打てる手が変わらない。往復数を見れば足りる
- トークンの細かい内訳 — 既に `org-tokens.py` がある。**これ以上は要らない**
- 実時間（wall-clock） — **中断待ちが混ざって使い物にならない**（今回34時間と出た）。
  **往復数のほうが正確である**

**測れなかった / 測るべきだった点（次に精度を上げるならここ）**

1. 🔴 **オーケストレーター自身の140往復を分解していない。**
   組織全体の63%・タスク外37%を占めるのはオーケストレーターであり、**最大の盲点である。**
   メインセッションの記録は同じ場所にあるので、同じスクリプトで読める
2. **どの往復が手戻りによるものか**をタグ付けできていない。差し戻しの前後で分ければ
   「手戻りの真の費用」が出る
3. **待ち時間（中断してから再開されるまで）の内訳** — PO 待ちか、オーケストレーター待ちか

**やってはいけないこと: 実行時に何かを記録させる仕掛けを足すこと。**
今回の分析は事後の集計だけで足りた。**測定で重くするのは本末転倒であり、
すべて事後集計で賄える。**

### ⚠️ 対策1の訂正 — 軸は「規模」ではなく「取り返しのつかなさ」

**上の対策表で「2ファイル以上なら設計を挟む」と書いたが、これは取り消す。**
確立した実務（一方通行の扉 / ADR / リスクベースドテスト / Tidy First? など）は、
**どれも行数やファイル数を軸にしていない。共通する軸は「戻せるか」と「壊れたとき何が巻き込まれるか」である。**

- CSS のクラス名を10ファイルで一括改名 → 10ファイルだが `git revert` 1回で完全に戻る。**軽くてよい**
- T-072 は3ファイル → だが**保存されたデータの形が変わり、本番に入ったら戻せない**
  （既存の重複行を統合する SQL は「安全に書けない」と判断されている）

**このリポジトリには既に正しい軸がある。`CLAUDE.md` の「不可逆な設計判断 🔒」である。**
新しい軸を発明せず、**既にある 🔒 に工程を紐づける。**

| 区分 | 判定（二値） | 要件書 | 設計成果物 | テスト | レビュー |
|---|---|---|---|---|---|
| **A 戻せる** | 振る舞いを変えない／`git revert` で完全に戻る（文言・CSS・コメント・整理） | 不要 | **不要** | 既存の回帰のみ | 差分だけ |
| **B 戻せるが影響が広い** | 振る舞いは変わるが、保存データ・公開URL・外部I/F に触れない | 不要 | **不要**（決定ログのみ） | 受け入れテスト | 通常 |
| **C 🔒 戻せない** | **保存するデータの形／URL／外部I/F／法令表示**に触れる | **要る** | **要る** | 受け入れ＋敵対的経路 | 通常＋**実装前に PO が実例を1件見る** |

**C は73タスク中おそらく1割未満。設計成果物を毎回作ることにはならない。**
区分は雛形のメタデータへ1行（既存の `- TDD適用可否:` と同じ場所）。

### 設計成果物について分かったこと（次セッションで必ず読むこと）

**設計成果物は `docs/tasks/T-XXX.md` とは別のファイルである。実在する。**

| タスク | 作られたもの | 規模 |
|---|---|---|
| T-031 | `docs/Marketing/X_growth_design.md` | 914行 |
| T-044 | `docs/Marketing/X_algorithm_findings.md`（新規）＋ 上を17節ぶん改訂 | — |
| T-061 | `docs/design/design.md` §4「要望の帯の組版」 | 約725行（854〜1579行） |
| T-053 | **独立した設計文書を作らず、コミットだけを成果物にしている** | — |

| | `docs/tasks/T-XXX.md` | 設計成果物 |
|---|---|---|
| 中身 | **何をやるか**（指示）＋**履歴**（決定ログ・作業ログ・証拠） | **どう作るか**（方式・却下案・注意点） |
| 寿命 | そのタスク限り（決定ログだけ恒久） | **恒久。タスクをまたいで読まれる** |

🔴 **置き場所がどこにも定められていない（仕組みの欠陥）。**
`org-design` は「置き場所とファイル名はこの組織の決まりに従う」と書き、
決まりの側（雛形・`org-task-ledger.md`）は「設計成果物のパスを書け」としか言っていない。**循環している。**
結果、`docs/design/` と `docs/Marketing/` にバラバラに生え、T-053 は設計文書を作っていない。
**ただし書式まで固めないこと** —— Google の実務は「形式は厳密に決めない。
プロジェクトごとに最も意味のある形で書く」としている（`Software Engineering at Google` ch.10）。

**データ・保存・API の領域には設計成果物が1本も無い。** T-070 / T-071 / T-072 がその領域である。

### 「参照すべき成果物」をどう埋めるか — 現状は仕組みが無い

`org-orchestrate` スキル全文（386行）に **`grep` / `検索` / `探す` / `入口` という語が1つも無い。**
存在する手順は**過去の決定を引くもの（`org-decisions.py` → `docs/decisions-index.md`）だけ**で、
**コードの入口を探す手順は存在しない。** 雛形も「既存コード」とだけ書いて見つけ方を書いていない。
**オーケストレーターは自分が既に知っていることしか書けず、セッション開始時はコードを知らない。63%が空なのは当然である。**

**この組織は Definition of Done（`## 完了条件`）は持ち、検査もされている**
（`org-check.py` が担当を付ける前に 完了条件 / 判断してよい範囲 / 変更範囲 / 禁止事項 の4項目を見る）。
🔴 **`## 参照すべき成果物` は検査対象に入っていない。Definition of Ready の側が緩い。**
**これが「探して読む140往復」の制度的な原因である。**

**埋め方は、変更の区分で分ける（PO 合意、2026-09-12）:**

| 区分 | 入口を誰が書くか |
|---|---|
| A 戻せる | オーケストレーターが手で（現状のまま） |
| B 影響が広い | **読み取り専用の探索エージェントに1回探させて貼る**（設計成果物は作らない。安い） |
| C 🔒 戻せない | **設計エージェント。設計成果物が残り、次のタスクの探索が消える** |

⚠️ **担当エージェントは自分でエージェントを起動できない**（道具に `Agent` が無い）。
**起動できるのはメインセッションだけなので、この判断はオーケストレーターが持つ。**
⚠️ **行番号だけを書かない。**日をまたぐとずれる。**ファイルパス＋関数名／記号名＋なぜそこを見るのか**を書かせる
（T-061 がその形になっている）。

### 進行中: 確立した実務の調査（2026-09-12 に投げた）

PO 指示で、工程の切り分け基準を一次情報から取る調査を走らせた。
**結果は `docs/research/dev-process-sizing.md`**（調査エージェントが作成・コミットする）。
**次セッションは折込の前にこれを読むこと。**上の A/B/C 区分は暫定であり、調査結果で補正する。

調べている5点:
1. Google の設計文書 —「大きなプロジェクト」の線引きは実際に何で決まるか
2. リスクベースドテスト（ISO/IEC/IEEE 29119）— テストの深さを何で決めるか
3. Kent Beck『Tidy First?』— 振る舞いを変えない整理と変える変更の分け方
4. ADR（Nygard）—「アーキテクチャ的に重要」の判定基準
5. Definition of Ready / Done と、一方通行の扉 / 両開きの扉（Bezos 1997・2015）

**この会話で調べ続けなかった理由: セッションが長くなり、1往復の単価が $10 を超えたため**
（$79 → $93 が検索1回）。**調べ物は、コンテキストを分けた担当へ出すこと。**
これは今回の分析結論（長い会話はキャッシュ読出が膨らむ）の実例でもある。

### この文書にしか無い文脈

- **T-070 → T-071 → T-072 は「同じ機能の3周」で計238M**（組織の生涯消費 2.152B の11%）。
  T-058 を含めれば272M。**2回とも差し戻したのは PO で、レビューとテストは正規に通過している。**
  **保存されるデータの形を PO が実装前に見る関門が、工程のどこにも無い。**
  ⚠️ **これは T-072 の所要時間とは別問題である。混ぜて対策を立てないこと**
- **`手戻り回数` はこの238M を1回も数えていない。** PO 差し戻しは新しいタスクとして起票されるため。
  **指標が「工程内の往復」しか見ておらず、最も高くつく「丸ごとやり直し」を見ていない**
- **要件書が実在する領域でだけ、設計エージェントが動いている。**
  73タスク中、設計が動いたのは7件（T-031/032/044/046/053/057/061）で、
  全部が見た目（`docs/design/design.md`）か X運用（`docs/Marketing/X_kgi_2weeks.md`）。
  **データ・保存・API の領域には要件書が無く、設計エージェントは一度も動いていない**
- **`docs/product/requirements.md` は事実上死んでいる。** 更新は2コミット（片方は `init実行`）で、
  73タスク中4件しか参照していない
- **初回の組織点検を実施した**（改善エージェント。完了31件で初回）。診断は3件に絞られている ——
  課題1＝既存 I-008 へ追記（PO に実例を見せる関門）/ 課題2＝新規（軽量化の条件）/
  課題3＝既存 I-003 の適用（測定台）。**`docs/org-improvements.md` へはまだ起票していない**
- **未起票の `## 組織への指摘` が 20〜21タスク・約78件ある**（種別内訳: 規約46 / 道具33 / 反復14 / ロール2）。
  改善エージェントが7つの束にまとめる案を出している。**約半数が「規約に従うと作業が進まない」という申告**
- **Q-011 が2026-09-08 から未回答のまま。** 今回 PO が提起した論点と同一で、
  組織側は推奨案 C（やり直しの回数そのものを減らす）まで添えて上げていた。**次セッションで閉じること**
- **`I-001`（完了条件が二値にならない）は適用済みだが効かなかった。** 起票後に完了した5タスクから約8件再発。
  **うち7件が見た目・配信の実態についてで、真の解決先は雛形（段2）ではなく測定台（段4）である可能性が高い**
- **PO が提案した形が、オーケストレーターの案より良かった点**: 見た目の確認について、
  オーケストレーターは「スキル（手順書）を1本作る」と提案したが、
  **それだと毎回スクリプトを書くことになる。** PO の案は
  「**実行できる道具をリポジトリに常駐させ、何を確認するかはタスクの .md に書く**」。
  こちらを採る。**4つの落とし穴（旧 headless 指定 / `document.fonts.ready` 待ち /
  `file://` では CSS が当たらない / 端末エミュレーション）を道具の中に封じ込める**

---


## 最終更新: 2026-09-08（要望導線のデザイン・DB・デプロイ）

### このセッションで終わったこと

| タスク | 内容 | 状態 |
|---|---|---|
| **T-061** | 要望の帯を2列で組み直し、CTA を「正式版のリリースを応援する」へ | **完了。** 最終の構図は **PO 自身が直した**（右列はボタンのみ・垂直中央） |
| **T-062** | 押下が D1 に残らない欠陥（送信失敗後に永久に沈黙する） | **完了・レビュー通過**（Blocker 0 / Major 0 / Minor 3） |

**`main` へマージし、push 済み（`155a277`）。100コミット。**
**Cloudflare Workers Builds が `main` への push で自動デプロイする**（`docs/ops/deploy.md` §4）。
**つまり `main` へ入れた時点で本番に出る。** このセッションで2回出した。

### 次のセッションが最初にやること

1. **T-070 に着手する（PO が次セッションでやると決めた。⚠️ 起票時は T-063 と重複していたため採番し直した）。** メールアドレスを入れなかった人の
   アンケート回答が捨てられている。**新しいテーブルを1つ足す方針まで決定ログに書いてある。**
   ⚠️ **本番の移行は PO しか流せず、かつテーブルを作る前にコードが `main` へ入ると書き込みが全部失敗する。**
2. **T-062 が本番で効いたかを PO に確認する。** デプロイ済み。確認用の SELECT は
   `docs/tasks/T-062.md` の `## 証拠` の冒頭にある。**行が1増えれば直っている**
3. **T-049 を片付ける。** `tests/hero_ranking.test.js` の2件が **`main` で赤のまま**。
   ヒーロー3件の手動指定が毎朝の価格更新に追随していない。**このセッションが壊したものではない**

### この文書にしか無い文脈

- **同じ帯で PO 差し戻しが3回起きた**（T-053 / T-057 / T-061）。3回目の原因は
  **オーケストレーターが書いた完了条件が、2列という構図を構造的に排除していた**こと。
  「軸が1本」を4要素の左端の一致と定義したため、設計が1列へ倒れ、丸ごとやり直しになった。
  **完了条件は、あり得る解を意図せず排除していないかを見てから出す**
- **PO 指示でデザインのベンチマークを取った** → `docs/design/benchmark-cta-band.md`。
  実在製品11例＋公開デザインシステムの実測。**次に見た目を決めるときは、まずこれを読む**
- **PO が自分でコードを直した。** その結果 🔒 コメント3箇所が実装と正反対になっていたので
  書き換えてある（「注記をボタンから離さない」→ 注記は左列へ移った）。**右列へ戻さないこと**
- **`request.lede` の β と 版 の間は U+00A0（分割禁止の空白）。**
  通常の空白へ直すと 320px で「版です。」が孤立行になる（実測済み）
- **セッションコストが $96 に達した。** 主因は帯の実測（28通り）を設計1回目・2回目で2度回したこと。
  **測定台がセッションの一時領域にしか無く、毎回書き直している**（I-003 / 設計・実装の双方から指摘）

---

## 最終更新: 2026-09-02（2セッション目）

### このセッションで終わったこと

| タスク | 内容 | 状態 |
|---|---|---|
| **T-006** | クロール許可と LLMO 基盤 | **完了。** 本番の3ファイルを curl で実測して証拠を埋めた |
| **T-011** | 待機リストフォームの2段階化 | **完了・レビュー通過**（Major 1件を修正後） |
| **T-015** | en の「配信停止できる」という約束を外す | **完了** |
| **T-016** | 古くなった運用・設計ドキュメントの是正 | **完了。** 食い違い4件を修正 |
| **T-017** | ボタンのコントラスト比 | **完了。**⚠️ **コードレビューは省いた**（理由は決定ログ） |
| **T-019** | T-009/T-010 のレビュー Minor 5件 | **完了。**⚠️ **コードレビュー未実施**（PO 判断） |
| **T-020** | ヒーローの CTA をβ版1本に | **完了。** 事後レビューを実施し、Blocker は Q-005 で解消 |

**`npm test` 332件 全通過 / fail 0 / skip 0。`npm run build -- --strict` 終了コード 0。**

### 次のセッションが最初にやること

**1. 台帳検査を走らせ、停滞と食い違いを見る。**

```
python .claude/scripts/org-check.py       # python3 は無い
python .claude/scripts/org-decisions.py   # 決定ログ索引
```

**2. PO の作業待ちが2件ある。着手前に状況を聞くこと。**

| | 内容 |
|---|---|
| **T-021 / Q-004** | **Cloudflare ダッシュボードで AI クローラのブロックを解除する。** 方針は A案で確定済み（ブロック解除、`Content-Signal: ai-train=no` は残す）。**操作後、オーケストレーターが `curl -sL https://pergram.site/robots.txt` で実測して閉じる** |
| **T-003 / T-004 / T-005** | 保留。**PO が「後でデータを渡す」と回答した。** 何が足りないかは各タスクの `## ブロッカー` にある |

**3. 優先度の高い未着手は T-026 → T-023 → T-024 の順。**

- **T-026**（高）`/api/waitlist` にレート制限が無い。**待機リストの回答は P1 の成分選定に直結するため、汚されると次に作るものを誤る**
- **T-023**（高）製品一覧の `--verified` を使う4箇所が AA 未達。**「1gあたり最安」の印と順位の数字が 3.56:1**
- **T-024**（中）「Waitlist」「登録」の言い換えがヘッダだけで止まっている

**4. `src/styles/` を触るタスクが3つある（T-018 / T-022 / T-023）。同時に走らせないこと。**

### 台帳から読み取れない文脈

**🔒 本番の robots.txt は、リポジトリの出力だけでは決まらない。**
Cloudflare が管理する断片が**前置**されており、GPTBot / ClaudeBot 等を `Disallow: /` で
全面ブロックしている。**ビルドやデプロイをやり直しても消えない。**
`dist/robots.txt` を見ても本番の実態は分からない（T-021）。

**🔒 独自ドメインは `pergram.site`。2026-08-24 から本番稼働している。**
`pergram.pergram-official.workers.dev` は 301 で転送される。
**curl するときは `-L` が要る**（付けないと 301 しか返らない）。

**🔒 「CTA の主従」はヒーローとヘッダで状態が違う。**
T-010 の決定（待機リストが主）は**ヘッダでのみ生きている**。T-020 が**ヒーローに限って**
上書きし、待機リストのボタンと注記を要素ごと出力しなくなった。
**「LP の CTA は待機リストが主」と一文で書くと、必ずどちらかと食い違う。**

**🔒 `hero_waitlist` の GA4 イベントは T-020 以降ゼロ。不具合ではない。**

**🔒 ヒーローの「最安ランキング」には比較基準が無い。PO が承知のうえで選んだ**（Q-005 / C案）。
**景表法の合理的根拠の資料は未整備**である。T-020 の決定ログに、残るリスク5点を書いた。
**次にこの文言を触る担当は、それを読んでから判断すること。**

**🔒 `role="status"` を `.waitlist__done`（器）へ戻さないこと。**
器に付けると、中のフォーム一式（見出し・チェックボックス16個・入力欄・送信ボタン）が
**通知として読み上げられる。** `focus()` も器に当てると中身をまるごと読む（T-011 / R-011-1）。

**🔒 `--signal` と `--verified` は敢えて同値にしていない。**
明度で「操作の橙」と「データ強調の橙」を読み分けさせる設計である。
**「同じ色なら1つにまとめられる」と読んで統合しないこと**（T-017）。

**🔒 規約に色の16進値を書かない。** 値の出所は `src/styles/tokens.css` ひとつ。
2026-09-02 まで `.claude/rules/pergram-ui-copy.md` は `--signal` を `#2454E6`（青）と
書いていたが、**実装は橙系**で、いつからか分からないまま食い違っていた。

**🔒 待機リストへの POST は、1人の登録につき2回になった**（ステップ1とステップ2）。
**レート制限の閾値を1回想定で置くと、登録数だけ伸びて回答がゼロという壊れ方をする**（T-026）。

**T-011 の「ステップ2到達率」は構造上つねに約100%になる。**
`waitlist_step2_view` を POST 成功直後に無条件で送るため。
**T-011 の `## 停止条件` 2つ目は、この実装では発火しえない**（R-011-8 → T-007 へ回付）。

**GA4 のカスタムディメンション4つ**（`selected_nutrients` / `purchase_channel` /
`has_nutrients_other` / `has_requests`）の**紐付け先が `waitlist_submit` →
`waitlist_step2_submit` へ移った。** T-007 で反映が要る。

### 実行環境の注意（毎回ここで詰まる）

- **`python3` は無い。`python`（3.12.7）**
- **`npm run build --strict` は効かない。`npm run build -- --strict`**（`--` が要る）
- **`npm run prices:refresh` はツールの実行許可で拒否される**
- **`git reset --hard` / `git clean -f` / `git restore` / `git checkout --` / `git push --force` は拒否される**
- **Python スクリプトは Write ツールでファイルへ書いてから `python <path>` で実行する。**
  bash の heredoc も `python -c "..."` も、日本語やバックティックを含むと壊れる
- **`Agent` ツールに `name` パラメータが無い。** `CLAUDE.md` が定めるエージェント間の
  直接通信（役割名＋タスク番号を宛先にする）は**この実行環境では構造的に使えない。**
  全タスクを `連絡してよい相手: 無し` で回した。**代わりに、終了したエージェントへ
  `SendMessage` で追加指示を送ると、文脈を保ったまま再開できる**（T-016 / T-017 / T-011 で使った。
  新しいエージェントを起こすより安い）
- **ブラウザでの目視確認**（→ 組織改善 I-003。スキル化の候補）:
  - `--window-size` だけでは **meta viewport が無視され、誤った観測になる。**
    Chrome を `--remote-debugging-port` で起動し、Node 22 標準の `WebSocket` から
    `Emulation.setDeviceMetricsOverride`（`mobile:true`）を叩く
  - **`dist/` を `file://` で開かない。** CSS が絶対パスのため当たらず、
    **エラーにならないままレイアウトの観測が誤る**。ローカル HTTP で配信する
  - ホバーは `CSS.forcePseudoState`

### ゴール健全性（2026-09-02 時点）

**判定: 前進している。ただし残作業は増えた。**

- 完了 12件 / 保留 4件 / 未着手 8件 / 合計 26件
- **このセッションで7件を完了させ、6件を新規起票した**（T-021 〜 T-026）
- **新規起票はすべて「実際に動かして初めて見つかったもの」である** ——
  本番の robots.txt の実測、コントラストの実測、レビューが見つけた既存事象。
  **分解の誤りによる増加ではない**ため、「ゴールが揺らいでいる」とは判定しない
- **手戻り 1件**（T-011 の Major 1件）。低い

### コスト

**このセッション 約 $94 / エージェント9体。** 前セッションは $69 / 9体。

**最も高くついたのは T-011**（実装 238k → テスト 148k → レビュー 171k トークン）。
**1タスクを長く1つのエージェントで回すと、キャッシュ読出が積み上がる。**
軽いタスク（文言・記録の整合）は実装1体で回せる。

**終了したエージェントへ `SendMessage` で追い指示を出す**のは、
新しく起こすより明確に安い。T-016 の追加1行の修正は、この方法で片付けた。

### 組織改善（`docs/org-improvements.md` を新規作成）

担当エージェント6体の指摘を吸い上げて4件起票した。**2件は再発している。**

- **I-001 完了条件が二値で判定できない形で書かれる（4件で発生）。段2以上が要る**
- **I-002 上流の完了で下流の指示欄が失効するが、伝える手段が起動メッセージしかない**
- **I-003 ブラウザ目視の手順が毎回書き捨て（セッションをまたいで再発）。段4（スキル）**
- I-004 範囲外テストの回避に、判定側の独立検証費用が付く

---

## 過去のセッション

### 2026-09-02（1セッション目）

T-009（ヒーローの掲載3件を実順位17/19/20位 → 4/5/8位へ差し替え）、T-010（CTA 導線を
待機リストへ）、T-014（価格の日次更新の復旧。楽天 API 403 は Referer の不一致だった）、
T-020（ヒーローの CTA をβ版1本に）を完了。T-011 の受け入れテスト24件を作成。

---

## 最終更新: 2026-09-03（3セッション目）

### このセッションのゴール

**「X 向けに pergram アカウントの投稿を AI で量産して KGI を必達させる」ために、
現状の `.media-agent` 機構の課題を抽出し、対策をタスクへ落とし込む。**
PO の指示により **アウトプットは課題抽出と対策提案まで。実装は行わない。**

### ゴール達成条件（二値）

1. `docs/Marketing/X_growth_design.md` が存在し、Day2〜Day14 の日次運用台本・
   リプライ文面テンプレ5本以上・表現の技術・型7本の取捨・Day3/Day7 の分岐を含む
2. `docs/Marketing/media-agent-gap-analysis.md` が存在し、機構の課題が
   「事象 / 根拠 / KGI への影響 / 深刻度」の形で一覧化され、対策と最小構成が付いている
3. 2つの成果物から導かれた対策が、`docs/task-list-pergram.csv` と `docs/tasks/T-XXX.md` へ
   登録されている（指示欄4つが埋まっていること）
4. `python .claude/scripts/org-check.py` の新規警告が 0 件

### PO 確認済みの前提（2026-09-03）

| # | 事項 | 回答 |
|---|---|---|
| 1 | 手作業に割ける時間 | **1日60〜90分を確保できる。** T1（手動リプ 8〜12件）の前提は維持 |
| 2 | 14日間の起点 | **Day1 = 2026-09-03。** Day7ゲートは 9/9、終端は 9/16 |
| 3 | 今回のアウトプット | **課題抽出と対策提案まで。実装しない** |

**2 の帰結が重い。** T-027（型別の流入計測）/ T-028（プロフィール・固定ポスト）/
T-029（表示名）が未着手のまま Day1 が始まっている。**Day1〜3 は測れないまま走る。**

### 完了（このセッション）

| タスク | 成果物 |
|---|---|
| **T-031** | `docs/Marketing/X_growth_design.md`（914行）。投稿設計・日次運用・リプ型8本・表現の技術・型の取捨・Day3/7/10 の分岐 |
| **T-032** | `docs/Marketing/media-agent-gap-analysis.md`（467行）。課題20件（Blocker 7）・対策20件・Day1〜3 の最小構成 |

**対策は T-033〜T-043 として台帳へ登録済み（11件、すべて未着手）。**
`org-check.py` の警告は 15件で、セッション開始時と同数 = **新規の警告 0**。

### 次のセッションが最初にやること

1. **Q-006 / Q-007 / Q-008 の回答を得る**（`docs/po-queue.md`）。T-038 / T-039 が待っている
2. **T-033 を最優先で着手する。** 薬機法・景表法の機械的な砦がゼロで、修正は15分
3. T-035（投稿台帳）→ T-036（検査）→ T-037（登録）の順。**T-034 は T-033 の後**（同じ `.media-agent/` を触る）
4. **並列に走れる組**: T-035 / T-039 / T-040 / T-041 は互いに独立

### 注意

- **T-043 は 2026-09-16 まで着手しない。** 変更先が `media-agent` 本体でリポジトリ外にある
- `org-check.py` の `section()` は `##` 節の中に `###` 小見出しがあると本文を空と判定する。
  **T-031 の `## 証拠` がこれで誤検知された**（導入文を1行足して解消）

### 着手前に判明していた事実（両タスクの申し送りへ転記済み）

- **手書き本文を投稿候補として登録する口が無い。** 9/3 の下書きは `BufferConnector` を
  直接呼ぶスクリプトで入れており、**PolicyEngine（承認キュー・1日2本の上限・重複判定）を
  通っていない**
- **Buffer はツリーを作れない・投稿を削除／更新できない・スロット表を API から変えられない**
- **実測の自前投稿は 17imp / 16imp。** バズる以前に、アカウントが認識されていない
- **既存資料の数字に誤りがある。** 含有率は「60%〜90%・1.5倍」ではなく **33%〜93.6%・2.8倍**

---

## 追記: 2026-09-04（3セッション目の終わりに）

### このセッションの終わり方

PO の判断で **B案 = ここで閉じ、次セッションを T-044 から始める**。
理由: 露出モデルの改訂が効くのは Day2 以降であり、9/3 の投稿2本は Buffer 登録済みで動かせない。
加えて、このセッションはコストが $53 を超えており、**コンテキストが軽い状態で調査を走らせたほうが安い**。

### 次のセッションが最初にやること

**T-044 から始める。** それ以外に着手しない。

`docs/tasks/T-044.md` の `## 申し送り` に、オーケストレーターが速報で集めた二次情報
（エンゲージメントの重み・初速・低フォロワー高インプの実例）と、
**最も重い疑い（T-031 の露出層リプの設計が逆を向いている可能性）**が書いてある。

### このセッションで起きた、記録しておくべき失敗

**1. 調べずに「できない」と断定した。**
T-032 は `connectors/buffer.py` を読んで「Buffer はツリーも画像も作れない」と Blocker 判定した。
**実際は Buffer の API が両方に対応しており、こちらのクライアントが送っていなかっただけだった。**
PO の「API Docs 見たか」という一言で崩れた。訂正は media-agent-gap-analysis.md の「## 訂正」節。
**クライアント実装に無いことは、API に無いことの証明にならない。**

**2. 担当エージェントの `## 組織への指摘` を、タスクに変換しなかった。**
T-031 は「X の実データを参照する手段が無く、伝聞の2件だけで露出モデルを組んだ」と自己申告していた。
オーケストレーターはこれを「道具の不足」として受け取っただけで、**調査タスクを立てなかった。**
PO の指摘（「一般的なリサーチだけだと無理」）で初めて T-044 が立った。
**指摘欄に書かれた道具の不足は、そのまま品質の欠落として読むこと。**

### 台帳の状態

- 新規タスク **T-033〜T-044（12件）。すべて未着手**
- `python .claude/scripts/org-check.py` の警告 **15件 = セッション開始時と同数（新規 0）**
- PO確認待ち: **未回答 1件（Q-001 Cloudflare プラグイン）**。Q-006 / Q-007 / Q-008 は回答済み

### まだ誰も手を付けていない、期限のあるもの

| | 期限 | 現状 |
|---|---|---|
| **T-033**（禁止表現の機械検査） | 早いほどよい | 法令の砦がゼロのまま。**所要15分** |
| **旧下書き4件の手動削除** | 早いほどよい | Buffer の画面でしか消せない。**人にしかできない** |
| **T-038**（型別集計） | **9/9 の2日前まで** | Day7 ゲートの判定材料 |
| 9/3 に出た2本のツリー2投稿目 | 済んでいるか未確認 | **人が手でぶら下げる必要があった**（T-037 未着手のため） |

## 2026-09-04 セッション

### ゴール健全性: **前進している**

完了 16 / 未完了 29 / 停滞 10 / ブロック中 13 / PO確認待ち 2 / 手戻り累計 2。
このセッションで完了 14 → 16。**残作業は 29 で、T-044 の分だけ減って T-045 が増えたので実質横ばい。**
ただし T-045 は T-044 の検証中に見つかった既存の不具合であり、**新しく発生した作業ではなく、
見えていなかった作業が見えたもの**である。「ゴールが揺らいでいる」には当たらないと判定した。

### このセッションで完了したもの

| | 内容 |
|---|---|
| **T-044** | X のアルゴリズムを一次調査（`xai-org/x-algorithm` @ `9b0dc31` を直接読んだ）。`docs/Marketing/X_algorithm_findings.md` を新設し、`X_growth_design.md` を改訂。レビュー往復2回で合格 |
| **T-045** | 決定ログ索引の `上書き対象` が別の決定を失効させていた不具合を修正。TDD。レビュー往復1回で合格 |

### 次のセッションが最初に知るべきこと

1. **露出層リプの選定軸が変わった。** `min_faves:50` 以上 → **相手のフォロワー12万超**（`from:` の
   watchlist で列挙）。**watchlist 式に `min_faves:` を付けてはならない。** 正典は `X_growth_design.md` §4.3
2. **`from:` が最新タブで効くことは未実測である。** 効かなければ露出層リプの主経路が成立しない。
   **T-034 の完了条件に実測を追加済み。ここが最初の関門である**
3. **Q-009 が未回答**（`docs/po-queue.md`）。自前ポストの評価が過小である可能性。
   選択肢は3つ（①可視性ラベル ②投稿の中身 ③新規著者ブーストがそもそも適用されていない）。
   **③は観測できないため、自前ポストへ全面的に振り替える判断はできない。** 推奨は A案
4. **`org/x-media-ops` は main へマージしていない。** まとまり（X運用の機構）がまだ未完のため。
   T-033〜T-043 が残っている
5. **申し送りは T-028 / T-030 / T-034 / T-037 / T-041 へ転記済み。** 着手時にそれぞれの
   `## 申し送り` を読むこと

### コストの記録（PO への材料。対策は打っていない）

- T-044 全体で **46.5M トークン**。うち**キャッシュ読出が 43.5M（94%）**。目安の9割を超えている
- 内訳: 調査 21.6M / レビュー 11.8M / オーケストレーター 2.5M ほか。レビューを2往復させた分が効いている
- **レビューは費用を回収した**（日次台本の誤った禁止事項1件、索引の取り違え2件、テストの穴1件を検出）
- **未決**: 以降のタスクでレビューの厚みを落とすかを PO へ問うたが、回答は得ていない

## 2026-09-05 追記 — 骨格が変わった

**PO の判断（Q-009）で、露出の主軸が「他人へのリプライ」から「自前ポスト」へ移った。**
理由: リプライは人間が手で打つ作業で、機構が肩代わりできない。**主経路が機構の外にあるなら
機構を作る意味が薄い。** リプライは PO 個人の裁量作業として切り離した。

- **T-046 完了**: `docs/Marketing/X_own_post_method.md` を新設（単体で実行できる形）。
  最大化するのは `share_via_copy_link`（いいねの40倍）と `reply`（10倍）。
  **`profile_click` は 0.0 なので、配信量を伸ばす変数ではない**（事業上の変換指標としてのみ使う）
- **PO の手作業は 60〜90分/日 → 自前ポスト側 15〜20分/日**（リプライ分は PO 個人へ移管）
- **`X_growth_design.md` の本文は組み替えていない**（冒頭に注記1箇所のみ）。
  全体の組み替えは未起票。**必要になったら別タスクで起こす**

### 次のセッションが最初に見るもの

1. **`docs/Marketing/X_own_post_method.md`** — 自前ポストの正典。これだけで運用できる
2. **T-039**（KGI改訂）の優先度が上がった。§6 に改訂が要る6箇所の一覧がある
3. **T-041（カード画像）と T-037（Bufferツリー）の価値が上がった。** T-034 は要検討
4. **`docs/Marketing/x-drafts-2026-09-06.md` が未追跡のまま残っている。**
   9/6 の投稿ドラフト。**由来が確定していない。中身を確認してから扱うこと**

### レビューの方針（2026-09-05 時点）

T-044 / T-045 は独立レビューを付け、**誤りを4件検出して費用を回収した**。
**T-046 は独立レビューを付けていない** —— セッションのコストが $53 に達しており、
一次情報の主張は T-044（レビュー済み）から引き継いだものだけだったため。
オーケストレーターが重み表と投稿例の法令順守を自分で確認したにとどまる。
**必要なら後から org-review を掛けられる。**

## 2026-09-08 追記 — T-070（メール未入力時のアンケート保存）完了。**PO の作業が1つ残っている**

### ⛔ 次のセッションが最初に見ること

**`org/request-survey` を `main` へ merge してよいのは、PO が本番 D1 へ移行を流した後だけである。**

```bash
npx wrangler d1 execute pergram --remote --file worker/migrations/2026-09-08_request_survey.sql
npx wrangler d1 info pergram    # num_tables が 4 になっていることを確認
```

順序を逆にすると、`main` への merge で自動デプロイが走り、テーブルができるまで
`/api/request-survey` への書き込みが全部 503 になる。**利用者の画面は何事もなく進むので、
失敗が誰にも見えない。** T-058 で実際に踏んだ危険である。

### T-070 でできたもの

匿名のアンケート回答を入れる `request_survey`（6列 / `id` が主キー）。
**メールアドレスを入れなかった人の回答が、これで残る。**
`waitlist`（6列）と `request_signal`（3列）の定義は1文字も変えていない。

- レビュー通過（往復2回 / Blocker 0 / Major 0）、テスト判定合格
- `npm test` 564件中 562 pass（赤2件は `tests/hero_ranking.test.js` = T-049 の担当分。**このタスクとは無関係**）
- **実環境での書き込みは未確認。** PO が移行を流した後に確認する

### ⚠️ ID の重複を1件直した

前のセッションが `docs/tasks/T-063.md` を上書きし、**同名の完了済みタスク**（media 系コマンドの
手順を4段へ書き換える。T-065 / T-066 がブロッカーとして依存）**の詳細が消えていた。**
T-063 を復元し、アンケート保存のタスクを **T-070** へ採番し直した（`4b59d58`）。

### この文書にしか無い文脈 — 直接通信が、この環境では成立しない

**`CLAUDE.md`「エージェント間の直接通信」が前提にしている `Agent` ツールの `name` パラメータが、
この実行環境に無い。** `org-test-T-070` のような宛先は「到達できない」で送信が失敗する。
オーケストレーターは `agentId` で送れるが、**担当エージェントへ他の担当の宛先を教える手段が無い。**

**T-070 で実害が出た** —— テスト判定とレビュー判定が正面衝突し（再送を入れるべきか否か）、
本来なら「テスト ⇄ レビューの観点の受け渡し1往復」で早く見つかるはずのものが、
オーケストレーターの裁定まで持ち越された。裁定の結果、**テスト側が前提にしていた
`tests/mini_dom.js` の 🔒 の文言が規約と逆を向いていた**ことが判明した（修正済み）。

`docs/org-improvements.md` の **I-006** に起票した。**タスクの `## 連絡してよい相手` には
「無し（すべてオーケストレーター経由）」と書くこと。** 宛先を書くと担当が使えない指示になる。

## 2026-09-09 追記 — T-070 / T-071 は本番へ出た。**T-072 が最優先で残っている**

### ⛔ 次のセッションが最初にやること

**T-072。**同じ人がアンケートとメールの両方に答えると `waitlist` が2行に分かれ、
**同じ回答が両方の行に入る。**「クレアチンを見たい人」を数えると同じ人を2回数える。
**`docs/research/validation-plan.md` の判定に直接効く。**

**直し方はタスクファイルに書いてある**（メールが来た時点で匿名の行へ `email` を書き、
同時に `id` を捨てる）。🔒 **`id` と `email` の同居は、この修正でも許さない。CHECK を外さない。**

### 今日の到達点

| | |
| --- | --- |
| **T-070** | 匿名アンケートの送信経路 + 送りきれなかった回答をブラウザに控えて送り直す仕組み（出荷箱） |
| **T-071** | 保存先を `waitlist` へ（PO 判断）。7列へ作り直し、`id`/`email` の同居を CHECK で禁止 |
| **T-026** | レート制限の指摘を統合（対象を無認証の書き込み口3本へ拡大） |
| **T-063** | 重複採番を解消（前セッションが完了済みタスクのファイルを上書きしていた） |
| **I-006** | この環境では直接通信が成立しないことを起票 |

**本番で疎通を確認済み**（`POST /api/request-signal` → 204、`POST /api/request-survey` → 204）。
移行は PO が実施済み。`main` は `b33527b`。

⚠️ **診断で入れたテスト行の削除を PO へ依頼してある。**未確認なら聞くこと。
`request_signal`: `b5cf6aab-dfd5-42ea-a792-e3eb5a56b102` /
`waitlist`: `513bf257-4a24-46b2-a5ea-de029248c6db`

### この文書にしか無い文脈 — 今日ひっくり返った前提が2つある

**どちらも、それを前提に判断を積んでいた。同じ誤りへ戻らないこと。**

1. **「ブランチへの push では本番に出ない」は誤り。**Cloudflare の Deploy command が
   `npx wrangler deploy` なので、**どのブランチへ push しても本番の Worker が置き換わる。**
   `docs/ops/deploy.md` §3 を訂正済み
2. **「`email` が主キーだから匿名の行は物理的に入らない」は誤り。**SQLite は
   `INTEGER PRIMARY KEY` と WITHOUT ROWID 以外の PRIMARY KEY 列に **NULL を許す**。
   実際に入らなかったのは、**受け口が `email` を省いた INSERT を出していなかったから**である。
   **T-070 を起票した根拠そのものが、この誤りだった**（結論としてのテーブル設計は PO 判断で別途決まった）

### コストの記録

**このセッションで $102。**内訳の主因は、**同じ機能を2度作ったこと**である
（T-070 で `request_survey` を作り、レビュー2往復とテスト判定まで通した後、
PO 判断で `waitlist` への相乗りへ差し替えた）。
**「回答をどこに保存するか」は、受け入れテストを書く前に PO へ確認できた論点だった。**
テスト担当・実装担当の双方が `[反復]` として指摘している。

