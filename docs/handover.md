# セッション引き継ぎ記録

**書けるのはオーケストレーターだけ。** 新しいセッションはここを最初に読む。
台帳（`docs/task-list-pergram.csv` と `docs/tasks/T-XXX.md`）が正であり、
この文書は**そこから読み取れない文脈**だけを持つ。

🔒 **1枚。常に上書きする。追記して膨らませない。** 過去分は git 履歴にある。
分析結果・課題・決定は、**ここではなくそれぞれの正典へ書く**
（組織の課題 → `docs/org-improvements.md` / 決定 → タスクの `## 決定ログ`）。

---

## 更新日時

**2026-09-12**

## ゴール

**PO 提起（2026-09-12）: T-072 型の所要時間を減らす。** TDD は維持したまま、
工程の軽重のルールとオーケストレーターの振る舞いを見直す。

**達成条件**: 対策が層2へ折り込まれ、次の同規模タスク1件で API 往復数の前後比較が取れること。

## 現在地

**分析だけを行った。コード・規約・雛形は1文字も変えていない。**

- 初回の組織点検を実施（改善エージェント。完了31件で初回）。診断は3件
- T-072 の全担当エージェントの記録を集計し、750ターンの内訳を実測
- **課題を `docs/org-improvements.md` へ I-009〜I-012 として起票済み。折込は次セッション**
- 一次情報での調査を投げた → **`docs/research/dev-process-sizing.md`**（調査エージェントが作成）

進行中のタスクID: **無し**（組織改善のみ。開発タスクは動かしていない）

## 直近の判断

| 判断 | 内容 |
| --- | --- |
| 折込先 | **pergram 先行**（`org-` は配布物だが、PO が消えるリスクを承知で選択）。🔒 **パッチとして退避し、`org-improvements.md` に `状態: 適用済み（このリポジトリ先行）` で記録すること** |
| 工程の切り分けの軸 | **「規模」ではなく「取り返しのつかなさ」**。既にある 🔒 に紐づける。**A/B/C 区分は暫定で、調査結果で補正する** |
| 設計成果物の書式 | **固めない**（Google の実務は「形式は厳密に決めない」）。**置き場所だけ決める** |

## 次の一手

1. **`docs/research/dev-process-sizing.md` を読む**（調査結果）。A/B/C 区分をこれで補正する
2. **`docs/org-improvements.md` の I-009〜I-012 を処理する。** 対策の本体はここにある
3. **Q-011 を閉じる**（2026-09-08 から未回答。今回 PO が提起した論点と同一で、
   組織側は推奨案 C まで添えて上げていた）
4. 折込後、次の同規模タスクで往復数を測って前後比較する

## PO確認待ち

| ID | 登録日 | 放置 | 件名 |
| --- | --- | --- | --- |
| **Q-011** | 2026-09-08 | **4日** | 見た目のタスクに標準フルフローを回し続けるか（**最優先で閉じる**） |
| Q-001 | 2026-09-02 | **10日** | Cloudflare 公式プラグインを導入するか（止まるタスクは無い） |

## 直近の健全性評価

| 指標 | 値 | 前回 |
| --- | --- | --- |
| 完了タスク | 31 | — |
| **停滞（未着手・未割当のまま2日以上）** | **24** | — |
| **台帳の整合性の警告** | **50** | — |
| 手戻り回数の合計 | 8 | — |
| **担当からの指摘（未起票）** | **95件中 約78件** | — |
| PO確認待ち（未回答） | 2 | — |

⚠️ **警告50件と停滞24件は毎セッション印字されているが、何日も動いていない。**
**この「出続けて誰も動かない」状態そのものを I-010 として起票した。**

## 環境・前提

| | |
| --- | --- |
| ブランチ | **`org/process-weight`**（`main` から。未マージ） |
| 未コミットの作業 | **無し** |
| テスト | `npm test` → **612件中610件が緑。赤2件は `tests/hero_ranking.test.js`（T-049。未着手）** |
| ビルド | `npm run build` |
| 実行時間 | テスト一式は約5.5秒。**開発中に一式を回す費用は無視できる** |
| ⚠️ デプロイ | **`main` へ push した時点で本番に出る**（Cloudflare Workers Builds） |
