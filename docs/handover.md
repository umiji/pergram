# セッション引き継ぎ記録

**書けるのはオーケストレーターだけ。** 新しいセッションはここを最初に読む。
台帳（`docs/task-list-pergram.csv` と `docs/tasks/T-XXX.md`）が正であり、
この文書は**そこから読み取れない文脈**だけを持つ。

🔒 **1枚。常に上書きする。追記して膨らませない。** 過去分は git 履歴にある。
分析結果・課題・決定は、**ここではなくそれぞれの正典へ書く**
（組織の課題 → `docs/org-improvements.md` / 決定 → タスクの `## 決定ログ`）。

---

## 更新日時

**2026-09-13**（セッション2。測定の道具を直す作業に着手した）

## ゴール

**PO 提起（2026-09-12）: T-072 型の所要時間を減らす。** TDD は維持したまま、
工程の軽重のルールとオーケストレーターの振る舞いを見直す。

**PO 追加指摘（同日）:** 「**組織にルールとして存在するから OK ではない。
Maturity が十分か、それを遂行できる組織になっているか、各項目で細かなチェックが必要。
結論を急ぎすぎ。もう一度原典に当たって最適な構成を検討してくれ。**」

**達成条件**: 対策が層2へ折り込まれ、次の同規模タスク1件で API 往復数の前後比較が取れること。

🔴 **達成条件は、まだ満たしていない。** 折込（前半）は済んだが、**後半の「前後比較が取れる」に届いていない。**
2026-09-13 の実測で、**測る道具の側が壊れていることが分かった**（下の「次セッションの作業」）。

### 宿題は2つある。1つ目（これ）は折込が済んだ

**PO 提起（2026-09-12、2つ目）: この組織の開発・ドキュメント・テストの品質が、十分に高くない。**
速くしながら、同時に品質を上げたい。

**PO の指示は「議論の順序」だった ——**
**まず時短対策5本を折り込む。品質の議論はその後。**
🟢 **折込は 2026-09-12 に完了した。**
⚠️ **ただし宿題1の達成条件（前後比較が取れること）は未達で、測る道具が壊れている。**
**次セッションはまず「次セッションの作業」の A〜D を片付ける。品質の議論はその後。**


## 現在地

🟢 **対策5本の折込は完了した。** 5本すべてが層2へ入っている
（`docs/org-improvements.md` の D-001「適用の記録」に、場所・コミット・退避先の表がある）。

🔴 **しかし宿題1は終わっていない。** 達成条件は「前後比較が取れること」であり、
**2026-09-13 の実測で、測る道具の側が壊れていることが分かった。**

| 何が壊れているか | 実測 |
| --- | --- |
| ~~**往復数の単位**~~ | 🟢 **T-074 で直った。** 列は `往復回数` へ差し替え。`--task T-072` が「担当エージェントだけ: API 往復 366 回 / 記録 6本」を出す |
| ~~**トークン数そのもの**~~ | 🟢 **T-074 で直った。** 同じ requestId の各行が `input` / `cache_read` / `cache_creation` を同一の値で持つため重複していた（担当エージェント73本で 8.72億 → 束ね後 **466,489,431**。申し送りの実測値と1トークンまで一致）。`output` だけが最後の行で完全値 |
| ⚠️ **単位の混在が1箇所だけ残る** | **会話記録がもう残っていない16行**（すべて記録元 `main`、5.01億トークン・往復2,666）は束ね直せず、**往復の値が応答の行数のまま。** 台帳全体の34.2%が混成になる。**担当エージェントの行にこの取りこぼしは0件**なので、効果比較（担当側の数字を使う）には効かない |
| **基準値そのもの** | 担当は5体ではなく**6体**（339 + 落としていた27 = **366**）。並列率は0%ではなく **12.5%**。「テスト2回目133往復」は応答行で、実往復は **69** |
| ~~**タスクIDの取り違え**~~ | 🟢 **T-074 で直った。** `grep -cE "^T-[0-9]{1,2}," docs/token-usage-pergram.csv` が **0**（着手時 8）。3桁化による取りこぼしは無い（消えたIDは存在しないタスクの行） |

🟢 **基準値も道具も、これで揃った**（基準値の訂正は D-001、道具の修正は T-074）。
**次に開発タスクを1件回せば、前後比較が取れる。**

🔴 **比較には担当エージェント側の数字を使うこと。** メインセッションの紐付けは時系列の近似で、
記録が伸びるたびに揺れる（実際 T-072 のオーケストレーター込みの往復は 597 → 587 と動いた）。
**担当エージェント側は起動時の指示文から直接引くので揺れない。**

進行中のタスクID: **無し**。**T-074 は完了した**（テスト判定 合格 / レビュー通過。Blocker 0・Major 0・Minor 4、往復1回）。
**T-075・T-076 が着手可能になっている**（ブロッカーは解けた）。

🟢 **測る道具は直った。** `org-tokens.py` が `requestId` で束ねるようになり、
列は `返答回数` → **`往復回数`** へ差し替わった。集計のテストも新設された
（`tests/org_tokens.test.py` 16件 / fixture 付き。`PYTHONIOENCODING=utf-8 python tests/org_tokens.test.py`）。
**束ねを外すとテストが赤になることを、テスト担当が実装本体に対して確認済み。**

⚠️ **ただし「前後比較が取れること」はまだ未達である。** 道具が直っただけで、
**次に開発タスクを1件回すまで、折込の効果は1件も測れていない。**

## 本セッションで完了したこと

### 1. 対策5本を、PO 指示の順（5 → 3 → 2 → 4 → 1）で折り込んだ

1本1コミット。

| # | 対策 | 折り込んだ場所 |
| --- | --- | --- |
| 5 | 独立した確認は1往復にまとめる | 担当4種の定義 |
| 3 | 差し戻しは同じエージェントを呼び戻す（枝番で起こし直さない） | オーケストレーターの手順 |
| 2 | 開発中は関連テストだけ。一式は完了報告の直前に1回 | 実装・テストの定義 |
| 4 | `## 連絡してよい相手` の既定を「書く」へ | 雛形 / 台帳の規約 / オーケストレーターの手順 |
| 1 | `## 参照すべき成果物` を担当を付ける時点の必須に | `org-check.py` / 雛形 / オーケストレーターの手順 |

- **退避を整えた** — `.claude/patches/` に変更前の版7本・`D-001.patch`・当て直し手順の README
- **台帳の整合性の警告は 2件のまま**（対策1の検査を足しても増えないことを実測で確認）
- `npm test` **612件中610件が緑**（赤2件は T-049。折込前と同じ）
- **5本の内容そのものは変えていない。** 加えた判断5点は D-001 の「適用の記録」に理由付きで書いた

### 2. 実測パッケージの調査（`docs/research/agent-perf-measurement.md`、232行）

**結論だけ。本文は文書を読む。**

- 🔴 **所要時間の自己申告は指標にできない。** METR の RCT（熟練16名・実課題246件・無作為割付）で、
  AI 併用は**実測19%遅く**、事前予想は「24%速い」、**事後も「20%速い」と信じたまま**だった
- **規模の物差しは oracle solution の手数**（触るファイル数 / 編集回数 / 跨ぐ工程）。
  **人間の所要時間は使わない** —— 実務がそれを使うのは歴代モデルを年またぎで並べるためで、
  fixture が毎回同一なら校正が要らない（PO 指摘で訂正）
- **既製品はある**（DevBench/DevEval の5工程、MetaGPT SoftwareDev の70課題、Harbor、
  Workspace-Bench）。**無いのは「同じ組織の規約変更の前後」を比べる形だけ。**
  導入はしないが3つ借りる: 工程の切り方 / 指標の定義 / 1課題4点セット
- 🟢 **Workspace-Bench が「1課題あたりの平均トークン」と「平均ターン数」を主要指標にしている。**
  D-001 の指標が実務と一致していることの裏取りになった
- **DevEval を土台にする案は不採用**（最終更新 2024-05-30。2年以上停止。PO 判断）

### 3. D-001 の基準値を実測で訂正した

上の「現在地」の表のとおり。会話記録を `requestId` で束ね直して数えた結果である。**推測ではない。**

---

## 🔴 次セッションの作業 — 測定の精度を上げる

**PO 指示（2026-09-13）: 下の作業を次セッションで対応する。**

🟢 **登録済み（2026-09-13 セッション2）。以降は台帳が正であり、この節は背景として読む。**

| 台帳 | 範囲 | 状態 |
| --- | --- | --- |
| **T-074** | **§A（単位を直す）＋ §D（集計のテスト）** | **テスト作成中**（`org-test-T-074`） |
| **T-075** | §B（指標を5つ増やす） | 未着手。T-074 がブロッカー |
| **T-076** | §C（帰属の精度と、タスク外の内訳） | 未着手。T-074 がブロッカー |

⚠️ **§A の範囲は、登録時に広がっている。** `返答回数` 列だけでなく、
**トークン数そのものの重複計上**も T-074 で直す（上の「現在地」の表を見ること）。

§E-2（未 push）は**済んでいる**（セッション頭に確認。`origin/org/process-weight` と同一）。
§E-1（改行コードの混在）は未着手で、台帳に登録していない —— 差分の読みやすさの問題だけであり、
A〜D を優先するという PO の指示に従った。

### A. 単位を直す 🔴 最優先。ここが直るまで前後比較に意味が無い

| # | 作業 | 完了条件（二値） |
| --- | --- | --- |
| **A-1** | `org-tokens.py` に **`requestId` で束ねて API 往復を数える**処理を足す。既存の `返答回数` 列を差し替えるか、列を足すかも決める（差し替えるなら過去分の再集計が要る） | T-072 を集計すると **API往復 366** が出る |
| **A-2** | タスクIDの抽出を3桁に狭める（`TASK_ID = re.compile(r"T-\d+")`、`.claude/scripts/org-tokens.py:116`）。壊れた行を消して再集計 | 台帳のタスクID列に **3桁でない `T-` 行が0件** |
| **A-3** | 直した集計で D-001 の基準値を検算 | **366往復 / 並列12.5% / 起動6体**が再現する |

**A-1 の根拠（実測済み）**: 1回の API 応答が、会話記録では複数行に分かれて書かれる
（文・思考・道具呼び出しが別行）。同じ `requestId` を持つ行を束ねれば正しい往復数が出ることは、
本セッションで使い捨てスクリプトにより確認した（T-072: 671行 → 366往復。比 1.83）。

### B. 指標を増やす — どれも会話記録から取れることを実測で確認済み

| # | 指標 | 取り方 | 完了条件 |
| --- | --- | --- | --- |
| **B-1** | **並列率** | `requestId` で束ね、道具呼び出しが2個以上の往復を数える | T-072 で **12.5%**（道具往復353のうち44）が出る |
| **B-2** | **テスト実行回数** | 道具呼び出しの引数が `npm test` / `node --test` に一致する数 | T-072 の実装担当で **27** が出る |
| **B-3** | **壁時計** | 各行の `timestamp` の差（1091行中874行にある） | タスク別に「最初の指示から最後の応答まで」が出る |
| **B-4** | **起動回数** | 担当エージェントの記録ファイルの本数をタスク別に | T-072 で **6** が出る |
| **B-5** | **持ち越し比・やり直し税** | キャッシュ読出 ÷ 入力合計、および `手戻り回数` との突き合わせ | タスク別に出る |

⚠️ **B-3 には決めることがある。** 「指示投入 → 完了報告」を素直に測ると、
**PO の確認待ち・人間の応答待ち・セッションの中断が丸ごと混ざる。**
**除くのか、除かずに別立てで出すのかを先に決める**（決めずに実装すると、後で数字の意味が分からなくなる）。

### C. 帰属の精度を上げる

| # | 作業 | いまの状態 |
| --- | --- | --- |
| **C-1** | **メインセッションの紐付けが近似である**ことへの対処 | 全トークンの **62.9%** がメインセッションで、その紐付けは「IDが会話に現れたら以降そのタスク」という当て方。**担当エージェント側（37.1%）は指示文から取るので正確。** まず誤差の大きさを測る手段が要る |
| **C-2** | **「タスク外」34.5%（8.1億トークン）の内訳**を見る | 欠陥ではなく運営作業の分だが、**組織全体の3分の1がここに落ちている。** どこが重いのかが分かっていない |

### D. 検算を自動化する 🔴 これが無いと、次に直したときに黙って壊れる

| # | 作業 | 完了条件 |
| --- | --- | --- |
| **D-1** | 集計スクリプトのテストを足す（`tests/org_decisions.test.py` と同じ形で `tests/org_tokens.test.py`） | 固定の小さな会話記録を置き、**往復数・並列率・テスト実行回数・帰属**が期待どおり出ることを検査する。`python tests/org_tokens.test.py` が緑 |

**決定ログ索引には既にテストがある**（11件）。**トークン集計には無い。** 本セッションで
「返答回数は往復ではなかった」という誤りが1年近く気づかれずに残っていたのは、これが理由である。

### E. 後片付け（小さいが、放っておくと差分が読めなくなる）

| # | 作業 |
| --- | --- |
| **E-1** | `docs/org-improvements.md` の**改行コードの混在を直す**。本セッションのスクリプトが CRLF を LF へ一括変換してしまい、実質68行の変更が git 上は1229行の差分になっている（`git diff --ignore-all-space` で確認済み）。**6行だけ CRLF が残っている** |
| **E-2** | 未 push のコミットを push する（作業ブランチ `org/process-weight`） |

### F. ここまで済んでから — 効果の実測

**A〜D が終われば、次に開発タスクを1件回した時点で前後比較が取れる。**
比較の相手は D-001 の「効果の測り方」の表（**訂正後の値**を使うこと）。

### G. まだ手を付けない（設計方針だけ調査で固まっている）

| | 状態 |
| --- | --- |
| **毎回同じ条件の課題（fixture）** | 方針のみ: 1課題4点セット（指示 / 環境 / 検証テスト / **oracle solution**）、族→バリアント、生成に LM を使わない（AST 改変・巻き戻し）。**実物は無い** |
| **質の測定** | **現状ゼロ。** テストの合否しか無く、設計やコードの質を見るものが無い。宿題2の一部 |

---

## 宿題2 — 品質を上げながら速くする（**上の A〜D の後**）

### 🔴 調査4本が何のためのものか（PO が明言。取り違えないこと）

**あの調査は、時短対策の根拠を探したものではない。**

**PO の見立て**: 品質を上げながら速度も上げるには、**ケースによって
ワークフローを簡略化する／フルラインナップで回す、の切り分けが要る。**
その**切り分けの判定基準（クライテリア）と、切り分けの枠組み（フレームワーク）**を
実務の一次情報から探すために調査した。**調査の位置づけはこれである。**

したがって調査の成果は、**この宿題2でだけ使う。** 対策5本の折込には使わない
（対策5本は実測から出ており、調査とは別系統である）。

| 読むもの | 何が書いてあるか |
| --- | --- |
| `docs/research/dev-process-sizing.md` | 工程の重さを決める軸。**どの実務も「規模」を軸にしていない** |
| `docs/research/dev-process-sizing-gaps.md` | 上の未確認5点を潰したもの。**2つの結論が覆っている**（Fowler の引用、ISO のリスク式） |
| `docs/research/change-classification-practice.md` | 変更の種類分けの実務（ITIL / SRE / DORA）。**切り分けの枠組みの本命はここ** |
| `docs/research/rule-maturity-compliance.md` | PO 指摘「ルールがあるだけでは OK ではない」への答え。**成熟度5段と、遵守を測る道具の落とし穴** |

### 検討の順序

**🔴 前セッションはここを飛ばして A/B/C 区分を作り、3つとも外した。**

1. **調査4本を読む**（上の表）
2. **「タスクの種類」の一覧を、73タスクの実績から作る。** これが実務との最大の差である
   —— 実務は**種類に対して一度だけ**工程を決めており、**個々の変更を人が都度判定していない**
3. **種類ごとに一度だけ工程を決める。** タスク1件ごとに人が判定する形にしない
4. **品質の側の現状を測る。** 速度だけでなく、何がどう足りていないのかを事実で押さえる
   （手戻り8回、レビュー指摘の内訳、テストが実際に何を見張っているか）
5. **Q-011 を閉じる**（PO の回答待ち。2026-09-08 から）

## 調査が出した、設計に直接効く結論

| # | 結論 | 出典 |
| --- | --- | --- |
| 1 | 🔴 **個々の変更に人が都度クラスを判定する設計を、どの実務も採っていない。** 判定は**種類に対して一度だけ前もって**行われ、個々は照合に還元される。**この組織には「タスクの種類」の一覧が無い。実務との差はクラスの切り方ではなく、ここである** | ITIL 4 |
| 2 | **軽い工程とは、審査の省略ではなく、審査対象を一段上げること。** 手続きが作られた／変わったときに一度だけ完全なリスク評価を通す | ITIL 4 |
| 3 | **「手続きが既に整備されているか」という軸が増える。** 変更の性質ではなく**組織の履歴**で決まる。同じ変更が1回目は normal、5回目は standard | ITIL 4 |
| 4 | **「1タスク＝1つの重さ」で全工程を決めている実務が一つも無い。** 設計＝曖昧さ / テスト＝リスク / 決定の記録＝不可逆性 / レビュー＝全件やって観点を変える | Ubl, ISO 29119, Nygard, Google |
| 5 | **クラス数は2〜3が最多。4超は皆無。** クラス分けを使っていない実務が3つ（SRE＝連続量、DORA＝承認の所在、WHO＝全件一律19項目） | 横断 |
| 6 | 🔴 **Fowler は「不可逆性＝工程を重くする軸」の根拠に引けない。** 原文は "things people **perceive as** hard to change"、発想は Ralph Johnson、**論文の結論は "eliminate irreversibility"（不可逆性を取り除け）** | Fowler 2003 |
| 7 | **不可逆性と「変更の種類」は独立の軸ではない。** 「構造の変更は安く戻せる。振る舞いの変更は戻せない」 | Beck |
| 8 | **「リスク＝影響×発生可能性」は ISO 29119 の定義ではない**（`such as` 付きの例示）。**算定方法は規格の要求ではないので独自に決めてよい。** 低エクスポージャを費用制約で落とすことは規格上の正規の手順 | ISO 29119 |
| 9 | **レビューは省かない。** DORA 2019: 外部の変更承認委員会を持つ組織は低パフォーマー **2.6倍**、変更失敗率を下げる証拠なし。⚠️ **否定されているのは外部機関の承認で、ピアレビューは推奨側** | DORA, Google |
| 10 | **規模は「決める軸」ではなく「操作する対象」。** 「小さいから省く」ではなく「リスクを閾値以下に収めるため小さく切れ」 | ITIL, Google |

## PO 指摘「Maturity と遂行能力」への答え — 3段ではなく5段

| 段 | 判定の証拠 | この組織の現状 |
| --- | --- | --- |
| 定義 | 手順・entry/exit 条件が書かれているか | おおむね足りている |
| **配備** | 役割が割り当てられ**伝達され**、**力量と資源があるか** | 🔴 **I-013 がここで落ちている** |
| 遂行 | 実際に行われているか | 証拠・入口は実際に書かれていた |
| **管理** | **逸脱したとき是正措置が取られるか** | 🔴 **CMMI の Performed / Managed の境目はこの1点。検知だけでは届かない** |
| **保証** | **遂行者以外**が評価し、不適合が **クローズまで追跡**されているか | 🔴 **閉じる経路が無い。警告は翌セッションも印字される** |

**I-013 が「②遂行できる組織か」の教科書的な実例である** ——
`org-review` の定義に `Write` も `Edit` も無く、**規約が全担当へ求める
「組織への指摘を自分のタスクファイルへ書く」を物理的に実行できない。**
指摘が上がらないことが「指摘が無い」と読める。同種が5件ある。

### 「止める」か「測る」かの答え

**どちらを採っても、不適合を閉じる経路が無ければ段3に到達しない。それが分かれ目。**

順序は **精度を上げる → 件数を減らす → 閉じる経路を作る → 止める。**
本セッションで1段目と2段目を行った（50件 → 2件）。

🔴 **成立条件がある。Google は「偽陽性が10%を超えると開発者は警告を無視する」とし、
繰り返しそれを観測した結果、警告を出し続けるという選択肢を最初から捨てている。**
**この組織の警告は偽陽性94%だった。この状態で割り当てを止めれば組織はほぼ全面停止する。**

## 直近の判断

| 判断 | 内容 |
| --- | --- |
| 折込先 | **pergram 先行**（`org-` は配布物だが、PO が消えるリスクを承知で選択）。🔒 **退避済み** — `.claude/patches/` に変更前の版・`D-001.patch`・当て直し手順の README |
| 工程の切り分けの軸 | 🔴 **「規模」でも「取り返しのつかなさ」単独でもない。工程ごとに軸が違う。** 前セッションの A/B/C 区分は**採用しない**（I-011 の補正節） |
| 設計成果物の書式 | **固めない**（Google の実務は「形式は厳密に決めない」）。**置き場所だけ決める**（I-012） |
| 対策1の検査の範囲 | **完了したタスクには問わない。** 既存4節と違う扱いにした。終わったタスクの入口は誰も読まず、警告にすると偽陽性が増えて警告全体が無視される |
| 規模の物差し | 🔴 **「人間の所要時間」は使わない**（PO 指摘）。fixture が毎回同一なら校正が要らない。代わりに **oracle solution の手数**。測る対象は「指示投入 → 完了報告」 |
| 既製品 | **導入しない。形式を3つ借りるだけ**（工程の切り方 / 指標の定義 / 1課題4点セット）。**DevEval 案は不採用**（2024-05 で停止） |

## PO確認待ち

| ID | 登録日 | 放置 | 件名 |
| --- | --- | --- | --- |
| **Q-011** | 2026-09-08 | **4日** | 見た目のタスクに標準フルフローを回し続けるか。**B案（レビューを省く）は一次情報に正面から反することが判明**（上記9）。**未回答のまま** |
| Q-001 | 2026-09-02 | **10日** | Cloudflare 公式プラグインを導入するか（止まるタスクは無い） |

## 直近の健全性評価

| 指標 | 値 | 前回 |
| --- | --- | --- |
| 完了タスク | 31 | 31 |
| **停滞（未着手・未割当のまま2日以上）** | **24** | 24 |
| **台帳の整合性の警告** | **2** | **50** |
| 手戻り回数の合計 | 8 | 8 |
| **担当からの指摘（未起票）** | **0** | 95件中 約78件 |
| PO確認待ち（未回答） | 2 | 2 |

⚠️ **停滞24件は動いていない。** 本セッションは組織改善に絞ったため、開発タスクへ
担当を割り当てていない。**規約（原因確認 → 再分割 → 別担当）は未実施のまま。**

## 環境・前提

| | |
| --- | --- |
| ブランチ | **`org/process-weight`**（`main` から。未マージ） |
| 未コミットの作業 | **無し**（`docs/decisions-index.md` と `docs/token-usage-pergram.csv` はフックが書く生成物） |
| テスト | `npm test` → **612件中610件が緑。赤2件は `tests/hero_ranking.test.js`（T-049。未着手）** |
| ビルド | `npm run build` |
| 実行時間 | テスト一式は約5.5秒。⚠️ **「だから開発中に何度回してもよい」ではない**（対策2）。費用の主因は実行時間ではなく、回すたびに挟まる思考の往復（1回 約18秒）である |
| ⚠️ デプロイ | **`main` へ push した時点で本番に出る**（Cloudflare Workers Builds） |
| ⚠️ コスト | 前々セッション **$101.27** / 前セッション **$104**（折込＋調査＋基準値の訂正）。**組織改善だけでこの額が続くのは持たない。次セッションは A〜D に絞ること** |
| ⚠️ 未 push | **作業ブランチに未 push のコミットがある。** セッション頭で確認する |
| ⚠️ 改行コード | **CRLF と LF が混在している。** `.claude/` 配下はおおむね CRLF、`docs/handover.md` と新規作成分は LF。**書き換えスクリプトを走らせる前に必ず確認する**（本セッションで1ファイル汚した） |
| Python | `python`（`python3` は無い）。**日本語を出すときは `PYTHONIOENCODING=utf-8` を付ける**。付けないとコンソールが cp932 で落ちる |
| 会話記録の置き場 | `~/.claude/projects/c--Users-kaiki-Workspace-03-Dev-pergram/`（セッション89本、担当エージェントの記録72本） |
