# T-004: Google広告 審査提出と初動の計測確認

## メタデータ
- 作成日: 2026-08-18
- 更新日: 2026-09-02
- 状態: 保留
- 優先度: 中
- 担当: 未割当
- 進捗: 33%（(4-1) 提出済み。(4-2) 審査結果と (4-3) UTM到達確認が残り）
- 依存: T-003（ブロッカー: 入稿しないと審査に出せない）/ T-001（ブロッカー: 本番で `lp_view` が飛んでいないと UTM の到達確認ができない）
- TDD適用可否: 適用
- 手戻り回数: 0

## 目的

審査を通し、**配信初日のうちに計測が正しく届いていることを確認する。**

UTMの付け忘れを検知できる最後のタイミングがここ。付け忘れたまま7日走ると、
全流入が `source=(direct)` / `keyword=(none)` になり、
「どの成分に需要があるか」という広告テストの第一目的が測れないまま予算だけが消える。

出典: [docs/ops/google-ads-first-campaign.md](../ops/google-ads-first-campaign.md) フェーズ4

## 変更範囲

コード変更なし。管理画面の操作とGA4での確認のみ。
ただし否認された場合、該当箇所のみ修正して再提出する（広告文の修正は T-003 の禁止事項に従う）。

## 禁止事項

- 否認された際に、指摘箇所以外をまとめて書き換えない。**何が否認理由だったかが分からなくなる**
- 🔒 否認回避を目的に、効能をほのめかす婉曲表現へ差し替えない。より保守的な文言に落とす方向でのみ修正する
- 計測が届いていないことが分かった時点で配信を止める。「後で直す」まま走らせない

## 完了条件

1. キャンペーンの審査ステータスが「有効」になっている
2. 配信開始後、GA4のリアルタイムレポートで `lp_view` の `source` パラメータが `google` になっている
   - 併せて `utm_term` に実際のキーワードが入っていることを確認する
3. GA4「集客 → トラフィック獲得」で `google / cpc` のセッションが1件以上計上されている
4. 上記2〜3の確認結果（確認時刻・観測値）を `## 証拠` に記録している
5. 否認があった場合、否認理由と修正内容が `## 決定ログ` に記録されている

## 判断してよい範囲

**2026-09-02 追記（オーケストレーター）。** このタスクは実施当時この節が空のまま進んだ。
保留として整理するにあたり、**実際に適用されている境界**を明文化する。
新しい制約を足したものではない。

- 決めてよい: 記録の書き方・置き場、証拠の整理の仕方
- **決めてはいけない**: **広告の配信設定そのもの**（Google 広告の管理画面の操作は PO の領分であり、
  組織からアクセスする手段が無い）、予算、判定基準の数値
  （`docs/research/validation-plan.md` が正）、禁止語リストの中身

## テスト方法

- GA4リアルタイムレポートの目視。配信開始当日（Day 1）のうちに必ず実施する
- 自分自身のクリックで確認する場合、GA4のIP除外設定と計上のずれに注意する
  （リアルタイムに出れば到達確認としては十分。数値の正確さは Day 1 では問わない）

## 停止条件

- §4の共通のものに加えて:
- `source` が `google` にならない場合、**配信を一時停止してから**原因を調べる。走らせながら直さない
- 否認理由が「広告文の表現」ではなく「サイトの内容」に関するものだった場合、
  修正せず停止して報告する（LPの構成・免責表示の設計に関わるため）

## 参照すべき成果物
<!-- 設計成果物、既存コード、過去の決定（docs/decisions-index.md で引ける）。
     実在するパスだけを書く -->

## 連絡してよい相手
無し（すべてオーケストレーター経由）
<!-- 直接やりとりしてよい相手が居るなら、その宛先をそのまま書く。
     宛先は起動時に付けた名前であり、タスクごとに一意でなければならない
     （例: org-implementation-T-007）。役割名だけを書くと、同じ名前が
     使い回された時点で送信が拒否される -->

## 申し送り
無し
<!-- 上流タスクの完了報告から、この担当が知らないと判断を誤る事実を、
     オーケストレーターが転記する。登録した後で埋まる唯一の指示欄 -->

## 成果物
<!-- 完了時に何ができたか。ファイルパスとコミット -->

## ブロッカー

**証拠が「（未着手）」のまま。** 完了条件2〜4（GA4 のリアルタイムレポートで
`lp_view` の `source` が `google` になっていること、`utm_term` に実キーワードが入っていること、
「集客 → トラフィック獲得」で `google / cpc` のセッションが1件以上計上されていること、
およびその確認時刻と観測値の記録）が埋まっていない。
**2026-09-02、PO が「後でデータを渡す」と回答したため保留。**

必要なのは、GA4 の画面で観測した**確認時刻と実際の値**。
<!-- 今このタスクを止めているもの。PO確認待ちなら Q-XXX を書く -->

## 決定ログ

（特記なし）

## 作業ログ

- 2026-08-18: タスクとして台帳に登録
- 2026-08-18: (4-1) キャンペーンを保存し審査へ提出。**審査中**。
  次は (4-2) 審査結果の確認 → 承認され次第 (4-3) GA4リアルタイムで `lp_view` の `source` を確認する

## 証拠

（未着手）

## 組織への指摘
無し
<!-- 組織の仕組み（規約・道具・進め方・役の分け方）について気づいたことを、
     1行1件で書く。書式は必ず `- [種別] 内容`。種別は次の4つだけ:

       規約 … この規約に従うと作業が進まない、または矛盾する箇所に当たった
       道具 … 必要な道具・スキル・外部アクセスが無く、遠回りをした
       反復 … 同じ種類の手戻りを、このタスクで2回以上経験した
       ロール … 役の分け方が、いまの仕事の流れに合っていない

     例:
       - [道具] Python の書き方のスキルが割り当てられておらず、毎回自分で確認した
       - [規約] 完了条件に実環境での確認が要るが、実施者が決まっていない
       - [ロール] レビュー待ちが3タスク積み上がり、自分の次の着手が2日止まった

     書くのは観測した事実だけ。対応策も「新しい役が要る」という結論も書かない。
     「質問をさばく役が要る」ではなく「レビュー待ちで2日止まった」と書く。
     どう直すかは改善エージェントとオーケストレーターと PO が決める。

     ロール を書く条件は6つ。詳細は .claude/rules/org-improvement-feedback.md

     これはエスカレーションではない。**返事は来ないし、作業も止めない。**
     判断を仰ぎたいなら、ここではなくエスカレーションの経路を使う。

     ここに書いたものは、完了報告にも含める。オーケストレーターがそれを
     吸い上げ、組織改善の台帳（docs/org-improvements.md）へまとめて起票する。

     台帳へ自分で書きに行かないこと——同時に走る他のタスクの書き込みを消す。

     気づきが無ければ「無し」のまま残す。行ごと消さない -->
