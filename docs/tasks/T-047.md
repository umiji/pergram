# T-047: CSPがGA4のビーコンをブロックし全計測が落ちている

## メタデータ
- 作成日: 2026-09-07
- 更新日: 2026-09-07
- 状態: テスト作成中
- 優先度: 高
- 担当: org-test-T-047
- 進捗: 受け入れテストを先行作成済み（RED）。実装待ち
- 依存: 無し
- TDD適用可否: 適用（`tests/` に CSP の許可リストを検査するテストを先に書ける）
- 手戻り回数: 0

## 目的

**本番の CSP が GA4 の計測ビーコンをブロックしており、サイト全体の計測が機能していない。**

現在の GA4（gtag.js）は計測データを `https://analytics.google.com/g/collect` へ送るが、
`src/build/headers.js` の `connect-src` は `https://www.google-analytics.com` しか許可していない。
このためブラウザが送信を拒否し、`page_view` を含む全イベントが GA4 に届いていない。

**タグの読み込み自体は成功するため、画面上は完全に正常に見える。**
落ちるのは最後の送信の一歩だけで、痕跡はブラウザのコンソールにしか出ない。

飛ばすと何が壊れるか: 広告費を使っても効果が測定できない。実害が出ている
（Google 広告 110 クリックに対し GA4 のアクティブユーザーが 2 人）。
X 運用の KGI（14日で 300 アクティブユーザー）も同じ理由で測定不能。

## 変更範囲

- `src/build/headers.js` — CSP の `connect-src` と `script-src`
- `tests/` — CSP の許可リストを検査するテスト（既存ファイルに追記、または新規）

## 禁止事項

- `connect-src` に `https:` や `*` を書いて全許可にしない。**必要なホストだけを列挙する**
- `default-src` を緩めない
- `frame-ancestors 'none'` / `object-src 'none'` / `base-uri 'self'` / `form-action 'self'` を触らない
- `script-src` の `'unsafe-eval'` の条件分岐（支援ウィジェットのある市場だけ）を変えない
- GA4 の測定 ID やイベント定義（`src/assets/lp.js` / `src/templates/layout.js`）を触らない。**本タスクは CSP だけ**
- 🔒 個人識別情報を GA4 へ送る変更を一切入れない（`.claude/rules/pergram-compliance.md`）

## 完了条件

1. `npm test` が通る
2. CSP を検査するテストが、`connect-src` に以下すべてを含むことを確認して通る
   - `https://analytics.google.com`（🔒 これが今回の本丸）
   - `https://www.google-analytics.com`
   - `https://*.analytics.google.com`
   - `https://*.google-analytics.com`
   - `https://*.googletagmanager.com`
   - `https://www.google.com`
   - `https://www.googleadservices.com`
   - `https://*.g.doubleclick.net`
3. `script-src` に `https://static.cloudflareinsights.com` を含む
4. `npm run build` が成功し、`dist/_headers` の `Content-Security-Policy` に上記が反映されている
   （`grep 'Content-Security-Policy' dist/_headers` の実出力を証拠に貼る）
5. 支援ウィジェット（Codoc）のオリジンが従来どおり `script-src` / `style-src` / `connect-src` に
   付き、`supportOrigin` が null の市場では付かないことを検査するテストが通る（既存の挙動の回帰）

## 判断してよい範囲

- テストを既存ファイルに足すか新規ファイルにするか
- ホストの列挙順、`withOrigin` を使うか定数配列にするかなどの実装の書き方
- ワイルドカード（`https://*.google-analytics.com`）と完全一致を併記するか、
  ワイルドカードだけにするか。**ただし `analytics.google.com` 自体はワイルドカードに
  マッチしないため、完全一致で必ず書くこと**

## テスト方法

- 単体: `npm test`（実装担当が書く）
- 受け入れ: 完了条件 2〜5 を機械的に検査する
- **実環境確認は本タスクの完了条件に含めない。** デプロイは PO の操作であり、
  デプロイ後の実測（ヘッドレス Chrome でコンソールに CSP 違反が出ないこと、
  GA4 のリアルタイムに反映されること）は**オーケストレーターが別途行う**

## 停止条件

- 共通の停止条件に加えて、**CSP を緩める以外の方法（GA4 の送信先を変える等）が
  必要だと判明した場合**は止めて報告する

## 参照すべき成果物

- `src/build/headers.js` — CSP の唯一の出所
- `.claude/rules/pergram-code-invariants.md` L37 — headers.js の不変条件
- `docs/ops/google-ads-first-campaign.md` L481 —「CSP許可リストの出所は src/build/headers.js」
- `src/templates/layout.js` L52-62 — GA4 の初期化スニペット（触らない。参照のみ）

## 連絡してよい相手

無し（すべてオーケストレーター経由）

## 申し送り

**原因はオーケストレーターがヘッドレス Chrome で実測して特定済み。調査からやり直さないこと。**

実測で得られたブロック済みホストの全量（`connect-src` 違反 17 件 / `script-src` 違反 3 件）:

```
analytics.google.com          ← GA4 の page_view。これが致命傷
www.google.com
google.com
www.googleadservices.com
googleads.g.doubleclick.net
stats.g.doubleclick.net
ad.doubleclick.net
static.cloudflareinsights.com ← script-src 側
```

実際に出ていたコンソールエラー:

```
Connecting to 'https://analytics.google.com/g/collect?v=2&tid=G-VNXR1NWKDR
  ...&en=page_view&...' violates the following Content Security Policy
directive: "connect-src 'self' https://www.google-analytics.com https://codoc.jp".
The action has been blocked.
```

補足: `img-src` は既に `https:` を許可しているため、ピクセル経由で送られる
一部の広告タグ（`www.google.com/ccm/collect` / `rmkt/collect`）は実際に発火していた。
**落ちていたのは fetch / beacon 経由のものだけ**である。この非対称性が
「広告タグは動いているのに GA4 だけ空」という紛らわしい状態を作っていた。

## 成果物

（完了時に記入）

## ブロッカー

無し

## 決定ログ

特記なし

## 作業ログ

- 2026-09-07 受け入れテストを `tests/headers.test.js` へ追記（既存ファイルに集約。CSP の不変条件が
  1ファイルに揃っているほうが、外部オリジンを足すときに発火させ忘れないため）。
  完了条件 2 / 3 / 5 に対応する4本と、禁止事項（全許可で塞がない）を守らせる guard 1本、
  `dist/_headers` の値が CSP 本体と一致することを見る1本を追加した。
- 2026-09-07 `npm test` を実行。337 件中 2 件が落ち、落ちたのは新規の受け入れテストのみ。
  既存 335 件は全て通過（TDD の RED として正しい状態）。
- 実装（`src/build/headers.js`）には一切触れていない。

## 証拠

### RED の確認（実装前）

```
$ npm test
not ok 104 - 🔒 connect-src が GA4 と広告タグの送信先をすべて許可する
not ok 106 - 🔒 script-src が Cloudflare Insights のビーコンを許可する
# tests 337
# pass 335
# fail 2
```

落ちている理由（`node --test tests/headers.test.js` の error 行）:

```
error: 'connect-src に https://analytics.google.com がありません（supportOrigin=null）'
error: 'script-src に https://static.cloudflareinsights.com がありません（supportOrigin=null）'
```

### 完了条件との対応

| 完了条件 | 検査するテスト（`tests/headers.test.js`） | 現時点 |
| --- | --- | --- |
| 2. connect-src に8ホスト | `🔒 connect-src が GA4 と広告タグの送信先をすべて許可する` | **落ちる（正しい）** |
| 3. script-src に Cloudflare Insights | `🔒 script-src が Cloudflare Insights のビーコンを許可する` | **落ちる（正しい）** |
| 5. 支援ウィジェットの出し分けの回帰 | `🔒 回帰: 支援ウィジェットのオリジンは3経路に付き、出さない市場には付かない`／既存の3本 | 通る（回帰の基準線） |
| 禁止事項（全許可禁止） | `🔒 connect-src をスキーム全許可・ワイルドカード単独で塞がない` | 通る（実装後も通ること） |
| 4. dist/_headers への反映 | `🔒 _headers の Content-Security-Policy が CSP 本体と一致する`（乖離の検出のみ） | 通る。**実出力の証拠は実装担当が貼る** |

判定方法の注記: 部分一致（`String.prototype.includes`）ではなく、ディレクティブを空白で
分割したトークンの完全一致で判定している。部分一致だと
`https://analytics.google.com.evil.test` のような別ホストでも通ってしまうため。
また `https://*.analytics.google.com` は1ラベルしか埋めず `analytics.google.com` 自身に
マッチしないので、完全一致で別に列挙されているかを見ている。

完了条件 1（`npm test` 全体が通る）と 4（`npm run build` の実出力）は実装後に満たされる。
本段階では未達で正しい。

## 組織への指摘

- 無し
