# T-072: 同じ人がアンケートとメールに答えると waitlist が2行に分かれ、回答が二重に数えられる

## メタデータ
- 作成日: 2026-09-09
- 更新日: 2026-09-10
- 状態: 完了
- 優先度: 高
- 担当: 未割当
- 進捗: **完了（2026-09-10）。** 受け入れ判定・レビューともに合格。main へマージ済み。**push（=本番反映）は PO 判断待ち**
- 依存: T-071（ブロッカー: T-071 が作った7列の `waitlist` が前提。T-071 は完了・本番へ反映済み）
- TDD適用可否: 適用
- 手戻り回数: 2

## 目的

**PO 指摘（2026-09-09）:**

> 同じセッションで成分アンケートの内容を送ったあとにメールアドレスを送ったらレコード2つに
> 分かれるのは流石にだめでしょ。同じ人がアンケートとメールアドレス回答してんのに
> レコード分かれるのがOKなわけなくない？

**指摘は正しい。T-071 の設計判断（オーケストレーター）が誤っていた。**

いまこうなっている。

| 利用者の行動 | 書かれる行 |
| --- | --- |
| アンケートに答えて送信 | `waitlist` に1行（`id` のみ / **回答4項目**） |
| 続けてメールアドレスを登録 | `waitlist` に**もう1行**（`email` のみ / **同じ回答4項目**） |

**2行に分かれるだけでなく、同じ回答が両方の行に入る。**`worker/waitlist.js` も回答4項目を書くためである。
つまり「クレアチンを見たい人」を数えると**同じ人を2回数える**。
**`docs/research/validation-plan.md` の判定に直接効く。**次に何を作るかの判断を誤る。

## 何を読み違えたのか — 次の担当が同じ結論に戻らないために

分けた理由は「`waitlist.id` と `email` が同じ行に載ると、`request_signal` の匿名の押下が
すべてメールアドレスへ紐づく」ことだった。**この懸念自体は正しい。**
🔒 **紐づけてよいという話には、この修正でもならない。**

**誤っていたのは「だから行を分ける」という結論のほうである。両立する形が在った。**

> **メールアドレスが送られた時点で、匿名の行に `email` を書き込み、同時に `id` を捨てる。**

- 行は**1つ**になる。回答は最初に答えたものが残る
- 残るのは `email` だけ。**`id` は消えるので、`request_signal` と突き合わせる鍵はどこにも保存されない**
- `CHECK (id IS NULL OR email IS NULL)` はそのまま効く（`id` を消してから `email` を入れる順序になるため）

**T-071 には「両立しないと判断したら止めて報告」という停止条件（S-7）が置いてあった。
両立する形を探しきらずに2行で確定させたのは、オーケストレーターの判断ミスである。**

## 変更範囲

- `worker/waitlist.js` — 匿名の行を引き取って1行へまとめる
- `src/assets/request.js` — メールの段の送信に、そのブラウザの匿名の識別子を添える
- `tests/` — 受け入れテストと単体テスト
- `.claude/rules/pergram-compliance.md` — 「データ保護」の記述の更新（**まとめる時に `id` を捨てること**）
- `worker/schema.sql` — 冒頭コメントの失効した記述の更新（**オーケストレーターが 2026-09-09 に追加**。決定ログ参照）
- `worker/request_survey.js` — まとめた後に匿名の行が作り直される経路の是正（**オーケストレーターが 2026-09-09 に追加**。R-1 / D-1）

## 禁止事項

- 🔒 **`id` と `email` を同じ行に保存しない。CHECK 制約を外さない。**
  まとめるときは**必ず `id` を捨てる**。「1行にするために CHECK を外す」は解ではない
- 🔒 **`request_signal` の3列に手を触れない**
- 🔒 IP・User-Agent・リファラ・ページ内の位置を保存しない
- **移行SQL を書かない。**テーブルの形は T-071 のままで足りる
- **既存の回答を消さない。**まとめるときに、匿名の行の回答が失われないこと
- 自由記述の中身を解釈して弾かない。サーバがやるのは長さで切ることだけ
- 帯の見た目（T-061 で確定）を触らない

## 完了条件

- A-1 **アンケート → メールの順に送ると、`waitlist` は1行になる。**`email` を持ち、`id` は NULL
- A-2 その1行に**アンケートの回答が残っている**（空で上書きされない）
- A-3 **`id` と `email` を両方持つ行は、どの経路でも作られない**（CHECK は外さない）
- A-4 メールだけを送った人（アンケート未回答）の経路が壊れていない
- A-5 アンケートだけを送った人の行が、そのまま `id` で残る
- A-6 同じメールアドレスで2回目を送っても行が増えない（既存の上書きが効く）
- A-7 **匿名の識別子を持たないブラウザからのメール送信でも 400 にならない**（識別子は任意）
- C-1 `npm test` が通る（**`tests/hero_ranking.test.js` の2件は着手時点で既に赤**。T-049）
- C-2 `npm run build` が成功する

## 判断してよい範囲

- まとめ方（匿名の行を UPDATE して `email` を入れ `id` を NULL にする / 回答を移してから匿名の行を消す）
- 匿名の識別子を `/api/waitlist` の本文へどう載せるか（キー集合の検査を通る形にすること）
- 既に本番にできてしまった重複行を統合する SQL を書くか（**書くなら PO が流す。自分では流せない**）
- テストの置き場

## テスト方法

- 単体・受け入れ: `npm test`
- 実環境確認: **必要。ただし PO が行う**（`npx wrangler d1 execute` は deny）

## 停止条件

- **S-7（このタスク固有）**: 「1行にまとめる」と「`id` を保存しない」が両立しないと判断した場合。
  **両立する形は上に書いてある。**それでも両立しないという結論に至ったら、必ず止めて報告すること
- 共通の停止条件（`.claude/rules/org-escalation.md`）

## 参照すべき成果物

- `worker/waitlist.js` / `worker/request_survey.js` / `worker/schema.sql`
- `src/assets/request.js` の `pergram.request_signal_id` の扱い（T-062）
- `docs/tasks/T-071.md` の決定ログ2件 — **何を守るために分けたのか**
- `docs/tasks/T-070.md` の決定ログ — 出荷箱（送りきれなかった回答の控え）

## 連絡してよい相手

**無し（すべてオーケストレーター経由）。**この実行環境ではエージェントに名前を付けられない（I-006）。

## 申し送り

1. **どのブランチへ push しても本番へ出る**（Deploy command が `npx wrangler deploy`）。
   ⚠️ ただし**このタスクは移行を伴わない**ので、T-070 / T-071 のような順序の危険は無い
2. **本番には既に重複行ができている可能性がある**（アンケートとメールの両方に答えた人の分）。
   統合の SQL を書くかは `## 判断してよい範囲` に入れてある。**流せるのは PO だけ**
3. **出荷箱（`pergram.request_survey_outbox`）の契約を壊さないこと。**
   `/api/request-survey` のパスと本文の形を変えると、控えられた回答が永久に送れなくなる
4. **着手時点で `tests/hero_ranking.test.js` の2件が既に赤である**（T-049）。自分の失敗と読まないこと

## 成果物

- `worker/waitlist.js` — 匿名の行を引き取って1行へまとめる（照会 → `DELETE` → `UPSERT` を `batch()` で）
- `src/assets/request.js` — メールの段の送信に匿名の識別子（`signal_id`）を添える
- `tests/waitlist_merge_unit.test.js`（新規・単体2件）— 受け入れテストが通っていない2経路
  （メールの行が先にある場合の UNIQUE 衝突 / 匿名の行の照会が失敗した場合）
- `.claude/rules/pergram-compliance.md` — 「データ保護」を、まとめる時に `id` を捨てる記述へ更新
- コミット: `feat(T-072): 匿名の行とメールの行を1行へまとめる（まとめる時に id を捨てる）`

## ブロッカー

無し

## 決定ログ

### 2026-09-10 引き取り済みでも回答は控えに残す。打ち切りは控えを書いた後ろに置く

- 対象: アンケートの保存 / まとめた後の再送
- 決定: `sendRequestSurvey()` の `if (isMerged()) return;` を、**控えを書いた後ろへ動かす**。
  匿名の受け口へは送らないまま、回答は控え（`pergram.request_survey_outbox`）に残り、
  次に待機リストの登録が成功したときに `/api/waitlist` の本文へ相乗りする。
  あわせて `flushRequestSurveyOutbox()` は、引き取り済みのとき**控えを消さずに送信だけ止める**
- 経緯: **D-2 / m-5（Major）。**打ち切りが控えを書く手前にあったため、先にメールアドレスだけを
  登録した人が**あとから初めて答えた回答**が、送信も控えも無いまま消えていた。
  失われるのが「答え直し」ではなく**その人の最初の回答**である
- 却下案: 引き取り済みの回答は捨てる（修正前の挙動）→ **T-070 が直した壊れ方に戻る。**
  自分の決定ログ（2026-09-09「引き取り済みの控えは捨てず、送り先を移す」）が置いた原則が、
  引き取り済みの後の回答にだけ適用されていなかった
- 🔒 **打ち切りを控えの手前へ戻さないこと。** 戻すと D-2 がそのまま再発する
- 出典: `src/assets/request.js` の `sendRequestSurvey()` /
  `tests/waitlist_merge_unit.test.js`「D-2 引き取り済みのブラウザが後から答えた回答も…」

### 2026-09-10 登録が一度も成功しなければ、その回答は手元に残ったまま届かない（受容）

- 対象: アンケートの保存 / まとめた後の再送
- 決定: 引き取り済みのブラウザが答えた回答は、**そのブラウザが再び待機リストの登録に
  成功するまでサーバへ届かない。**一度も成功しなければ永久に届かない。**これを受容する**
- 却下案: 匿名の受け口へ送る → 匿名の行が作り直され、同じ人が2行に戻る（D-1 の再発）
- 却下案: サーバ側で「引き取り済みの識別子」を持って重複を弾く → **引き取り済みの識別子を
  保存することになり、匿名の押下とメールアドレスの紐づけが復活する。**T-072 の目的を裏切る
- 🔒 **サーバに鍵を持たせずに届ける手立てが無い。**これは「気づかず放置した」のではなく
  **記録の上で受容した**穴である。悪影響が実測されたら再検討する
- 出典: セッション内の判断、成果物なし

### 2026-09-10 受容した窓の説明を訂正する（「一時的に2行」ではない）

- 対象: アンケートの保存 / まとめた後の再送
- 決定: **受容そのものは覆さない**（オーケストレーター判断）。**記述だけを事実へ訂正する。**
  - **次の訪問では解消しない。** 引き取りの印があるのでブラウザは何も送らない。
    解消するのは**同じ人がもう一度メールアドレスを登録したとき**だけである
  - 窓は控えの送り直しに限らない。**初回の通し（押下 → アンケート → メール）でも開く** ——
    アンケートの送信の応答を待たずに次の段が開くためである
- 経緯: **m-4（Minor / 記録の訂正）。**受け入れ判定の実測が、前の記述と食い違った。
  「一時的」と読んだ次の担当が「放っておけば直る」と考えるのを防ぐ
- 上書き対象: T-072 2026-09-09（同一ページ内の送信中のアンケートと引き取りが競合する窓は、
  受容する）
- 出典: 受け入れ判定の実測（証拠「再判定」節）

### 2026-09-09 D-1 の修正はブラウザ側だけで閉じる。受け口の契約は1文字も変えない

- 対象: アンケートの保存 / まとめた後の再送
- 決定: 「まとめた後に匿名の行が作り直される」（D-1 / R-1）は、**`src/assets/request.js` だけ**で直す。
  引き取りが済んだ印を localStorage に置き（`pergram.request_merged`）、印がある間は
  匿名の受け口へ**何も送らない**（控えの送り直しも、答え直しも）。
  `worker/request_survey.js` は変更しない
- 却下案: 受け口の側で「この識別子はもう引き取り済みだ」と判定する →
  🔒 **設計上できない。** 引き取りのときに識別子を捨てるのが T-072 の目的そのもので、
  サーバに判定させるには**引き取り済みの識別子を保存する**ことになる。
  その瞬間に匿名の押下とメールアドレスの紐づけが復活し、修正が目的を裏切る
- 却下案: `/api/request-survey` の本文へ「引き取り済みか」を足す → **出荷箱の契約が壊れる。**
  T-070 の控え（`pergram.request_survey_outbox`）は**旧い形の本文のまま**利用者のブラウザに
  残っており、受け口はキー集合ちょうど一致でしか受け取らない。**キーを1つ足した時点で、
  控えられた回答は永久に 400 を貰い続けて失われる**（`## 申し送り` 3）。
  やむを得ず変えるなら、旧い形も受ける期間を設ける設計が別途要る。**今回は変えていない**
- 付帯条件: パスも本文の形も変えていないので、控えられた回答は今までどおり送れる
- 出典: `src/assets/request.js` の `MERGED_STORAGE_KEY` / `tests/waitlist_merge_unit.test.js` の D-1 節

### 2026-09-09 引き取り済みの控えは捨てず、送り先を `/api/waitlist` へ移す

- 対象: アンケートの保存 / まとめた後の再送
- 決定: メールの段を送るとき、このセッションで答えていなければ**控えの回答を送信本文へ載せる**。
  登録が成功したときにだけ控えを消す
- 経緯: 控えを残せば次の訪問で匿名の行が作り直される（D-1）。かといって黙って捨てると
  **前の訪問の回答が失われる** —— T-070 が直した壊れ方そのものに戻る。
  **送り先を移すことが、どちらも起こさない唯一の形である**
- 却下案: 引き取り済みなら控えを黙って捨てる → 別セッションで控えだけが残っている人
  （そのセッションでは回答が手元に無い）の回答が消える。`tests/waitlist_merge_unit.test.js` の
  「控えが前の訪問のものしか無くても、回答は待機リストの行へ載って失われない」が見張る
- 却下案: 失敗しても控えを消す → 回答が匿名の受け口にもメールの行にも届かないまま消える
- 出典: `src/assets/request.js`（メールの段の送信本文の組み立て）

### 2026-09-09 m-2 / R-5（任意の UUID で他人の匿名の行を奪える）は、このタスクでは受容する

- 対象: アンケートの保存 / 受け口の検証
- 決定: `/api/waitlist` に他人の識別子を `signal_id` として送れば、その匿名の行を消して
  回答を自分の行へ移せる。**このタスクでは直さない**（オーケストレーター判断、2026-09-09）
- 経緯: 実害が小さい。**攻撃者は狙う相手の UUID v4 を知る必要があり**、当てずっぽうでは当たらない。
  奪えるのは「見たい成分の選択と自由記述」だけで、メールアドレスも閲覧履歴も含まない。
  影響は集計の1行が動くことに留まる
- 却下案: 押下の受領（`request_signal`）と突き合わせて本人性を確かめる → **本人性を確かめる鍵を
  サーバに持つことになり、匿名性の設計と衝突する。** 直すなら別の設計判断として起票する
- 🔒 **「受容した」という記録であって、正しいという記録ではない。** 悪用の実例が出たら再検討する
- 出典: セッション内の判断、成果物なし

### 2026-09-09 同一ページ内の送信中のアンケートと引き取りが競合する窓は、受容する

- 対象: アンケートの保存 / まとめた後の再送
- 決定: **控えの送り直しがまだ応答を待っている間**にメールアドレスの登録が済むと、
  遅れて届いた匿名の回答が行を作り、一時的に2行になる。**この窓は塞がない**
- 経緯: 送り直しはページの読み込み時に走り、メールの登録はその数秒後の操作である。
  重なるのは、送り直しの応答が数秒返らず、かつ同じ表示の中でメールを登録した場合だけ
- 却下案: サーバ側で後から来た匿名の回答を弾く → **引き取り済みの識別子を保存することになり、
  匿名の押下とメールアドレスの紐づけが復活する**（上の決定と同じ理由で採れない）
- 却下案: 送り直しの応答を待ってからメールの段を出す → 保存の都合で利用者の操作を待たせる。
  この経路の設計方針（保存は計測の都合であって利用者の用ではない）と逆向き
- 出典: セッション内の判断、成果物なし

### 2026-09-09 まとめ方は「匿名の行を消してから、メールアドレスの行へ書く」

- 対象: アンケートの保存 / waitlist の受け口
- 決定: `/api/waitlist` は、識別子付きの送信を受けたら
  (1) 匿名の行を `SELECT` して回答と `created_at` を引き取り、
  (2) `DELETE FROM waitlist WHERE id = ?` と既存の `INSERT … ON CONFLICT(email)` を
  **`env.DB.batch()` の1トランザクションで**流す。**`id` 列へは一切書かない。**
- 却下案: `UPDATE waitlist SET email = ?, id = NULL WHERE id = ?` の1文で済ませる →
  **先にメールアドレスだけ登録していた人**（`email` の行が既にある）で
  `email TEXT UNIQUE` に当たって落ちる。回答の突き合わせも別途要る。
  この経路は受け入れテストに無かったので `tests/waitlist_merge_unit.test.js` で固定した
- 却下案: `DELETE` と `INSERT` を逐次で流す → 消せたのに書けなかった回で回答が消える。
  D1 の `batch()` は1トランザクションなので、原子性が無料で手に入る
- 付帯条件: **順序を入れ替えないこと。**「書いてから消す」にすると上の UNIQUE 衝突が戻る
- 出典: `worker/waitlist.js`（`handleWaitlist` の `if (carried)` 節のコメント）

### 2026-09-09 送信本文に載せる識別子のキー名は `signal_id`。保存列の `id` と同じ名前にしない

- 対象: アンケートの保存 / waitlist の受け口
- 決定: メールの段が添える匿名の識別子は、本文では **`signal_id`** という名前で運ぶ
  （`worker/waitlist.js` の `SIGNAL_ID_KEY`）。保存列 `id` とは別の名前にする
- 却下案: 本文でも `id` にする（保存列と同じ名前）→ **受け取った値をそのまま `id` 列へ
  入れる実装へ自然に手が伸びる。** その瞬間に匿名の押下がすべてメールアドレスへ紐づく。
  名前が違うことが「これは保存する値ではない」という手掛かりになる
- 🔒 **「保存列と揃っていないから」と `id` へ改名しないこと。** 揃っていないのが目的である
- 出典: `worker/waitlist.js` の `SIGNAL_ID_KEY` のコメント / `src/assets/request.js`

### 2026-09-09 まとめるときの回答は「送られてきた値が空なら、匿名の行の回答で埋める」

- 対象: アンケートの保存 / waitlist の受け口
- 決定: 引き取った回答は**既定値として**使う。送信本文に回答があればそちらを採り、
  空のときだけ匿名の行の値を入れる。`created_at` だけは**引き取った行の値をそのまま使う**
  （最初に答えた日時を後ろへずらさない。`worker/request_survey.js` と同じ理由）
- 経緯: T-072 本文の「回答は最初に答えたものが残る」は**回答が失われないこと**を
  言っており、優先順位の指定ではない。既存の `ON CONFLICT` の向き
  （「送られてきた値が空なら、今ある値を残す」）と揃えた
- 却下案: 匿名の行の回答を常に優先する → 同じ送信で回答を後から書き換えられなくなる。
  既存の上書き規則と向きが逆になり、経路によって挙動が変わる
- 出典: `worker/waitlist.js` / `tests/waitlist_merge.test.js` の A-2 群

### 2026-09-09 既にできている重複行を統合する SQL は書かない

- 対象: アンケートの保存 / 本番データ
- 決定: 本番に既にできている重複行（同じ人の匿名の行とメールの行）を統合する SQL は、
  このタスクでは**書かない。**修正は今後の登録にだけ効く
- 経緯: `## 判断してよい範囲` に「書くか」が入っていたので、書かない側を選んだ
- 却下案: 統合 SQL を用意して PO に流してもらう → **既存データを消す操作であり
  （共通の停止条件 S-2）、実装担当は `npx wrangler d1 execute` が deny のため
  検証できない。** 検証していない破壊的な SQL を本番へ渡すことになる。
  「どの匿名の行がどのメールの人か」を後から突き合わせる鍵は
  **設計上どこにも保存されていない**ので、機械的に安全な統合はそもそも書けない
  （突き合わせられるなら、それ自体が匿名性を壊している）
- 付帯条件: **必要なら別タスクとして起票し、突き合わせ方を PO 判断で決めること。**
  件数の把握（`SELECT` だけ）なら破壊的でないので先に切り出せる
- 出典: セッション内の判断、成果物なし

### 2026-09-09 匿名の行の照会が失敗しても、メールアドレスの登録は 200 で通す

- 対象: アンケートの保存 / waitlist の受け口
- 決定: 引き取る行の `SELECT` が落ちたら、ログだけ残して**まとめずに登録を続ける**
  （`findAnonymousRow` が例外を握って null を返す）。行が2つ残るのは劣化として許す
- 却下案: 書き込みと同じく 503 を返す → **まとめは記録の都合、登録は利用者の用である。**
  記録の都合で利用者が待機リストに載れなくなるほうが害が大きい
- 🔒 **「一貫性がないから」と 503 へ揃えないこと。** 意図的に非対称にしてある
- 出典: `worker/waitlist.js` の `findAnonymousRow` / `tests/waitlist_merge_unit.test.js`

### 2026-09-09 匿名の行とメールの行は1行へまとめる。まとめる時に匿名の識別子を捨てる
- 対象: アンケートの保存 / データ保護
- 決定: メールアドレスが送られた時点で、**匿名の行へ `email` を書き込み、同時に `id` を捨てる。**
  行は1つになり、保存されるのは `email` だけになる
- 経緯: **PO 指摘。**T-071 の「2行に分かれる」設計が、同じ人の回答を二重に数える
- 却下案: 2行のまま残す（T-071 の設計）→ **同じ回答が両方の行に入り、集計の分母が壊れる。**
  `docs/research/validation-plan.md` の判定に直接効く
- 却下案: CHECK を外して `id` と `email` を同居させる → **`request_signal` の匿名の押下が
  すべてメールアドレスへ紐づく。**`request_signal` が3列しか持たない理由が丸ごと崩れる
- 上書き対象: T-071 2026-09-08（匿名の識別子とメールアドレスを、同じ行に置かない）
  ⚠️ **「同じ行に置かない」という結論そのものは生きている。**失効するのは
  「だから行を2つに分ける」という部分だけである
- 出典: セッション内の PO 指摘

### 2026-09-09 受け入れテストは、識別子を載せるキーの名前を決め打ちしない
- 対象: アンケートの保存 / 受け入れテスト
- 決定: `tests/waitlist_merge.test.js` は、**ブラウザ（`src/assets/request.js`）を1度走らせて、
  匿名の識別子の値が載っているキーを見つけてから**（`IDENTIFIER_KEY`）サーバ側の検査に使う。
  キー名をテスト側で固定しない。見つからない間（未実装の間）は `id` を仮に使う
- 経緯: T-072 `## 判断してよい範囲` に「匿名の識別子を `/api/waitlist` の本文へどう載せるか」が
  入っている。テストがキー名を決め打ちすると、**完了条件ではなく実装手段のほうを縛る**ことになる
- 却下案: 本文のキーを `id` に固定する → 実装が `signal_id` などを選んだ瞬間に、
  正しい実装が受け入れテストで落ちる（偽の Blocker）
- ⚠️ **この仕掛けを「回りくどいから」と定数へ畳まないこと。** 畳むと上の縛りが戻る
- 出典: tests/waitlist_merge.test.js の `detectIdentifierKey()`

### 2026-09-09 「待機リストへ送る本文に id を混ぜない」検査を2件、緩めた
- 対象: アンケートの保存 / 受け入れテスト
- 決定: `tests/request_survey.test.js`（A-2 メールアドレスの段…）と
  `tests/request_flow_behavior.test.js`（🔒 送信本文は保存してよい列の範囲だけ）の
  **キー集合の検査を、「匿名の識別子だけは載ってよい」形へ緩めた。**
  保存列の外の項目（年齢・体調など）を弾く役目はそのまま残してある
- 経緯: この2件は T-071 の「行を2つに分ける」設計を前提に書かれており、**その前提が
  T-072 の PO 指摘で失効した。** 残すと、正しい実装が既存テストで落ちる。
  実装担当がテストを書き換える形になるのを避け、テスト担当の側で先に直した
- 却下案: そのまま残して実装担当に任せる → 「落ちているテストを通すためにテストを書き換える」
  形になり、規約上も判別がつかなくなる
- 却下案: 検査ごと削除する → 保存列の外の項目を送らせない見張りまで失う
- 🔒 **守るべき線は変わっていない。「`id` と `email` を同じ行に保存しない」である。**
  送信本文に識別子が載ることと、保存された1行が両方を持つことは別の話であり、
  後者は DB の CHECK 制約と `tests/waitlist_merge.test.js` の A-3 群が見張る。
  **「🔒 が消えている」と見て元へ戻さないこと**
- 上書き対象: T-071 2026-09-08（匿名の識別子とメールアドレスを、同じ行に置かない）のうち、
  **送信本文に関する部分だけ。**「同じ行に保存しない」という結論そのものは生きている
- 出典: tests/request_survey.test.js / tests/request_flow_behavior.test.js の該当箇所のコメント

## 作業ログ

- 2026-09-09 オーケストレーターが PO 指摘を受けて起票。**PO 判断で次セッションへ回した**（当セッションのコストが $102 に達したため）
- 2026-09-09 `org-test-T-072` が受け入れテストを作成（TDD の RED）。
  `tests/waitlist_merge.test.js` を新設し、完了条件 A-1〜A-7 を23件のテストへ変換。
  **23件中8件が落ちる**（A-1 の4件 / A-2 の3件 / A-6 の1件 = まとめる処理が未実装だから）。
  残る15件（A-3 / A-4 / A-5 / A-6 の一部 / A-7）は**回帰の見張り**で、着手時点から緑である
- 2026-09-09 上流の設計変更で失効した既存テスト2件を、テスト担当の判断で緩めた（決定ログ参照）。
  `tests/request_survey.test.js` / `tests/request_flow_behavior.test.js`。どちらも緩めた後も緑
- 2026-09-09 `npm test` 全体は 599件中10件が赤。内訳は**新規の受け入れテスト8件**と、
  **着手時点から赤の `tests/hero_ranking.test.js` の2件**（T-049。このタスクとは無関係）
- 2026-09-09 `ecc:e2e-testing` を呼んだが、中身が Playwright 専用だった。
  このリポジトリは**依存パッケージ0**で、ブラウザ相当の実行は `tests/mini_dom.js` の
  `runLpScript()` が担う。**依存を足さないため採用せず**、既存の作法に合わせた
  （待ちは `dom.flush()` で決定的。時間待ちは使っていない）
- 2026-09-09 `org-implementation-T-072` が着手。着手時の `npm test` で RED を確認
  （599件中10件が赤 = 受け入れテスト8件 + T-049 の2件）
- 2026-09-09 受け入れテストが通っていない2経路を先に単体テストへ落とした
  （`tests/waitlist_merge_unit.test.js`）。**1件目は RED**（メールの行が先にあると2行のまま）、
  2件目は照会そのものが存在しないため素通りで緑だった。後述の red-green の確認で意味を持たせた
- 2026-09-09 `worker/waitlist.js` にまとめる処理を実装（照会 → `DELETE` → `UPSERT` を `batch()`）。
  `src/assets/request.js` のメールの段が `signal_id` を添えるようにした
- 2026-09-09 `.claude/rules/pergram-compliance.md` の「データ保護」を更新。
  T-071 の「2行に分かれて残るのが正しい姿」が失効したことと、まとめる時に `id` を捨てることを書いた
- 2026-09-09 ⚠️ **`worker/schema.sql` の冒頭コメントに、失効した記述がそのまま残っている**
  （「同じ人が2行に分かれて残るのが正しい姿。**「重複しているから」とまとめないこと。**」）。
  **`## 変更範囲` に無いので直していない。**オーケストレーターへ報告する
- 2026-09-09 オーケストレーターが `## 変更範囲` に `worker/schema.sql` を追加（この1件だけ）。
  冒頭コメントの失効した記述を直した。**DDL は1文字も変えていない**（差分はコメント行のみ）。
  T-071 の「2行に分かれて残るのが正しい姿」を失効として `⚠️` で残し、
  まとめる時に `id` を捨てること・`signal_id` は保存されないことを書き足した。
  `npm test` は 601件中599件緑（赤は T-049 の2件のみ）

- 2026-09-09 `org-review-T-072` がレビュー。**判定は要修正（Major 1件）。往復1回目。**
  🔒 の線は4つとも守られている（確認したもの: `worker/schema.sql` の DDL 差分ゼロで
  `CHECK (id IS NULL OR email IS NULL)` が健在 / `/api/waitlist` の INSERT の列に `id` が無く、
  `id` に触るのは `DELETE` だけ / `signal_id` はどの列にも保存されずログにも出さない /
  `request_signal` の3列と `worker/request_signal.js` は無変更 / 自由記述は長さで切るだけ /
  GA4 の `track()` は `{}` でメールアドレスも自由記述も送っていない）。
  受け入れテストが緩めた2件も、**値が識別子と一致するキーだけ**を通す形で、
  保存列の外の項目を弾く役目は残っている（妥当）。
  `npm test` は 601件中599件緑（赤は T-049 の2件のみ）、`npm run build` は成功をレビュー側でも再実行して確認した
- 2026-09-09 レビューの指摘: **R-1（Major）まとめた後に、同じ識別子の匿名の行が
  もう一度作られる経路が残っている。**アンケートの送信が 5xx / 通信断で失敗すると
  出荷箱（`pergram.request_survey_outbox`）に控えが残り、**まとめが済んだ後の
  ページ読み込みで同じ識別子で送り直されて匿名の行が作り直される** ——
  同じ回答を持つ行が2つに戻り、**PO が指摘した二重計上そのものが復活する。**
  同じブラウザでアンケートへ答え直した場合も同じで、
  `tests/waitlist_merge.test.js` の A-3「どの順番・組み合わせ」の手順4が
  **まさにこの経路を通しているのに、行数を検査していないので緑のまま通る。**
  修正先が `worker/request_survey.js`（`## 変更範囲` 外）に及ぶ可能性が高く、
  **別タスク化の協議を推奨。判断はオーケストレーター。**
- 2026-09-09 レビューの指摘（Minor 4件、対応は任意）:
  R-2 `SELECT`（`findAnonymousRow`）と `batch()` の `DELETE` の間に届いた同じ識別子の
  アンケート回答は、`DELETE` で消えたうえで古い引き取り値が書かれる（窓は狭い）。
  R-3 `/api/waitlist` に本文 `null` を投げると `payload.email` の読み取りで例外になり 500
  （実測。`worker/waitlist.js` の入口。`/api/request-survey` は同じ形を弾いている）。**T-072 が作った欠陥ではない。**
  R-4 テストの D1 代替の `batch()` が逐次実行なので、**決定ログが 🔒 とした原子性を守る見張りがテストに無い**
  （`DELETE` と `INSERT` を2回の `.run()` に分けてもテストは緑のまま）。
  R-5 任意の UUID を `signal_id` に載せれば、その匿名の行を消して回答を自分の行へ移せる
  （UUID v4 は推測不能で識別子は外部へ出ないため実害はほぼ無い。記録として残す）。

- 2026-09-09 `org-test-T-072b` が受け入れ判定を実施。**判定は不合格（Major 1件 / Minor 3件）。**
  A-1〜A-7 と C-1 / C-2 は**自分で実行して**全て充足を確認した（証拠「受け入れ判定」節）。
  実装の自己申告に頼らず、リポジトリの外に置いた使い捨てのスクリプトで敵対的な経路も検査した
  （本文のキーが `id` のときサーバがそれを識別子として使わないこと / メールの行が先にある場合 /
  まとめた後の再アンケート → 再メール / 実トランザクションの `batch()` が落ちたときの巻き戻し /
  保存列の外の項目を送りつけた場合）。7件すべて期待どおり。
  🔒 の線は**コードとスキーマの実物で**確認した ——
  `CHECK (id IS NULL OR email IS NULL)` は健在で `worker/schema.sql` の非コメント行の差分はゼロ、
  `waitlist.id` へ書く経路は `worker/request_survey.js` の1つだけ、
  `request_signal` の3列と `worker/request_signal.js` は無変更、`worker/migrations/` への追加も無し
- 2026-09-09 緩めた既存テスト2件（`tests/request_survey.test.js` /
  `tests/request_flow_behavior.test.js`）の妥当性を**突然変異検査**で確かめた。
  `src/assets/request.js` の複製（**リポジトリの外に置いた複製。実装は触っていない**）に
  `payload.age = 30` を仕込んでブラウザを走らせ、緩めた述語が `age` を弾くことを確認した。
  **保存列の外の項目を弾く役目は残っている。緩和は妥当。**
  `id` と `email` の排他の線も緩んでいない（本物の SQLite に `worker/schema.sql` をそのまま流して
  検査しており、CHECK 制約の存在そのものも A-3 が見張っている）
- 2026-09-09 **不具合 D-1（Major）: まとめた後に匿名の行が作り直され、二重計上が戻る経路がある。**
  まず自分の走査で「まとめた後にもう一度アンケートへ答えると匿名の行が作り直される」ことを観測し、
  続けて**出荷箱（`pergram.request_survey_outbox`）の送り直し**の経路をブラウザごと通しで再現した。
  再現手順と実出力は証拠「D-1」節。**レビュー（`org-review-T-072`）が同じ経路を R-1 として
  先に挙げており、判定は独立に一致した（食い違い無し）。** レビューとの直接のやりとりは無い
  （このタスクの `## 連絡してよい相手` は「無し」。タスクファイルに commit されていた記録を読んだだけ）
- 2026-09-09 Minor 3件を記録（対応は任意。詳細は証拠「Minor」節）。
  m-1 テストの D1 代替の `batch()` が逐次実行で、決定ログが 🔒 とした原子性の見張りがテストに無い。
  m-2 任意の UUID を `signal_id` に載せれば他人の匿名の行を消して回答を自分の行へ移せる（実害は小）。
  m-3 緩めた述語は「値が識別子と一致するキー」を名前を問わず通すので、本文のキー名が `id` でも通る
  （決定ログ「キー名は `signal_id`。保存列の `id` と揃えない」を見張るテストが無い）
- 2026-09-09 **差し戻し（往復1回目）を受けて D-1 / R-1 を修正**（`org-implementation-T-072`）。
  オーケストレーターが `## 変更範囲` に `worker/request_survey.js` を足したが、**使っていない** ——
  受け口の側では原理的に閉じられないため（決定ログ参照）。直したのは
  `src/assets/request.js` だけで、`worker/waitlist.js` は R-3（`null` 本文で 500）の1行だけ触った
- 2026-09-09 回帰を `tests/waitlist_merge_unit.test.js` に4件足した（D-1 の4経路）。
  **4件とも red-green を確認した** —— 直しを一時的に外すと落ち、戻すと通ることを実測している
  （証拠「D-1 の修正」節）。`tests/waitlist_merge.test.js` は指示どおり触っていない
- 2026-09-09 記録のみ（直していない）: m-2 / R-5 は受容（決定ログに理由）。
  m-1 / R-4 はテスト担当へ。**m-3（本文のキー名を見張るテストが無い）はテスト担当の領分**なので
  こちらでは足していない —— ただし単体テストが `signal_id` を直に使うので、名前が変われば落ちる

- 2026-09-09 `org-review-T-072` が**再レビュー**（往復1回目の戻り。修正コミット `1366a04`）。
  **判定は要修正（Major 1件）。R-1 は閉じている。**
  確認したこと: 引き取り済みの印は**待機リストの登録が成功したときにだけ**立ち
  （`.then()` の中）、中身は識別子か `1` だけで**メールアドレスは入らない**（実測で確認）。
  印がある間は `sendRequestSurvey` も控えの送り直しも匿名の受け口へ送らない。
  登録が失敗し続けた場合は印も立たず控えも消えないので、次の訪問で匿名の受け口へ届く
  （回答は失われない）。送信本文は `email` + 回答4項目 + `signal_id` のままで、
  控えの `id` は載っていない。`worker/waitlist.js` は R-3 の門（`null` / 配列を 400）だけ。
  DDL 無変更。`npm test` は 607件中605件緑（赤は T-049 の2件）。
  受け口の側では原理的に閉じられない（引き取り済み識別子の保存 = 紐づけの復活）という
  判断は妥当で、出荷箱の契約を理由に `/api/request-survey` の本文を変えなかったのも正しい。
  受容した窓（送り直しの応答待ち中の登録で一時的に2行）と m-2 / R-5 の受容は、
  どちらも決定ログに理由付きで記録されていることを確認した
- 2026-09-09 再レビューの指摘: **D-2（Major）引き取り済みのブラウザが後から答えた回答は、
  どこにも保存されない。**印が立った後は `sendRequestSurvey` が
  **控えを書く前に**打ち切るため（`src/assets/request.js`）、その回の回答は
  匿名の受け口へも控えへも行かない。同じ表示の中でメールアドレスを送り直せば
  `/api/waitlist` へ相乗りするが、**送らずに離脱すると1文字も残らない。**
  実測で確認した（訪問1でアンケートを飛ばしてメール登録 → 訪問2で初めて回答 →
  送信先は0件、控えも空）。**アンケートを飛ばして登録した人が、後から初めて答えた
  回答**がこれに当たり、その人の行は空のままになる。
  決定ログ「引き取り済みの控えは捨てず、送り先を `/api/waitlist` へ移す」が
  **「黙って捨てると T-070 が直した壊れ方に戻る」と書いている、その形そのもの**であり、
  受容した2つの窓（競合の窓 / R-5）と違って**記録が無い。**
  期待する状態: この回答にも送り先があるか、**または受容として決定ログに残っていること**
  （直し方は指定しない。別タスク化の協議も可。判断はオーケストレーター）

- 2026-09-09 `org-test-T-072b` が**再判定**（差し戻し1往復目の戻り）。**判定は合格。**
  D-1 が閉じたことを、**自分で再現手順を走らせて**確かめた（証拠「再判定」節）。
  以前 2行になった手順が 1行で終わり、匿名の受け口への送信が 0 回になっている。
  **同じ手順を修正前の `src/assets/request.js`（`dc2b60c` の版を複製して差し替え）でも走らせ、
  2行になることを実測した** —— 直しが効いていることの確認であって、自己申告ではない
- 2026-09-09 「別セッションの控えしか無い場合に回答が失われない」ことを独立に確認した。
  1回目に答えて送信が 503 → メールを登録せず離脱、2回目は送り直しも 503 で、
  そのセッションでは回答を入力せずアンケートを飛ばしてメールだけ登録する経路。
  待機リストの送信本文に前の訪問の回答4項目が載り、控えが消え、行は1つで回答が残った。
  **修正前の版では送信本文に回答が載らない**（`{email, signal_id}` だけ）ことも実測しており、
  この運搬は修正で足された振る舞いである
- 2026-09-09 **受容された窓を検証した結果、決定ログの記述と事実が食い違っている（m-4 / Minor）。**
  「一時的に2行になる」とあるが、**次の訪問では解消されない**（引き取り済みの印があるので
  ブラウザは何も送らず、控えを畳むだけ）。解消されるのは**同じ人がもう一度メールアドレスを
  登録したときだけ**である。さらに窓は控えの送り直しに限らず、**初回の通しでも開く** ——
  アンケートの送信は応答を待たずに次の段が開くので、匿名の送信が届く前にメールの登録が
  処理されれば同じことが起きる。実測して証拠「m-4」節に残した。
  🔒 **受容そのものを覆すのはテスト担当の役ではない**（受容はオーケストレーターの判断）。
  求めるのは**記録の訂正**である —— 「一時的」と読んだ次の担当は、放っておけば直ると考える
- 2026-09-09 **修正で新たに落ちる回答がある（m-5 / Minor）。** 引き取りが済んだ後に
  もう一度アンケートへ答え、**メールの段を送らずに離脱すると、その回の回答はどこにも残らない**
  （匿名の受け口へは送らず、控えも作らないため）。修正前は匿名の行として残っていた
  （ただし二重計上の原因でもあった）。**意図された取引であり決定ログにも沿う**が、
  「回答が捨てられる」経路が1つ増えたことは記録に残す。実測は証拠「m-5」節
- 2026-09-09 自分の指摘 m-1 を是正した。`tests/waitlist_merge.test.js` の D1 代替の `batch()` を
  **本物のトランザクション**（`BEGIN` / `COMMIT` / `ROLLBACK`）にし、
  **まとめが1トランザクションで流れることを見張るテストを1件足した。**
  実装の複製を逐次実行（`DELETE` と `INSERT` を別々の `.run()`）へ変えると赤になることを実測（証拠）
- 2026-09-09 **D-1 の経路を受け入れテストへ3件足した**（控えの送り直し / まとめた後の答え直し /
  別セッションの控えしか無い場合の回答の運搬）。あわせて A-3「どの順番・組み合わせ」に
  **行数の検査**を足し、穴を塞いだ —— ただし**受け口だけを見れば手順4で匿名の行は作り直される**
  （サーバは引き取り済みを知り得ない）ので、そこは4行で固定し、
  作り直させない役目はブラウザ側（D-1 群）であることをテスト側にも書いた。
  `tests/waitlist_merge.test.js` は 27件すべて緑
- 2026-09-09 m-2 / R-5（任意の UUID で他人の匿名の行を奪える）の**受容の記録が決定ログにある**
  ことを確認した（2026-09-09「m-2 / R-5 … このタスクでは受容する」）。指摘には含めない。
  R-3（本文が `null` / 配列 / 文字列）も自分で叩いて 400 を確認した
- 2026-09-09 m-3（本文のキー名が `id` でも緩めた述語を通る）は**そのままにした。**
  実装が `signal_id` を使っており、単体テストがその名前を直に使うので名前の変更は検出される。
  述語をキー名で縛り直すと、決定ログ「識別子を載せるキーの名前を決め打ちしない」と衝突する

- 2026-09-09 ⚠️ **上の m-5 の重大度を訂正する（Minor → Major）。判定も合格 → 不合格へ差し替える。**
  同じ仕組みの、**もっと重い場合**を自分で走らせて確認したためである ——
  **アンケートを飛ばしてメールアドレスだけ登録した人**（行の回答4列は空のまま）が、
  次の訪問で**初めて**アンケートに答えると、その回答は匿名の受け口へも控えへも行かず、
  **どこにも残らない。**失われるのは「答え直し」ではなく**その人の最初の回答**であり、
  行は空のままになる。実測は証拠「m-5 の訂正」節。
  受容された2つの窓（競合の窓 / m-2）と違い、**この経路には受容の記録が無い。**
  ⚠️ **前の行（m-5 を Minor とした記述）は残してある。**判断が変わった経緯ごと残すのが
  この台帳の作法であり、**訂正はこの行が担う。**
  レビュー（`org-review-T-072`）も再レビューで同じ経路を D-2（Major）として挙げている。
  **直接のやりとりはしていない**（`## 連絡してよい相手` は「無し」）。
  台帳に commit されていた記録を読んで**同じ経路を自分で走らせ直し**、
  自分の重大度づけが甘かったと判断した。**判定は独立に一致した（食い違い無し）**
- 2026-09-10 **差し戻し（往復2回目・上限）を受けて D-2 / m-5 と m-4 を修正**
  （`org-implementation-T-072`）。触ったのは `src/assets/request.js`（打ち切りの位置と
  控えの畳み方）と `tests/waitlist_merge_unit.test.js` だけ。
  `tests/waitlist_merge.test.js` はテスト担当の持ち物なので触っていない
- 2026-09-10 単体テストを1件足し（「D-2 引き取り済みのブラウザが後から答えた回答も、
  次の登録で待機リストへ届く」）、**red-green を実測した**（打ち切りを控えの手前へ戻すと落ちる）。
  あわせて既存の1件（「まとめた後にもう一度アンケートへ答えても…」）の**期待を入れ替えた** ——
  「送らないのだから控えも作らない」から「送らないが控えには残す」へ。
  ⚠️ **落ちているテストを通すための書き換えではない。**控えを作らない挙動そのものが
  D-2 の原因であり、期待の側が誤っていた。入れ替えた理由はテストの本文にも書いてある
- 2026-09-10 m-4 は記述の訂正のみ。**過去の決定ログの行は書き換えず、上書きする新項目を追記**した

- 2026-09-10 `org-review-T-072` が**最終レビュー**（往復2回目・上限。修正コミット `9182aee`）。
  **判定は通過。Blocker 0 / Major 0。**
  D-2 は閉じた —— 打ち切りが控えを書いた後ろへ移り、引き取り済みのブラウザが
  後から初めて答えた回答も控えに残り、次の登録で `/api/waitlist` へ相乗りする
  （3訪問の通しの回帰テストが最後まで見ている）。
  **R-1 は再発していない**: `postRequestSurvey` の呼び出しは2箇所（`:531` / `:596`）で、
  どちらも直前の `isMerged()`（`:529` / `:588`）に守られている。控えを残すようにしても
  匿名の受け口への送信は復活しない。
  控えが消えない副作用も確認した —— 控えを消すのは待機リストの登録成功と
  `postRequestSurvey` の 2xx / 4xx だけで、**どの経路でも「控えのほうが DB の行より古い」
  状態にならない**（登録が成功するたびに控えは消えるため）。毎訪問で控えを読むが送信は起きない。
  既存1件の期待の入れ替え（「控えを作らない」→「送らないが控えには残す」）は妥当で、
  **その期待自体が D-2 の原因**だった。守るべき線（匿名の受け口へ送らない）は
  同じテストの上の assert が引き続き見張っている。
  m-4 の訂正は、過去の行を書き換えず `上書き対象` 付きの新項目で行われている（規約どおり）。
  `npm test` は 612件中610件緑（赤は T-049 の2件のみ）

- 2026-09-10 `org-test-T-072b` が**最終判定**（往復2回目・上限。修正コミット `9182aee`）。**判定は合格。**
  D-2 / m-5 が閉じたことを、**自分が実測に使ったのと同じ経路**で確かめた（証拠「最終判定」節）。
  訪問1でアンケートを飛ばしてメールだけ登録 → 訪問2で初めて回答 → **控えに残る**（以前は空だった）
  → 訪問3（ただ再訪しただけ）でも送信0件・控えは保持 → 訪問4でメールを登録し直すと
  `/api/waitlist` の本文へ相乗りし、**1行に回答が入り、控えが消える**
- 2026-09-10 D-1 の再発が無いことを確認した。控えを残すようにしても、
  **引き取り済みのブラウザから匿名の受け口への送信は 0 件のまま**（V1 / V3 / V4 の全経路で実測）。
  まとめた後の答え直しも、匿名の行を作らずに控えへ残るようになった（以前は失われていた）
- 2026-09-10 **既存の単体テスト1件の期待の書き換えは妥当と判断した。**
  以前の「送らないのだから控えも作らない」という期待は、**D-2 の欠陥そのものを固定していた。**
  書き換え後も「引き取り済みなら匿名の受け口へ送らない」という 🔒 の線は同じテストが見張っており、
  **相乗りして届くところまでを D-2 の新しいテストが最後まで見ている。**
  落ちているテストを通すための書き換えではない（期待の側が誤っていた）。
  判断は自分の実測（上の訪問1〜4）に基づいており、テストの記述には依っていない
- 2026-09-10 m-4 の記録訂正が事実と合っていることを確認した
  （「次の訪問では解消しない」「窓は初回の通しでも開く」の2点。過去の決定ログを書き換えず
  上書きする新項目として追記されている）。m-2 / R-5 と「登録が一度も成功しなければ
  回答が手元に残ったまま届かない」点は**受容の記録があることだけを確認**した（再指摘しない）
- 2026-09-10 **これで T-072 の完了条件は全て充足し、Blocker / Major は 0 件である。**

## 証拠

### C-1 `npm test`（実装後・最終）

```
$ npm test
not ok 115 - 完了条件2: ヒーローの各行が、その製品の実データの単価と袋価格を出している
not ok 118 - T-009 が記録した順位・単価が、いまの実データと一致する
# tests 601
# suites 0
# pass 599
# fail 2
# skipped 0
# todo 0
```

**赤は2件で、どちらも `tests/hero_ranking.test.js`（T-049。着手時点から赤で本件と無関係）。**
着手時は 599件中10件が赤だった（受け入れテスト8件 + この2件）。件数が 599 → 601 に増えているのは
単体テストを2件足したためである。

### A-1〜A-7 受け入れテスト

```
$ node --test tests/waitlist_merge.test.js
# tests 23
# pass 23
# fail 0
```

着手時に赤だった8件（A-1 群4件 / A-2 群3件 / A-6 の1件）が緑になり、
回帰の見張り15件（A-3 / A-4 / A-5 / A-7 ほか）も緑のままである。

### 単体テスト（受け入れテストが通っていない2経路）

```
$ node --test tests/waitlist_merge_unit.test.js
ok 1 - メールアドレスの行が先にあっても、匿名の行を引き取って1行にまとめる
ok 2 - 匿名の行の照会が失敗しても、メールアドレスの登録は成立する（503 にしない）
# tests 2
# pass 2
# fail 0
```

**red-green の確認**: 2件目は実装前には素通りで緑だったので、`findAnonymousRow` の
例外の握りを一時的に `throw err` へ変えて `not ok 2` になることを確認し、元へ戻して緑に戻した
（`grep -n "TEMP" worker/waitlist.js` が空であることも確認済み）。

### C-2 `npm run build`

```
$ npm run build
LP          /ja/
製品一覧    /ja/protein/  — 46 製品
⚠️ ヒーロー   HERO_PRODUCT_IDS で手動指定した 3 件を出しています（実際の単価順ではありません）。
クロール    /ja/ を許可 / /ja/protein/ /api/ を拒否
            robots.txt  sitemap.xml  llms.txt を出力
Search Console 未設定 — GOOGLE_SITE_VERIFICATION を渡すと所有権確認タグが入ります。
BUILD_EXIT=0
```

配信される資産に変更が入っていることも確認した。

```
$ grep -rn "signal_id" dist/ --include=*.js
dist/assets/request.js:668:      if (carriedSignalId) payload.signal_id = carriedSignalId;
```

### 未検証（実施者は PO）

- **本番の D1 での動作確認。**`npx wrangler d1 execute` は deny 済みで、実装担当は実行できない
- **本番に既にできている重複行の統合。**統合の SQL は**書かないことを選んだ**
  （`## 判断してよい範囲` に入っている判断）。理由は下の決定ログ
  「既にできている重複行を統合する SQL は書かない」を参照

### 受け入れ判定（`org-test-T-072b` が実行。2026-09-09）

**判定は不合格。** 完了条件 A-1〜A-7 / C-1 / C-2 はすべて充足しているが、
目的（同じ人を二重に数えない）が残る経路で崩れる（D-1）。

| 条件 | 判定 | どう検証したか |
| --- | --- | --- |
| A-1 | 合格 | `node --test tests/waitlist_merge.test.js` の A-1 群4件。本物の SQLite に `worker/schema.sql` をそのまま流したうえで、`/api/request-survey` → `/api/waitlist` を通し、行数1・`email` あり・`id` は NULL を確認 |
| A-2 | 合格 | 同 A-2 群3件。回答4項目と `created_at` が残る。回答を載せない再送でも消えない |
| A-3 | 合格 | 同 A-3 群4件 + 実物の確認。`worker/schema.sql` の `CHECK (id IS NULL OR email IS NULL)` が健在（非コメント行の差分ゼロ）、`waitlist.id` へ書く経路は `worker/request_survey.js` だけ、`/api/waitlist` の INSERT の列に `id` が無い |
| A-4 | 合格 | 同 A-4 群3件。メールだけ / 識別子付きで匿名の行が無い / 不正なメールは 400 のまま |
| A-5 | 合格 | 同 A-5 群2件。アンケートだけの行は `id` のまま残り、他人の登録では引き取られない |
| A-6 | 合格 | 同 A-6 群3件 + 独立検査 S3。2回目の送信で行は増えず、回答も消えない |
| A-7 | 合格 | 同 A-7 群4件。キー無し / `null` / 壊れた UUID / 識別子を作れないブラウザ、どれも 200 |
| C-1 | 合格 | `npm test` = 601件中599件緑。赤は `tests/hero_ranking.test.js` の2件だけ（T-049。同ファイル単体でも同じ2件が赤） |
| C-2 | 合格 | `npm run build` = `BUILD_EXIT=0` |

```
$ npm test
not ok 115 - 完了条件2: ヒーローの各行が、その製品の実データの単価と袋価格を出している
not ok 118 - T-009 が記録した順位・単価が、いまの実データと一致する
# tests 601
# pass 599
# fail 2
# skipped 0

$ node --test tests/waitlist_merge.test.js
# tests 23
# pass 23
# fail 0
# skipped 0        <- skip されていない（本物の SQLite で走っている）

$ node --test tests/waitlist_merge_unit.test.js
# tests 2
# pass 2
# fail 0

$ npm run build
BUILD_EXIT=0

$ grep -rn "INTO waitlist|UPDATE waitlist|DELETE FROM waitlist" worker/ src/ --include=*.js
worker/request_survey.js:159:  INSERT INTO waitlist (id, nutrients, channel, nutrients_other, requests, created_at)
worker/waitlist.js:169:        INSERT INTO waitlist (email, nutrients, channel, nutrients_other, requests, created_at)
worker/waitlist.js:197:        DELETE FROM waitlist WHERE id = ?1
   -> waitlist.id へ書くのは /api/request-survey だけ。/api/waitlist は消すだけである

$ git diff a5f07e5..HEAD -- worker/schema.sql | (コメント行と空行を除く)
   -> 出力なし（DDL は1文字も変わっていない）
$ git diff --stat a5f07e5..HEAD -- worker/migrations/
   -> 出力なし（移行SQL の追加・変更なし）
$ git diff --name-only a5f07e5..HEAD | grep request_signal
   -> 出力なし（request_signal の3列は無変更）
```

### D-1（Major）まとめた後に匿名の行が作り直され、二重計上が戻る

- **重大度**: Major（境界条件での誤動作。完了条件 A-1〜A-7 の文面は満たしている）
- **再現手順**（ブラウザ（`src/assets/request.js`）と受け口を通しで動かした）
  1. アンケートに答えて送信する。**このときだけ `/api/request-survey` が 5xx を返す**
     （通信断でも同じ。控えが `pergram.request_survey_outbox` に残る）
  2. 続けてメールアドレスを送る。`/api/waitlist` は成功し、回答を持つ**1行**ができる
  3. 次の訪問でページを開くと、控えが**同じ識別子で送り直される**（`flushRequestSurveyOutbox`）
  4. `/api/request-survey` が匿名の行を**作り直す**
- **期待する動作**: 同じ人の行は1つ（T-072 `## 目的` / A-1）
- **実際の動作**: 行が2つに戻り、**同じ回答が両方に入る**

```
$ node <リポジトリ外の再現スクリプト>
1回目の訪問の後: [{"id":null,"email":"merge@example.com","nutrients":"creatine",...}]
出荷箱: {"id":"3f2504e0-...","nutrients":["creatine"],"channel":["rakuten"],...}
2回目の訪問で送り直された本文: ["/api/request-survey"]
2回目の訪問の後: [{"id":null,"email":"merge@example.com","nutrients":"creatine",...},
                  {"id":"3f2504e0-...","email":null,"nutrients":"creatine",...}]
行数: 2（同じ人。1行であるべき）
「クレアチンを見たい人」の数え上げ: 2（1であるべき）
```

- **切り分け結果**: `/api/request-survey` は削除済みの識別子でも新しい行を作る（正しい単独動作）。
  ブラウザ側は、まとめが済んだ後も控えを持ち続け送り直す。**どちらも単独では正しく、
  組み合わせで戻る。** 同種の経路をもう1つ確認した ——
  まとめた後に**同じブラウザがアンケートへ答え直す**と、やはり匿名の行が作り直される
  （その後もう一度メールを送れば1行に戻る。実測済み）
- **T-072 が作った欠陥ではない**（この経路は T-071 の時点でも2行になっていた）。
  ただし**目的が満たされない経路が残っている**ため Major とした。
  ⚠️ **修正方法は指定しない。** 直せる場所が `## 変更範囲` の中（`src/assets/request.js`）か
  外（`worker/request_survey.js`）かで別タスク化の要否が変わるため、**判断はオーケストレーター**
- **受け入れテストの穴**: `tests/waitlist_merge.test.js` の A-3「どの順番・組み合わせで送っても」の
  手順4が**まさにこの経路を通しているのに、行数を検査していない**（見ているのは
  `id` と `email` が同居しないことだけ）。回帰テストは**修正の方針が決まってから**足す

### Minor（対応は任意。記録として残す）

- **m-1** `tests/waitlist_merge.test.js` / `tests/waitlist_merge_unit.test.js` の D1 代替の
  `batch()` が**逐次実行**（`BEGIN` / `COMMIT` が無い）。決定ログが 🔒 とした
  「消したのに書けなかったで回答が消えない」を見張るテストが無く、実装を `DELETE` と
  `INSERT` の2回の `.run()` に分けてもテストは緑のまま通る。
  **実装側は正しい** —— 代替を本物のトランザクションにして `batch()` の2文目を落としたところ、
  匿名の行は消えずに残り、応答は 503 だった（実測）
- **m-2** 任意の UUID を `signal_id` に載せて送ると、その匿名の行を消して回答を自分の行へ
  移せる（実測）。UUID v4 は推測できず、識別子は外部へ出ないので実害はほぼ無い
- **m-3** 緩めた2件の述語は「値が識別子と一致するキー」を**名前を問わず**通すため、
  本文のキー名が `id` でも通る。決定ログ「キー名は `signal_id`。保存列の `id` と揃えない」を
  見張るテストは無い。**保存列の外の項目を弾く役目は生きている**（突然変異検査で確認済み）

### 検証できなかった項目

- **本番の D1 での動作確認。** `npx wrangler d1 execute` は deny のためテスト担当も実行できない（実施者は PO）
- **本番に既にできている重複行の件数。** 同上

### D-1 の修正（差し戻し 往復1回目・実装担当）

直したのは **`src/assets/request.js` だけ**（R-3 の1行を除く）。回帰は
`tests/waitlist_merge_unit.test.js` の D-1 節4件。

```
$ node --test tests/waitlist_merge_unit.test.js
ok 1 - メールアドレスの行が先にあっても、匿名の行を引き取って1行にまとめる
ok 2 - 匿名の行の照会が失敗しても、メールアドレスの登録は成立する（503 にしない）
ok 3 - D-1 送りきれなかった控えは、まとめた後の訪問で送り直されない（匿名の行が復活しない）
ok 4 - D-1 控えが前の訪問のものしか無くても、回答は待機リストの行へ載って失われない
ok 5 - D-1 まとめた後にもう一度アンケートへ答えても、匿名の行は作られない
ok 6 - D-1 まとめた後に控えが書かれても、次の訪問で匿名の受け口へは送らない
ok 7 - R-3 本文が null の JSON でも 500 にならず 400 を返す
ok 8 - R-3 本文が配列の JSON でも 500 にならず 400 を返す
# tests 8
# pass 8
# fail 0
```

**red-green を実測した**（テストが本当に見張っているかの確認）。直しを一時的に外すと落ちる。

```
# 匿名の受け口への送信を止める判定を外す + 控えを送信本文へ載せるのを止める
not ok 4 / not ok 5     （他は緑）

# 控えの送り直しを止める判定を外す + null 本文の門を外す
not ok 6 / not ok 7     （他は緑）

# 引き取り済みの印と控えの削除を外す
not ok 3 / not ok 4 / not ok 5 / not ok 6
```

外した実装は毎回**採取しておいた複製から戻し**、`grep -c "TEMP"` が 0 であることを確認している。

**「控えが別セッションにしか無い場合に回答が失われないこと」の確かめ方**（テスト4）:
1回目の訪問でアンケートに答え、送信を 503 にして控えだけを残す。**メールは登録せず離脱。**
2回目の訪問で送り直しも 503 にし、**そのセッションでは回答を入力しないまま**アンケートを飛ばして
メールアドレスだけを登録する。このとき `/api/waitlist` の送信本文に前の訪問の回答4項目が
載っていること、控えが消えること、そして**両セッションの送信を通しで受け口へ流すと
1行になり回答が残っていること**を見ている。

```
$ npm test
not ok 115 / not ok 118   （tests/hero_ranking.test.js。T-049。着手時点から赤）
# tests 607
# pass 605
# fail 2

$ npm run build
BUILD_EXIT=0
$ grep -c "MERGED_STORAGE_KEY" dist/assets/request.js
3
```

### 再判定（`org-test-T-072b`。差し戻し1往復目の戻り。2026-09-09）

**判定は合格。** 完了条件は全て充足し、D-1 は閉じた。Minor 2件（m-4 / m-5）を記録する。

| 条件 | 判定 | 検証 |
| --- | --- | --- |
| A-1〜A-7 | 合格 | `node --test tests/waitlist_merge.test.js` = 27件緑（A 群23件 + 追加した D-1 群3件 + 原子性1件） |
| C-1 | 合格 | `npm test` = 611件中609件緑。赤は `tests/hero_ranking.test.js` の2件のみ（T-049） |
| C-2 | 合格 | `npm run build` = `BUILD_EXIT=0` |
| D-1 | **閉じた** | 下記の再現手順を自分で走らせ、修正前後の差を実測 |
| R-3 | 合格 | 本文 `null` / `[1,2]` / `"x"` のいずれも 400 `{"error":"invalid_json"}` |

**D-1 の再現手順（以前 2行になったもの）を、修正後と修正前の実装で走らせた。**

```
（修正後 = いまの src/assets/request.js）
1回目の後の行数: 1  控え: undefined  印: 3f2504e0-…
2回目の匿名の受け口への送信: 0
2回目の後の行数: 1

（修正前 = dc2b60c の src/assets/request.js を複製して差し替え）
1回目の後の行数: 1  控え: {"id":"3f2504e0-…", …}  印: undefined
2回目の匿名の受け口への送信: 1
2回目の後の行数: 2      <- 以前の不具合。修正で閉じたことの裏取り
```

**別セッションの控えしか無い場合**（送信も送り直しも 503 のまま、そのセッションでは回答が手元に無い）。

```
（修正後）待機リストの送信本文:
  {"email":"…","nutrients":["creatine"],"channel":["rakuten"],
   "nutrients_other":"グルタミン","requests":"送料込みで並べたい","signal_id":"3f2504e0-…"}
  行数: 1 / 回答4項目とも保存されている / 控えは消えている

（修正前）待機リストの送信本文: {"email":"…","signal_id":"3f2504e0-…"}   <- 回答が載らない
```

**追加した受け入れテスト**（`tests/waitlist_merge.test.js`）。

```
ok 24 - D-1 送りきれなかった控えは、まとめた後の訪問で匿名の行を作り直さない
ok 25 - D-1 まとめた後にもう一度アンケートへ答えても、行は1つのまま
ok 26 - D-1 控えが前の訪問のものしか無くても、回答は待機リストの行へ載って失われない
ok 27 - 🔒 まとめは1トランザクション。書けなかった回で匿名の行の回答が消えない
# tests 27
# pass 27
# fail 0
# skipped 0
```

**m-1 の是正の red-green**（テストが本当に見張っているか）。実装の複製の `batch()` を
逐次実行（`DELETE` と `INSERT` を別々の `.run()`）へ変えて走らせた。**実装は触っていない。**

```
逐次実行に変えた実装での結果:
  batchCalls = 0 (テストは 1 を要求)
  status = 200  匿名の行 = 0 (テストは 1 行の残存を要求)
=> 逐次実行ではテストが赤になる（見張れている）
```

### m-4（Minor）受容された窓は「一時的」ではなく、放っておくと残り続ける

- **重大度**: Minor（実害は小さいが、**決定ログの記述が事実と違う**）
- 決定ログ「同一ページ内の送信中のアンケートと引き取りが競合する窓は、受容する」は
  「**一時的に**2行になる」と書いている。**事実はそうではない。**
- 実測（匿名の回答が遅れて届く順序を作り、その後の訪問を通しで動かした）

```
競合直後の行数: 2
次の訪問の送信: []            <- 引き取り済みの印があるので何も送らない
次の訪問の後の行数: 2         <- 解消されない
メールを送り直した後の行数: 1  <- 同じ人がもう一度登録したときだけ解消される
```

- **窓は控えの送り直しに限らない。** アンケートの送信は応答を待たずに次の段が開くので、
  **初回の通しでも**「匿名の送信がまだサーバへ届いていないうちにメールの登録が処理される」
  という並びが起きうる（決定ログはこの経路に触れていない）
- 🔒 **受容そのものを覆すのはテスト担当の役ではない。**求めるのは記録の訂正である。
  「一時的」と読んだ次の担当は、放っておけば直ると考える。**残るのは1行の過大計上**で、
  `docs/research/validation-plan.md` の判定を覆すほどではないと見ている

### m-5（Minor）まとめた後の答え直しは、メールを送らずに離脱すると失われる

- **重大度**: Minor（**意図された取引**であり決定ログに沿う。記録として残す）
- 引き取りが済んだブラウザは匿名の受け口へ送らず、控えも作らない。
  したがって**答え直した回だけを入力してメールの段を送らずに離脱すると、その回答は残らない**
- 実測: 2回目の訪問で `hmb` / 「新しい要望」と答えて離脱 → 保存されている回答は1回目のまま。
  **修正前は匿名の行として残っていた**（ただしそれが二重計上の原因でもあった）
- 失われるのは**答え直し**であって、最初の回答ではない（最初の回答は既に保存されている）。
  メールの段まで送れば `/api/waitlist` へ相乗りして反映される（実測で確認済み）

### m-5 の訂正（Minor → Major）最初の回答が失われる場合がある

- **重大度**: Major（`## 目的` ではなく **T-070 が直した壊れ方（回答が捨てられる）に戻る**。
  受容の記録が無い）
- **再現手順**（ブラウザと受け口を通しで動かした）
  1. 訪問1: 要望ボタンを押し、**アンケートを飛ばして**メールアドレスだけ登録する
     （行はできるが、回答4列は空。引き取り済みの印が立つ）
  2. 訪問2: もう一度ボタンを押すと**アンケートの段は開く**ので、**初めて回答する**
  3. その回の送信は**0件**。控えも書かれない
  4. 離脱すると、その回答は**どこにも残らない**

```
訪問1の後の行: [{"id":null,"email":"…","nutrients":"","channel":"","nutrients_other":null,"requests":null,…}]
印: 3f2504e0-…
アンケートのフォームは出るか: true
訪問2の送信: []                      <- 匿名の受け口へも送らない
訪問2の控え: undefined                <- 控えも書かれない
メールの段のフォームは出るか（送り直せるか）: true
訪問2の後の行: [{… "nutrients":"", "requests":null …}]   <- 回答は1文字も残らない
訪問3の後の行: [{… "nutrients":"", "requests":null …}]   <- 次の訪問でも復活しない
```

- **期待する動作**: 回答に送り先があること。または**受容として決定ログに残っていること**
  （T-072 の決定ログ「引き取り済みの控えは捨てず、送り先を `/api/waitlist` へ移す」は、
  **黙って捨てると T-070 が直した壊れ方に戻る**と自ら書いている）
- **緩和されている点**: 同じ表示の中でメールの段まで送れば `/api/waitlist` へ相乗りする
  （メールの段のフォームは出る）。失われるのは**答えた直後に離脱した場合**である
- **修正方法は指定しない。**別タスク化の協議も含めて判断はオーケストレーター

### D-2 / m-5 の修正（差し戻し 往復2回目・実装担当。2026-09-10）

触ったのは `src/assets/request.js` の2箇所だけ。打ち切り（`if (isMerged()) return;`）を
控えを書いた**後ろ**へ動かし、控えの送り直しの側は**送信だけ止めて控えは消さない**ようにした。

```
$ node --test tests/waitlist_merge_unit.test.js
ok 5 - D-1 まとめた後にもう一度アンケートへ答えても、匿名の行は作られない
ok 6 - D-1 まとめた後に控えが書かれても、次の訪問で匿名の受け口へは送らない
ok 7 - D-2 引き取り済みのブラウザが後から答えた回答も、次の登録で待機リストへ届く
# tests 9
# pass 9
# fail 0

# red-green（打ち切りを控えの手前へ戻すと落ちる）
not ok 5 / not ok 7     （他は緑）

$ npm test
not ok 115 / not ok 118   （tests/hero_ranking.test.js。T-049。着手時点から赤）
# tests 612
# pass 610
# fail 2

$ npm run build
BUILD_EXIT=0
```

D-2 のテストが見ているのは3訪問の通しである。
**訪問1** アンケートを飛ばしてメールアドレスだけ登録（引き取りの印が立つ）。
**訪問2** ここで**初めて**アンケートに答える → 匿名の受け口への送信は0件のまま、控えに残ること。
**訪問3** 何も答えずにメールアドレスを登録し直す → その回答が `/api/waitlist` の本文へ相乗りし、
控えが消え、3訪問ぶんの送信を通しで受け口へ流すと**1行**になって回答が残っていること。

⚠️ **訪問3が来なければ、この回答はサーバへ届かない。**決定ログで受容として記録した
（サーバに鍵を持たせずに届ける手立てが無い）。


### 最終判定（`org-test-T-072b`。往復2回目・上限。2026-09-10）

**判定は合格。** Blocker / Major は 0 件。

| 見た点 | 判定 | 実測 |
| --- | --- | --- |
| D-2 / m-5 が閉じたか | **閉じた** | 下の訪問1〜4 |
| D-1 の再発 | **無し** | 引き取り済みからの匿名の受け口への送信が全経路で 0 件 |
| 単体テストの期待の書き換え | **妥当** | 誤った期待の是正。🔒 の線は同じテストが見張り続けている |
| C-1 `npm test` | 合格 | 612件中610件緑。赤は `tests/hero_ranking.test.js` の2件のみ（T-049） |
| C-2 `npm run build` | 合格 | `BUILD_EXIT=0` |
| m-4 の記録訂正 | 事実と一致 | 「次の訪問では解消しない」「初回の通しでも窓が開く」の2点 |
| A-1〜A-7 | 合格（据え置き） | 前回の再判定で充足を確認済み。作り直していない |

**D-2 の経路（前回「1文字も残らない」と実測したもの）を、修正後に走らせた。**

```
訪問1（アンケートを飛ばしてメールだけ登録）の後の行:
  [{"id":null,"email":"…","nutrients":"","channel":"","nutrients_other":null,"requests":null,…}]
訪問2（ここで初めて回答）の送信: []           <- 匿名の受け口へは送らない（D-1 は保たれている）
訪問2の控え: {"id":"3f2504e0-…","nutrients":["creatine"],…,"requests":"初めて書いた要望"}
                                              <- **以前は undefined。回答が残るようになった**
訪問3（ただ再訪しただけ）の送信: []            <- 送信は復活していない。控えも消えていない
訪問4（メールを登録し直す）の送信:
  /api/waitlist {"email":"…","nutrients":["creatine"],…,"requests":"初めて書いた要望",
                 "signal_id":"3f2504e0-…"}
訪問4の後の行: [{"id":null,"email":"…","nutrients":"creatine","requests":"初めて書いた要望",…}]
訪問4の後の控え: undefined                     <- 届いたときにだけ消える
```

**D-1 の再発が無いこと**（前回の再判定と同じ4経路を再実行）。

```
V1 控えの送り直し     : 2回目の匿名の受け口への送信 0 件 / 行数 1
V2 別セッションの控え : 送信本文に回答4項目が載る / 行数 1 / 控えは消える
V3 答え直し → メール再送 : 送信 0 件 / 行数 1 / 答え直した回答が保存される
V4 答え直し → 離脱    : 送信 0 件 / **控えに残る**（以前は失われていた）/ 行数 1
V5 受容された窓       : 変化なし（受容済み）
```

## 組織への指摘

- [道具] `ecc:e2e-testing` が Playwright 専用で、依存パッケージ0のこのリポジトリでは使えなかった（ブラウザ相当の実行は自前の `tests/mini_dom.js`）。シナリオテストの設計で毎回、割り当てられたスキルを読んでから自力に切り替えている
- [規約] 同じ不変条件の本文が `worker/schema.sql` の冒頭コメントと `.claude/rules/pergram-compliance.md` の2箇所にあり、更新対象として `## 変更範囲` に挙がっていたのは後者だけだった。前者に失効した記述（「2行に分かれて残るのが正しい姿」）が残ったまま完了することになる
- （レビュー担当 org-review-T-072 から）無し
- [道具] 実装を触らずに「テストが本当に見張っているか」を確かめる手立て（突然変異検査）が組織の道具に無く、作業ツリーを汚さないためにリポジトリの外へ複製を書き出して自前で組んだ。テスト担当は毎回これを組み直すことになる
- （レビュー担当 org-review-T-072 の再レビューから）無し
- （レビュー担当 org-review-T-072 の最終レビューから）無し
