# T-012: 登録インセンティブの実現手段を決める

## メタデータ
- 作成日: 2026-09-02
- 更新日: 2026-09-02
- 状態: 未着手
- 優先度: 中
- 担当: 未割当
- 進捗: 0%
- 依存: T-011（推奨: 完了状態の構造が決まってから、そこに載せる価値を決める）
- TDD適用可否: 適用
- 手戻り回数: 0

## 目的

現在の登録メリットは「公開時の通知」と「リクエストの優先」だけで、
まだ愛着のないサービスにメールアドレスを渡す動機として弱い。

ただし**提示できる価値は実装できる範囲に限られる**。現状のWorkerはD1に保存するだけで、
**メール送信の手段を持っていない**。したがって以下は今のままでは約束できない。

| 案 | 現状での実現性 |
|---|---|
| 登録すると単価比較表を即時配布 | ✗ 送信基盤なし |
| 値下がりをメール/LINEで通知 | ✗ 送信基盤なし。監視製品IDの保存は可 |
| セール時に最安トップ3を配信 | ✗ 送信基盤なし |
| **登録完了画面で現時点の単価一覧を全件見せる** | ○ ビルド済みデータで完結。送信不要 |

**履行できない約束を文言だけ先に書くことはしない。**
本タスクは実装ではなく、どの経路を採るかを決めるための調査。

## 変更範囲

調査とドキュメント化のみ。コード変更なし。

| 調べること | 判断への使い方 |
|---|---|
| Cloudflare Workers から使えるメール送信手段と費用・設定量 | 送信基盤を持つコストの見積り |
| 送信基盤を持つ場合に増える保存列と、データ保護方針との整合 | 列を増やすなら `worker/schema.sql` / migrations / tests を揃える対象 |
| 配信停止（オプトアウト）の実装義務と、特定電子メール法上の表示義務 | 送信を始める場合の必須要件 |
| 完了画面で単価一覧を見せる案の実装量 | 送信基盤なしで出せる価値の上限 |

## 禁止事項

- 🔒 **成分の効能・効果に触れる訴求を作らない（N-02）。** インセンティブは価格と更新頻度で語る
- 🔒 独自の総合スコア・品質スコアを配布物に含めない（N-03）
- 🔒 健康状態・体調・服薬情報を保存しない（N-05）。通知の希望を取る場合も監視製品IDまで
- 🔒 メールアドレスをGA4のイベントパラメータに含めない
- 🔒 禁止語を使わない。「安心」「話題の」等
- **実装しない。** 本タスクの成果物は決定であって、コードではない

## 完了条件

1. 上記4項目が調べられ、記録されている
2. 「送信基盤を持つ」「持たずに完了画面で価値を出す」のどちらを採るかが**明示的に決まっている**
3. 採る案について、必要な作業が後続タスクとして切り出せる粒度で書かれている
4. 採らなかった案について、なぜ採らなかったかが書かれている
5. 上記が `docs/` 配下のドキュメントとしてコミットされている

## 判断してよい範囲
<!-- どこまで自律的に決めてよいか。ここが空だと、担当は決めすぎるか、
     些末なことを全部エスカレーションしてくる -->

## テスト方法

コードを触らないためテスト対象外。

## 停止条件

- §4の共通のものに加えて:
- 送信基盤を持つ判断になった場合、保存列の追加を伴うなら実装前に相談する
  （[CLAUDE.md](../../CLAUDE.md) のデータ保護方針に列の増減が直結するため）

## 参照すべき成果物
<!-- 設計成果物、既存コード、過去の決定（docs/decisions-index.md で引ける）。
     実在するパスだけを書く -->

## 連絡してよい相手
無し（すべてオーケストレーター経由）
<!-- 直接やりとりしてよい相手が居るなら、その宛先をそのまま書く。
     宛先は起動時に付けた名前であり、タスクごとに一意でなければならない
     （例: org-implementation-T-007）。役割名だけを書くと、同じ名前が
     使い回された時点で送信が拒否される -->

## 申し送り
無し
<!-- 上流タスクの完了報告から、この担当が知らないと判断を誤る事実を、
     オーケストレーターが転記する。登録した後で埋まる唯一の指示欄 -->

## 成果物
<!-- 完了時に何ができたか。ファイルパスとコミット -->

## ブロッカー
無し
<!-- 今このタスクを止めているもの。PO確認待ちなら Q-XXX を書く -->

## 決定ログ

### 2026-09-02 インセンティブの文言より先に、実現手段を確定させる
- 決定: 「即時配布」「値下げ通知」を文言として出す前に、送信手段の有無を調査タスクとして切る
- 却下案: LPの文言だけ先に強くする案 → 送信基盤がないため**履行できない約束**になる
- 出典: セッションでの決定

## 作業ログ

- 2026-09-02: タスクとして台帳に登録

## 証拠

（未着手）

## 組織への指摘
無し
<!-- 組織の仕組み（規約・道具・進め方・役の分け方）について気づいたことを、
     1行1件で書く。書式は必ず `- [種別] 内容`。種別は次の4つだけ:

       規約 … この規約に従うと作業が進まない、または矛盾する箇所に当たった
       道具 … 必要な道具・スキル・外部アクセスが無く、遠回りをした
       反復 … 同じ種類の手戻りを、このタスクで2回以上経験した
       ロール … 役の分け方が、いまの仕事の流れに合っていない

     例:
       - [道具] Python の書き方のスキルが割り当てられておらず、毎回自分で確認した
       - [規約] 完了条件に実環境での確認が要るが、実施者が決まっていない
       - [ロール] レビュー待ちが3タスク積み上がり、自分の次の着手が2日止まった

     書くのは観測した事実だけ。対応策も「新しい役が要る」という結論も書かない。
     「質問をさばく役が要る」ではなく「レビュー待ちで2日止まった」と書く。
     どう直すかは改善エージェントとオーケストレーターと PO が決める。

     ロール を書く条件は6つ。詳細は .claude/rules/org-improvement-feedback.md

     これはエスカレーションではない。**返事は来ないし、作業も止めない。**
     判断を仰ぎたいなら、ここではなくエスカレーションの経路を使う。

     ここに書いたものは、完了報告にも含める。オーケストレーターがそれを
     吸い上げ、組織改善の台帳（docs/org-improvements.md）へまとめて起票する。

     台帳へ自分で書きに行かないこと——同時に走る他のタスクの書き込みを消す。

     気づきが無ければ「無し」のまま残す。行ごと消さない -->
