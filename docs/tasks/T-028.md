# T-028: X発信アカウントのプロフィールと固定ポストを整える

## メタデータ
- 作成日: 2026-09-02
- 更新日: 2026-09-02
- 状態: 未着手
- 優先度: 高
- 担当: 未割当
- 進捗: アカウントは運用中（@pergram_jp）。own_handle 設定済み。残るはプロフィールと固定ポストの整備
- 依存: 無し
- TDD適用可否: 適用外（外部サービス上の設定作業で、事前にテストを書けない）
- 手戻り回数: 0

## 目的
14日の運用計画は発信アカウントの存在を前提にしているが、**まだ繋がっていない**。
`x_reader.own_handle` は未設定、`publisher.connector` は `dummy_x`、`publisher.buffer.channel_id` は null。
読み取り用に設定されている `@uraraka07854666` は観測専用であり、pergram の発信主体ではない。
**2026-09-02 に判明**: 発信アカウント `@pergram_jp` は既に運用中で、Buffer への接続も済んでいた。
`x_reader.own_handle` に設定し、`media-agent x doctor` が @pergram_jp のプロフィールを開ける状態になった。
**このタスクに残っているのはプロフィール文と固定ポストの整備だけである。**

さらに、`media-agent x metrics`（自分の投稿の指標取り込み）が `own_handle` を要求するため、
**このタスクが終わるまで型別の効果分析が一切できない。** T-030 の Day7 判定が成立しない。

## 変更範囲
- X 上のアカウント設定（プロフィール、固定ポスト）
- `.media-agent/x/pergram-jp/config.yaml` の `x_reader.own_handle`

## 禁止事項
- 配信の段階（`mode`）を `test` から上げること。切替は PO が判断する
- `media-agent post` / `run` を実配信で走らせること
- ブランド表記を `Pergram` / `PerGram` と書くこと

## 完了条件
1. ~~発信用の X アカウントが存在し、ハンドルが決まっている~~ 済（@pergram_jp）
2. プロフィールに正式表記 `pergram`（全て小文字）とタグライン「1gあたり、いくら払ってる?」が入っている
3. 固定ポストに比較画面の画像と、T-027 の短縮パスのリンクが置かれている
4. ~~`x_reader.own_handle` が反映され、`x doctor` が「自分の投稿」を開ける~~ 済（`[INFO] 開けるページ: … @pergram_jp のプロフィール`）

## 判断してよい範囲
- ハンドル名の候補（正式表記の規約に反しない範囲）
- プロフィール本文の言い回し

## テスト方法
`media-agent x doctor` の出力で `own_handle` が認識されていること。実環境確認: 要。実施者は PO。

## 停止条件
- ハンドルの候補がすべて取得済みで確保できない → オーケストレーターへ上げる

## 参照すべき成果物
- docs/Marketing/X_kgi_2weeks.md §2（Day0）
- .claude/rules/pergram-ui-copy.md（ブランド表記と使ってはいけないビジュアル）

## 連絡してよい相手
無し（すべてオーケストレーター経由）

## 申し送り
使ってはいけないビジュアルがある（葉・植物・カプセル・錠剤・筋肉・ハート・体のシルエット・チェックマーク・人物写真）。
アイコンとヘッダーの選定時に効く。

**T-044（2026-09-04・Xアルゴリズムの一次調査）から。出典は `docs/Marketing/X_algorithm_findings.md`。**

- **このタスクの優先度の根拠が強まった。** 一次情報により、リプ経由の露出は
  **「リプ欄を開いた人がプロフィールを見るか」でほぼ決まる**ことが確認された
  （フォロー外のリプは For You から丸ごと捨てられる: `oon_retweet_reply_filter.rs`）。
  **プロフィールと固定ポストが、露出を流入へ変える唯一の関門である**
- 完了条件そのものは変わらない

## 成果物
## ブロッカー
無し

## 決定ログ
特記なし

## 作業ログ
## 証拠
