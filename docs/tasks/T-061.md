# T-061: 要望の帯を「応援する」の1ブロックへ作り直す（PO差し戻し3回目）

## メタデータ
- 作成日: 2026-09-08
- 更新日: 2026-09-08
- 状態: 設計中
- 優先度: 高
- 担当: org-design-T-061
- 進捗: 帯の視覚設計を完了。design.md §4 を全面差し替え、決定ログ8件を記録。実装へ渡せる状態
- 依存: T-057（ブロッカー: T-057 が入れた `@container` の2列がそのまま対象のため）
- TDD適用可否: 適用外（見た目の設計が主。順序は 設計 → 実装 → テスト → レビュー）
- 手戻り回数: 0

## 目的

**同じ帯に対する PO 差し戻しが3回目である**（T-053 → T-057 → 本タスク）。
1回目は「面がバラバラ」、2回目は「組版が壊れている」、3回目が **「文章とボタンの位置がバラバラ。UIデザイナーをアサインしろ」**。

2回とも直したのは**値**（角丸・余白・列幅）であって、**要素同士の関係**ではなかった。
現状の帯は、1つの呼びかけであるはずのものが**3つの独立した断片**として置かれている。

| 断片 | 現状 |
| --- | --- |
| リード文 | 左列。ボタン+注記の高さに対して**垂直中央**に浮く |
| ボタン | 右列。`--band-cta-max`(26rem) の巨大な面 |
| 注記 | ボタンの直下。左列のリード文とは**別の軸**に始まる |

**読む人の視線が置く場所が3つある。** どれが呼びかけの本体かが分からない。これが PO の言う「バラバラ」である。

あわせて、この導線の**言っていること自体を変える**（PO 指示）。
「成分・製品の追加をリクエスト」は**サービス側の都合**（何を掲載するか）を名乗っており、
押す人が得るものが無い。**「正式版リリースを応援する」**——押す人の意思表示そのものを名乗る形へ変える。

さらに CVR の仕掛けを2つ入れる（PO 指示）。
- **① ボタンの直近に置くマイクロコピー** — 「押しても安全・面倒がない」ことを、ボタンを読む視線の中で完結させる
- **② 押した後の完了フィードバック** — 押した瞬間に受け止める。現状はボタンの文字が変わるだけで、押した手応えが無い

## 変更範囲

- `src/styles/site.css` — 帯の幾何。**🔒 帯と面の幾何を決める値はここ1つだけ**
- `src/styles/tokens.css` — 必要なら値の追加のみ（既存値は不変）
- `src/templates/request.js` — 帯のマークアップ、完了フィードバックの器
- `src/assets/request.js` — 完了フィードバックの表示、押し直しの扱い
- `locales/ja.json` / `locales/en.json` — 文言
- `src/templates/lp/hero.js` / `src/templates/products/head.js` — ヘッダ CTA の文言の追随（キーの参照のみ。構造は変えない）
- `tests/` — 上記を観測するテスト
- `docs/design/design.md` — 帯の節の更新
- **⚠️ 2026-09-08 にオーケストレーターが追加**（設計担当の指摘を承認）:
  - `src/styles/lp.css` — `.waitlist-band .request-band { margin-top: 0 }`(675行) を `site.css` へ**移すためだけ**。新しい宣言を足さない
  - `src/styles/products.css` — 1735行のコメント1行の更新のみ。**宣言は足さない**
  - `src/templates/lp/support.js` および支援段の文言キー — 「応援」の衝突を有償側で解くため

## 禁止事項

- **`src/styles/products.css` / `src/styles/lp.css` へ帯の幾何を書かない。** この 🔒 は T-051 → T-053 → T-057 と**3回破られている**。4回目を作らない
- **`data-cta` の値（`list-top` 等）と `aria-controls` の値を変えない。** GA4 の前後比較が切れる（T-058 と同じ理由）
- **押下数・人数・「◯人が応援」を画面に出さない。** 景表法の「人気」表示（N-03 / 既存 🔒）
- **`request_signal` / `waitlist` のスキーマを触らない。** 6列打ち止め・3列打ち止めは維持
- **段の順序（要望 → アンケート → メール → 支援）を変えない**
- **完了フィードバックで、サービスが保持していないことを断言しない。**
  PO 提示の候補のうち **「優先的に対応を進めます」は使わない** — 優先順位を約束する事実が無い（景表法 / `pergram-ui-copy.md`「事実のみを書き、優劣を述べない」）
- **禁止語を入れない**: 効く / 効果 / 改善 / 高品質 / おすすめ / 人気No.1 / 安心 / 話題の / 選ばれる / 実感
- **「応援」の語が有償の支援段（`request.supportHeading`「開発を応援する」）と衝突する。**
  第1段階（無償・1タップ）と第4段階（Codoc・有償）が**同じ語を名乗ると、押した人が課金導線と誤認する。**
  どちらかの語を必ず分ける（判断は担当に委ねる。分けないという結論は取らない）
- スコープ外のリファクタリング（T-054 / T-056 / T-059 の領域へ踏み込まない）
- **⚠️ 2026-09-08 追記: 有償の支援段の語を変える側で解く。** PO 判断により、無償の第1段階は
  **「正式版のリリースを応援する」**（PO 指示の字句）を採る。衝突は `request.supportHeading` /
  `lp.support.buttonText` / `lp.support.message` の側を「寄付」など**お金だと一目で分かる語**へ
  改めて解く。**無償側の語を弱めない**

## 完了条件

**A 群 — 文言（文字列で判定できる）**

- A-1 `npm run build` 後、`dist/` 全走査で **「成分・製品の追加をリクエスト」が0回**
- A-2 `dist/ja/protein/index.html` と `dist/ja/index.html` の両方に、新しい CTA 文言が出る
- A-3 ボタンの**上**のマイクロコピーと**下**の注記が、両方 `dist/` に出る。文書順で `マイクロコピー < ボタン < 注記`
- A-4 `dist/` 全走査で「優先的に対応」が **0回**
- A-5 `locales/ja.json` と `locales/en.json` の**キー集合が一致する**（既存テストが見ている）
- A-6 ヘッダの CTA（`request.headerCta`）が帯の CTA と同じことを言っている。旧「機能追加リクエスト」は `dist/` 全走査で 0回
- A-7 第1段階（無償）と支援段（有償）が**別の語**を名乗る。`dist/` に「応援」が両方の意味で混在しない

**B 群 — レイアウト（Chrome ヘッドレス + CDP の実測で判定する）**

⚠️ **測定は `document.fonts.ready` を待った後に行う。** 待たないと文字幅が約16%細く出る（T-053 / T-057 で実際に誤りかけた）。
⚠️ `--headless=new` はこの環境で終了コード21で落ちる。**旧 `--headless` + 絶対パスの `--user-data-dir`** を使う。

- B-1 **⚠️ 2026-09-08 に PO 判断で書き直した。旧 B-1（4要素すべての左端が一致）は無効。**
  **構図は「左にテキスト、右にボタンのブロック」の2列とする**（PO 指示）。判定は次の3つ。
  - **B-1a 列の中では軸が1本。** 左列の要素（見出し・リード文）の左端が互いに 1px 以内で一致する。
    右列の要素（マイクロコピー・ボタン・注記・受領メッセージ）の左端が互いに 1px 以内で一致する
  - **B-1b 2列の上端が揃う。** 左列の中身の上端と右列の中身の上端が 4px 以内で一致する。
    **`align-items: center` による垂直中央合わせを使わない**（現状の「リード文がボタンより 27.93px 下」の原因）
  - **B-1c 死んだ空白を作らない。** 左列の中身の右端と右列の左端の距離が、全幅で `column-gap` の値と一致する
    （±2px）。1440px で 360px の穴が開いた旧実装（T-057 で修正済み）へ戻さない
- B-2 **LP と製品一覧で構図の作り方を割らない。** 同じ帯が両ページで同じ規則に従う
  （現状は製品一覧が左端一致・LP が中央一致で**割れている**）
- B-2b **1列へ落ちる幅を持ってよい。** 2列が成立しない狭い幅では1列にしてよく、そのとき B-1a は
  「全要素の左端が 1px 以内で一致」に読み替える。**切り替わる幅を設計書に明記すること**
- B-3 上記7幅で**横溢れ 0件**（`scrollWidth > clientWidth` の要素が帯の中に無い）
- B-4 上記7幅で**孤立行 0件**（リード文・マイクロコピー・注記の最終行が1〜2文字だけにならない）
- B-5 ボタンの**高さ 44px 以上**、マイクロコピーとボタンの間隔が**注記とボタンの間隔と同じか小さい**（マイクロコピーはボタンの条件であり、離すと関係が切れる）
- B-6 `.request-band` 系の幾何を決める宣言（`padding` / `margin` / `gap` / `grid-template-*` / `border-radius` / `max-width`）が `src/styles/products.css` と `src/styles/lp.css` に **0件**（`grep -c` で確認）

**C 群 — 完了フィードバック（実ブラウザの操作で判定する）**

- C-1 ボタン押下の**直後**に受領メッセージが画面に出る。`role="status"` を持ち、テキストは JS が書き込む（最初から入れて `hidden` を外すだけにしない。読み上げが飛ぶ）
- C-2 受領メッセージの中に**数字が入らない**（押下数・人数）
- C-3 押下 → アンケートを飛ばす → **もう一度押下** の順で操作しても、**枠だけの空箱が復活しない**（T-053 完了条件 B-5b の回帰）。`request_survey_view` が**二重に飛ばない**
- C-4 `prefers-reduced-motion: reduce` で、受領メッセージの出現にアニメーションが掛からない
- C-5 JS が動かない環境でも帯が壊れない（受領メッセージの器は初期状態で `hidden`）

**D 群 — 回帰**

- D-1 `npm test` が全件通る
- D-2 `npm run build` が成功する
- D-3 `npm test -- tests/panel_unify.test.js` が通る（T-053 の面の統一が壊れていない）

## 判断してよい範囲

- **帯の構図そのもの**（1列 / 2列 / 中央寄せ / 左寄せ、要素の順序、面の持ち方）。B 群を満たす限り自由
- 新しい文言の**最終的な言い回し**（PO 提示は「〜的な表記」であり、字句の指定ではない）。禁止事項に触れない範囲で決める
- マイクロコピーを**何本置くか**（1本でも2本でもよい）。PO 提示の3案から選ぶ／書き換えるのも可
- 完了フィードバックの**出し方**（インライン / トースト）。ただしトーストを採る場合は C-4 と C-5 を満たすこと
- トークンの**追加**（既存値の変更は不可）
- 「応援」の語の衝突を**どちらの段の語を変えて**解くか

## テスト方法

- 単体: `npm test`（実装担当が書く）
- 受け入れ: B 群 / C 群は **Chrome ヘッドレス + CDP の実測**。テスト担当が7幅の測定スクリプトを書き、JSON で出す
- 回帰: `npm test -- tests/panel_unify.test.js` / `tests/render.test.js` / `tests/worker.test.js`
- 実環境確認: **不要**（本番デプロイは別タスク）。ローカルの `.preview` で足りる

## 停止条件

- **S-7（このタスク固有）**: 「軸を1本にする」と「2列にする」が両立しないと判明した場合。
  1列へ倒すのは判断してよい範囲だが、**PO は 1440px で帯が縦長になることを嫌う可能性がある**。
  1列へ倒すなら、**倒した後の見た目を実測値付きで報告してから実装へ渡す**
- 共通の停止条件（`.claude/rules/org-escalation.md`）

## 参照すべき成果物

- `docs/design/design.md`「面の体系と字送りの階層（2026-09-08 / T-053）」§1〜§4
- `docs/tasks/T-053.md` / `docs/tasks/T-057.md` の `## 決定ログ` — **なぜ今の形になっているか。ここを読まずに壊すと4回目の差し戻しになる**
- `docs/decisions-index.md` の `対象: 要望の帯` / `対象: 面の体系`
- `.claude/rules/pergram-ui-copy.md` — 禁止語・使う語・UI 実装の下限
- `.claude/rules/pergram-compliance.md` — N-03（順位）・景表法・ステマ規制の常時表示
- `src/styles/site.css` 325-450行 — 現在の帯の全定義と 🔒 の理由

## 連絡してよい相手

- 設計 → 無し（すべてオーケストレーター経由）
- 実装 → `org-design-T-061`（軽微な問合せに限る。1往復）
- レビュー → `org-implementation-T-061`（2往復）
- テスト → `org-implementation-T-061`（2往復）

## 申し送り

- **PO は同じ帯で3回差し戻している。** 「値を揃える」ではなく「**関係を作る**」で解くこと。T-053 も T-057 も値を揃えて通し、次の指摘を招いた
- **PO 提示の完了フィードバック候補のうち「リクエストを受け付けました！優先的に対応を進めます ✨」は使えない。** 優先順位を約束する事実がサービス側に無い。「ありがとうございます！開発の参考にさせていただきます」側を土台にする
- **絵文字は使ってよいが、`pergram-ui-copy.md` の「使ってはいけないビジュアル」（葉・植物・カプセル・錠剤・筋肉・ハート・体のシルエット・チェックマーク・人物写真）に当たるものを選ばない。** 🙇 は人物の姿勢であり、避けるほうが安全
- **`request.note`（「メールアドレスの登録は不要です」）は消さない。** 対価が要らないことが押せる理由そのもので、消すと押下率の意味が変わる（既存 🔒）。マイクロコピーはこれの**置き換えではなく追加**である
- 測定の道具立て（Chrome の起動オプション、`document.fonts.ready`、`Emulation.setDeviceMetricsOverride`）は T-053 / T-057 で確立済み。**作り直さない**
- 静的配信: `node scripts/preview.js --build-only` → 任意の静的サーバで `.preview` を配る。`node scripts/serve.js` は**動かない**（モジュールであって CLI ではない）

## 成果物

- `docs/design/design.md` §4「要望の帯の組版（2026-09-08 / T-061 で全面改訂）」— 帯の視覚設計書。**T-057 版の §4 を置き換えた**（854〜1462行）
- `docs/design/design.md` §3「字送りの階層」— T1 / T2 / T3 / T4 の割り当て表を更新し、T-061 の追記を挿入（790行〜）
- `docs/tasks/T-061.md` `## 決定ログ` — 8件

## ブロッカー

無し

## 決定ログ

### 2026-09-08 完了条件 B-1 を「4要素の左端が一致」から「2列。列の中で軸1本・列の上端が一致・死んだ空白なし」へ書き直す
- 対象: 要望の帯 / 構図
- 決定: 構図は**左にテキスト、右にボタンのブロック**の2列とする（PO 判断）。旧 B-1（全要素の左端が 1px 以内で一致）は**無効**にし、B-1a / B-1b / B-1c の3条件へ置き換える
- 却下案: 1列へ倒す（設計担当の案）→ 実測では条件を完全に満たしていたが、**その条件自体をオーケストレーターが過剰に書いていた。** 「軸が1本」を4要素すべての左端の一致と定義したため、2列が構造的に排除された。PO の指摘のとおり、**2列であること自体は問題ではなく、2列の中身の組み方が雑だったことが問題だった**（リード文の垂直中央浮き / 注記のボタンからの孤立 / 右の死んだ空白）
- 付帯条件: 設計をやり直す前に**デザインのベンチマークを取る**（PO 指示）。実在の製品の「テキスト左 / CTA 右」の帯を集め、寸法・階層・間隔の取り方を根拠にする
- 上書き対象: T-061 2026-09-08（帯を1列へ倒し、軸は `text-align` ではなく「箱の幅」で作る 🔒）
- 出典: セッション内の PO 判断、成果物なし

### 2026-09-08 「応援」は無償の第1段階に残し、衝突は有償の支援段の側を改名して解く
- 対象: 要望の帯 / 文言
- 決定: 第1段階の CTA は PO 指示の字句どおり **「正式版のリリースを応援する」**。有償の支援段（`request.supportHeading` / `lp.support.buttonText` / `lp.support.message`）を「寄付」など**お金だと一目で分かる語**へ改める
- 却下案: 無償側を「正式版のリリースを希望する」にする（設計担当の案。書き換えが1箇所で済む）→ **書き換えのコストが PO の指定した語感より優先されてはならない。** 「希望する」は押す動機が弱い
- 出典: セッション内の PO 判断、成果物なし


### 2026-09-08 帯の3回目の差し戻しを「値の調整」ではなく「構図の作り直し」として扱う
- 対象: 要望の帯 / 面の体系
- 決定: T-053・T-057 の完了判定は取り消さず、**構図（要素同士の関係）を作り直す新タスク**として起票する。完了条件 B-1 を「**4要素の軸が1本に揃うこと**」という関係の条件で書き、幅ごとの値の条件では書かない
- 却下案: T-057 を再開して条件を足す → レビュー通過・push 済みの完了判定を後から書き換えると「何をもって完了としたか」の記録が失われる（T-053 / T-057 の起票時と同じ理由）
- 却下案: 値（余白・列幅）をもう一度調整する → **2回それをやって2回とも差し戻された。** 3回目に同じ手を採る根拠が無い
- 出典: セッション内の PO 指示（スクリーンショット添付）、成果物なし

### 2026-09-08 帯を1列へ倒し、軸は `text-align` ではなく「箱の幅」で作る 🔒
- 対象: 要望の帯 / 構図
- 決定: 帯を**全ての幅・両ページで1列**にし、5要素（リード文・マイクロコピー・受領メッセージ・ボタン・注記）すべてに `width: 100%` + `max-width: var(--band-measure)` を当てて**同じ幅・同じ位置の箱**にする。横位置を決める規則を構造として1つだけにする。実測で 7幅・両ページ・両状態の `xSpread` / `cxSpread` がすべて **0**（design.md §4.7）
- 却下案: 2列のまま左列を上そろえにする → 縦の浮きは消えるが**軸は2本のまま**。リード文が第1列・ボタンが第2列にある限り、左端は列間隔ぶん必ず離れる。完了条件 B-1 と**構造的に両立しない**
- 却下案: 箱の幅を揃えず `text-align` だけで軸を作る → 製品一覧（左そろえ）と LP（中央そろえ）で満たす条件が変わり、B-1 の「混在は不可」の解釈がテストの主観になる。箱を揃えれば左端・中央が同時に一致する
- 却下案: 値（余白・列幅）をもう一度調整する → **2回やって2回とも差し戻された**（起票時の決定ログのとおり）
- 付帯条件: **停止条件 S-7 に該当する。** 1440px / 1280px で帯が 128.4px → 209.9px（+81.5px）に伸びる。うち 38.5px だけが2列をやめた代償で、残り 43px はマイクロコピーと主文の追加ぶん。**捨てたのは「1440px で帯が横に埋まること」、取ったのは「軸が1本であること」で、この2つは両立しない。** 実測値は design.md §4.8
- 上書き対象: T-057 2026-09-08（列の幅は中身の実寸から決める。余りからは決めない） / T-057 2026-09-08（1列と2列の切り替えは、画面幅ではなく帯そのものの幅で判定する） / T-053 2026-09-08（要望ボタンは 900px 以上で「左に文・右にボタン」の2列にする）
- 出典: docs/design/design.md §4.0〜§4.2 / §4.7 / §4.8。実測は Chrome ヘッドレス + CDP（`document.fonts.ready` 後）

### 2026-09-08 帯のリード文を T3 から T1 へ上げ、帯に「頭」を1つ作る
- 対象: 要望の帯 / 字送りの階層
- 決定: `.request-band__lede` を `--size-md`（18px）/ 700 / `--ink` / 1.5 にする。§3 の T1（面の見出し）へ割り当て、**新しい段（T0 等）は作らない**。マイクロコピーと注記はどちらも T4 に揃え、ボタンを同じ強さの2行で上下から挟む
- 却下案: リード文を T3（12.5px）のまま置く → 帯の中で最も重要な文が注記と同じ大きさになり、**どれが呼びかけの本体かが決まらない。** PO 差し戻し3回目の指摘の半分はこれである
- 却下案: 帯専用の新しい段（T0）を足す → 「面の頭」という役割は T1 が既に持っている。段を増やすと §3 が防いでいる「同じ役割が段ごとに違う値を持つ」へ戻る
- 却下案: 1440px だけリード文を `--size-lg` へ上げて横を埋める → 「同じ役割の文字が幅で違う値を持つ」。T-053 が直した状態そのものへ戻る
- 付帯条件: T-057 は同じ変更を「左列を埋めるため」として却下し、**判断自体は申し送りにしていた**（T-057 §4.11 最終行）。列が消えた今、問いは「列を埋めるか」ではなく「帯の頭は何か」であり、別の問いなので別の答えを採る
- 上書き対象: T-057 2026-09-08（リード文の字送り（T3）は変えない）
- 出典: docs/design/design.md §3 の追記 / §4.3

### 2026-09-08 受領メッセージはマイクロコピーと入れ替える。別の場所へ足さない 🔒
- 対象: 要望の帯 / 完了フィードバック
- 決定: `.request-band__received`（`role="status"` / 初期状態 `hidden`）をマイクロコピーの直後に置き、押下時に**マイクロコピーを `hidden` にして同じ枠へ**出す。文言は JS が `data-message` から書き込む。既に出ていれば押し直しで何もしない
- 却下案: 注記の下に受領メッセージを足す → 押した瞬間に帯が 22.5px 伸び、**ボタンが下へ動く**。入れ替えなら高さが変わらない（実測で 7幅・両ページとも押下前後の帯の高さが完全一致）
- 却下案: トーストで出す → 押下点から遠く、視線が3つ目の場所へ飛ぶ。**この帯の問題がまさにそれだった**
- 却下案: 文言を HTML に埋めて `hidden` を外すだけにする → `role="status"` は中身が変化したときに読み上げるので、読み上げが飛ぶ（`finishStep` の既存 ⚠️ と同じ理由）
- 付帯条件: `prefers-reduced-motion` の打ち消しを**新しく書かない**。`site.css` 末尾の全称セレクタの打ち消しが効く（完了条件 C-4）。2つ目を足すとどちらが効いているか分からなくなる
- 出典: docs/design/design.md §4.6 / §4.12

### 2026-09-08 第1段階（無償）の語は「応援」ではなく「希望」にする
- 対象: 要望の帯 / 文言
- 決定: `request.cta` を「正式版のリリースを希望する」、`request.headerCta` を「正式版を希望する」、`request.received` を「ご希望を受け取りました」にする。**有償の支援段は1文字も変えない**
- 却下案: PO 提示のまま「応援する」を第1段階に使い、有償側の語を変える → **有償側は3箇所で「応援」を使っている**（`request.supportHeading` / `lp.support.buttonText` / `lp.support.message` の本文）。後ろ2つは Codoc へ渡す PO 執筆のコピーで、書き換える面が大きく、無償の押下が課金導線と誤認される危険も残る。**動かす側を無償にするほうが安い**
- 付帯条件: 起票時の決定ログは「『正式版リリースを応援する』系へ変える（最終字句は担当が決める）」であり、字句の指定ではない。禁止事項の「どちらかの語を必ず分ける」はこれで満たす（完了条件 A-7）
- 出典: docs/design/design.md §4.10

### 2026-09-08 マイクロコピーは「匿名であること」だけを言う。注記と役割を分ける
- 対象: 要望の帯 / 文言
- 決定: ボタンの上（`request.micro`）＝**安全**「匿名で1件として数えます。個人を特定できる情報は残しません。」、ボタンの下（`request.note`、変更なし）＝**対価**「メールアドレスの登録は不要です。押すだけで送信します。」
- 却下案: マイクロコピーで「1タップで終わります」と言う → 注記の「押すだけで送信します。」と同じことを言う。2行あることの意味が消えて1行の冗長になる
- 却下案: 受領メッセージに ✨ を足す（PO 提示候補） → 帯の他の文が事実の平叙文なので、祝祭の記号だけが浮く
- 付帯条件: 「匿名」「個人を特定できる情報は残しません」は事実である（`request_signal` は UUID v4 / 日時 / どのページか の3列のみ）。**保持する列を増やしたらこの文言も見直す**
- 出典: docs/design/design.md §4.2 / §4.10

### 2026-09-08 帯の本文の1区切りは 17 文字以内（リード文は 13 文字以内）に収める 🔒
- 対象: 要望の帯 / 折り返し
- 決定: 句読点折り返し（`keep-all` + `overflow-wrap`）は T-057 から引き継ぎ、**適用先をマイクロコピーと受領メッセージへ広げる**。あわせて「1区切りの上限文字数」を設計の制約として明文化する。最も狭い測定幅 320px で中身が使える幅は 254px、`--size-sm` は 12.5px なので 20 文字が限界。17 文字なら 41px の余裕が残る
- 却下案: 上限を決めず実測だけで確かめる → **文言を書き換えるたびに 7幅の実測が要る。** 上限を1つ書けば、文言の側で機械的に守れる
- 付帯条件: リード文は `--size-md`（18px）なので上限は 13 文字（254 / 18 = 14.1）。**文言を書き換えたら design.md §4.5 の字数表を引き直す**
- 出典: docs/design/design.md §4.5

### 2026-09-08 `--band-cta-max` を `--band-measure` へ改名する（値は 26rem のまま）
- 対象: 要望の帯 / トークン
- 決定: トークン名を `--band-measure` へ変える。**値は変えない。** 帯の5要素が共有する箱の幅であって、CTA 列の上限ではなくなったため
- 却下案: 名前を据え置く → 2列が無くなった以上「CTA 列の上限」という名前は事実に反する。名前が嘘をつくと、次に触る担当がボタンだけに当てて軸を割る
- 却下案: 新しい名前を足して旧名を別名として残す → 1つの寸法に2つの名前があると、片方だけ直る事故が起きる（この帯で3回起きた形そのもの）
- 付帯条件: `site.css` / `lp.css` / `tokens.css` のコメントに旧名の参照が残っている。**改名時にすべて消す**（design.md §4.11 / §4.14）
- 出典: docs/design/design.md §4.11

### 2026-09-08 `lp.css` の `margin-top: 0` を `site.css` へ移す（変更範囲の拡張が要る）
- 対象: 要望の帯 / CSS の出所
- 決定: `.waitlist-band .request-band { margin-top: 0 }` を `site.css` へ移す。セレクタはそのままでよい（`.waitlist-band` は製品一覧に存在しないので無害）
- 却下案: `lp.css` に残す → **完了条件 B-6 は `lp.css` に帯の幾何の宣言が0件であることを `grep -c` で検査する。** `margin` はその一覧に入っており、残すと B-6 が落ちる
- 付帯条件: **タスクの `## 変更範囲` に `src/styles/lp.css` が無い。** 実装担当は着手前にオーケストレーターへ範囲の拡張を確認すること。変更は「宣言を1つ減らし、コメントの参照先を直す」だけで、宣言を増やす方向ではない。`products.css` も 1735行のコメント1つだけ直す必要がある（宣言は足さない）
- 出典: docs/design/design.md §4.9 / §4.11

### 2026-09-08 第1段階の CTA を「サービスの都合」から「押す人の意思表示」へ言い換える
- 対象: 要望の帯 / 文言
- 決定: `request.cta` を「成分・製品の追加をリクエスト」から「正式版リリースを応援する」系へ変える（最終字句は担当が決める）。ヘッダの `request.headerCta` も同じことを言う形へ追随させる
- 却下案: 帯だけ変えてヘッダは据え置く → **同じ導線が2つの名前を持つ**状態は、T-024 で既に指摘されている失敗の形そのもの
- 付帯条件: 有償の支援段（`request.supportHeading`「開発を応援する」）と語が衝突するため、どちらかを必ず分ける
- 出典: セッション内の PO 指示、成果物なし

## 作業ログ

- 2026-09-08 オーケストレーターが起票。設計（UIデザイナー役）へ割り当て
- 2026-09-08 `frontend-design` / `ecc:design-system` を読み込み。着手前の充足確認（完了条件・判断してよい範囲・参照すべき成果物の実在・目的の一意性）を通過
- 2026-09-08 `docs/decisions-index.md`（対象: 要望の帯 / 面の体系）と T-053 / T-057 の決定ログ、`design.md` §1〜§4、`site.css` 325-450 を確認
- 2026-09-08 Chrome ヘッドレス + CDP の測定台を組み、**現状**を 7幅・両ページで実測（軸が2本・リード文が 27.93px 下に浮く・LP と製品一覧で軸の作り方が割れている）
- 2026-09-08 帯の**近傍**（`h1` / `.explainer` / `.p-item` / LP の帯見出し）を実測し、ページの軸を確定
- 2026-09-08 新しい構図を実ページへ流し込んで 7幅・両ページ・**未押下と受領の両状態**で実測。`xSpread` / `cxSpread` がすべて 0 であることを確認
- 2026-09-08 受領の入れ替えで帯の高さが変わる幅の窓（404〜439px）を追加測定して特定
- 2026-09-08 `docs/design/design.md` の §3 を追記、§4 を全面差し替え（T-057 版を置換）

## 証拠

**設計タスクなのでコードの証拠は無い。以下は設計判断の根拠となった実測値である。**
測定は Chrome ヘッドレス（旧 `--headless`）+ CDP、`.preview` の `/ja/protein/` と `/ja/`、
`document.fonts.ready` を待ってから。モバイル幅は `Emulation.setDeviceMetricsOverride`。

**現状（差し戻された姿）— 製品一覧 1440px**

```
lede   x=365     w=361.25  y=479.20  （2列の左列）
button x=774.25  w=416     y=451.27  （2列の右列）
note   x=774.25  w=416     y=507.14
→ 左端が 2 箇所（365 / 774.25、409px 離れている）
→ リード文の上端はボタンの上端より 27.93px 下（align-items: center による垂直中央）
```

**現状 — LP 1440px（軸の作り方が製品一覧と割れている）**

```
lede   x=539.38  cx=720
button x=512     cx=720
note   x=552.5   cx=720
→ 中央は一致するが左端は 3 箇所（製品一覧は左端が一致し中央がバラバラ）
```

**新しい構図 — 7幅・両ページ・両状態（未押下 / 受領）**

```
xSpread（4要素の左端の最大差）  = 0px  … 320/390/768/900/1024/1280/1440 の全て
cxSpread（4要素の中央の最大差） = 0px  … 同上
間隔                            = 24 / 8 / 8 px … 全幅で一定
ボタン高                        = 47.88px（製品一覧）/ 44px（LP）… 🔒 44px 以上
横溢れ                          = 0 件（帯の中・文書全体とも）
最終行の文字数                  = 最短 9 字（孤立行なし）
押下前後の帯の高さ              = 完全一致（7幅・両ページ）
```

**帯の高さ（S-7 の代償）**

| 画面幅 | T-057（現行） | T-061 | 差 |
|---|---|---|---|
| 製品一覧 1440 / 1280 | 128.4 | 209.9 | +81.5 |
| 製品一覧 1024 / 900 | 166.9 | 209.9 | +43.0 |
| 製品一覧 768 | 150.9 | 193.9 | +43.0 |
| 製品一覧 390 / 320 | 195.9 | 265.9 | +70.0 |
| LP 1440 | 163 | 206 | +43.0 |

**⚠️ 例外1件** — 製品一覧の画面幅 **404〜439px** では、押下で帯が 22.5px **縮む**
（マイクロコピー 2行 → 受領メッセージ 1行）。完了条件の7幅には含まれない。

## 組織への指摘

- [道具] ヘッドレスブラウザで幅ごとの実測を取る手順が、道具として組織に無い。T-053 / T-057 / T-061 の3タスクが同じ測定台（Chrome の起動オプション、`document.fonts.ready` 待ち、`Emulation.setDeviceMetricsOverride`、依存なしの CDP クライアント）を毎回ゼロから書いている。タスクファイルの申し送りは「作り直さない」と書いているが、**残っているのは注意書きだけで、コードはどこにも残っていない**
- [反復] 同じ帯で3回、「値は正しいが関係が壊れている」という同じ種類の手戻りが起きた。完了条件が幅ごとの値（何 px 以内か）で書かれているあいだは、値を合わせて通り、次の目視で差し戻される。T-061 で初めて関係の条件（軸が何本か）で書かれた
- [規約] タスクの `## 変更範囲` に `src/styles/lp.css` が無いが、完了条件 B-6（`lp.css` に帯の幾何の宣言が0件）は `lp.css` を編集しないと満たせない。**完了条件と変更範囲が正面から食い違っている**
