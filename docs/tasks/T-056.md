# T-056: lp.css に site.css と同値の .request-* の上書きが残っている

## メタデータ
- 作成日: 2026-09-08
- 更新日: 2026-09-08
- 状態: 未着手
- 優先度: 低
- 担当: 未割当
- 進捗: T-053 のレビュー指摘 R-053-5 を範囲外として切り出した
- 依存: T-053（ブロッカー: T-053 が同じブロックを書き換えている最中のため）
- TDD適用可否: 適用
- 手戻り回数: 0

## 目的

`src/styles/lp.css:679-686` に、`site.css` と**同じ値**の指定が2つ残っている
（`justify-items: stretch` と `.waitlist-band .request-band__button { width: 100% }`。
出所は `src/styles/site.css:374` と `:381`）。

**T-053 は同じブロックの `:first-child` の規則を「値が同じなので写すと2つ目の出所になる」
という理由で削除した。** 残り2行にはその判断が適用されていない。

`.request-*` は LP と製品一覧の共用部品で、**出所が2つに割れていたことが T-051 の発端**である。
site.css 側を変えたとき、LP だけが古い値で固まる。

## 変更範囲
- `src/styles/lp.css` の `.waitlist-band .request-*` の上書き規則のみ
- `tests/panel_unify.test.js`（または `tests/styles.test.js`）に検査を足す場合

## 禁止事項
- `src/styles/site.css` を変えない（このタスクは重複を消すものであって、共通側の値を変えるものではない）
- LP の見た目を変えない。**削除後も LP の要望の帯が全幅1列のままであること**が前提
- `lp.css` 冒頭の 🔒（段そのものは site.css が唯一の出所）に反する追記をしない

## 完了条件
- 1. `src/styles/lp.css` に、`src/styles/site.css` と同値の `.request-*` の指定が残っていない
- 2. LP（`/ja/`）の実画面で、要望の帯のボタンが**全幅1列のまま**である
     （390px と 1280px の両方。削除で崩れていないこと）
- 3. LP と製品一覧の両方で横溢れ 0（`scrollWidth == clientWidth`）
- 4. 重複を戻すと落ちる検査がある（`src/styles/` のテキストを読む形でよい）
- 5. `npm test` が全件通り、`npm run build` が成功する

## 判断してよい範囲
- 検査をどのテストファイルへ置くか、どう書くか
- 削除だけで足りるか、site.css 側に LP 向けの条件が要るか

## テスト方法
- 単体: 実装担当。`src/styles/` のテキスト検査
- 実環境: ヘッドレス Chrome + CDP で LP の帯を 390px / 1280px で実測。実施者はオーケストレーター

## 停止条件
共通の停止条件のみ

## 参照すべき成果物
- `docs/design/design.md`「面の体系と字送りの階層（2026-09-08 / T-053）」§4 / §5
- `src/styles/lp.css` 冒頭の 🔒 コメント
- `docs/tasks/T-053.md` の決定ログ「LP の先頭の段の余白は `:has()` へ移さず、規則ごと削除する」
- `docs/tasks/T-051.md`（出所が2つに割れた発端）

## 連絡してよい相手
無し（すべてオーケストレーター経由）

## 申し送り
無し

## 成果物
（完了時に記入）

## ブロッカー
無し

## 決定ログ
特記なし

## 作業ログ
- 2026-09-08 T-053 のレビュー（R-053-5）で発見。範囲外として起票

## 証拠
（完了時に記入）

## 組織への指摘
無し
