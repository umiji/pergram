# T-017: ボタンのコントラスト比が WCAG 2.2 AA を下回っている（主CTA と 従CTA）

## メタデータ
- 作成日: 2026-09-02
- 更新日: 2026-09-02
- 状態: 未着手
- 優先度: 高
- 担当: 未割当
- 進捗: 未着手。T-010 のレビュー R-010-1（Major）を合流済み
- 依存: 無し（T-010 は完了済み）
- TDD適用可否: 適用
- 手戻り回数: 0

## 目的

`btn--signal`（背景 `#ea580c` に白文字）のコントラスト比が **約 3.6:1** で、
WCAG 2.2 AA が本文相当のテキストへ求める **4.5:1** を下回っている。

**T-010 でこれが悪化したわけではない**（signal ボタンの数も色も同じ）。
ただし T-010 によって**待機リスト登録が主導線になった**ため、
**最も押させたいボタンが読みにくい**という状態になった。測っているのはメール登録の件数である。

### 追記（2026-09-02・T-010 のレビュー R-010-1 を合流）

**従（ベータ版導線）の側にも同じ種類の問題がある。こちらのほうが重い。**

T-010 で従へ落とした2つのベータ版導線は `btn--quiet`（`src/styles/lp.css:105-113`）を当てており、
背景 transparent・文字色 `--ink`（本文と同色）・境界 `--line`=rgba(22,24,29,0.1) だけになった。
`--paper`(#fafaf9) に対する**境界のコントラスト比は約 1.25:1**（WCAG 2.2 AA の非テキスト 3:1 未満）。
**「押せる操作である」ことを示す手掛かりが、面でも文字色でも無い。**

T-010 の停止条件（「導線を弱めた結果、流入が実質ゼロになりそうなら相談する」）に該当すると
オーケストレーターが判定した。**`btn--subtle` へ替えても約 1.5:1 で解決しない。**
主従の関係は保ったまま、**従が「押せる」と分かる下限**を確保すること。

## 変更範囲

| ファイル | 変更内容 |
|---|---|
| `src/styles/tokens.css` | `btn--signal` に使う色の調整（🔒 色の唯一の出所） |
| `src/styles/lp.css` | 必要ならボタン側の当て方 |
| `tests/styles.test.js` | コントラスト比の下限を検査するテストの追加 |

## 禁止事項

- 🔒 **色をコンポーネントへ直書きしない。** `src/styles/tokens.css` が唯一の出所である
- 🔒 ブランドの視覚的な一貫性を壊さない。**橙系の主張は保つ**（別系統の色へ振り替えない）
- 🔒 `btn--signal` が付く**場所**を変えない。それは T-010 の領分
- 🔒 **CTA の主従を逆転させない。** 待機リストが主、ベータ版が従（T-010 の決定）。
  従を見えるようにすることと、従を主と同じ強さにすることは違う
- 🔒 文字を小さくして「大きな文字」の緩和基準（3:1）へ逃げない。**背景色側で解く**
- `src/templates/` を触らない

## 完了条件

1. `btn--signal` の前景色と背景色のコントラスト比が **4.5:1 以上**である
1b. `btn--quiet`（および `btn--subtle` を使う場合はそれも）の**境界または面**が、
   ページ背景に対して**非テキストコントラスト 3:1 以上**である
2. その判定が `tests/styles.test.js` の自動テストになっており、下回ると落ちる
3. 変更前の色を入れるとそのテストが**落ちる**ことを確認済み
4. `npm test` が通っている
5. `npm run build -- --strict` が終了コード 0
6. 他の `btn--*` のコントラスト比が、変更前より悪化していない

## 判断してよい範囲

- 具体的な色の値（橙系を保ち、4.5:1 を満たす範囲で）
- コントラスト比の計算のテスト実装（WCAG 2.x の相対輝度の式を使うこと）
- 他のボタンも併せて検査対象にするか

**決めてはならない**: ブランドカラーの系統変更。`btn--signal` を使う箇所の変更。

## テスト方法

- `npm test`
- `ecc:accessibility` スキルを呼ぶこと
- 実環境確認: 実装後に LP を表示し、見た目の印象が壊れていないこと。**実施者はテスト担当**

## 停止条件
- 共通の停止条件に加えて:
- 橙系を保ったまま 4.5:1 を満たせないと判明した場合（ブランドカラーの判断は PO の領分）

## 参照すべき成果物

| パス | 何を見るか |
|---|---|
| `src/styles/tokens.css` | 色の定義 |
| `src/styles/lp.css` | `btn--signal` / `btn--quiet` の当て方 |
| `tests/styles.test.js` | 既存のスタイル検査の書き方 |
| `.claude/rules/pergram-ui-copy.md` | ブランド表記と UI 実装の下限 |
| `docs/design/design.md` | 視覚の方向性 |

## 連絡してよい相手
無し（すべてオーケストレーター経由）

## 申し送り

- **発見の経緯**: T-010 の実装担当が `ecc:accessibility` スキルを呼んで得た所見。
  同担当はタップ領域 44px を割るテキストリンク化を却下する判断にも同スキルを使っている。
- **T-010 で悪化はしていない。** T-010 の合否には影響させない、と裁定済み。

## 成果物
（未着手）

## ブロッカー
無し

## 決定ログ

### 2026-09-02 従CTA の非テキストは「面と境界の良い方」で判定する
- 対象: LP のボタン / コントラスト検査
- 決定: 完了条件1b の判定を `max(面 vs ページ背景, 境界 vs ページ背景) >= 3:1` とする。
  面か境界のどちらか一方が満たせばよく、両方は求めない。
- 却下案: 境界だけを見る → 面（塗り）で解く直し方を、テストが理由なく塞いでしまう。
  完了条件1b の本文が「**境界または面**」と書いているので、テストもその通りにする。
- 出典: docs/tasks/T-017.md §完了条件1b、tests/styles.test.js「完了条件1b」

### 2026-09-02 完了条件3 は一回限りの手作業確認ではなく恒久テストにする
- 対象: LP のボタン / コントラスト検査
- 決定: 「変更前の色を入れるとテストが落ちる」を、変更前の色（`#ea580c` / 白、
  `rgba(22,24,29,0.1)` を `#fafaf9` に重ねたもの）を計算関数へ直接渡し、
  3.56:1 / 1.23:1 という**実測値と一致すること**を検査するテストに変換した。
  併せて黒白 21:1・同色 1:1・`#777` 4.48:1 で式そのものを自己検査する。
- 却下案: 実装時に一度だけ色を戻して落ちるのを目視で確認し、証拠に貼る
  → 確認した瞬間しか効かない。以後、計算側が壊れても誰も気付けない。
  色の判定はコードを読んでも正しさが分からない種類の処理なので、
  「測る道具が本当に測れているか」を恒久的に残す方を採った。
- 出典: tests/styles.test.js「完了条件3: コントラスト比の計算が、変更前の色を実際に落とせる」

### 2026-09-02 範囲外の2つのコントラスト不足を、テストにせず記録に残す
- 対象: LP のボタン / コントラスト検査
- 決定: 次の2つは同種の AA 未達だが、**アサーションにしない**。テストファイルの
  コメントと本タスクの記録に残すだけにする。
  (a) `.btn--quiet:hover` の文字（`--signal` を `--paper` の上に置く。変更前 3.41:1）
  (b) `products.css` の `.btn--ghost` の境界（変更前 1.23:1。`.btn--quiet` と同じ問題）
- 却下案: 併せてテストにする → (a) を 4.5:1 にすると、白地で 4.5:1 を満たす橙より
  さらに暗い橙が必要になる（白地で 4.5:1 ちょうどの色は `--paper` 地では約 4.31:1）。
  完了条件が名指ししていない条件のために停止条件（橙系を保てるか）へ踏み込ませることになる。
  (b) は `products.css` が T-017 の変更範囲に入っていない。
- 出典: tests/styles.test.js 冒頭の「測っていないもの（範囲外）」

## 作業ログ
- 2026-09-02: T-010 の実装担当の報告を受けてオーケストレーターが起票
- 2026-09-02（テスト担当）: `ecc:accessibility` を呼び、しきい値が
  文字 4.5:1（SC 1.4.3）・非テキスト 3:1（SC 1.4.11）であることを確認。T-017 の完了条件と一致。
- 2026-09-02（テスト担当）: 変更前の実測を取得。主CTA 3.56:1 / 従CTA の境界 1.23:1 で、
  T-010 のレビュー R-010-1 の「約 3.6 / 約 1.25」と一致した。
- 2026-09-02（テスト担当）: `tests/styles.test.js` へ受け入れテスト5件を追加。
  依存パッケージは足していない（相対輝度・アルファ合成・CSS の var 解決を自前で書いた）。
- 2026-09-02（テスト担当）: skip を外して実行し、**RED 2件 / GREEN 3件**を確認。
  さらに scratchpad へ `src/styles/` を複製して仮の修正を当てたところ 8件すべて GREEN になり、
  **テストが「直せば通る」ものであること**（＝満たしえない条件になっていないこと）を確認した。
  `src/` は一切変更していない。
- 2026-09-02（テスト担当）: RED 2件に `PENDING`（一時 skip）を付けて main の CI を赤にしない
  ようにした。アサーションの削除・緩和はしていない。

## 証拠

（受け入れテストの作成まで。実装は未着手）

```
$ node --test tests/styles.test.js
# tests 8 / # pass 6 / # fail 0 / # skipped 2
```

skip を外した状態（RED の確認。作業ツリーは変更せず、複製に対して実行）:

```
# tests 8 / # pass 6 / # fail 2
  not ok - 🔒 完了条件1: 主CTA (.btn--signal) の文字が背景に対して 4.5:1 以上
      lp.css .btn--signal: 3.56:1 / products.css .btn--signal: 3.56:1
  not ok - 🔒 完了条件1b: 従CTA の面か境界がページ背景に対して 3:1 以上
      .btn--quiet: 面 1:1 / 境界 1.23:1 （良い方でも 1.23:1）
```

完了条件5（変更前の時点での確認）:

```
$ npm run build -- --strict   → 終了コード 0
```

完了条件4 は**このタスクの原因ではない理由で未達**。`npm test` は 324件中5件が
`tests/waitlist_step2.test.js`（T-011、同じ作業ブランチで並行実装中）で落ちている。
`tests/styles.test.js` は0件失敗。

## 組織への指摘
- [規約] 完了条件に `npm test` が通ることを置くと、同じ作業ブランチで並行している
  別タスクの未完成分（今回は T-011 が skip を外した受け入れテスト5件）で常に落ち、
  このタスクの合否が自力で二値判定できない
