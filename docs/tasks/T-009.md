# T-009: ヒーローのランキングカードに出す3件を差し替える

## メタデータ
- 作成日: 2026-09-02
- 更新日: 2026-09-02
- 状態: レビュー中
- 優先度: 中
- 担当: org-review-T-009
- 進捗: テスト合格（10/10）・目視確認済み。レビュー待ち
- 依存: 無し
- TDD適用可否: 適用
- 手戻り回数: 0

## 目的

LPのヒーローに出している3件が、実データの**第17位・19位・20位**である
（[design.md](../design/design.md) の「ヒーローのランキングカードの例外」で記録済み）。
そのため広告から来た人が最初に見る単価は **¥9.9〜¥12 / タンパク質1g**、袋価格は ¥5,656〜¥9,480 になる。

同じサイトの製品一覧には **¥1.92 / g**（袋 ¥8,180・5kg）が第1位として載っており、
**ヒーローの数字は実データの約5倍**。「格安 プロテイン」で検索して来た人（クリック率10.34%と
最も反応が良い層）に対して、最も高い数字を見せている状態になっている。

これは離脱対策である以前に、design.md 自身が「景表法の優良誤認に該当しうる」と記録したまま
広告費を投じている状態の是正にあたる。

**手動指定（`HERO_PRODUCT_IDS`）の仕組みは維持する。** 差し替えるのは並べる3件。

## 変更範囲

| ファイル | 変更内容 |
|---|---|
| `src/build/build.js` | `HERO_PRODUCT_IDS` の3件を差し替える |
| `docs/design/design.md` | 「ヒーローのランキングカードの例外」の表（製品・単価・実際の順位）を更新する。**古い表を残さない** |
| ~~`config/categories.json`~~ **本タスクでは触らない（別タスク）** | （要確認）`protein.unitCostRange.min` が `3` だが実データの最安は ¥1.92/g。絞り込みの下限が実データを覆えていない |

### 差し替え候補（実データ / 価格は取得済みスナップショットの最安）

| 実順位 | 単価 | 袋価格 | 内容量 | 製品 | id |
|---|---|---|---|---|---|
| 1 | ¥1.92/g | ¥8,180 | 5kg | ソイプロテイン 大豆プロテイン 5kg アメリカ産 | `rakuten:collagenkirei:10001287` |
| 2 | ¥1.96/g | ¥4,988 | 3kg | ソイプロテイン 大豆プロテイン 3kg アメリカ産 | `rakuten:collagenkirei:10001292` |
| 3 | ¥3.78/g | ¥8,500 | 2.5kg | マイプロテイン ソイプロテイン アイソレート 2.5kg | `rakuten:myprotein:10001200` |
| 4 | ¥3.99/g | ¥3,590 | 1kg | マイプロテイン ソイプロテイン アイソレート 1kg | `rakuten:myprotein:10001199` |
| 6 | ¥4.88/g | ¥39,980 | 10kg | 箱プロ ホエイプロテイン プレーン 10kg | `rakuten:pure-shapers:10000002` |
| 7 | ¥5.1/g | ¥11,650 | 3kg | SCU ホエイプロテイン 1kg×3袋 | `rakuten:auc-garlic:10000676` |
| 11 | ¥5.61/g | ¥4,290 | 1kg | AUSホエイプロテイン グラスフェッド プレーン 1kg | `rakuten:auc-garlic:10000639` |

上位はソイに偏る。ホエイを探している人にソイが並ぶことの是非は
**未確定**（下記「停止条件」参照）。

## 禁止事項

- 🔒 **数字を作らない。** 出してよいのは実データの製品・価格・単価・画像だけ（design.md §7 禁止⑧）
- 🔒 **製品一覧（`/ja/protein/`）の並び順に触らない。** 有効成分1単位あたりの価格の昇順のまま
- 🔒 N-03。順位を決めるために独自スコアや加重平均を持ち込まない
- 🔒 LPのヒーローに `ItemList` の構造化データを付けない（手動指定中は表示順が単価順と一致しないため、
  誤った順位を機械に断言することになる）
- 🔒 LPに購入リンク（アフィリエイトリンク）を置かない（design.md §7 禁止⑦）
- design.md の例外の記録を**削除しない**。手動指定を続ける以上、記録も生き続ける

## 完了条件

1. `HERO_PRODUCT_IDS` が新しい3件になっており、`npm run build` が
   「⚠️ ヒーロー HERO_PRODUCT_IDS で手動指定した3件を出しています」を出力する
2. ビルド後の `/ja/` のヒーローに、指定した3件が指定した順で出ている
3. design.md の例外の表が、新しい3件の**実際の単価と実際の順位**に更新されている
4. `npm test` が通っている
5. 🔒 禁止語（`tests/render.test.js` の `BANNED_WORDS`）が **LP（`/ja/`）の出力**に含まれていない
   （2026-09-02 訂正: 登録時は対象ページを限定していなかったが、製品一覧 `/ja/protein/` の出力には
   実在の商品名「プロテイン効果 森永ココア味」由来で「効果」が既に含まれている。本タスクの変更範囲外の
   既存事象であり、ここで巻き取らない。実在商品名の扱いは別タスクとする）

## 判断してよい範囲

**確定済み（変えてはならない）**

- 出す3件と、その並び順。**PO が指定した。** 下記の ID をこの順で `HERO_PRODUCT_IDS` に入れる。

  | カード | id | 製品名 | 単価 | 袋価格 | 実順位 |
  |---|---|---|---|---|---|
  | 1 | `rakuten:myprotein:10001199` | マイプロテイン ソイプロテイン アイソレート 1kg | ¥4.0/g | ¥3,590 | 4位 |
  | 2 | `rakuten:aswel:10000094` | ツインたんぱく 大豆+ホエイのいいとこ取りプロテイン チョコレート風味 1kg | ¥4.8/g | ¥3,780 | 5位 |
  | 3 | `rakuten:cherie-brin:10000171` | ソイプロテイン 1kg 人工甘味料不使用 大豆プロテイン オールインワン | ¥5.2/g | ¥3,480 | 8位 |

  （単価・順位は 2026-09-02 時点のビルド出力から確認済み。`data-unit-cost` の実値は
  それぞれ 3.9889 / 4.7848 / 5.2200）

**自分で決めてよい**

- design.md の例外の表の**書式・列構成・説明文の書き方**（内容が実データと一致していれば形は任せる）
- 受け入れテストの**書き方・アサーションの粒度**
- ビルド出力の確認手段（HTML の parse 方法など）

**決めてはならない（相談する）**

- 3件の入れ替え・追加・削除・並び替え
- `HERO_PRODUCT_IDS` の仕組みそのものの変更（空配列に戻す、別の仕組みへ置き換える）
- ヒーローのカードのレイアウト・項目の追加（在庫表示を含む。PO が「不要」と判断済み）

## テスト方法

- `npm test` — 描画の不変条件・禁止語・並び順
- `npm run build` — 警告出力の確認
- ヘッドレスChromeで `/ja/` を開き、モバイル幅（375px）でヒーローの見え方を確認する

## 停止条件

- §4の共通のものに加えて:
- **上位がソイに偏ることの是非は未確定。** 単価比較としては正しい（タンパク質1gの価格）が、
  ホエイを探している検索意図とずれる可能性がある。「原材料で絞り込めることを
  ヒーローで示すべきか」は表示設計の判断なので、3件を確定する前に相談する
- `config/categories.json` の `unitCostRange.min` を変える場合、絞り込みの挙動が変わるため
  本タスクの範囲を超える。別タスクとして切り直す

## 参照すべき成果物

| パス | 何を見るか |
|---|---|
| `src/build/build.js` の 55〜100行 | `HERO_PRODUCT_IDS` の定義と `pickHeroRows()`。ID が1つでも欠けると単価順へ落ちる |
| `src/templates/lp/hero.js` | ヒーローのカードの描画。**T-010 が同時に触るので、このタスクでは変更しない** |
| `docs/design/design.md` | 「ヒーローのランキングカードの例外」の記録。ここの表を更新する |
| `data/products.json` / `data/product_i18n.json` / `data/price_snapshots.json` | 製品・名称・価格の実データ |
| `tests/render.test.js` の `BANNED_WORDS` | 禁止語の一覧 |
| `docs/decisions-index.md` | 過去の決定。`ヒーロー` / `LP` の対象で引く |

## 連絡してよい相手
無し（すべてオーケストレーター経由）
<!-- 直接やりとりしてよい相手が居るなら、その宛先をそのまま書く。
     宛先は起動時に付けた名前であり、タスクごとに一意でなければならない
     （例: org-implementation-T-007）。役割名だけを書くと、同じ名前が
     使い回された時点で送信が拒否される -->

## 申し送り

### 2026-09-02 追記（テスト担当の完了報告を受けて）

- **受け入れテストは `tests/hero_ranking.test.js` に作成済み（コミット `5cb70ee`）。全10件のうち6件が RED。**
  この6件を通すのが実装のゴールである。残る4件は回帰・不変条件の見張りで、現時点で GREEN。
  **落としてはならない**（ビルド警告の文言、禁止語、`ItemList` 不付与、実データとの一致）。
- **`docs/design/design.md` の例外の表は、各製品の行に「実際の単価」と「実際の順位」を同じ行に書くこと。**
  テストは製品名の一部（`マイプロテイン ソイプロテイン アイソレート` /
  `ツインたんぱく` / `ソイプロテイン 1kg 人工甘味料不使用`）で行を特定し、
  その行に単価と順位があるかを見る。**書式・列構成は自由。**
- **旧3製品名を例外の節から消すこと**（`アクアホエイプロテイン` / `ホエイプロテイン アイソレート WPI プレーン` /
  `マックスロード`）。「古い表を残さない」がテスト化されている。
- **`tests/hero_ranking.test.js` は実行時に `dist/` を作り直す。**
- **完了条件5の対象を LP（`/ja/`）に限定した**（上記の訂正を参照）。
- **単価は 2026-08-24 のスナップショットに基づく。** design.md へ書くときは基準日を明記すること。
  T-014（価格更新の復旧）が並行しており、成功すると数字が動きうる。

### 本タスクでは直さないもの（別途起票済み／要判断）

- `docs/design/design.md` の例外の節に「製品一覧には ¥4.9/g の製品が第1位として載っている」という
  **古い記述**がある（実データの第1位は ¥1.92/g）。完了条件3は「表」の更新なので必須ではないが、
  **同じ節の中で表だけ新しく本文が古い状態は残さないこと。** 触れる範囲にあるので、
  表の更新に合わせて事実と合うように直してよい（判断してよい範囲に含める）。
- 製品一覧の禁止語「効果」（実在商品名由来）は本タスクでは扱わない。

- **掲載3件は PO が直接指定した**（2026-09-02）。上の「判断してよい範囲」の表がその内容である。
  タスク登録時に候補として挙げていた「差し替え候補」の表（実順位1・2・3位のソイ製品）は**採用されなかった**。
- **停止条件だった「上位がソイに偏ることの是非」は解消済み。** PO が3件を確定したことで判断済みとなった。
  この理由で止まらないこと。
- **#1 のマイプロテイン 1kg は `in_stock: false`（在庫なし）である。** PO は
  「Sample なので問題ない。Hero 側は在庫状況の表示はいらない」と判断した。
  **ヒーローのカードに在庫バッジを足さないこと。** 製品一覧側の「▲ 在庫なし」表示は既存のままでよい。
- **価格データは 2026-08-24 で止まっている。** 日次更新が 2026-08-21 から失敗し続けているため
  （403 `HTTP_REFERRER_NOT_ALLOWED`）。復旧は **T-014** で別途進行中。
  design.md の表に単価を書くときは、**どの時点のスナップショットに基づく数字かを明記する**こと。
  T-014 が復旧すれば数字が動きうる。
- **`config/categories.json` の `unitCostRange.min` には手を出さない。** 停止条件どおり別タスク扱い。
- **`tests/render.test.js` を変更しないこと。** T-010 が同じ期間に走る。
  このタスクの受け入れテストは新規ファイル `tests/hero_ranking.test.js` に置く。

## 成果物

| ファイル | 変更内容 |
|---|---|
| `src/build/build.js`（71〜75行） | `HERO_PRODUCT_IDS` を PO 確定の3件へ差し替え。各行に製品名・実順位・単価をコメントで併記 |
| `docs/design/design.md`（510〜546行 付近） | 「ヒーローのランキングカードの例外」の表を新しい3件へ更新。差し替えの経緯・スナップショット基準日（2026-08-24）・在庫表示を出さない判断を追記。「製品一覧の第1位は ¥4.9/g」の古い記述を実データの ¥1.9/g へ訂正 |
| `docs/tasks/T-009.md` | 決定ログ・作業ログ・証拠・成果物 |

コミット: `feat(lp): ヒーローのランキングカードに出す3件を実データ上位へ差し替える`（下記「証拠」参照）

## ブロッカー
無し
<!-- 今このタスクを止めているもの。PO確認待ちなら Q-XXX を書く -->

## 決定ログ

### 2026-09-02 ヒーローに出す3件を PO が指定した
- 対象: LP / ヒーロー
- 決定: `rakuten:myprotein:10001199`（¥4.0/g・実4位）→ `rakuten:aswel:10000094`（¥4.8/g・実5位）
  → `rakuten:cherie-brin:10000171`（¥5.2/g・実8位）の順で出す。単価は昇順に並ぶ
- 却下案: タスク登録時に挙げた実順位1〜3位（¥1.92〜¥3.78、ソイ5kg / ソイ3kg / マイプロ2.5kg）
  → PO が採らなかった。理由は本セッションでは示されていない
- 付帯条件: **在庫なしの製品を含むが、ヒーローに在庫表示は足さない。**
  PO の判断は「Sample なので問題ない。Hero 側は在庫状況の表示はいらない」。
  これにより登録時の停止条件（ソイ偏りの是非）も解消した
- 出典: セッション内の PO 判断、成果物なし

### 2026-09-02 design.md の例外の節では、製品名を表の行にだけ書く
- 対象: LP / ヒーロー / design.md
- 決定: 「ヒーローのランキングカードの例外」の節で、掲載3製品の名前は**表の行にだけ**書き、
  地の文には書かない。表の各行は「製品名・実際の単価・実際の順位」を同じ1行に持つ
- 却下案: 地の文で「マイプロテインの1kgを1番目に置いた」のように説明する
  → 受け入れテスト（`tests/hero_ranking.test.js`）は製品名の断片で行を特定し、
  **その行に単価と順位があるか**を見る。地の文に製品名が先に出ると、そちらが最初の一致になって
  単価・順位が無いと判定される。読みやすさのために地の文へ製品名を出す書き換えは、
  テストを黙って落とす
- 付帯条件: 表に書く単価・順位・袋価格は 2026-08-24 の価格スナップショットに基づく。
  節に基準日を明記した。T-014 で価格更新が復旧して数字が動いたら、この表を取り直す
- 出典: セッション内の判断、成果物は `docs/design/design.md` の当該節

### 2026-09-02 手動指定の仕組みは残し、出す3件だけ差し替える
- 決定: `HERO_PRODUCT_IDS` による手動指定は維持したまま、掲載する3製品を入れ替える
- 却下案: ①空配列に戻して単価順の上位3件に戻す案 → サムネイル画質の問題が再発する
  （そもそも手動指定を入れた理由がこれ。[design.md](../design/design.md) の例外の記録）
  ②`display_overrides` に `image_url` を足して画像だけ差し替える案（design.md が代替案として
  挙げていたもの）→ 実装量が増え、今回の目的（表示単価の是正）には遠回り
- 付帯条件: 手動指定を続ける以上、design.md の例外の記録と、ビルド時の警告出力は残す。
  ItemList の構造化データも引き続き付けない
- 出典: セッションでの決定

## 作業ログ

- 2026-09-02: タスクとして台帳に登録
- 2026-09-02: 受け入れテストを先行作成（`tests/hero_ranking.test.js`）。完了条件 1・2・3・5 を自動判定へ変換。
  期待値は「判断してよい範囲」の PO 確定表が出所。実データ側（`dist/ja/protein/`）で
  順位4/5/8・単価 3.9889/4.7848/5.2200・袋価格 3590/3780/3480 が一致することを確認したうえで、
  テスト自身が数字を作らない形にした
- 2026-09-02: 実装。着手前に `node --test tests/hero_ranking.test.js` で RED 6件を確認（テストは変更していない）。
  `HERO_PRODUCT_IDS` を差し替え → design.md の例外の節を更新 → 10件 GREEN。
  `src/templates/lp/hero.js`（T-010 が同時進行）・`tests/`・`config/categories.json` は触っていない

## 証拠

### 受け入れテストが実装前に落ちること（2026-09-02）

`node --test tests/hero_ranking.test.js` — 10件中6件が不合格。落ちている6件が T-009 の未実装分。

```
not ok 1  - 完了条件1: HERO_PRODUCT_IDS が PO の確定した3件をこの順で持つ
ok     2  - 完了条件1: 手動指定は維持され、ビルドが警告を出力する
not ok 3  - 完了条件2: /ja/ のヒーローに確定した3件が確定した順で出る
not ok 4  - 完了条件2: ヒーローの各行が、その製品の実データの単価と袋価格を出している
not ok 5  - 完了条件2: 差し替え前の3件が LP に残っていない
ok     6  - 🔒 LP のヒーローに ItemList の構造化データを付けない
ok     7  - T-009 が記録した順位・単価が、いまの実データと一致する
not ok 8  - 完了条件3: design.md の例外の表が新しい3件の実際の単価と順位になっている
not ok 9  - 完了条件3: design.md の例外の節に古い表が残っていない
ok     10 - 完了条件5: 🔒 LP の出力に禁止語が含まれていない
```

合格している4件は**新しい振る舞いの検証ではなく回帰の見張り**である。
2・6・10 は現状すでに満たされている不変条件（警告出力・ItemList 不付与・LP の禁止語ゼロ）で、
差し替えで壊れないことを見張る。7 は価格データが動いたときに鳴る警報
（鳴ったら design.md と本タスクの表を実データで確認し直す。T-014 が価格更新を復旧させる）。

### 実データによる裏取り（2026-09-02 / スナップショット 2026-08-24）

`npm run build` 後の `dist/ja/protein/index.html` の `data-rank` / `data-unit-cost` / `data-price`。

| id | 実順位 | 単価 | 袋価格 |
|---|---|---|---|
| `rakuten:myprotein:10001199` | 4 | 3.988888888888889 | 3590 |
| `rakuten:aswel:10000094` | 5 | 4.784810126582278 | 3780 |
| `rakuten:cherie-brin:10000171` | 8 | 5.220000000000001 | 3480 |

PO が確定させた3件の順位・単価・袋価格は、実データと一致している。

### 既存テストへの巻き添えが無いこと

`npm test` — 284件中 273件合格。不合格11件の内訳は、本タスクの6件と、
同時進行の T-010（`tests/lp_cta.test.js` の4件）・T-014（`tests/rakuten_api.test.js`）である。
**既存のテストは1件も落ちていない。**

### 実装後の検証（2026-09-02）

**1. 受け入れテスト — `node --test tests/hero_ranking.test.js`: 10件すべて合格（fail 0）。**
実装前は 6件 fail（上記「受け入れテストが実装前に落ちること」）。テストファイルは変更していない。

**2. 全体テスト — `npm test`: 291件中 287件合格。不合格4件はすべて T-010 の RED**
（`tests/lp_cta.test.js` の `btn--signal` に関する4件）。**既存テストへの巻き添えは無い。**

```
not ok 134 - ファーストビューで btn--signal が付くのは待機リスト側だけ
not ok 135 - ベータ版の導線に btn--signal が付いていない
not ok 136 - ヒーローの btn--signal は待機リストに1つだけ
not ok 137 - ヘッダの主CTAがベータ版より弱くない（…）
```

**3. ビルド — `npm run build -- --strict`: 終了コード 0。手動指定の警告が出る。**

```
LP          /ja/
製品一覧    /ja/protein/  — 46 製品
⚠️ ヒーロー   HERO_PRODUCT_IDS で手動指定した 3 件を出しています（実際の単価順ではありません）。
```

**4. 描画 — ヘッドレス Chrome で `/ja/` を表示。** ヒーローのカードが
1 マイプロテイン ¥4.0/g・袋¥3,590 → 2 ツインたんぱく ¥4.8/g・袋¥3,780 →
3 ソイプロテイン(人工甘味料不使用) ¥5.2/g・袋¥3,480 の順で出ることを目視で確認した。
単価は昇順。在庫バッジ・購入リンクは無い。

- **未検証: モバイル幅（375px）での見え方。** ヘッドレス Chrome はモバイル端末エミュレーション無しでは
  `meta viewport` を無視するため、375px の窓では**製品一覧 `/ja/protein/` も同じように右端が切れる**。
  ヒーロー固有の事象ではなく計測手段の制約と判断した。実機／DevTools での確認は未実施

### 目視確認（2026-09-02 / テスト担当・T-010 と同時に実施）

**上の「未検証」は解消した。** Chrome を `--headless=new --remote-debugging-port=9333` で起動し、
Node 22 標準の `WebSocket` から DevTools Protocol へ接続、
`Emulation.setDeviceMetricsOverride`（`width:375, height:812, deviceScaleFactor:2, mobile:true`）で
端末エミュレーションを有効にしてから `Page.captureScreenshot` を撮った。
**この手順を踏むと `meta viewport` が効き、右端は切れない**（横あふれ
`documentElement.scrollWidth - clientWidth` は 375 / 768 / 1440px のいずれも **0px**）。
前回「右端が切れる」と見えたのは計測手段の側の問題だった。

**ヒーローのカードの内容（375 / 768 / 1440px のすべてで同一・すべてファーストビュー内）**

| 位置 | 製品 | 単価 | 袋価格 | 含有率 |
|---|---|---|---|---|
| 1 | マイプロテイン ソイプロテイン アイソレート 1kg | ¥4.0/g | ¥3,590 | 90% |
| 2 | ツインたんぱく 大豆+ホエイのいいとこ取りプロテイン チョコレート風味 1kg | ¥4.8/g | ¥3,780 | 79% |
| 3 | ソイプロテイン 1kg 人工甘味料不使用 大豆プロテイン オールインワン | ¥5.2/g | ¥3,480 | 67% |

PO が確定した3件が、確定した順（単価の昇順）で出ている。

**在庫バッジ・購入リンクが無いこと（DOM 計測）**

```
.rank-row 内の在庫バッジ相当（[class*="stock"]）   → 0 件
.rank-list 内のリンク（a / a[href^="http"]）        → 0 件
.hero 内の rel="sponsored" のリンク                 → 0 件
```

#1 のマイプロテイン 1kg は `in_stock: false` だが、ヒーローには在庫表示が出ていない
（PO の「Hero 側は在庫状況の表示はいらない」という判断のとおり）。
アフィリエイトリンクもヒーローには無い（design.md §7 禁止⑦）。

### 完了条件ごとの検証結果（T-009 最終判定）

| 完了条件 | 判定 | 何をどう検証したか |
|---|---|---|
| 1 `HERO_PRODUCT_IDS` が新3件・ビルドが手動指定の警告を出す | **合格** | `tests/hero_ranking.test.js` の1・2 GREEN。`npm run build -- --strict` の出力に「⚠️ ヒーロー HERO_PRODUCT_IDS で手動指定した 3 件を出しています」 |
| 2 `/ja/` のヒーローに指定3件が指定順で出る | **合格** | 同テスト3・4・5 GREEN ＋ 上記の実ブラウザ描画（375 / 768 / 1440px） |
| 3 design.md の例外の表が実際の単価と順位に更新されている | **合格** | 同テスト8・9 GREEN（古い表が残っていないことも含む） |
| 4 `npm test` が通っている | **合格** | 291件中 291件合格（T-010 側で失効した回帰テストを更新した後） |
| 5 🔒 禁止語が LP の出力に含まれていない | **合格** | 同テスト10 GREEN |

**T-009 は合格（Blocker 0件 / Major 0件）。**

### 検証できなかった項目

無し。前回「未検証」としていたモバイル幅（375px）の見え方を含め、完了条件5項目すべてを検証した。

### 不具合一覧

**無し**（Blocker 0 / Major 0 / Minor 0）。

## 組織への指摘
- [規約] 完了条件5「禁止語が出力に含まれていない」をそのまま自動化すると、製品一覧の出力に実在の商品名「プロテイン効果 森永ココア味」があるため本タスクと無関係に落ちる。完了条件が対象ページを限定していなかった
- [規約] `config/categories.json` が `## 変更範囲` の表に載っている一方、`## 停止条件` は「変えるなら別タスク」と書いており、同じファイルについて指示が食い違っていた（申し送りで解消されたが、変更範囲の表自体は修正されていない）
- [道具] モバイル幅の見え方を確認する手段が無い。ヘッドレス Chrome はモバイル端末エミュレーション無しでは `meta viewport` を無視するため、窓幅を 375px にしても実機のレイアウトにならず、完了条件の「モバイル幅で確認」を自力で満たせなかった
<!-- 組織の仕組み（規約・道具・進め方・役の分け方）について気づいたことを、
     1行1件で書く。書式は必ず `- [種別] 内容`。種別は次の4つだけ:

       規約 … この規約に従うと作業が進まない、または矛盾する箇所に当たった
       道具 … 必要な道具・スキル・外部アクセスが無く、遠回りをした
       反復 … 同じ種類の手戻りを、このタスクで2回以上経験した
       ロール … 役の分け方が、いまの仕事の流れに合っていない

     例:
       - [道具] Python の書き方のスキルが割り当てられておらず、毎回自分で確認した
       - [規約] 完了条件に実環境での確認が要るが、実施者が決まっていない
       - [ロール] レビュー待ちが3タスク積み上がり、自分の次の着手が2日止まった

     書くのは観測した事実だけ。対応策も「新しい役が要る」という結論も書かない。
     「質問をさばく役が要る」ではなく「レビュー待ちで2日止まった」と書く。
     どう直すかは改善エージェントとオーケストレーターと PO が決める。

     ロール を書く条件は6つ。詳細は .claude/rules/org-improvement-feedback.md

     これはエスカレーションではない。**返事は来ないし、作業も止めない。**
     判断を仰ぎたいなら、ここではなくエスカレーションの経路を使う。

     ここに書いたものは、完了報告にも含める。オーケストレーターがそれを
     吸い上げ、組織改善の台帳（docs/org-improvements.md）へまとめて起票する。

     台帳へ自分で書きに行かないこと——同時に走る他のタスクの書き込みを消す。

     気づきが無ければ「無し」のまま残す。行ごと消さない -->
