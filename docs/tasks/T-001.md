# T-001: GA4イベント計測を本番で完成させる

## メタデータ
- 作成日: 2026-08-18
- 更新日: 2026-08-18
- 状態: 未着手
- 進捗: 0%
- 依存: 無し

## 目的

広告出稿の判定は、LPのファネル（`lp_view` → `cta_click` → `waitlist_start` → `waitlist_submit`）を
GA4で読めることが前提になっている（[docs/ops/google-ads-first-campaign.md](../ops/google-ads-first-campaign.md) §3・§4）。

送信コード側はほぼ入っているが、**ヘッダCTAの計測用属性が未コミットのまま**で、
GA4管理画面側にもβ版画面の新イベント（`filter_click` / `list_show_more`）が登録されていない可能性がある。
このまま広告を回すと、支払ったクリックがどこで落ちたのか分からないまま7日が終わる。
広告テストの3つの目的（[同 §0](../ops/google-ads-first-campaign.md)）のうち「LPのメッセージが刺さるか（CVR）」が測れなくなる。

## 変更範囲

| 対象 | 内容 |
|---|---|
| [src/templates/lp/hero.js](../../src/templates/lp/hero.js) | ヘッダCTA2箇所への `data-cta` 属性（未コミット差分の確定） |
| GA4管理画面 | カスタムイベントの登録とキーイベント指定。コード変更を伴わない |
| デプロイ | 上記を本番（`*.workers.dev`）へ反映 |

送信イベントの現状（コード側は実装済み）:

| ファイル | イベント |
|---|---|
| [src/assets/lp.js](../../src/assets/lp.js) | `lp_view` / `scroll_depth` / `demo_interact` / `cta_click` / `waitlist_start` / `waitlist_submit` |
| [src/assets/products.js](../../src/assets/products.js) | `affiliate_click` / `filter_click` / `list_show_more` |

## 禁止事項

- 🔒 自由記述（`nutrients_other` / `requests`）の**本文をGA4に送らない**。書かれたかどうかの 0 / 1 だけを数える
- 🔒 メールアドレスその他の個人識別情報をイベントパラメータに含めない
- 🔒 健康状態・体調・服薬に関する値をパラメータに含めない（[requirements.md §11.1](../product/requirements.md)）
- **新しい外部スクリプトを足さない。** Google広告専用のコンバージョンタグを別途入れると
  [src/build/headers.js](../../src/build/headers.js) のCSP許可リスト変更が必要になる。GA4連携で完結させる
- イベント名・パラメータ名の設計変更はこのタスクの範囲外。今あるものを本番で動かすことに限定する
- `.env.example` の削除差分はこのタスクと無関係。ここでは触らない（別途扱いを決める）

## 完了条件

1. `npm test` が通る（失敗0）
2. `git status --short` の出力に `src/` 配下の差分が無い
3. `npm run build` 後、`dist/ja/index.html` に `data-cta` が5箇所出力される
   （`header_beta` / `header_waitlist` / `hero_beta` / `hero_waitlist` / `features_banner`）
   - 確認: `grep -o 'data-cta="[^"]*"' dist/ja/index.html | sort`
4. 本番へデプロイ済みで、公開URLのHTMLにも同じ5箇所が出ている
   - 確認: `curl -s <本番URL>/ja/ | grep -o 'data-cta="[^"]*"' | sort`
5. GA4のリアルタイムレポートで、実際に操作して以下が観測できる
   - LP: `cta_click`（`location` パラメータが付いていること）
   - `/ja/protein/`: `filter_click` / `list_show_more`
6. GA4管理画面の「カスタム定義」に、上記イベントのパラメータがカスタムディメンションとして登録されている
   （登録しないとレポートで内訳が見られず、イベント総数しか読めない）

## テスト方法

- **ユニット**: `npm test`（描画の不変条件。広告表示があっても並び順が単価昇順のままであること等も含む）
- **ローカル描画**: `npm run build` → `dist/` の生成物を grep で確認
- **実環境**: デプロイ後、Chromeヘッドレスで公開URLを開いて属性の有無を確認。
  イベントの実発火はGA4リアルタイム / DebugView で人間が目視する（自動化不可）

## 停止条件

- §4の共通のものに加えて:
- イベントパラメータに自由記述の本文・メールアドレス・健康情報が混入しうる設計になっていると分かった場合、
  実装せずに停止して報告する

## 決定ログ

### 2026-08-18 新規カスタムイベントとして追加する（既存イベントへのディメンション追加ではなく）
- 決定: β版画面の絞り込み・もっと見るは `filter_click` / `list_show_more` という新規イベントで送る
- 却下案: 既存イベントにカスタムディメンションを足してパラメータで区別する案 → GA4上の探索が
  イベント名で絞れなくなり、ファネル定義が読みにくくなるため。効果に大差がないならイベント名で明示的に分ける方を採る
- 出典: 2026-08-17 のセッション判断（成果物なし）

## 作業ログ

- 2026-08-17: `src/assets/products.js` に `filter_click` / `list_show_more` を追加（コミット済み）。
  `src/templates/lp/hero.js` のヘッダCTA2箇所に `data-cta` を追加（**未コミット**）
- 2026-08-18: タスクとして台帳に登録

## 証拠

（未着手）
