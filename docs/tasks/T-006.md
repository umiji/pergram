# T-006: クロール許可と LLMO 基盤の実装

## メタデータ
- 作成日: 2026-08-18
- 更新日: 2026-08-19
- 状態: ローカル検証済み
- 進捗: 100%（実環境デプロイ後の curl 確認のみ未実施）
- 依存: 無し（T-003〜T-005 の広告配信とは独立。robots.txt を開けても
  AdsBot-Google は `User-agent: *` を無視するため、広告審査には影響しない）

## 目的

`robots.txt` が `Disallow: /` で全面クロール拒否のため、検索エンジンにも
LLM にも一切認識されていない。validation-plan.md の判定が「継続 → SEO 起点に
切り替え」に振れたときに慌てて用意するのではなく、**LP だけ先にクロールを開け、
LLM 時代の流入経路（ChatGPT / Claude / Perplexity 等の回答内での引用）に必要な
機械可読の土台を今のうちに敷く。**

現状 sitemap.xml / llms.txt / JSON-LD はいずれも存在しない（`grep` で0件）。

## 変更範囲

- `src/build/crawl.js`（新規）— robots.txt / sitemap.xml / llms.txt の生成。
  **公開範囲の唯一の出所**を1つのポリシー定数に置き、3ファイルすべてをそこから導く
- `src/lib/jsonld.js`（新規）— JSON-LD の組み立て
- `src/build/build.js` — 上記の配線、`robots.txt` 直書きの削除
- `src/templates/layout.js` — robots メタ、JSON-LD 差し込み口、GSC 所有権確認タグ
- `src/templates/lp.js` — WebSite / Organization の JSON-LD
- `src/templates/products.js` — BreadcrumbList / ItemList の JSON-LD
- `src/templates/products/item.js` — プレースホルダ行に `data-nosnippet`
- `tests/crawl.test.js`（新規） / `tests/render.test.js`
- `docs/ops/deploy.md` — robots.txt の記述を実態に合わせる
- `CLAUDE.md` — 壊しやすい箇所の表に `src/build/crawl.js` を追加

## 禁止事項

- 🔒 製品一覧 `/ja/protein/` をクロール許可しない。β版の暫定措置で
  `¥X` / `¥XXXX` のプレースホルダ価格が出ており（`src/templates/products/item.js`）、
  AI クローラに読ませると LLM が偽の金額を引用しうる
- 🔒 JSON-LD に `aggregateRating` / `review` を入れない（N-08）
- 🔒 llms.txt に成分の効能・効果を書かない（N-02）。独自スコアも書かない（N-03）
- 🔒 llms.txt / JSON-LD は既存の禁止語（効く・効果・改善・高品質・おすすめ 等）を使わない
- 🔒 LP のヒーローに ItemList を付けない。`HERO_PRODUCT_IDS` の手動指定で
  行番号と実際の単価順が食い違っており、構造化データにすると誤った順位を機械可読で断言することになる
- ドメインを `src/lib/site.js` 以外に書かない
- hreflang を `/ja/` と `/en/` の間に張らない（不可逆な設計判断）

## 完了条件

1. `npm run build` が成功し、`dist/robots.txt` が `/ja/` を Allow・
   `/ja/protein/` を Disallow し、`Sitemap:` 行を含む
2. `dist/sitemap.xml` が生成され、**Disallow 対象の URL を1つも含まない**
3. `dist/llms.txt` が生成され、禁止語を1つも含まない
4. `dist/ja/index.html` に `WebSite` と `Organization` の JSON-LD が入り、
   `JSON.parse` が通る
5. `npm test` が全て通る

## テスト方法

- 単体: `tests/crawl.test.js` — robots とサイトマップの整合（Disallow の URL が
  サイトマップに出ない）、llms.txt の禁止語、AI クローラの許可
- 回帰: `tests/render.test.js` — JSON-LD が `JSON.parse` 可能、禁止語なし、
  `aggregateRating` を含まない
- 実環境: デプロイ後に `curl` で `/robots.txt` `/sitemap.xml` `/llms.txt` を取得。
  Search Console への登録はドメイン確定後（本タスクの範囲外）

## 停止条件

- §4 の共通条件に加え、独自ドメインへの移行が必要になった場合（`SITE_ORIGIN` が
  `workers.dev` のままサイトマップを提出すると、移行時に全 URL の申告やり直しになる）

## 決定ログ

### 2026-08-18 LP のみクロール許可し、製品一覧は Disallow のままにする
- 決定: `/ja/` を Allow、`/ja/protein/` を Disallow。AI クローラ（GPTBot /
  ClaudeBot / PerplexityBot / Google-Extended / CCBot 等）は学習系も含めて全許可
- 却下案: ①全ページ許可＋プレースホルダ行に `data-nosnippet` → `data-nosnippet` は
  Google のスニペットにしか効かず、GPTBot 等の取得自体は止められないため
  「偽の金額を LLM に読ませない」を満たさない。②全部そのまま許可 → 同上でリスクが残る
- 付帯条件: `Disallow` はクロールを止めるがインデックス登録は止めない。LP から
  `/ja/protein/` へリンクがあるため Google が URL のみで登録する可能性が残る。
  完全に消すには `noindex` メタが要るが、それはクロールを許可しないと読まれないため両立しない。
  被リンクゼロの新規サイトのため確率は低いと判断して受け入れる
- 出典: session decision, no artifact

### 2026-08-18 公開範囲を1つの定数から robots / sitemap / llms の3ファイルに導く
- 決定: `src/build/crawl.js` に公開ポリシーを1つ置き、3ファイルすべてをそこから生成する
- 却下案: ファイルごとに別モジュールへ分ける → robots.txt で Disallow した URL を
  sitemap.xml に載せる、という自己矛盾が静かに起きる。実際に多いミスであり、
  同居させればテストで固定できる
- 出典: session decision, no artifact

### 2026-08-18 LP のヒーローには ItemList を付けない
- 決定: JSON-LD は LP に WebSite / Organization、製品一覧に BreadcrumbList / ItemList
- 却下案: LP のヒーロー3件を ItemList にする → `HERO_PRODUCT_IDS` の β版暫定措置で
  表示順が実際の単価順と一致していない。人間が読む分には「Sample」と明示してあるが、
  構造化データにすると誤った順位を機械可読で断言することになる
- 出典: CLAUDE.md「ヒーローのランキングカードの例外」/ src/templates/lp/hero.js:6-7

## 作業ログ

- 2026-08-18 タスク登録。現状調査（robots.txt の Disallow、sitemap/llms/JSON-LD の不在、
  プレースホルダ価格の存在、製品46件）を完了。
- 2026-08-19 実装完了。`src/build/crawl.js` / `src/lib/jsonld.js` を新規作成し、
  build.js・layout.js・lp.js・products.js・item.js に配線。
  `tests/crawl.test.js`（12件）を新規作成、`tests/render.test.js` に構造化データの
  検査11件を追加。ローカル配信（`npm run preview:dist`）で3ファイルの
  HTTP 応答と Content-Type を確認。

## 証拠

### 完了条件 1・2・3: 生成される3ファイル

```
$ npm run build
LP          /ja/
製品一覧    /ja/protein/  — 46 製品
クロール    /ja/ を許可 / /ja/protein/ /api/ を拒否
            robots.txt  sitemap.xml  llms.txt を出力
Search Console 未設定 — GOOGLE_SITE_VERIFICATION を渡すと所有権確認タグが入ります。

$ cat dist/robots.txt
（コメント略）
User-agent: *
Disallow: /ja/protein/
Disallow: /api/

Sitemap: https://pergram.pergram-official.workers.dev/sitemap.xml

$ cat dist/sitemap.xml
<?xml version="1.0" encoding="UTF-8"?>
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">
  <url>
    <loc>https://pergram.pergram-official.workers.dev/ja/</loc>
    <lastmod>2026-08-16</lastmod>
    <changefreq>daily</changefreq>
    <priority>1.0</priority>
  </url>
</urlset>
```

サイトマップに `/ja/protein/` は入っていない（完了条件2）。
llms.txt の禁止語ゼロは `tests/crawl.test.js` の
「🔒 llms.txt に禁止語を書かない」で固定（完了条件3）。

### 完了条件 4: JSON-LD が parse できる

```
$ node -e "... dist/ja/index.html から ld+json を取り出して JSON.parse ..."
parsed types: Organization, WebSite
robots meta: max-snippet:-1, max-image-preview:large, max-video-preview:-1

$ node -e "... dist/ja/protein/index.html ..."
raw < in JSON-LD: false
parsed types: BreadcrumbList, ItemList
first item: {"@type":"ListItem","position":1,"name":"ソイプロテイン 大豆プロテイン 5kg ..."}
```

### 完了条件 5: テスト

```
$ npm test
# tests 256
# pass 256
# fail 0
```

（本タスク前は 233 件。新規 `tests/crawl.test.js` 12 件 + `tests/render.test.js` 11 件を追加）

### 実配信の確認（ローカル）

```
$ npm run preview:dist  &  curl -D- -o /dev/null http://127.0.0.1:4174/robots.txt
HTTP/1.1 200 OK
Content-Type: text/plain; charset=utf-8

$ curl -D- -o /dev/null http://127.0.0.1:4174/sitemap.xml
HTTP/1.1 200 OK
Content-Type: application/xml; charset=utf-8

$ curl -D- -o /dev/null http://127.0.0.1:4174/llms.txt
HTTP/1.1 200 OK
Content-Type: text/plain; charset=utf-8
```

### 途中で見つけて直した不具合

JSON-LD のエスケープ処理で `'<'` と書くべきところが `'<'` の
単一バックスラッシュ（= JS では文字 `<` そのもの）になっており、置換が
無効化されていた。製品名に `<` が入ると `<script>` がそこで打ち切られ、
**構造化データが丸ごと壊れたうえページ本体の描画も崩れる**。
`tests/render.test.js` の「🔒 JSON-LD の中で < をエスケープする」が検出した。

### 未検証

- **本番環境での応答は未確認。** デプロイ後に `/robots.txt` `/sitemap.xml`
  `/llms.txt` を curl して確認する必要がある（Cloudflare Workers の
  静的アセットが `.xml` にどの Content-Type を付けるかはローカルの
  `scripts/serve.js` とは別経路）。
- Search Console への登録は未実施。独自ドメイン確定後に行う（本タスク範囲外）。
- 実際に AI クローラが llms.txt を読むかは検証不能（各社が公開していない）。
