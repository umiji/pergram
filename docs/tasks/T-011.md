# T-011: 待機リストフォームを2段階にする

## メタデータ
- 作成日: 2026-09-02
- 更新日: 2026-09-02
- 状態: 実装中
- 優先度: 高
- 担当: 未割当
- 進捗: 受け入れテスト24件作成済み（RED11件はskip中）。実装が未着手
- 依存: 無し（2026-09-02 に PO の指示でブロッカーを解除。経緯は `## 申し送り`）
- TDD適用可否: 適用
- 手戻り回数: 0

## 目的

流入の**100%がスマートフォン**であるのに対し、現在のフォームは1画面に4項目ある
（メールアドレス／見たい成分チップ＋自由記述／普段の購入先チップ／その他ご要望の自由記述）。
狭い画面でのアンケート項目数は離脱に直結する。

**ステップ1をメールアドレス1項目だけにし、残りを登録完了後のステップ2へ移す。**

## 変更範囲

| ファイル | 変更内容 |
|---|---|
| `src/templates/lp/form.js` | ステップ1をメールのみに。残り3項目をステップ2（完了状態の中）へ |
| `src/assets/lp.js` | ステップ2の送信。GA4イベントを追加（ステップ2の到達と回答） |
| `worker/waitlist.js` | 既存レコードへの追記経路。**列は増やさない** |
| `worker/schema.sql` / `worker/migrations/` | 追記経路に必要な変更があれば |
| `tests/worker.test.js` | 上記と必ず揃える |
| `locales/*.json` | 文言 |

**保存してよい列は現行の6列のまま**（`email` / `nutrients` / `channel` / `nutrients_other` /
`requests` / `created_at`）。2段階化は入力の分割であって、取得項目の追加ではない。

## 禁止事項

- 🔒 **送信後に別ページへ飛ばさない。** 同一ページ内で完了状態に切り替える（`form.js` の既存の 🔒）。
  ステップ2は完了状態の中に置く
- 🔒 **自由記述の注記（`lp.form.freeTextNote`）を一緒にステップ2へ移す。消さない。**
  これはN-01 / N-05（症状・服薬の書き込み口）に対する唯一の防波堤。自由記述だけ移して注記を
  置き去りにしない
- 🔒 **自由記述の中身を解釈して弾かない。** サーバがやるのは長さで切ることだけ
  （上限は `src/lib/waitlist_fields.js`）
- 🔒 チップの並びも許可リストも `src/lib/waitlist_fields.js` が唯一の出所。
  LP側とWorker側で二重に持たない
- 🔒 年齢・性別・体調・服用中サプリを取らない。列を増やさない
- 🔒 GA4に自由記述の本文を送らない。書かれたかどうか（0/1）だけ
- ステップ2を必須にしない（ステップ1の送信時点で登録は完了している）

## 完了条件

1. ステップ1の入力欄がメールアドレス1つだけになっている
2. ステップ1の送信だけでD1にレコードが作られる（ステップ2に進まなくても登録は成立する）
3. ステップ2の回答がD1の**同一レコードに追記**される（新規レコードを作らない）
4. GA4で「ステップ2に到達した数」と「ステップ2を送信した数」が別々に数えられる
5. 自由記述の注記がステップ2に存在する
6. `npm test` が通っている（`tests/worker.test.js` を含む）
7. 🔒 禁止語が出力に含まれていない

## 判断してよい範囲

**自分で決めてよい**

- ステップ2の UI の作り方（表示のしかた、項目の並び、見出しの有無）
- GA4 の**イベント名の綴り**。ただし決めた綴りを**完了報告に必ず書くこと**
  （T-007 が GA4 管理画面へ登録するのに要る）
- ステップ2の送信の実装方法（同一エンドポイントか専用パスか）と、
  D1 側の追記のしかた（`ON CONFLICT(email)` か `UPDATE ... WHERE email` か）
- `tests/mini_dom.js` への DOM API の追加（**道具であって期待値ではない**。足りなければ足してよい）
- `tests/worker.test.js` の更新（自分の実装と揃える。これは実装担当の領分）
- エラー時の見せ方

**決めてはならない（相談する）**

- **保存する列を増やすこと。** 現行6列のまま（`email` / `nutrients` / `channel` /
  `nutrients_other` / `requests` / `created_at`）
- ステップ2を必須にすること
- 送信後に別ページへ遷移させること
- `src/lib/waitlist_fields.js` の許可リスト・チップの並びを LP 側や Worker 側で二重に持つこと
- 自由記述の中身をサーバ側で解釈して弾くこと
- 受け入れテスト `tests/waitlist_step2.test.js` のアサーションを緩めること

## テスト方法

- `npm test` — 描画・バリデーション・Worker
- 本番相当環境で、①ステップ1だけで離脱 ②ステップ2まで完了 の2経路を実際に送信し、
  D1のレコードがそれぞれ期待どおりになることを確認する
- ヘッドレスChrome 375px でステップ1・ステップ2の両方を確認

## 停止条件

- §4の共通のものに加えて:
- 既存レコードへの追記に、現行スキーマの主キー設計では対応できないと分かった場合、
  マイグレーションの是非を相談する（既存データへの影響があるため）
- ステップ2の到達率が著しく低く「見たい成分」の回答数がステップ1の件数を大きく下回る場合、
  検証の主目的が測れなくなる。§決定ログの付帯条件に従って構成を見直す

## 参照すべき成果物

| パス | 何を見るか |
|---|---|
| `tests/waitlist_step2.test.js` | **受け入れテスト（先行作成済み・24件）。これを通すのがゴール** |
| `tests/mini_dom.js` | `src/assets/lp.js` を `node:vm` 上で動かす最小 DOM。テスト担当が新規作成 |
| `src/templates/lp/form.js` | 現在のフォーム。🔒 送信後に別ページへ飛ばさない旨のコメントがある |
| `src/assets/lp.js` | 送信処理と GA4 イベント |
| `src/lib/waitlist_fields.js` | チップの並び・許可リスト・長さの上限の**唯一の出所** |
| `worker/waitlist.js` / `worker/schema.sql` / `worker/migrations/` | 保存経路 |
| `tests/worker.test.js` | Worker のテストの組み立て方（実 D1 無しで書ける） |
| `.claude/rules/pergram-compliance.md` | 保存してよい列、自由記述の扱い、GA4 へ送ってよいもの |
| `docs/decisions-index.md` | 対象 `待機リスト` / `フォーム` で引く |

## 連絡してよい相手
無し（すべてオーケストレーター経由）
<!-- 直接やりとりしてよい相手が居るなら、その宛先をそのまま書く。
     宛先は起動時に付けた名前であり、タスクごとに一意でなければならない
     （例: org-implementation-T-007）。役割名だけを書くと、同じ名前が
     使い回された時点で送信が拒否される -->

## 申し送り

### 2026-09-02 追記3（RED 11件に skip を付けてある）

**`tests/waitlist_step2.test.js` の未実装ぶん 11 件には `PENDING`（skip）が付いている。**
作業ブランチを `main` へマージするにあたり、**CI を赤にしたままにしないための一時措置**として
オーケストレーターが付けた。**テストの中身は一切変えていない**（アサーションの削除も緩和もしていない）。

**実装担当は、着手したらまず `PENDING` を外して RED を確認すること。**
RED を見ずに実装を始めると、テストが何を要求しているのかを取り違える。
実装が終わった時点で `PENDING` の定義ごと削除する。ファイル冒頭にも同じことが書いてある。

**skip を付けた11件**（`PENDING` を外すと落ちる）:
完了条件1の2件 / 完了条件5の2件 / ステップ2の3項目 / ステップ2を必須にしない /
完了条件3の2件 / 完了条件4の3件。**回帰の見張り13件は skip していない。**


### 2026-09-02 追記2（テスト担当の完了報告からの転記）

**受け入れテストは `tests/waitlist_step2.test.js`（新規・24件、コミット `49b2b25`）。
RED 11件・回帰の見張り GREEN 13件。** この11件を通すのがゴールで、13件は落としてはならない。

**実装が知らないと詰まる契約が4つある。**

1. **ステップ1 = `.waitlist__done` の外のフォーム、ステップ2 = `.waitlist__done` の中のフォーム。**
   テストはこの構造で両者を探す（禁止事項「ステップ2は完了状態の中に置く」に基づく）
2. **GA4 のイベント名の綴りはテストで固定されていない。** 到達時と送信時に
   「それまでに無いイベントが増える」ことだけを見る。**ただし決めた綴りを完了報告に書くこと**
   （T-007 が GA4 管理画面へ登録するのに要る）
3. **ステップ2の送信は `/api/waitlist` で始まるパスへ、ステップ1と同じメールアドレスを載せて送る**
   （同一レコードへの追記のため）
4. **`tests/mini_dom.js` は道具であって期待値ではない。** 実装が使う DOM API が足りなければ
   足してよい。**アサーションを緩めるのはこれとは別の話であり、禁止である**

**テスト担当が検証できなかったもの（実装担当も同じ制約を受ける）**

- **実 D1 での行数**（本当に同一レコードか）。偽 D1 は SQL とバインド値しか見えないため、
  SQL の形までしか判定できない。**本番相当環境での2経路の実送信は引き続き必要**であり、
  これは PO の領分として残る
- GA4 管理画面での計測（T-007 の領分。判定対象外）
- 375px の見え方（実装後にテスト担当が実施）

### 2026-09-02 追記（着手にあたっての申し送り）

**T-007 のブロッカーを PO の指示で解除した。** 元の理由は
「計測の疎通が確認できていない状態でフォームの構造を変えない」だった。

- **懸念は消えていない。** T-007（コンバージョン計測の疎通確認）は未着手のままである。
  この状態でフォームを変えると、**登録数が動かなかったときに「フォームが悪い」のか
  「計測が落ちている」のかが区別できない。** オーケストレーターはこれを PO へ伝えたうえで着手する。
- **したがって完了条件4（GA4でステップ2の到達数と送信数が別々に数えられる）は、
  「イベントが正しく送出されていること」までを判定範囲とする。**
  GA4 の管理画面で実際に数えられるかは T-007 の領分であり、このタスクでは判定しない。
  **送出の確認は、ブラウザのネットワーク記録か `src/assets/lp.js` の単体テストで行う。**

### 変更範囲について

- **`locales/*.json` は T-020 も同時に触る。** 自分が足す・変えるキーだけを操作し、
  他のキーの行を並べ替えたり整形したりしないこと。
- **`src/templates/lp/hero.js` / `src/styles/` は T-020 の持ち物である。触らないこと。**
- `tests/render.test.js` と `tests/lp_cta.test.js` も T-020 が触る。**触らないこと。**
  このタスクの受け入れテストは**新規ファイル**に置く。

### 環境の注意

- **`npx wrangler` を使う作業（D1 のローカル実行など）は、実行許可で拒否される可能性がある。**
  拒否されたら回避策を探さず報告すること（共通の停止条件 S-4）。
  `tests/worker.test.js` の既存の書き方を見れば、Worker のテストが実 D1 無しで書けることが分かる。
無し
<!-- 上流タスクの完了報告から、この担当が知らないと判断を誤る事実を、
     オーケストレーターが転記する。登録した後で埋まる唯一の指示欄 -->

## 成果物
<!-- 完了時に何ができたか。ファイルパスとコミット -->

## ブロッカー
無し
<!-- 今このタスクを止めているもの。PO確認待ちなら Q-XXX を書く -->

## 決定ログ

### 2026-09-02 ステップ1をメールアドレスのみにする
- 決定: 1画面目の入力をメールアドレス1項目に絞り、見たい成分・購入先・要望をステップ2へ移す
- 却下案: **ステップ1に「見たい成分」チップを残す案**（メール＋チップの2項目）
  → 「見たい成分」は[validation-plan.md §6](../research/validation-plan.md)で
  **P1で作る成分の決定に直結する検証の最重要項目**と定義されており、`form.js` にも
  🔒「見たい成分はメールアドレスの直下に置く。ここが検証の主目的」と書かれている。
  ステップ2へ移すと、この回答数がステップ2の到達率に丸ごと依存する。
  それでも離脱の削減を優先する判断をユーザーが行った
- 付帯条件: ①ステップ2の到達率と送信率をGA4で必ず別々に計測する
  ②[validation-plan.md §5](../research/validation-plan.md) の判定指標「成分アンケート回答率」の
  **分母がステップ2到達者に変わる**ため、次回の判定時に読み替えを明示する
  ③到達率が低く成分の回答が集まらない場合は、チップをステップ1へ戻すことを再検討する
- 出典: セッションでの決定（2026-09-02）

### 2026-09-02 受け入れテストは GA4 のイベント名の綴りを固定しない
- 対象: 待機リスト / 計測
- 決定: 完了条件4（到達数と送信数を別々に数える）の判定を、**「ステップ2に到達した時点で
  それまでに無いイベントが増える」「送信した時点でさらに別のイベントが増える」**という
  差分で行う。イベント名の綴りは実装が決めてよい
- 却下案: 受け入れテスト側で `waitlist_step2_view` / `waitlist_step2_submit` と綴りを固定する
  → 完了条件が求めているのは「別々に数えられること」であって特定の綴りではない。
  綴りを先に決めるとテストが実装手段を縛る。**ただし GA4 管理画面への登録（T-007 の領分）で
  綴りが要るので、実装が決めた綴りは完了報告に書く必要がある**
- 出典: tests/waitlist_step2.test.js の冒頭コメント

### 2026-09-02 ステップ2は「完了状態（.waitlist__done）の中のフォーム」と定義する
- 対象: 待機リスト / LP のフォーム
- 決定: 受け入れテストは、ステップ1を「`.waitlist__done` の外のフォーム」、ステップ2を
  「`.waitlist__done` の中のフォーム」として探す。この構造そのものを契約として固定する
- 却下案: 特定のクラス名（`.waitlist-step2` など）を決め打ちする → 命名は実装の裁量。
  一方「完了状態の中に置く」はタスクの禁止事項（別ページへ飛ばさない）から来る不変条件なので、
  こちらを契約にするほうが強く、かつ実装を縛らない
- 出典: docs/tasks/T-011.md §禁止事項

### 2026-09-02 ブラウザ相当の検証を自前の最小 DOM（tests/mini_dom.js）で行う
- 対象: テスト基盤
- 決定: `src/assets/lp.js` を `node:vm` の上で実行する最小の DOM を `tests/mini_dom.js` に置き、
  GA4 へ送ったイベント・`/api/waitlist` への送信・別ページへの遷移の有無を機械的に見る
- 却下案: ①jsdom を入れる → **このリポジトリは依存パッケージ無しが方針**（package.json に
  dependencies が無い）。テストのためだけに破らない ②`src/assets/lp.js` を文字列として
  正規表現で検査する → 「イベントを書いた」ことしか見えず、「到達時に送られる」かどうかを
  判定できない。完了条件4は送出のタイミングまで含む ③ヘッドレス Chrome で LP を開く →
  受け入れテストは `npm test` の中で完結させたい（CI・他タスクからも回るため）
- 付帯条件: **mini_dom.js はテストの道具であって期待値ではない。** 実装が使う DOM API が
  足りなければ mini_dom.js に足してよい（assert を緩めるのは別の話）
- 出典: tests/mini_dom.js の冒頭コメント

## 作業ログ

- 2026-09-02: タスクとして台帳に登録
- 2026-09-02 (org-test): 完了条件を受け入れテストへ変換。`tests/waitlist_step2.test.js`（24件）と
  ブラウザ相当の実行環境 `tests/mini_dom.js` を新規作成。`src/` `worker/` `locales/` および
  T-020 が触るテストには一切触れていない
- 2026-09-02 (org-test): `npm test` を実行し、未実装ぶん11件が落ち、禁止事項の見張り13件が
  通ること、既存307件が巻き添えで落ちていないことを確認
- 2026-09-02 (org-test): 受け入れテストが**実装されれば通る**ことを、リポジトリ外（scratchpad）に
  置いた2段階フォームの模擬実装で確認（テストが原理的に通らない状態で実装へ渡さないため）

## 証拠

### 受け入れテストの状態（実装前・2026-09-02）

```
$ npm test
# tests 319
# pass 308
# fail 11
```

落ちている11件はすべて `tests/waitlist_step2.test.js` の**ステップ2に依存する**もの。
既存テスト（308件のうち307件）は巻き添えで落ちていない。

| # | テスト | 対応する完了条件 | 状態 |
|---|---|---|---|
| 1 | ステップ1の入力欄がメールアドレス1つだけである | 1 | RED |
| 2 | 見たい成分・購入先・自由記述はステップ1に残っていない | 1 | RED |
| 3 | 🔒 自由記述の注記がステップ2にある（ja / en） | 5 | RED |
| 4 | 🔒 注記は自由記述と同じ側にある | 5 | RED |
| 5 | ステップ2に3項目が揃っている（チップの出所は waitlist_fields.js） | 1 / 3 | RED |
| 6 | 🔒 ステップ2を必須にしない（required を付けない） | 2 | RED |
| 7 | 🔒 入力欄の name が保存列の範囲を超えていない | 🔒 列を増やさない | GREEN |
| 8 | 🔒 年齢・性別・体調・服薬を取る入力欄が無い | 🔒 N-05 | GREEN |
| 9 | 🔒 どのフォームも action / target を持たない | 🔒 別ページへ飛ばさない | GREEN |
| 10 | 🔒 自由記述の maxlength が waitlist_fields.js と一致 | 🔒 出所はひとつ | GREEN |
| 11 | 🔒 禁止語が待機リストの出力に無い（ja / en） | 7 | GREEN |
| 12 | ステップ1の送信だけで /api/waitlist へ飛ぶ | 2 | GREEN |
| 13 | 送信に失敗したら完了状態に切り替えない | 2 | GREEN |
| 14 | ステップ2の送信が同じメールアドレスで飛ぶ | 3 | RED |
| 15 | ステップ2の送信に3項目が載っている | 3 | RED |
| 16 | ステップ2への**到達**が別イベントとして送られる | 4 | RED |
| 17 | ステップ2の**送信**が到達とは別のイベントとして送られる | 4 | RED |
| 18 | 🔒 GA4 に自由記述の本文とメールアドレスを送らない（0/1 だけ） | 4 | RED |
| 19 | 🔒 GA4 にメールアドレスを送らない（ステップ1の経路） | 🔒 | GREEN |
| 20 | メールアドレスだけの送信でレコードが作られる（Worker・6列） | 2 | GREEN |
| 21 | 🔒 ステップ2の書き込みが既存の行を狙う（新しい行を作らない） | 3 | GREEN |
| 22 | ステップ2で送った3項目が保存の引数に載る | 3 | GREEN |
| 23 | 🔒 スキーマの waitlist テーブルの列が6つのまま | 🔒 | GREEN |
| 24 | 🔒 移行 SQL が保存列の外に列を足していない | 🔒 | GREEN |

### 受け入れテストが「実装されれば通る」ことの確認

RED の11件が原理的に通らないテストでないことを、scratchpad に置いた2段階フォームの
模擬実装（HTML ＋ ステップ2に対応した lp.js の写し）に対して回して確認した。
到達 `waitlist_step2_view` / 送信 `waitlist_step2_submit` の2イベントで判定を満たす。
**この綴りは例であって、テストは綴りを要求していない。**

### このタスクで検証できないもの（実装後も残る）

- 実 D1 での**行数**（同一レコードへの追記であること）。テストの偽 D1 は SQL とバインド値しか
  見ないため、`ON CONFLICT(email)` か `UPDATE ... WHERE email` であることまでしか判定できない。
  タスクの §テスト方法にある**本番相当環境での2経路の実送信**が引き続き必要
- GA4 管理画面で到達数・送信数が実際に数えられること（T-007 の領分。申し送りのとおり
  このタスクの判定範囲外）
- 375px での見え方（ヘッドレス Chrome での目視確認）

## 組織への指摘
- [規約] タスクファイルの `## 判断してよい範囲` と `## 参照すべき成果物` が雛形の案内文のまま空だったが、同じ内容が起動時のメッセージ側に書かれていた。指示欄が空でも担当が付いている状態を検査は警告するはずで、指示の出所が2箇所に分かれている
- [道具] `src/assets/lp.js` はブラウザ用の IIFE で、実行して検証する道具がリポジトリに無かった（依存パッケージ無しの方針で jsdom も入らない）。最小の DOM（`tests/mini_dom.js`）を自作した。今後 LP のクライアント側の挙動を検証するタスクは、毎回ここに当たる
<!-- 組織の仕組み（規約・道具・進め方・役の分け方）について気づいたことを、
     1行1件で書く。書式は必ず `- [種別] 内容`。種別は次の4つだけ:

       規約 … この規約に従うと作業が進まない、または矛盾する箇所に当たった
       道具 … 必要な道具・スキル・外部アクセスが無く、遠回りをした
       反復 … 同じ種類の手戻りを、このタスクで2回以上経験した
       ロール … 役の分け方が、いまの仕事の流れに合っていない

     例:
       - [道具] Python の書き方のスキルが割り当てられておらず、毎回自分で確認した
       - [規約] 完了条件に実環境での確認が要るが、実施者が決まっていない
       - [ロール] レビュー待ちが3タスク積み上がり、自分の次の着手が2日止まった

     書くのは観測した事実だけ。対応策も「新しい役が要る」という結論も書かない。
     「質問をさばく役が要る」ではなく「レビュー待ちで2日止まった」と書く。
     どう直すかは改善エージェントとオーケストレーターと PO が決める。

     ロール を書く条件は6つ。詳細は .claude/rules/org-improvement-feedback.md

     これはエスカレーションではない。**返事は来ないし、作業も止めない。**
     判断を仰ぎたいなら、ここではなくエスカレーションの経路を使う。

     ここに書いたものは、完了報告にも含める。オーケストレーターがそれを
     吸い上げ、組織改善の台帳（docs/org-improvements.md）へまとめて起票する。

     台帳へ自分で書きに行かないこと——同時に走る他のタスクの書き込みを消す。

     気づきが無ければ「無し」のまま残す。行ごと消さない -->
