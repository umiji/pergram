# T-016: 古くなった運用・設計ドキュメントの記述を実態へ合わせる

## メタデータ
- 作成日: 2026-09-02
- 更新日: 2026-09-02
- 状態: 未着手
- 優先度: 中
- 担当: 未割当
- 進捗: ドキュメント更新済み。完了条件1〜4を確認済み（3 は下記の但し書きあり）
- 依存: T-010（推奨: CTA 主従の記述は T-010 の結果が確定してから直すほうが二度手間にならない）
- TDD適用可否: 適用外（ドキュメントの記述の正しさは自動テストへ変換できない。順序は 実装 → レビュー）
- 手戻り回数: 0

## 目的

実装が進んだ結果、**ドキュメントの記述が実態と食い違っている箇所**が見つかっている。
いずれも、読んだ担当が誤った前提で作業を始めうる。

| # | ファイル | 現在の記述 | 実態 |
|---|---|---|---|
| 1 | `docs/ops/deploy.md` 末尾 | 「独自ドメイン 未取得。当面は `*.workers.dev` で動かす」 | `pergram.site` が稼働中 |
| 2 | `docs/design/design.md` / `docs/design/service.md` | LP の CTA 主従が 2026-08-10 の「主CTA＝製品一覧」のまま**の可能性**（未確認） | T-010 で待機リストが主になった |

**#2 は未確認である。** まず該当記述があるかを確かめ、無ければ「無し」と記録して終える。

## 変更範囲

| ファイル | 変更内容 |
|---|---|
| `docs/ops/deploy.md` | 独自ドメインの記述を実態へ |
| `docs/design/design.md` | CTA 主従の記述があれば更新 |
| `docs/design/service.md` | 同上 |

## 禁止事項

- 🔒 `docs/design/design.md` の「ヒーローのランキングカードの例外」の節を触らない（T-009 が更新済み）
- 🔒 `docs/ops/deploy.md` の secret の置き場の記述を触らない（T-014 が訂正済み）
- 🔒 コード・テスト・`locales/` を触らない。**ドキュメントだけのタスクである**
- 🔒 決定の記録（過去の経緯）を消さない。**更新であって削除ではない**
- ドキュメント間の相互リンクを壊さない（相対リンクで参照し合っている）

## 完了条件

1. `docs/ops/deploy.md` に「独自ドメイン 未取得」に相当する記述が残っていない
2. `docs/design/design.md` と `docs/design/service.md` の CTA 主従の記述が、T-010 後の実装と一致している
   （該当記述が無い場合は、無かったことを `## 証拠` に記録する）
3. `npm test` が通っている（ドキュメントのみの変更なので影響しないはずだが、確認する）
4. 変更した各記述について、**何を根拠に実態と判断したか**が `## 証拠` に書かれている

## 判断してよい範囲

- 文章の書き方・構成
- 経緯をどこまで残すか（消さない範囲で）
- #2 の該当有無の判定

**決めてはならない**: 設計そのものの変更。ドキュメントの新規作成・分割・移動。

## テスト方法

- `npm test`（回帰）
- 相対リンクの参照先が実在すること

## 停止条件
- 共通の停止条件に加えて:
- ドキュメントの記述と実装のどちらが正しいか判断できない場合（設計判断に踏み込むため）

## 参照すべき成果物

| パス | 何を見るか |
|---|---|
| `docs/ops/deploy.md` | 独自ドメインの記述 |
| `docs/design/design.md` / `docs/design/service.md` | CTA 主従 |
| `docs/tasks/T-010.md` の決定ログ | 主従を入れ替えた理由 |
| `docs/tasks/T-014.md` の証拠 | ドメインまわりの実測結果 |
| `wrangler.toml` | 稼働中のドメイン |

## 連絡してよい相手
無し（すべてオーケストレーター経由）

## 申し送り

- #1 は T-014 の実装担当が発見し、範囲外として残したもの。
- #2 は T-010 の実装担当が可能性として挙げたもので、**未確認**。

## 成果物

| ファイル | 変更 |
|---|---|
| `docs/ops/deploy.md` | L119-120「確認する URL」に本番オリジン `https://pergram.site` と出所（`src/lib/site.js` の `SITE_ORIGIN`）を明記。L230-241「決めていないこと」の独自ドメインの項を、取得済み・稼働中・旧ドメインは恒久転送、へ書き換え。Search Console のプロパティも `https://pergram.site/` に確定 |
| `docs/design/design.md` | L425「LP からの動線」を、ヘッダ／ヒーロー／製品一覧の3行の表 + 主従の注記へ書き換え。`hero_waitlist` の GA4 イベントが 2026-09-02 以降ゼロになることを明記 |
| `docs/design/service.md` | **変更なし**（CTA 主従の記述が存在しないことを確認。下記 証拠） |

コミット: `fcb7117`（`org/lp-quality-and-waitlist-step2` へ push 済み）

## ブロッカー
無し

## 決定ログ

### 2026-09-02 LP の CTA 主従は「LP 全体」ではなく「ヘッダ／ヒーロー」の粒度で書く
- 対象: ドキュメント / LP / CTA
- 決定: `docs/design/design.md` の「LP からの動線」で、CTA の主従を**場所ごとの表**として書く。
  「LP の主 CTA は待機リストである」という一文に畳まない。ヘッダは T-010（待機リストが主）、
  ヒーローは T-020（β版1本）で状態が違い、1文に畳むとどちらかが必ず実態と食い違うため。
- 却下案: (a)「LP の主 CTA は待機リスト」と1文で書く → ヒーロー（β版のみ）と食い違う。
  (b) T-020 が新しいので「LP の主 CTA はβ版」と書く → ヘッダ（`btn--signal` は待機リスト）と食い違う。
  (c) 経緯（2026-08-08 の `btn--quiet` 2本）を消して現状だけ書く → 禁止事項「決定の記録を消さない」に反する。
  そのため経緯は末尾の1行に畳んで残した。
- 出典: `src/templates/lp/hero.js`（`siteHeader` L30-49 / `hero` L128-145）、
  `tests/lp_cta.test.js` L51-60・L176-236、`tests/render.test.js` L422-454

### 2026-09-02 `hero_waitlist` の GA4 がゼロになることを、設計ドキュメント側にも書く
- 対象: ドキュメント / LP / CTA / GA4
- 決定: 計測の記述がある `docs/design/design.md` の動線の節に、
  「`hero_waitlist` は 2026-09-02 以降ゼロになる。計測漏れではない」「登録数は `header_waitlist` と
  フォーム送信で見る」「綴りは予約したままにし、他の要素へ付け替えない」を明記した。
- 却下案: T-020 の決定ログと `tests/lp_cta.test.js` のコメントに書いてあるので設計ドキュメントには書かない
  → 数字を見る人（PO・広告の運用）はタスク台帳やテストを読まない。**ゼロを不具合と読み違える経路が残る。**
- 出典: `tests/lp_cta.test.js` L54-60（`RETIRED_CTAS`）、`docs/decisions-index.md` L57

## 作業ログ
- 2026-09-02: T-014 / T-010 の実装担当の報告を受けてオーケストレーターが起票
- 2026-09-02: #1（独自ドメイン）を調査。`docs/ops/deploy.md` 内の `workers.dev` の記述は1箇所のみ（変更前 L232）で、
  他に旧ドメイン前提の記述は無いことを `grep` で確認。オーケストレーターから渡された本番の実測
  （`pergram.site` 稼働・`workers.dev` は 301・`robots.txt` / `sitemap.xml` / `llms.txt` は 200 で
  `Sitemap:` と `<loc>` は `pergram.site` を指す）と、`wrangler.toml` / `src/lib/site.js` /
  `worker/index.js` のコードを根拠に書き換えた。**「未取得」を消すだけでなく、旧ドメインの転送方式
  （閲覧 301 / それ以外 308）と `run_worker_first` の前提も併記した。**
  「決めていないこと」の節に残したのは**旧ドメインを畳む時期**であり、ドメインの取得可否ではない。
- 2026-09-02: #2（CTA 主従）を調査。`docs/design/design.md` L427 に該当記述を発見（1件）。
  `docs/design/service.md` には**該当記述が無い**ことを確認（本体サービスの製品一覧の定義書で、LP を扱っていない）。
- 2026-09-02: `docs/` 全体を `workers.dev` で `grep`。範囲外に3ファイル（下記 証拠）。**直さず報告のみ。**
- 2026-09-02: `npm test` を実行。失敗11件はすべて `tests/waitlist_step2.test.js`（T-011。同じ作業ツリーで
  別の担当が作業中）。ドキュメント関連の4ファイルは 124件すべて通過。

## 証拠

### 完了条件1: `docs/ops/deploy.md` に「独自ドメイン 未取得」に相当する記述が残っていない

```
$ grep -n "未取得\|当面は" docs/ops/deploy.md
（出力なし。終了コード 1）

$ grep -n "workers\.dev" docs/ops/deploy.md
232:  **`https://pergram.site/`**。旧 `*.workers.dev` では登録しない（…）
237:  `https://pergram.site/` を指している）。旧 `pergram.pergram-official.workers.dev` は
```
残った2件はいずれも「**旧**ドメイン」「登録しない」という文脈であり、稼働先として書いていない。
`workers.dev` を消しきらないのは意図的で、**旧アドレスがまだ 301 で生きていること自体が
運用上必要な事実**（`run_worker_first` を false に戻してよい条件がこれに紐づく）だからである。

書き換え後の根拠（すべて実測またはコード）:

| 記述 | 根拠 |
|---|---|
| `pergram.site` が本番として稼働 | オーケストレーターの curl 実測（2026-09-02）。`/robots.txt` `/sitemap.xml` `/llms.txt` が 200、`Sitemap:` と `<loc>` は `https://pergram.site/` |
| 本番オリジンの出所は `SITE_ORIGIN` | `src/lib/site.js` L13 `process.env.SITE_ORIGIN ?? 'https://pergram.site'` |
| 旧 `pergram.pergram-official.workers.dev` は恒久転送 | 実測 301。コード側は `worker/index.js` L32 `LEGACY_HOSTS` / L46-53（閲覧 301・それ以外 308） |
| `run_worker_first = true` が前提 | `wrangler.toml` L18-23（「旧ドメインを畳んだら false に戻してよい」） |
| 移行日 2026-08-24 | `docs/ops/deploy.md` L98（T-014 が記録済み） |

### 完了条件2: CTA 主従の記述が T-010 後の実装と一致している

**該当記述は `docs/design/design.md` L427 の1件のみ。`docs/design/service.md` には無い。**

```
$ grep -n "CTA\|Waitlist\|waitlist\|ベータ\|待機リスト" docs/design/service.md
82:初回接触時（LP・OGP・広告）は**必ずタグラインとセットで出す**。…
```
→ CTA の主従に触れた行は0件。service.md は本体サービス（製品一覧）の UI/UX 定義書であり、
LP の CTA を扱っていない。**「無かった」ことをここに記録する。**

実装の実態（コードとテストで裏を取ったもの。推測は無い）:

| 場所 | 実装 | 出所 |
|---|---|---|
| ヘッダ 待機リスト | `btn btn--signal` / `href="#waitlist"` / `data-cta="header_waitlist"` | `src/templates/lp/hero.js` L46-49 |
| ヘッダ β版 | `btn btn--quiet` / `data-cta="header_beta"` | 同 L30-36 |
| ヒーロー β版 | `btn btn--dark btn--block hero__beta` / `data-cta="hero_beta"` | 同 L138-140 |
| ヒーロー 待機リスト | **出力されない**（要素ごと無い。CSS で隠していない） | 同 L128-134 のコメント／`hero()` の `hero__actions` に `${beta}` のみ |
| `lp.ctaNote` | ヒーローに出力されない（`locales` にキーは残る） | 同上 |

テストが固定している不変条件:
- `tests/lp_cta.test.js` L51-52 `HEADER_CTAS = ['header_beta','header_waitlist']` / `HERO_CTAS = ['hero_beta']`
- 同 L54-60 `RETIRED_CTAS = ['hero_waitlist']`（「これ以降 `cta_click` の内訳から hero_waitlist が消えるのは仕様」）
- 同 L205「ヘッダで btn--signal が付くのは待機リスト側だけ」
- 同 L223「ヒーローのCTAはβ版の1つだけである」
- `tests/render.test.js` L422-454「ベータ版への導線はヘッダとヒーローの両方に出る」

旧記述が食い違っていた点は3つで、すべて直した:
1. 「ヘッダとヒーロー両方に…を置いた（`btn--quiet`）」→ ヒーローは `btn--dark btn--block`
2. 「主 CTA の Waitlist 登録より一段弱い見た目」→ ヒーローに待機リストのボタンは無い（ヘッダのみ真）
3. 文言「ベータ版を使ってみる」→ 現在は `lp.nav.beta`「ランキングを見る」／
   `lp.hero.beta`「プロテインの最安ランキングを見る（β版）」（`locales/ja.json` L139・L141）

### 完了条件3: `npm test`

```
$ npm test
1..319
# pass 308
# fail 11
```

**失敗11件はすべて `tests/waitlist_step2.test.js`（T-011）。このタスクの変更とは無関係である。**
根拠は2つ:
- 本タスクの変更は `.md` 3ファイルのみ。テストが読む `.md` は `docs/design/design.md` だけで
  （`tests/hero_ranking.test.js` L65 `DESIGN_DOC`）、それを含むテストは通っている。
- `git status` に `M tests/waitlist_step2.test.js` があり、別の担当が同じ作業ツリーで作業中。

ドキュメントに関係する4ファイルだけを実行した結果:

```
$ node --test tests/hero_ranking.test.js tests/lp_cta.test.js tests/render.test.js tests/worker.test.js
# tests 124
# pass 124
# fail 0
```

### テスト方法「相対リンクの参照先が実在すること」

変更した2ファイル + service.md の Markdown 相対リンクを全件解決した。

```
$ python <scratchpad>/checklinks.py
checked files: 3 broken: 0
```

### 人間用ビューの生成

```
$ python .claude/scripts/org-docs.py --root . --check
[org-docs] エージェント用マスタ（docs/features/ docs/guides/ docs/api/）が1本も無い。何もしない。
（終了コード 0）
```
このリポジトリはエージェント用マスタの2階建て構造を採っておらず、生成対象が無い。

### 変更範囲の外で見つかった、実態と食い違う記述（直していない。S-4）

| ファイル | 行 | 記述 | 実態 |
|---|---|---|---|
| `docs/design/design.md` | L354 | 付録「未確定事項」の表に `| ドメイン | 未取得 |` | `pergram.site` 取得済み。**同じファイルだが、本タスクの `変更範囲` は「CTA 主従の記述があれば更新」であり、ドメインの行は範囲外と判断した** |
| `docs/design/ad-lp.md` | L499 | 同上（`| ドメイン | 未取得 |`） | 同上。design.md に置き換え済みの検討記録なので、そもそも直すかの判断が要る |
| `docs/tasks/T-006.md` | L78, L159, L165 | `workers.dev` を前提とした sitemap / robots の記述 | 完了済みタスクの証拠であり、**当時の実測として正しい。書き換えるべきではないと考えるが、判断はオーケストレーターに委ねる** |

`docs/po-queue.md` L49-57 の `workers.dev` は「旧ドメイン」として言及しており、実態と一致している（食い違いではない）。

## 組織への指摘
無し
