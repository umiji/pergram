# T-071: 匿名のアンケート回答を waitlist へ相乗りさせる（PO 判断で T-070 の設計を上書き）

## メタデータ
- 作成日: 2026-09-08
- 更新日: 2026-09-08
- 状態: 実装中
- 優先度: 高
- 担当: org-implementation-T-071
- 進捗: 実装完了。受け入れテスト42件＋単体テスト16件が緑。npm test は既知の赤2件（T-049）を除き全緑。**commit のみ・push していない**（移行SQL を PO が流すまで push 禁止）
- 依存: T-070（ブロッカー: T-070 の実装を差し替えるため。T-070 は完了・未マージ）
- TDD適用可否: 適用
- 手戻り回数: 0

## 目的

**PO 判断（2026-09-08）:**

> waitlist テーブルに挿入されるようにしてくれ。メールが PK になってるから PK 制約でレコード
> 入ってないんじゃないの。その制約外すだけでいい。余計なテーブル追加しないでくれ

**選択肢を提示したうえで PO が「waitlist を作り直して相乗りさせる」を選んだ。**
T-058 の 🔒「`waitlist` は6列で打ち止め。匿名の押下を相乗りさせない」と、
T-070 の決定「匿名の回答は新しいテーブルへ入れる」を、**この判断が上書きする。**

## 変更範囲

- `worker/schema.sql` — `waitlist` の定義を作り直す。`request_survey` の定義を消す
- `worker/migrations/` — 作り直しの移行SQL を1本追加。`2026-09-08_request_survey.sql` は削除
- `worker/waitlist.js` — 書き込みを新しい形へ
- `worker/request_survey.js` — 保存先を `waitlist` へ。**受け口のパスは変えない**
- `worker/index.js` — 必要なら
- `tests/` — 受け入れテストと単体テストの更新
- `.claude/rules/pergram-compliance.md` —「サーバが保持してよいもの」の更新
- `docs/ops/deploy.md` — 移行手順と**デプロイの実態**の訂正

## 禁止事項

- 🔒 **1行が `id` と `email` を同時に持ってはならない。DBの CHECK 制約で強制すること。**
  理由は「決定ログ / 匿名の識別子とメールアドレスを同じ行に置かない」に書いた。**最重要。**
- 🔒 IP・User-Agent・リファラを保存しない
- 🔒 ページ内の位置（上の帯 / 下の帯）を保存しない。位置は GA4 の `data-cta` が持つ
- 🔒 `request_signal` の3列に手を触れない
- **自由記述の中身を解釈して弾かない。**サーバがやるのは長さで切ることだけ
- **自由記述の本文を GA4 へ送らない**
- 回答数・人数を画面に出さない（N-03 / 景表法）
- **⛔ `git push` をしない。** 理由は `## 申し送り` の1番目。**commit だけして止まること。**
- 帯の見た目（T-061 で確定）を触らない

## 完了条件

**A 群 — 保存の経路**

- A-1 メールアドレスを入れずにアンケートを送ると、**`waitlist` に1行残る**（`email` は NULL）
- A-2 メールアドレスを入れた既存の経路が壊れていない。**`id` は NULL のまま入る**
- A-3 同じブラウザが同じ `id` で上書きされる（何度答えても行が増えない）
- A-4 **`id` と `email` を両方持つ行を作ろうとすると、DB が拒否する。**これを消すと落ちるテストがある
- A-5 受け口 `/api/request-survey` の**パスと本文の形を変えない**（申し送りの2番目）
- A-6 自由記述は `src/lib/waitlist_fields.js` の上限で切る。弾かない

**B 群 — スキーマと移行**

- B-1 `worker/schema.sql` の定義と移行SQL が一致する
- B-2 **既存4行が移行後も残る**（`email` / 回答 / `created_at` が変わらない）
- B-3 `request_survey` テーブルの定義と移行SQL が**リポジトリから消えている**
- B-4 `docs/ops/deploy.md` に、作り直しの移行手順と**「どのブランチへ push しても本番へ出る」**という
  デプロイの実態が書かれている

**C 群 — 回帰**

- C-1 `npm test` が通る（**`tests/hero_ranking.test.js` の2件は着手時点で既に赤**。T-049）
- C-2 `npm run build` が成功する

## 判断してよい範囲

- 索引の張り方（`email` / `id` の UNIQUE をどう作るか）
- upsert の衝突先の指定方法
- 移行SQL の書き方（作り直しの手順）
- テストの置き場

## テスト方法

- 単体・受け入れ: `npm test`
- 実環境確認: **必要。ただし PO が行う**（`npx wrangler d1 execute` は deny）

## 停止条件

- **S-7（このタスク固有）**: 「1行が `id` と `email` を同時に持たない」と
  「メールを入れた人の回答が今までどおり残る」が両立しないと判断した場合。
  **両立する形は在る**ので、両立しないという結論に至ったら止めて報告すること
- 共通の停止条件（`.claude/rules/org-escalation.md`）

## 参照すべき成果物

- `worker/waitlist.js` / `worker/request_survey.js` / `worker/schema.sql` / `src/lib/waitlist_fields.js`
- `docs/tasks/T-070.md` の決定ログ — 何を上書きするのか
- `docs/tasks/T-058.md` の決定ログ — 6列で打ち止めにした理由
- 本番の退避: `<scratchpad>/waitlist_backup.sql`（4行。**メールアドレスを含むので中身を報告に貼らない**）

## 連絡してよい相手

**無し（すべてオーケストレーター経由）。**この実行環境ではエージェントに名前を付けられない（I-006）。

## 申し送り

1. **⛔ どのブランチへ push しても本番へ出る。**Cloudflare の Deploy command が `npx wrangler deploy` で、
   ブランチに関係なく本番の Worker を置き換える。**今日それを実測で確認した**
   （`org/request-survey` のコードが `pergram.site` で配信されている）。
   **したがって、移行が流れる前に push すると、`waitlist` への登録が全部壊れる**
   （`id` 列の無い表へ `id` を入れようとして失敗する）。**今は登録が動いているので、これは改悪になる。**
   **commit だけして push しないこと。push はオーケストレーターが移行の後に行う。**
2. **受け口 `/api/request-survey` のパスと本文の形を変えないこと。**
   T-070 で入れた「出荷箱」により、**503 で送れなかった回答が利用者のブラウザに控えられている。**
   パスか本文の形を変えると、その控えは永久に送れず、**回答が失われる。**
   保存先を `waitlist` へ変えるだけにする
3. **`request_survey` テーブルは本番に一度も存在していない**（`num_tables` は 3 のまま）。
   だから消すのは安全である。移行SQL ごと消す
4. **着手時点で `tests/hero_ranking.test.js` の2件が既に赤である**（T-049）。自分の失敗と読まないこと

## 成果物

- `tests/request_survey.test.js` — 受け入れテスト42件（A群 / B群）。実装前は17件が赤
- `tests/request_survey_regression.test.js` — 回帰・シナリオ14件。実装前は4件が赤
- `tests/waitlist_step2.test.js` / `tests/request_unify.test.js` — 失効した 🔒 を1件ずつ更新
- `worker/schema.sql` — `waitlist` を7列＋CHECK 制約へ作り直し。`request_survey` の定義を削除。
  `request_signal` の「waitlist へ相乗りさせない」の理由書きを、失効した部分だけ更新
- **`worker/migrations/2026-09-08_waitlist_rebuild.sql`（新規。⚠️ PO が流す移行SQL）**
- `worker/migrations/2026-09-08_request_survey.sql` — **削除**（本番に一度も流れていない）
- `worker/request_survey.js` — 保存先を `waitlist` へ。**パスと本文の形は T-070 のまま**
- `worker/waitlist.js` — **変更なし**（元から `id` に触っていない。決定ログ参照）
- `tests/request_survey_upsert.test.js` — 単体テスト6件（保存先の変更に追随＋
  「匿名の回答が登録者の行を書き換えない」を1件追加）
- `tests/request_survey_outbox.test.js` — **変更なし**（ブラウザ側だけを見るため保存先に依存しない）
- `.claude/rules/pergram-compliance.md` — 「サーバが保持してよいもの」を4項目→3表へ。
  `waitlist` の7列と CHECK 制約、失効した 🔒 2件を明記
- `docs/ops/deploy.md` — §3 を作り直しの手順へ。**「どのブランチへ push しても本番へ出る」へ訂正**

## ブロッカー

無し

## 決定ログ

### 2026-09-08 匿名のアンケート回答は waitlist へ相乗りさせる。email の主キーを外す
- 対象: アンケートの保存 / データ保護
- 決定: **`waitlist` を作り直し、`email` の PRIMARY KEY を外して匿名の行を入れられるようにする。**
  `request_survey` テーブルは作らない（本番に一度も存在していないので取り下げるだけでよい）
- 経緯: **PO の判断。**選択肢を提示したうえで選ばれた
- 却下案: `request_survey` を作る（T-070 の実装）→ **PO が「余計なテーブル追加しないでくれ」と却下**
- 付帯条件: SQLite は PRIMARY KEY を後から外せないため、**表の作り直し**（新表を作る→移す→消す→改名）になる。
  本番データを直接触るので、**移行の前に退避を取る**（取得済み: 4行）
- 上書き対象: T-070 2026-09-08（匿名のアンケート回答は新しいテーブルへ入れる。既存2つには列を足さない）
- 出典: セッション内の PO 判断

### 2026-09-08 匿名の識別子とメールアドレスを、同じ行に置かない
- 対象: データ保護 / アンケートの保存
- 決定: **1行が `id` と `email` を同時に持つことを、DB の CHECK 制約で禁じる。**
  匿名の行は `id` だけを持ち、メールを入れた人の行は `email` だけを持つ
- 理由: **`waitlist.id` に入る値は `request_signal.id` と同じ匿名の識別子である。**
  もし同じ行が `id` と `email` を両方持てば、**それまで匿名だったボタン押下の1行1行が、
  すべてメールアドレスへ紐づく。**`request_signal` が3列しか持たない理由
  （「個人を識別できるものを何も持たないことが、この行の存在理由である」）が丸ごと崩れる。
  **表を1つにするという PO の判断は、この線を越えることまでは含んでいない**
- 却下案: 制約を付けず運用で気をつける → **理由を知らない担当が将来必ず両方入れる。**
  DB が拒否する形にしておけば、間違えた時点で落ちる
- 却下案: メールを入れた時点で匿名の行に `email` を書き足して1行にまとめる →
  **まさにこの紐づけが起きる。**重複して2行残るほうがはるかに安全である
- 出典: `.claude/rules/pergram-compliance.md`「データ保護」、`worker/request_signal.js` 冒頭の 🔒

### 2026-09-08 受け口が書く列を「自分の鍵の列だけ」に固定した

- 対象: アンケートの保存 / waitlist の書き込み
- 決定: **`/api/waitlist` は `email` + 回答4 + 日時の6値だけを書き、`id` 列に触らない。
  `/api/request-survey` は `id` + 回答4 + 日時の6値だけを書き、`email` 列に触らない。**
  触らない列は NULL のまま残る
- 理由: **「もう一方の鍵を、うっかり空文字で埋める」経路をそもそも作らないため。**
  CHECK 制約は `id` と `email` が同時に非 NULL の行を止めるが、片方を空文字 `''` で
  埋める実装を書かれると制約は通ってしまい（空文字は NULL ではない）、
  **匿名の行にメールアドレスの列が生えたのと同じ状態**になる。書かなければ起きない
- 副次的な利点: どちらの受け口もバインドは6値のままなので、
  **`tests/worker.test.js` の `STORED_COLUMNS = 6` を書き換えずに済む。**
  失効していない検査を巻き添えで緩めない
- 却下案: 7列すべてを両方の受け口が書き、使わない側に NULL を明示的に入れる →
  上の空文字の事故と、列の追加のたびに2箇所を直す手間を招く
- 出典: セッション内のテスト設計。目印は `tests/request_survey.test.js` の
  「A-4 🔒 匿名の受け口の SQL は email 列に一切触らない」と定数 `BOUND_VALUES`

### 2026-09-08 CHECK 制約は「両方は持てない」までにとどめる

- 対象: データ保護 / waitlist の制約
- 決定: **`id` と `email` が同時に非 NULL の行だけを拒否する。**
  「どちらも無い行」は禁じない
- 理由: 禁止事項に書かれているのは「1行が `id` と `email` を同時に持ってはならない」であり、
  **書かれていない縛りをテストで足さない。** かつ、どちらも無い行はアプリからは作れない
  （`/api/waitlist` は不正なメールを 400、`/api/request-survey` は不正な UUID を 400 で捨てる）
- 却下案: 排他的論理和（ちょうど片方）を要求する → **実装が
  `CHECK (id IS NULL OR email IS NULL)` と書いた場合に、仕様上正しいのに落ちる。**
  完了条件 A-4 が求めているのは「両方持つ行を DB が拒否する」ことだけである
- 出典: `docs/tasks/T-071.md` の `## 禁止事項` と完了条件 A-4

### 2026-09-08 既存4行の生き残りは、退避ファイルではなく合成データで検査する

- 対象: 移行の検証
- 決定: 本番の退避（`<scratchpad>/waitlist_backup.sql`）を**テストから読まない。**
  代わりに**本番と同じ形の旧スキーマ（`email TEXT PRIMARY KEY` の6列）を `node:sqlite` に
  組み立て、合成した4行を入れ、`worker/migrations/` の新しい移行SQL を実際に流して**、
  `email` / 回答4種 / `created_at` が1文字も変わらないことを見る（完了条件 B-2）
- 理由: 退避ファイルは**本物のメールアドレスを含む**うえ、リポジトリの外にあるので
  他の環境でテストが走らない。**移行SQL そのものを実行して確かめる**ほうが、
  検査としても強い（同じ仕掛けで B-1「移行後の姿が schema.sql と一致」も見られる）
- 却下案: 退避ファイルをテストの入力にする → 個人情報がテスト環境へ広がる。
  ファイルが無い環境でテストが落ちる
- 付帯条件: 既に本番へ流れた移行SQL 3本はファイル名の集合（`SHIPPED_MIGRATIONS`）で
  除外する。**T-071 で足す分はその集合の外にある**という前提で新旧を見分けている
- 出典: `tests/request_survey.test.js` の `PRODUCTION_SCHEMA` / `EXISTING_ROWS` / `migrated()`

### 2026-09-08 一意性は PRIMARY KEY ではなく列の UNIQUE で作る

- 対象: waitlist の構造 / 上書きの実現方法
- 決定: `id` と `email` の両方を **`TEXT UNIQUE`** にする。**どちらも PRIMARY KEY にしない**
  （表は rowid を主キーとして持つ）。`ON CONFLICT(id)` / `ON CONFLICT(email)` の宛先は
  この UNIQUE 制約が作る索引である
- 理由: **SQLite の UNIQUE は NULL を互いに違う値として扱う。** そのため
  「`email` が NULL の匿名行」も「`id` が NULL のメール行」も**何行でも入る**。
  一方 PRIMARY KEY にすると（`INTEGER PRIMARY KEY` を除き NOT NULL 相当に扱われるため）
  片方の行が物理的に作れなくなる —— **`email TEXT PRIMARY KEY` が匿名の行を弾いていた、
  まさにその問題が形を変えて戻る**
- 却下案: 部分索引（`CREATE UNIQUE INDEX ... WHERE email IS NOT NULL`）→ 効果は同じだが、
  `ON CONFLICT` の宛先に使うには**衝突対象の側にも同じ WHERE 句を書く**必要があり、
  受け口の SQL が索引の定義に依存する。索引を1つ足すたびに受け口を直すことになる
- 却下案: `id` を PRIMARY KEY にして `email` を UNIQUE にする → メールの行が `id` を
  持てなくなる…のではなく、**`id` が NULL の行を入れられなくなる**（同上）
- 出典: `worker/schema.sql` の `waitlist`。目印は `tests/request_survey.test.js` の
  「A-3 識別子が違えば別の行になる」と「B-2 移行後の表でも、同じ鍵の2度目は行を増やさない」

### 2026-09-08 移行SQL は作り直しの4文にとどめ、2度流せる形にしない

- 対象: 移行の手順
- 決定: `worker/migrations/2026-09-08_waitlist_rebuild.sql` は
  **新表を作る → 列を明示して移す → 消す → 改名**の4文だけにする。
  `IF NOT EXISTS` や「既に移行済みなら何もしない」の分岐を**入れない**
- 理由: **2度流すと途中まで走る形のほうが危険である。** 例えば `CREATE TABLE IF NOT EXISTS`
  にすると、2度目は作成を飛ばして**空の（あるいは古い）表へ移し替えたうえで元の表を落とす**
  経路が生まれうる。**素直に失敗するほうが、既存データにとって安全である。**
  流したかどうかは `PRAGMA table_info(waitlist)` に `id` があるかで判定できる
- 却下案: `INSERT INTO ... SELECT *` で移す → **列順の思い込みが崩れたときに黙って別の列へ入る。**
  列を明示すれば、食い違った時点でエラーになる
- 付帯条件: `DROP TABLE waitlist` を含む。**流す前に退避を取る手順を `docs/ops/deploy.md` に併記した**
- 出典: `worker/migrations/2026-09-08_waitlist_rebuild.sql` と `docs/ops/deploy.md` §3

### 2026-09-08 `worker/waitlist.js` は1文字も変えなかった

- 対象: waitlist の書き込み
- 決定: 既存の `/api/waitlist` の実装を**変更しない**
- 理由: 元から `INSERT INTO waitlist (email, nutrients, channel, nutrients_other, requests,
  created_at)` と**列を明示して**書いており、`id` に触っていなかった。
  衝突先も `ON CONFLICT(email)` で、作り直した表の `email TEXT UNIQUE` がそのまま宛先になる。
  **「7列になったから7列書く」形に直すと、決定「受け口が書く列を自分の鍵の列だけに固定した」に
  正面から反する**（`id` を空文字で埋める事故の入口を自分で作ることになる）
- 却下案: `id` に明示的に NULL を入れる（`VALUES (NULL, ?1, ...)`）→ バインド値が7つになり、
  `tests/worker.test.js` の `STORED_COLUMNS = 6` を巻き添えで書き換えることになる。
  失効していない検査を緩めない
- 出典: `worker/waitlist.js`（差分なし）。目印は `tests/request_survey.test.js` の
  「A-2 メールアドレスを入れた行は id が NULL のまま入る」

## 作業ログ

- 2026-09-08 オーケストレーターが PO 判断を受けて起票。本番 `waitlist` の退避を取得（4行）
- 2026-09-08 `org-test-T-071` が受け入れテストを作成。**着手前の `git status` は clean、
  ブランチは `org/request-survey`**（切り替えていない）
- 2026-09-08 **コスト警告（CRITICAL）で2度停止し、オーケストレーターへ報告して指示を仰いだ。**
  2度とも PO 判断で続行。2度目に停止条件が引き上げられた（$90 超、または1手順で $10 以上積む見込み）
- 2026-09-08 書き換えたのは4ファイル。**失効した 🔒 に関する部分だけを直し、他の検査は触っていない**
  - `tests/request_survey.test.js` — T-070 の受け入れテストを新契約へ全面的に書き換え（42件）
  - `tests/request_survey_regression.test.js` — 「2つの表は突き合わせられない」等の 🔒 を反転（14件）
  - `tests/waitlist_step2.test.js` — 「🔒 列が6つのまま」→ `id` を足した7つ（1件のみ）
  - `tests/request_unify.test.js` — 「🔒 waitlist の定義を変えていない」→ 7列・主キーが外れている（1件のみ）
- 2026-09-08 実装前の実行結果 = `npm test` 570件中 **24件が赤**。
  内訳は **本タスクの22件 + `tests/hero_ranking.test.js` の2件**（T-049。着手時点で既に赤）。
  その後 B-4 の検査を書き直して赤が1件増えた（下記）
- 2026-09-08 **B-4 の検査が空振りしていたので書き直した。** `docs/ops/deploy.md` には
  「ブランチ」「wrangler deploy」「順序」の語が既に在ったため、語の有無を見る検査が
  実装前から緑になっていた。**書いてある内容が逆**（「`main` へ merge した時点で本番へ出る」）
  だったので、**その記述が消えていることと、「どのブランチ」相当の記述が在ること**を見る形へ変えた

- 2026-09-08 `org-implementation-T-071` が着手。**着手前の `git status` は clean**、
  ブランチは `org/request-survey`（切り替えていない）
- 2026-09-08 実装。`worker/schema.sql` の `waitlist` を7列＋CHECK 制約へ、`request_survey` の
  定義と移行SQL を削除、`worker/request_survey.js` の保存先を `waitlist` へ、
  移行SQL `2026-09-08_waitlist_rebuild.sql` を新規作成 →
  受け入れテストは B-4 の2件を残して緑
- 2026-09-08 `docs/ops/deploy.md` §3 を作り直しの手順へ。**「`main` へ merge した時点で本番へ出る」を
  「どのブランチへ push しても本番へ出る」へ訂正**（2026-09-08 の実測）→ 受け入れテスト42件が緑
- 2026-09-08 自分の単体テストを保存先の変更に追随させ、
  「匿名の回答が登録者の行を書き換えない」を1件追加（同じ表を共有して初めて壊れうる境界）
- 2026-09-08 `.claude/rules/pergram-compliance.md` の「データ保護」を更新（4項目→3表）
- 2026-09-08 検証: `npm test` 571件中 569件が緑（赤2件は既知の `hero_ranking`）、
  `npm run build` は exit 0。**⛔ 指示どおり push はしていない**
- 2026-09-08 コスト警告（CRITICAL、$66.78）が出たが、引き上げられた停止条件（$90 超、または
  1手順で $10 以上積む見込み）に達していないため続行した

## 証拠

実装後（2026-09-08、ブランチ `org/request-survey`。**push していない**）。

```
$ node --test tests/request_survey.test.js
# tests 42 / # pass 42 / # fail 0        （実装前は17件が赤）

$ npm test
# tests 571 / # pass 569 / # fail 2
not ok 115 - 完了条件2: ヒーローの各行が、その製品の実データの単価と袋価格を出している
not ok 118 - T-009 が記録した順位・単価が、いまの実データと一致する
  → どちらも tests/hero_ranking.test.js。着手時点で既に赤（T-049 の担当分）

$ npm run build
build exit=0

$ git log --oneline -1 && git status --short
（commit のみ。push していない — 移行が流れる前に push すると本番の登録が壊れる）
```

**未検証（PO が行う）**: 本番 D1 での移行SQL の実行と、実環境での書き込み確認。
移行SQL は **`worker/migrations/2026-09-08_waitlist_rebuild.sql`**。
⚠️ `DROP TABLE waitlist` を含むので、**流す前に退避を取ること**（手順は `docs/ops/deploy.md` §3）。

## 組織への指摘

- [反復] 同じ受け入れテストを、PO 判断の設計変更で**2度書いた**（T-070 で `request_survey` 前提、
  T-071 で `waitlist` 前提）。1度目の実装・レビュー・テストが完了・push まで進んだ後に
  設計が覆っている。**保存先をどこにするかは、受け入れテストを書く前に PO へ確認できた論点である**
- [反復] （実装）**同じ機能を2度実装した。** T-070 で `request_survey` 表を作り、レビュー修正・
  push まで終えた後に、PO 判断で保存先が `waitlist` へ変わった。**T-070 の実装・レビュー2往復・
  テスト判定・T-071 のテスト再作成が、保存先1点の確認で置き換えられた分である**
- [規約] （実装）`docs/ops/deploy.md` に書かれていた「`main` へ merge した時点で本番へ出る」が
  誤りで、**実際にはどのブランチへ push しても本番へ出る。** T-070 の完了報告で
  「merge 前に移行を流してください」と書いたが、その時点で既に本番へ出ていた。
  **手順書の記述が誤っていても、それを検知する仕掛けがどこにも無い**（今回は実測で偶然判明した）
- [道具] コスト警告のフックが「停止して報告せよ」と指示する一方、担当エージェントは
  PO へ直接問い合わせられない。**警告のたびにオーケストレーター経由の往復が発生し、
  節約額より往復のコストが上回る場面があった**（2度目の停止で運用が緩和された）
