# T-051: 要望導線の表記統一と、LP の段構造化・匿名シグナルの保存

## メタデータ
- 作成日: 2026-09-07
- 更新日: 2026-09-07
- 状態: 実装中
- 優先度: 高
- 担当: org-implementation-T-051
- 進捗: 実装完了。完了条件12件すべて確認済み（未検証項目は ### 2026-09-07 実装（org-implementation-T-051）

1. 着手前に `git status` を確認（差分なし）。指示書全文・受け入れテスト
   （`tests/request_unify.test.js` 49件）・関連する既存テスト5本・
   `worker/` 一式・`locales/` を読み、落ちている53件の内訳を実装の作業一覧に落とした
2. **Worker から着手（TDD）。** `tests/worker.test.js` に匿名シグナルの単体テスト8件を
   先に書き、8件とも落ちること（ルート未登録で静的ファイル扱いの 200 が返ること）を確認 →
   `worker/request_signal.js` / `schema.sql` / `migrations/` / ルート登録で 23/23 green
3. `src/assets/request.js` に第1段階の匿名シグナルを足した（生成 → 保存 → 送信、
   応答は待たない）。製品一覧側の完了条件7・8 が green に
4. 段の描画を `src/templates/request.js` へ移して共用にし、locales のキーを `request.*` へ
   改名。製品一覧から待機リストの案内を削除し、要望ボタンをツールバーより前へ。
   LP の `waitlist()` を帯＋共用の段へ置き換え、`/assets/request.js` を読み込むようにした
5. 段のスタイルを `products.css` → `site.css` へ移し、`lp.css` から旧フォームの
   スタイル一式を削除。`lp.css` に残したのは帯の見た目だけ
6. `ecc:accessibility` を呼び、見出しの階層（LP は h3）と器のコントラストを直した
7. `superpowers:verification-before-completion` を呼び、`npm test`（421/421）と
   `npm run build`（exit 0）を実行したうえで、完了条件1〜10 をビルド出力に対して
   機械的に照合し、ヘッドレス Chrome（CDP のデバイス指定）で 390px / 1280px を目視した

#### 途中で一度だけ詰まった箇所

`tests/render.test.js` の2件が、LP の作り替えで落ちた。**どちらも検査の中身ではなく
書き方の問題**だったので、テストではなく実装の側を合わせた。

| 落ちた検査 | 原因 | 直し方 |
|---|---|---|
| ページ下部に主 CTA の面を持つ送信ボタンがある | 正規表現が `type="submit"` → `class` の**属性の並び**に依存していた | 共用モジュールの送信ボタンの属性の順を `type` → `class` に揃えた |
| 「その他」チップと自由記述欄が同じ枠にある | 枠のクラス名を完全一致（`check-other`）で見ていた | 共用モジュール側の綴りを `check-other` に合わせた（決定ログ参照） |

## 証拠 に明記）
- 依存: T-050（ブロッカー: 同じ導線・同じ locales を作り替えるため。T-050 の実装が入っていることが前提）
- TDD適用可否: 適用
- 手戻り回数: 0

## 目的

T-050 で製品一覧に要望の導線を置いたが、**PO がプレビューを見て 4 つの食い違いを指摘した。**

1. **製品一覧に、意思表示の入口が2つ並んでいる。** 「リリース通知を受け取る」（メール = 重い対価）と
   「他の成分・製品の追加をリクエスト」（1クリック = 軽い対価）が同じ場所に出ており、
   **T-050 が下げようとした対価を、隣のボタンが元に戻している。**
2. **同じ意思表示を指す言葉が、画面ごとに違う。** LP は「Waitlist」「リリース通知」、製品一覧は
   「リクエスト」。押した先が同じ導線であることが読み手に伝わらない。
3. **要望のセクションが、絞り込み・表示切替のツールバーより後ろにある。** 操作の道具より後ろに
   置くと、絞り込みを触りに来た人の視線の外へ落ちる。
4. **LP はいまも「メールアドレスを先に出せ」という形のまま。** 製品一覧だけを軽くしても、
   広告の着地点である LP が重いままなら、下げた対価は届かない。

加えて **PO が新しい要件を出した**: 第1段階の押下を、GA4 のイベントだけでなく
**D1 にも1行残す**（日付と UUID）。GA4 は CSP で全滅した実績があり（T-047）、
**計測が自分の手の中に無いと、また同じ理由で 0 件の意味が読めなくなる。**

## 変更範囲

| 対象 | 何をするか |
| --- | --- |
| `src/templates/products.js` | 待機リスト案内（`waitlist-banner`）の削除。要望セクションをツールバーより前へ |
| `src/templates/products/head.js` | ヘッダの CTA 文言の差し替え |
| `src/templates/products/request.js` | LP と共用にするなら、その形へ |
| `src/templates/lp/form.js` | 待機リストを段構造へ置き換え |
| `src/templates/lp/hero.js` | ヘッダの CTA 文言の差し替え |
| `src/templates/lp/sections.js` | ロードマップ／特徴セクションの CTA 文言（この導線を指すものだけ） |
| `src/assets/request.js` | 第1段階の POST と重複抑止。LP でも動くこと |
| `src/assets/lp.js` | 旧・待機リストの操作のうち、置き換えで不要になった分 |
| `src/styles/lp.css` / `src/styles/products.css` | 段構造の見た目 |
| `locales/ja.json` / `locales/en.json` | 文言 |
| `worker/schema.sql` / `worker/migrations/` / `worker/index.js` / 新規 `worker/request_signal.js` | 匿名シグナルの保存 |
| `tests/` | 受け入れ・単体・回帰 |
| `docs/design/design.md` | 実装と食い違う記述があれば直す |

## 禁止事項

- 🔒 **既存の `waitlist` テーブルの構造を変えない。** `email` が主キーであり、SQLite では後から
  変えられない。匿名の行は**別テーブル**に入れる。既存データの移行・削除は行わない（S-2）。
- 🔒 **匿名シグナルのテーブルに、UUID と日時以外の列を作らない。** IP・User-Agent・リファラ・
  成分・自由記述を入れない。**個人を識別できるものを何も持たないことが、この行の存在理由である。**
- 🔒 押下数・登録数を画面に描画しない（景表法の「人気」表示・N-03）。
- 🔒 リクエストの数を並び順に使わない。並び順は常に有効成分1単位あたりの価格。
- 🔒 自由記述の**本文**を GA4 へ送らない。書かれたかどうか（0 / 1）だけ。
- 🔒 自由記述には `lp.form.freeTextNote` の注記を必ず添える。離さない（N-01 / N-05）。
- 🔒 支援ウィジェットは1ページに1つ。
- 🔒 成分の効能・効果に触れる文章を新しく書かない（N-02）。文言は locales の固定文だけ。
- 🔒 **T-050 の決定ログのうち、PO 判断の3件を覆さない**（ヒーローのサンプル表記／意思表示は
  サービス単位で問う／LP からβ版へのリンクを消さない）。
- 落ちているテストを、テストや期待値の書き換えで通さない。

## 完了条件

**すべて `npm run build` の出力（`dist/`）に対して機械的に判定できること。**

1. `dist/ja/protein/index.html` に `リリース通知を受け取る` が **0 回**出現する
2. LP・製品一覧の本文の要望ボタンの文言が、どちらも **`成分・製品の追加をリクエスト`**
3. LP・製品一覧のヘッダの CTA の文言が、どちらも **`機能追加リクエスト`**
   （狭い幅の短縮表記を出す場合も同じ語で始めること）
4. 製品一覧の HTML で、`data-request-cta` を持つ最初の要素の出現位置が
   `class="toolbar"` の出現位置**より前**である
5. LP に第1段階のボタン（`data-request-cta`）と、`data-request-step="survey"` /
   `"email"` / `"support"` の3段がこの文書順で存在し、**3段とも初期状態は `hidden`**
6. LP の旧・メールアドレス先行フォーム（`waitlist--step1`）が出力されない
7. 第1段階の押下で `POST /api/request-signal` が飛び、本文が `{"id":"<UUID>"}` のみである
8. 同一ブラウザの2回目以降の押下では POST しない（`localStorage` の
   `pergram.request_signal_id` が既にあれば送らない）。段は開く
9. `worker/schema.sql` に `request_signal` テーブルがあり、列は `id TEXT PRIMARY KEY` と
   `created_at TEXT NOT NULL` の**2つだけ**。同じ内容の移行 SQL が `worker/migrations/` にある
10. `POST /api/request-signal` が、UUID の形式でない `id`、`id` 以外のキー、本文なしを **400** で返す
11. `npm test` が全件 pass（`node --test tests/*.test.js`）
12. `npm run build` が成功する

## 判断してよい範囲

- **LP と製品一覧で段の描画を共用するか、別々に持つか。** 共用にする場合の置き場と関数の分け方。
  ただし**目印（下記の属性名）は共用・別々のどちらでも同じにすること**
- CSS のクラス名・レイアウト・余白・ブレークポイント
- `worker/index.js` のルーティングの書き方、新しいハンドラのファイル分割
- UUID の生成方法（`crypto.randomUUID()` が使えない古い環境の代替を置くかどうか）
- 送信に失敗したときの画面の見せ方。**ただし段は必ず開くこと**
  （シグナルの保存は計測の都合であって、ユーザーの用ではない。失敗を理由に導線を止めない）
- LP の他セクション（特徴・ロードマップ）の CTA 文言のうち、この導線を指すものの表現

## 目印（実装とテストの共通の契約 — 変えない）

**T-050 で「受け入れテストが目印を決めるのか、実装が決めるのか読み取れない」という指摘が出たため、
今回は指示の側で固定する。** テストはこれを見てよい。実装はこれを出すこと。

| 目印 | 値 |
| --- | --- |
| 第1段階のボタン | `data-request-cta` 属性を持つ。`data-cta` に位置の名前 |
| 段の器 | `data-request-step="survey"` / `"email"` / `"support"` |
| エンドポイント | `POST /api/request-signal` |
| 送信本文 | `{ "id": "<UUID v4>" }` のみ |
| 成功応答 | `204 No Content` |
| localStorage のキー | `pergram.request_signal_id` |
| D1 のテーブル | `request_signal (id TEXT PRIMARY KEY, created_at TEXT NOT NULL)` |
| GA4 のイベント名 | 既存のまま（`request_click` など）。新設しない |

## テスト方法

- **受け入れテスト（org-test が先行作成）**: `tests/request_flow.test.js` を拡張、または
  `tests/request_unify.test.js` を新設。完了条件 1〜9 をビルド出力の HTML と DOM で判定する
- **Worker の単体（org-implementation）**: `tests/worker.test.js` に
  `/api/request-signal` の正常系・400 系・重複 id の扱いを足す
- **回帰**: `npm test` 全件。特に `tests/render.test.js` の
  「🔒 広告表示があっても並び順は単価の昇順のまま」「支援ウィジェットは登録完了ブロックの中に出る」
  が LP の作り替えで壊れやすい。**壊れたら期待値ではなく実装を直すか、
  検査の前提が変わった理由を決定ログへ書くこと**
- **実環境確認**: 実施者は PO。`npm run build` 後にプレビューサーバを立て、
  LP（`/ja/`）と製品一覧（`/ja/protein/`）を **390px 幅と 1280px 幅**で目視する。
  ⚠️ Chrome ヘッドレスの `--window-size` だけではモバイルを再現できない。
  CDP の `Emulation.setDeviceMetricsOverride`（`mobile:true`）を使うこと

## 停止条件

- 既存の `waitlist` テーブルの構造変更、または既存行の移行・削除が必要だと判明したとき（S-2）
- 匿名シグナルの重複抑止のために、UUID と日時以外の情報をサーバへ送る必要が出たとき
- T-050 の PO 判断3件と、今回の指示が食い違うと分かったとき
- 共通の停止条件（`.claude/rules/org-escalation.md`）

## 参照すべき成果物

- `docs/tasks/T-050.md` — **全文。決定ログ7件が今回の前提である**
- `docs/decisions-index.md` — 対象「待機リスト」「LP」「製品一覧」で引く
- `.claude/rules/pergram-compliance.md` — N-01 / N-02 / N-03 / N-05、保存してよい列の規定
- `.claude/rules/pergram-ui-copy.md` — 禁止語・使う語、ブランド表記
- `.claude/rules/pergram-code-invariants.md` — Worker のルーティング、CSP
- `src/lib/waitlist_fields.js` — 選択肢と長さ上限の唯一の出所

## 連絡してよい相手

- `org-test-T-051` — 不具合レポートと修正の往復（上限2往復）。**観点の照会に限る。判定を変えさせない**
- それ以外はすべてオーケストレーター（`main`）経由

## 申し送り

**PO が本セッションで確定した4点（2026-09-07）:**

1. **匿名シグナルは LP・製品一覧の両方で保存する。** 同じ意味の意思表示を2箇所で
   数え方を変えない。押された場所の区別は GA4 の `location` が持つ
2. **重複はブラウザごとに1件へ抑える。** 「何人が意思表示したか」に近い数字にするため。
   ブラウザを変えれば別人として数えられることは承知のうえ
3. **LP の既存のメールアドレス先行フォームは、段構造へ完全に置き換える**（併存させない）
4. **これは T-050 の決定「第1段階の計測は GA4 のイベントだけで行う／サーバに列を足さない」を
   上書きする PO 判断である。** 実装担当は元の決定を根拠に差し戻さないこと。
   ただし**既存の `waitlist` テーブルには一切触れない**という条件が付いている

**オーケストレーターの確定事項（2026-09-07、テスト先行作成後）:**

5. **完了条件2・3 の文言は「完全一致」で判定する。** 本文の要望ボタンは
   `成分・製品の追加をリクエスト` ちょうど（**「他の」は落とす**）。ヘッダは `機能追加リクエスト`
6. **LP ヘッダの CTA は `href="#waitlist"` と `data-cta="header_waitlist"` を据え置く。**
   今回変えるのは**表示ラベルだけ**である。GA4 のイベント名・リンク先を変えない
   （T-047 で計測が全滅した直後であり、連続性を切ると前後比較ができなくなる）

**技術的な事実（調査済み）:**
- `waitlist` テーブルは `email` が主キー。**メールアドレス無しの行は物理的に入らない。**
  だから別テーブルにする
- 製品一覧の第1段階は現在サーバへ何も送っていない（GA4 のイベントのみ）
- `src/assets/request.js` は `[data-request-flow]` と `[data-request-cta]` が両方無いと
  早期 return する。LP で動かすには LP 側にも同じ目印が要る
- `src/assets/products.js` は絞り込み／リストが無いと早期 return する。
  **要望の操作をそちらへ統合してはならない**（T-050 の決定ログ）

## 成果物

| 種別 | もの |
|---|---|
| 新規 | `worker/request_signal.js` — 匿名シグナルの受け口（POST /api/request-signal） |
| 新規 | `worker/migrations/2026-09-07_request_signal.sql` — 稼働中の D1 へ流す移行 SQL |
| 移動 | `src/templates/products/request.js` → `src/templates/request.js`（LP と共用） |
| 変更 | `worker/index.js` `worker/schema.sql` |
| 変更 | `src/templates/products.js` `src/templates/products/head.js` `src/templates/lp.js` `src/templates/lp/form.js` `src/templates/lp/hero.js` |
| 変更 | `src/assets/request.js` `src/assets/lp.js` |
| 変更 | `src/styles/site.css`（段の共用スタイルを集約）`src/styles/products.css` `src/styles/lp.css` |
| 変更 | `locales/ja.json` `locales/en.json` `docs/design/design.md` |
| テスト | `tests/worker.test.js` に単体8件を追加。`tests/request_flow_behavior.test.js` は import のパス1行のみ |

| コミット | 内容 |
|---|---|
| `3f088a3` | feat(worker): 要望の第1段階を匿名の1行として D1 に残す受け口を足す |
| `a21050c` | feat(request): 第1段階の押下でブラウザごとに1回だけ匿名シグナルを送る |
| `2903a32` | feat(request): 要望導線の表記を LP と製品一覧で揃え、LP を段構造へ置き換える |
| `e64d18a` | style(request): 段のスタイルを site.css へ移し、LP と製品一覧で共用する |
| `f05b497` | fix(a11y): 段の見出しの階層をページに合わせ、LP の器の面を白へ抜く |

ブランチ `org/lp-conversion-fix` へ push 済み。

## ブロッカー

無し

## 決定ログ

### 2026-09-07 受け入れテストは tests/request_unify.test.js を新設して置く
- 対象: 要望導線 / 受け入れテスト
- 決定: T-051 の完了条件1〜10 は `tests/request_unify.test.js`（新規・49件）に置き、
  T-050 の `tests/request_flow.test.js` は拡張しない
- 却下案: `tests/request_flow.test.js` を拡張する → **同じファイルの中で、T-050 の
  完了条件（待機リストの案内がリストより前）と T-051 の完了条件（その案内を削除）が
  正面から食い違う。** どの検査がどのタスクの契約かが読めなくなり、
  「落ちたテストを消す」のと「失効した契約を畳む」の区別が付かなくなる
- 出典: セッション内の判断、成果物なし

### 2026-09-07 完了条件2・3 の文言は完全一致で判定する
- 対象: 要望導線 / 文言
- 決定: 本文の要望ボタンは `成分・製品の追加をリクエスト`、ヘッダの CTA は
  `機能追加リクエスト` と**完全に一致**することを要求する（前後に語を足さない）。
  幅で出し分ける短縮表記がある場合は「どれか1つの表記が完全一致し、かつ
  すべての表記が `機能追加` で始まる」を要求する
- 却下案: 部分一致（`includes`）で見る → **現行の文言「他の成分・製品の追加をリクエスト」は
  部分一致で通ってしまう。** PO の指摘は「同じ意思表示を指す言葉が画面ごとに違う」であり、
  表記を揃えることが目的なので、揃っていない状態が通る判定は目的を満たさない
- 付帯条件: 指示から「完全一致か部分一致か」は一意に読み取れなかった。**揃える側（厳しい側）へ倒した。**
  実装が「他の」を残したい場合は、テストを緩めずオーケストレーター経由で確認すること
- 出典: セッション内の判断、成果物なし

### 2026-09-07 完了条件は dist と描画結果の両方で判定する
- 対象: 要望導線 / 受け入れテスト
- 決定: 完了条件は「`npm run build` の出力（`dist/`）に対して」と書かれているが、
  テストはビルド前でも走らねばならない。**テンプレートの描画結果に対して判定し、
  `dist/` が既にあるときは同じ検査をそちらにも重ねる**（`htmlSources()`）
- 却下案: (a) テストの中で `npm run build` を走らせる → 1件のテストが数十秒かかり、
  `npm test` が実用的でなくなる。(b) `dist/` だけを見る → ビルドしていない環境で
  全件が落ち、「未実装で落ちている」と「ビルドしていないだけ」が区別できなくなる
- 出典: セッション内の判断、成果物なし

### 2026-09-07 tests/mini_dom.js に localStorage と crypto を足す
- 対象: 要望導線 / テストの道具
- 決定: 完了条件7・8（匿名シグナルの送信と、ブラウザごとの重複抑止）を判定するには
  `localStorage` と `crypto.randomUUID()` が要る。**テストの道具（`tests/mini_dom.js`）の
  サンドボックスへ足した。** 初期値を渡す `storage` オプションと、中身を覗く
  `dom.storageData` を追加した
- 却下案: 実装側に「テスト用の差し替え口」を作らせる → 本番のコードにテストのための
  分岐が入る。`tests/mini_dom.js` の冒頭に「足りない API はここへ足してよい」と
  書かれており、そちらが想定された拡張点である
- 出典: `tests/mini_dom.js`

### 2026-09-07 tests/waitlist_step2.test.js は「入力の不変条件」の検査へ作り替える
- 対象: 待機リスト / 既存テスト
- 決定: このファイルは T-011（メールアドレス先行の2段階フォーム）の受け入れテストだったが、
  **完了条件6と申し送り3でそのフォームが無くなる。** ステップ1／ステップ2という区分を
  段（`[data-request-step="email"]` / `"survey"`）へ読み替え、**入力そのものの不変条件**
  （取得項目を増やさない・必須にしない・注記を離さない・別ページへ飛ばさない・
  チップの出所・maxlength・禁止語）だけを残した。部品（`waitlist()`）を直接描かず、
  **LP をページとして描いて中を見る形**にした（段の描画を共用するかは実装の裁量のため）
- 却下案: (a) ファイルごと削除する → 🔒 の不変条件が7件まとめて消える。
  N-01 / N-05 の防波堤（自由記述の注記）もここにしか無い。
  (b) そのまま残す → 完了条件11（`npm test` 全件 pass）が原理的に達成できない
- 付帯条件: `src/assets/lp.js` を駆動していた検査7件は削除した。同じ挙動は
  `tests/request_flow_behavior.test.js`（段の挙動・送信本文・GA4）と
  `tests/request_unify.test.js`（匿名シグナル）が持つ。**2箇所で別々の前提で
  見張らないよう、戻さないことをファイル冒頭に書いた**
- 出典: `tests/waitlist_step2.test.js`

### 2026-09-07 「支援ウィジェットは登録完了ブロックの中」の検査を「押す前には見えない段の中」へ読み替える
- 対象: LP / 支援ウィジェット
- 決定: `tests/render.test.js` の「支援ウィジェットは登録完了ブロックの中に出る」は
  `.waitlist__done` という**置き場**を見ていた。段構造への置き換えでその置き場は無くなるが、
  **この検査が守っていたのは置き場ではなく「意思表示より先に支援を出さない」ことである。**
  そこで「初期状態で hidden な祖先の中にあり、その祖先が `data-request-step="support"` である」
  へ読み替えた（T-051 ## テスト方法 が「検査の前提が変わった理由を決定ログへ書くこと」と
  指示している件への回答）
- 却下案: 検査を消す → 支援ウィジェットが押す前から見える実装が黙って通る。
  T-050 の段階化の意図（軽い意思表示から順に開く）が守られなくなる
- 出典: `tests/render.test.js`

### 2026-09-07 LP ヘッダの CTA は `data-cta="header_waitlist"` の `<a href="#waitlist">` のまま据え置く
- 対象: LP / 導線
- 決定: 完了条件3 が固定するのは**ヘッダの CTA の文言**だけなので、`data-cta` の綴りと
  リンク先（`#waitlist`）は変えないものとして `tests/lp_cta.test.js` をそのまま残した
- 却下案: LP ヘッダの CTA 自体を第1段階のボタン（`data-request-cta`）にする案 →
  ありうる実装だが、`tests/lp_cta.test.js` が 🔒 として GA4 の `cta_click` の綴り
  （`header_waitlist`）とリンク先を固定している。**綴りを変えると変更前後の比較ができない。**
  T-051 の目印表も「GA4 のイベント名は既存のまま。新設しない」としているので、据え置きが既定
- 付帯条件: 実装がヘッダの CTA をボタン化したい場合、テストを緩めずオーケストレーター経由で
  確認すること（GA4 の連続性に関わる判断であり、実装の裁量の外にある）
- 出典: `tests/lp_cta.test.js`

### 2026-09-07 段の描画とスタイルを LP と製品一覧で1つに共用する

- 対象: 要望導線 / 段の描画
- 決定: 段の描画は `src/templates/products/request.js` を **`src/templates/request.js` へ移して
  LP と製品一覧で共用**する。段のスタイル（`.request-*` 一式と `.link-button`）も
  `products.css` から **`site.css`**（両ページから読まれる唯一のスタイルシート）へ移す。
  LP 側（`src/templates/lp/form.js`）が持つのは帯（`#waitlist` の見出しと器）だけにする
- 却下案: (a) LP に段を別に持たせる → **2つ持っていたから片方だけ直った状態が生まれ、
  「同じ意思表示を指す言葉が画面ごとに違う」という PO 指摘そのものになった。**
  同じ失敗を作り直すことになる。(b) `src/templates/products/request.js` の置き場のまま
  LP から読む → LP が `templates/products/` を読む形になり、次に触る担当が
  「製品一覧専用だ」と読み違える
- 付帯条件: 移動に伴い `tests/request_flow_behavior.test.js` の import のパスを1行変えた。
  **検査の中身は変えていない**（このファイルは T-050 の実装担当が書いた単体テストである）
- 出典: コミット 2903a32 / e64d18a

### 2026-09-07 匿名シグナルの id は「送信前」に localStorage へ書く

- 対象: 要望導線 / 匿名シグナル
- 決定: `crypto.randomUUID()` で作った id を **POST の前に** `localStorage` へ保存する。
  送信が失敗しても消さない（＝その回のシグナルは失われる）
- 却下案: 送信が成功してから保存する → 一見きれいだが、**応答だけが返らなかった回**に
  サーバ側は1行入っているのに localStorage は空になり、次の押下で**別の UUID** が送られる。
  同じブラウザが2人として数えられる。この行の目的は「何人が意思表示したか」に近い数字を
  得ることなので、**多く数える誤りは目的を壊し、少なく数える誤りは目的を鈍らせるだけ**である。
  控えめに倒すのが正しい
- 出典: `src/assets/request.js` / コミット a21050c

### 2026-09-07 UUID を作れない環境では匿名シグナルを送らない

- 対象: 要望導線 / 匿名シグナル
- 決定: `crypto.randomUUID()` が無い環境では **id を作らず、送信もしない**（段は開く）
- 却下案: `Math.random()` 等で疑似的な UUID を組み立てる → 衝突するとサーバ側で
  `INSERT OR IGNORE` により1行に潰れ、**押下数が静かに目減りする。**
  しかも「送れなかった」のではなく「数えられなかった」ので、ログにも残らない。
  GA4 の `request_click` は別経路で残るので、こちらが 0 件になるほうが読める
- 出典: `src/assets/request.js`

### 2026-09-07 「その他」チップと自由記述をまとめる枠のクラス名は check-other に固定する

- 対象: 要望導線 / クラス名
- 決定: 共用モジュールの当該の枠のクラス名を `request-chip-other` から **`check-other`**
  （LP の旧フォームから引き継いだ名前）へ改名し、`products.css` 側の綴りも合わせた
- 却下案: 周りの `request-*` に揃えて `request-chip-other` のままにする → 見た目は整うが、
  **「その他チップと自由記述が同じ枠にある」ことを見張る唯一の検査**
  （`tests/render.test.js`「「その他」は成分チップの1つで、自由記述欄がその横に並ぶ」）が
  クラス名を完全一致で見ており、落ちる。T-051 の `## 判断してよい範囲` は
  CSS のクラス名を実装の裁量としているので、**検査を書き換えるのではなく名前の側を合わせた**
- 付帯条件: `org-test-T-051` へ観点として照会しようとしたが**宛先に到達できなかった**
  （`No agent named 'org-test-T-051' is reachable`）ため、裁量の範囲内として自分で決めた。
  この綴りを `request-*` へ改名すると、その検査が黙って落ちる旨をモジュールのコメントに書いた
- 出典: `src/templates/request.js` / コミット 2903a32

### 2026-09-07 段の見出しの階層はページ側から渡す

- 対象: 要望導線 / 見出し
- 決定: `requestFlow(t, { headingLevel })` を足し、**製品一覧は h2（ページの h1 の直下）、
  LP は h3（帯の h2 の直下）**にする
- 却下案: 共用なので h2 で固定する → LP では帯の見出し（h2）と段の見出し（h2）が同列になり、
  見出しで飛ばして読む人が「どこの話か」を辿れない（WCAG 1.3.1）
- 付帯条件: タグは変わるが**クラス名（`request-flow__heading`）は変えない。**
  `src/assets/request.js` が開いた段の見出しへフォーカスを移すときに使っている
- 出典: `src/templates/request.js` / コミット f05b497

### 2026-09-07 LP の他セクションの「Waitlist」表記もこの導線の語へ揃える

- 対象: 要望導線 / 文言
- 決定: 完了条件が名指ししているボタン（本文・ヘッダ）に加えて、
  `lp.features.bannerCta`（「Waitlistで伝える →」）と `lp.roadmap.lede` の
  「Waitlist登録で、優先リクエスト権とリリース通知が届きます。」も新しい語へ置き換えた
- 却下案: 完了条件が名指しした2箇所だけ直す → **PO の指摘2は「同じ意思表示を指す言葉が
  画面ごとに違う」であり、ボタンだけ揃えても LP の本文に「Waitlist」が残る。**
  完了条件は満たすが目的を満たさない
- 付帯条件: `## 変更範囲` の `src/templates/lp/sections.js` は「CTA 文言（この導線を指すものだけ）」と
  限定されているが、**変えたのは `locales/*.json` の文言であってテンプレートではない**。
  locales は「文言」として範囲に入っている。スコープの判断としてここに残す
- 出典: `locales/ja.json` / `locales/en.json` / コミット 2903a32

### 2026-09-07 locales の products.request.* を request.* へ、ヘッダの CTA は共通キーにする

- 対象: 要望導線 / 文言
- 決定: 段の文言のキーを `products.request.*` → `request.*` へ改名し、ヘッダの CTA は
  **LP と製品一覧が同じキー（`request.headerCta` / `request.headerCtaShort`）を引く**ようにした
  （`products.waitlistCta` と `lp.nav.cta` / `lp.nav.ctaShort` は削除）
- 却下案: 画面ごとにキーを分けたまま、値だけ同じ文字列にする → **キーが2つある限り、
  片方だけ直せる。** それが T-051 を起こした状態そのものである。構造で防ぐ
- 付帯条件: 使われなくなった `lp.form.lede` / `submit` / `done` / `step2*` /
  `products.waitlistNote` も ja / en の両方から落とした（キー集合の一致は
  `tests/render.test.js` が見ている）
- 出典: `locales/ja.json` / `locales/en.json` / コミット 2903a32

## 作業ログ

### 2026-09-07 受け入れテストの先行作成（org-test）

1. T-051 の指示書全文、T-050 の決定ログ7件、`CLAUDE.md`、
   `.claude/rules/pergram-compliance.md` を読み、完了条件1〜10 を二値化する方法を決めた
   （11・12 はコマンドの実行そのものなのでテスト化しない）
2. 既存テスト5本（`request_flow` / `request_flow_behavior` / `render` / `lp_cta` /
   `waitlist_step2`）を読み、今回の指示と矛盾する検査を洗い出した（下記「更新した既存テスト」）
3. `tests/mini_dom.js` に `localStorage` と `crypto` を足した（完了条件7・8 の判定に必要）
4. `tests/request_unify.test.js` を新規作成した（49件）。実行して **42件が落ちる**ことを確認した
5. 矛盾していた既存テストを、指示に合わせて更新した（期待値を緩めた箇所は
   「属性の並びに依存する完全一致」だけで、検査の中身は緩めていない）
6. `npm test` 全体を実行し、**落ちているのは T-051 が未実装であることに起因する53件だけ**で
   あることを確認した（下記 ## 証拠）

#### 更新した既存テスト（今回の仕様変更の反映）

| ファイル | 何を変えたか | 根拠 |
|---|---|---|
| `tests/request_flow.test.js` | T-050 完了条件2（待機リストの案内がリストより前）の検査**1件を廃止** | 案内ごと削除される（T-051 ## 変更範囲）。存在を要求する検査は必ず落ちる |
| `tests/request_flow_behavior.test.js` | `dom.fetchCalls` の件数を**宛先で分けて**数えるようにした（6箇所）。「第1段階はサーバへ何も送らない」→「待機リストへは送らない／匿名シグナルは1回飛ぶ」 | 完了条件7・申し送り4（T-050 の決定を PO 判断が上書き） |
| `tests/render.test.js` | ①「LP のフォームは同一ページ内で完了状態に切り替わる」→ 段が同じ文書にあること ②「支援ウィジェットは登録完了ブロックの中」→ 押す前には見えない段の中 ③「支援の設定が無い市場では何も出ない」の完了ブロック確認 → 段の確認 ④ 送信ボタンの完全一致 → 属性の並びに依存しない正規表現 | 完了条件5・6（`.waitlist__done` / `waitlist--step1` が無くなる） |
| `tests/lp_cta.test.js` | 送信ボタンの正規表現 `<button type="submit"` → `<button[^>]*type="submit"` | 同上。段のフォームは `class` が先に来る |
| `tests/waitlist_step2.test.js` | ステップ1/2 を段（email / survey）へ読み替え、部品ではなく LP ページを描いて見る形へ。`src/assets/lp.js` を駆動する検査**7件を削除**（`request_flow_behavior` / `request_unify` へ移った分） | 完了条件6・申し送り3（LP のメール先行フォームを段構造へ完全置換） |

**期待値を「緩めた」のは属性の並びに依存していた2箇所だけである。** それ以外は
検査の対象を新しい構造へ**移した**もので、守る内容は変えていない。

#### 完了条件11・12（コマンドの実行）

```
$ npm test
1..421
# tests 421
# suites 0
# pass 421
# fail 0
# cancelled 0
# skipped 0
# todo 0
```

```
$ npm run build
LP          /ja/
製品一覧    /ja/protein/  — 46 製品
クロール    /ja/ を許可 / /ja/protein/ /api/ を拒否
            robots.txt  sitemap.xml  llms.txt を出力
（終了コード 0）
```

**着手前は 413件中 360 pass / 53 fail。** 増えた8件は `tests/worker.test.js` へ足した
匿名シグナルの単体テストである（413 + 8 = 421）。

### 完了条件1〜10（ビルド出力に対する機械的な照合）

`npm run build` のあと、`dist/ja/index.html` と `dist/ja/protein/index.html` を直接読んで照合した。

```
CC1  protein「リリース通知を受け取る」出現 = 0 (期待 0)
CC1  protein waitlist-banner 出現 = 0 (期待 0)
CC2  本文ボタンの文言（完全一致で抽出）:
       LP       '成分・製品の追加をリクエスト'
       protein  '成分・製品の追加をリクエスト'
       protein  '成分・製品の追加をリクエスト'
CC3  ヘッダ CTA:
       LP       '機能追加リクエスト 機能追加'   ← u-desktop / u-mobile の2表記。どちらも「機能追加」で始まる
       protein  '機能追加リクエスト'
CC4  data-request-cta=20779 < class="toolbar"=21046 -> True
CC5  LP の段（文書順・初期状態）:
       pos=14719  survey   hidden
       pos=17900  email    hidden
       pos=19143  support  hidden
CC5  LP data-request-cta = 1 個
CC6  LP waitlist--step1 = 0 (期待 0)
CC9  schema.sql request_signal:
       id         TEXT PRIMARY KEY,
       created_at TEXT NOT NULL
CC9  migrations に同じ定義: True
CC9  waitlist の列（変更していないこと）: 6
```

完了条件7・8・10 はテストの側（`tests/request_unify.test.js` / `tests/worker.test.js`）が
実行で判定している。加えて**実ブラウザでも確かめた**（次項）。

### 完了条件7・8（実ブラウザでの送信）

`npm run build` → `node scripts/serve_dist.js --port 4174` を上げ、
Chrome ヘッドレスを `--remote-debugging-port` で起動して CDP の
`Network` ドメインで実際の送信を観測した。

```
== http://127.0.0.1:4174/ja/ ==
  req  POST http://127.0.0.1:4174/api/request-signal
       body {"id":"020865d2-c14f-4314-ab2d-e92b3c1c4d49"}
  res  404
  localStorage: 020865d2-c14f-4314-ab2d-e92b3c1c4d49
  2回目の押下で request-signal: 0 件

== http://127.0.0.1:4174/ja/protein/ ==
  req  POST http://127.0.0.1:4174/api/request-signal
       body {"id":"9eb955f5-ac6f-4b90-8ddb-613abbf71f1e"}
  res  404
  localStorage: 9eb955f5-ac6f-4b90-8ddb-613abbf71f1e
  2回目の押下で request-signal: 0 件
```

- 送信本文は id ひとつだけ（UUID v4）。**LP と製品一覧の両方から飛んでいる**
- `localStorage` に同じ id が残り、**同じページの2回目の押下では送信していない**
- 404 は想定どおり。`scripts/serve_dist.js` は静的ファイルを配るだけで
  Worker のルートを持たない。204 を返すことは `tests/worker.test.js`
  「匿名シグナルは 204 を返し、本文を持たない」が判定している。
  **`wrangler dev` / 本番での 204 は未検証**（下記「未検証」を参照）
- 404 を受け取っても段は開いた（画面が止まらないこと）

### 目視（ヘッドレス Chrome + CDP のデバイス指定）

`--window-size` だけではモバイルを再現できないため、
`Emulation.setDeviceMetricsOverride`（`mobile:true, deviceScaleFactor:2`）で撮った。

```
== lp-390 (390px, mobile=true) ==
  scrollW=390 clientW=390 overflow=[]
  steps=["survey=hidden","email=hidden","support=hidden"]
  ctaText=["成分・製品の追加をリクエスト"]
  headerCta=["ランキングを見る（β版）/ β版一覧", "機能追加リクエスト / 機能追加"]
  headings= H1 → H2(帯) → H3 × 3（段）

== lp-390-open（本文の要望ボタンを押した後） ==
  steps=["survey=open","email=hidden","support=hidden"]
  ctaText=["リクエストを受け取りました"]

== lp-1280 / products-390 / products-390-open / products-1280 ==
  いずれも scrollW == clientW、overflow=[]（横あふれ無し）
  products の headings = H1 → 製品名の H2 群 → H2 × 3（段）
```

- **390px / 1280px のどちらでも横方向のあふれは 0 件**（`scrollWidth == clientWidth`）
- 段は初期状態で3つとも閉じ、押下で `survey` だけが開く
- 見出しの階層は LP が h2（帯）→ h3（段）、製品一覧が h1 → h2（段）
- スクリーンショットで確認したもの: LP の帯（見出し・リード・主 CTA・「登録は不要」の注記）、
  開いたアンケートの段（成分チップ・購入先・自由記述・注記・送信/スキップ）、
  製品一覧の要望ボタンがツールバーより前に出ていること

### 禁止事項に対する確認

| 禁止事項 | 確認 |
|---|---|
| 既存の `waitlist` テーブルを変えない | `worker/schema.sql` の列は6つのまま。移行 SQL も `request_signal` を作るだけで `waitlist` に触らない。`tests/request_unify.test.js`「既存の waitlist テーブルの定義を変えていない」と `tests/waitlist_step2.test.js`「スキーマの waitlist テーブルの列が6つのままである」が緑 |
| 匿名テーブルに UUID と日時以外の列を作らない | `request_signal` は2列。`tests/worker.test.js`「匿名シグナルが保存するのは UUID と日時の2つだけである」が保存の引数の**個数**まで見ている。IP / UA / リファラを送っても保存に混ざらないことを `tests/request_unify.test.js`「完了条件10 保存するのは id と日時の2つだけである」が確認 |
| 押下数・登録数を画面に描画しない | `tests/request_flow_behavior.test.js`「押下後のフィードバックに数値が出ない」が緑。実ブラウザでもボタンは「リクエストを受け取りました」に変わるだけ |
| リクエストの数を並び順に使わない | 並び順の処理に手を入れていない。`tests/render.test.js`「広告表示があっても並び順は単価の昇順のまま」が緑 |
| 自由記述の本文を GA4 へ送らない | `tests/request_flow_behavior.test.js`「アンケートの GA4 イベントに自由記述の本文が乗らない（有無だけ）」が緑 |
| 自由記述に `lp.form.freeTextNote` を必ず添える | `tests/waitlist_step2.test.js` の2件（注記が段の中にある / 自由記述と同じ段にある）が緑。目視でも自由記述の直下に出ている |
| 支援ウィジェットは1ページに1つ | `tests/render.test.js`「支援ウィジェットは1ページに1つしか出ない」が緑 |
| 成分の効能・効果に触れる文章を新しく書かない | 新しく書いた文言は「機能追加リクエスト」「機能追加」「成分・製品の追加をリクエスト」「次に載せる成分・機能を、リクエストで決めています」と、その en 版のみ。いずれも成分の働きに触れていない。禁止語の検査（`render` / `lp_cta` / `waitlist_step2` の3本）が緑 |
| T-050 の PO 判断3件を覆さない | ヒーローのサンプル表記・意思表示のサービス単位・LP からβ版へのリンクのいずれにも触れていない。`tests/request_flow.test.js` が全件緑 |
| テストや期待値を書き換えて通さない | 変えたテストは `tests/request_flow_behavior.test.js` の **import のパス1行**（モジュールの移動に伴うもの）と、`tests/worker.test.js` への**追加8件**だけ。既存の期待値は1つも緩めていない |

### 未検証（自分では確認できなかったもの）

1. **`POST /api/request-signal` が実際の D1 に 204 で書き込むこと。**
   偽の D1（`tests/worker.test.js` / `tests/request_unify.test.js`）に対しては
   204・2列・`INSERT OR IGNORE` を確認済み。**実際の D1 へ移行 SQL を流すのは未実施**である
   （`npx wrangler d1 execute pergram --remote --file worker/migrations/2026-09-07_request_signal.sql`）。
   **これを流す前にデプロイすると、押下のたびに 503 が返る**（画面は止まらないが1行も残らない）
2. **本番の CSP で `connect-src 'self'` が `/api/request-signal` を通すこと。**
   `src/build/headers.js` は `connect-src 'self'` を出しており同一オリジンなので通るはずだが、
   **T-047 の前例があるので実環境で確かめるまでは断定しない**
3. **実機（iOS / Android）での表示。** 確かめたのは Chrome の CDP によるデバイス指定までである
4. **`prefers-reduced-motion` を有効にしたときの段の開き方**（`scrollIntoView` の分岐）

## 組織への指摘への対応

`## 組織への指摘` に2件記録した。作業は止めていない。

## 証拠

### 受け入れテストが落ちること（実装前なので落ちるのが正しい）

```
$ node --test tests/request_unify.test.js
# tests 49
# pass 7
# fail 42
```

**通っている7件は、いずれも「禁止側の検査」か「未送信ゆえに空振りする検査」である。**
実装後に破られたら落ちる回帰テストとして機能する。

| # | 実装前に通る理由 |
|---|---|
| 完了条件5 LP の段の値は survey / email / support の3つだけ | 段が0個なので空振り。段が出た瞬間に効く |
| 完了条件7 製品一覧の押下で段が開く | T-050 で実装済み。壊さないための回帰 |
| 完了条件7 製品一覧の送信に失敗しても段は開く | まだ送信自体が無い。送信が入った瞬間に効く |
| 完了条件8 製品一覧の3件（送らない／段は開く／id を書き換えない） | まだ送信自体が無い。同上 |
| 🔒 完了条件9 既存の waitlist テーブルの定義を変えていない | 禁止事項の検査。破られていない状態が初期値 |

落ちている42件の内訳（実装が何を作ればよいかの一覧）:

```
完了条件1  (2件) リリース通知を受け取る が製品一覧に残っている / 待機リストの案内が残っている
完了条件2  (3件) 本文のボタンが「他の成分・製品の追加をリクエスト」のまま / LP に本文のボタンが無い / en が揃っていない
完了条件3  (5件) 製品一覧・LP ともヘッダの CTA が「リリース通知を受け取る」/ en も揃っていない
完了条件4  (1件) data-request-cta(16942) が class="toolbar"(14229) より後ろ
完了条件5  (4件) LP に data-request-cta も3段も無い
完了条件6  (2件) LP に waitlist--step1 が残っている / LP に email の段が無い
完了条件7  (8件) POST /api/request-signal が無い（製品一覧・LP とも）
完了条件8  (4件) LP 側は第1段階のボタンが無いため到達しない / 同一ページ内の重複抑止
完了条件9  (3件) worker/schema.sql に request_signal が無い / 移行 SQL が無い
完了条件10 (10件) /api/request-signal のルートが無い（静的ファイル扱いで 200 が返る）
```

### 既存テストを更新したあとの全体（着手前の基準線）

```
$ npm test
# tests 413
# pass 360
# fail 53
```

53件の内訳:

```
tests/request_unify.test.js          42件  （新規。T-051 の完了条件1〜10）
tests/waitlist_step2.test.js          7件  （LP が段構造になるまで落ちる）
tests/render.test.js                  3件  （同上。支援ウィジェットの置き場・段の存在）
tests/request_flow_behavior.test.js   1件  （匿名シグナルが飛ばない）
```

**T-051 と無関係に落ちているテストは無い。** 実装側は「新しく落ちたものは自分の変更が原因」と
判断してよい。`tests/hero_ranking.test.js` は10件すべて通っている。

## 組織への指摘
- [規約] 完了条件2・3 が「文言が、どちらも `X`」という書き方で、**完全一致か部分一致かが
  一意に定まらない**。現行の文言は部分一致では通ってしまうため、テスト側で完全一致と決めた。
  文字列を名指しする完了条件には、一致の種類（完全一致／前方一致／含む）まで書く必要がある
- [規約] 完了条件が「`npm run build` の出力（`dist/`）に対して機械的に判定できること」と
  書かれているが、**受け入れテストは実装前・ビルド前に走る**。判定対象をテスト側で
  決め直すことになり（描画結果と dist の両方で見る形にした）、TDD の前提と完了条件の
  書き方が噛み合っていない

- [道具] エージェント間の直接通信の宛先 `org-test-T-051` へ観点を照会しようとしたが
  `No agent named 'org-test-T-051' is reachable` で届かなかった。タスクファイルの
  `## 連絡してよい相手` に宛先が書かれていても、**相手が既に終了していると経路が使えない**。
  規約は「終了したエージェントはメッセージが届いた時点で再開される」としているので、
  書かれている前提と実際の挙動が食い違っている
- [規約] 完了条件が「`dist/` に対して機械的に判定できること」と書かれている一方、
  受け入れテストはビルド前に走る。**同じ食い違いを org-test も `## 組織への指摘` に
  書いており、実装側でも同じところで判断を1回挟んだ**（描画結果で通っているのに
  古い `dist/` のせいで3件落ちる、という中間状態が出た）
- [反復] LP の作り替えで既存テストが落ちた2件が、どちらも**検査の中身ではなく
  「属性の並び」「CSS のクラス名」への結合**だった。T-050 でも同種の指摘が出ている
  （`tests/lp_cta.test.js` の送信ボタンの正規表現）。**構造を移す種類の変更のたびに、
  実装が「テストを直すか、実装を検査の書き方に合わせるか」の判断を強いられている**
