# T-070: メールアドレスを入れなかった人のアンケート回答が捨てられている

## メタデータ
- 作成日: 2026-09-08
- 更新日: 2026-09-08
- 状態: 完了
- 優先度: 高
- 担当: org-implementation-T-070（レビュー通過）
- 進捗: 実装完了。受け入れテスト36件＋単体テスト5件が緑。npm test は既知の赤2件（T-049）を除き全緑。移行SQL は未実行（PO が流す）
- 依存: T-062（推奨: 同じ匿名の識別子を鍵に使うため。T-062 は完了・デプロイ済み）
- TDD適用可否: 適用
- 手戻り回数: 1

## 目的

**PO 報告（2026-09-08）:**

> Request signal に id, created_at は追加されてるが、メールアドレス入力をしないと waitlist 側に
> レコードが追加されてない。メールアドレス追加しなかった場合もアンケート内容がレコードに
> 追加されるようにしておいてくれ。

**これは仕様どおりの挙動である。** `src/assets/request.js` の 🔒 にこう書いてある ——
「アンケートの回答は、メールアドレスの段を送ったときに相乗りする
（**メールアドレスが無ければ保存しない。保存の鍵がそれしかない**）」。

`waitlist` は `email` が主キーなので（`worker/schema.sql`）、**メールアドレスの無い行は
物理的に入らない。** つまり導線はこうなっている。

| 利用者の行動 | いま残るもの |
| --- | --- |
| ボタンを押す | `request_signal` に1行（id / created_at / page） |
| 成分アンケートに答える | **何も残らない** |
| メールアドレスを入れる | `waitlist` に1行（回答も相乗り） |

**「押した → 成分を答えた → メールは入れない」人の回答を、いま全部捨てている。**
次に何を載せるかを決めるための情報そのものであり、`docs/research/validation-plan.md` の
「成分アンケート回答率」の分子でもある。

## 変更範囲

- `worker/schema.sql` — 新しいテーブルの定義
- `worker/migrations/` — 移行SQL を1本
- `worker/index.js` — ルートを1行
- `worker/` に受け口を1ファイル（`request_survey.js` を想定）
- `src/assets/request.js` — アンケートの段の送信
- `tests/` — 受け入れテストと単体テスト
- `.claude/rules/pergram-compliance.md` —「サーバが保持してよいもの」の一覧の更新
- `docs/ops/deploy.md` — 移行SQL と**デプロイ順序の危険**の追記

## 禁止事項

- **🔒 `waitlist` に列を足さない。6列で打ち止め**（T-058）。`email` が主キーである以上、
  匿名の行はそもそも入らない
- **🔒 `request_signal` に列を足さない。3列で打ち止め。**
  「個人を識別できるものを何も持たないことが、この行の存在理由である」（`worker/request_signal.js` が正典）。
  **自由記述をこの行に混ぜない**
- **🔒 IP・User-Agent・リファラを保存しない**（どのテーブルでも）
- **🔒 ページ内の位置（上の帯 / 下の帯）を保存しない。** 位置は GA4 の `data-cta` が持つ
- **自由記述の中身を解釈して弾かない**（N-01 / N-05）。誤検知で正当な要望を捨てるほうが害が大きい。
  サーバがやるのは**長さで切ることだけ**（上限は `src/lib/waitlist_fields.js`）
- **自由記述の本文を GA4 へ送らない。** 書かれたかどうか（0 / 1）だけを数える
- **回答数・人数を画面に出さない**（N-03 / 景表法）
- **メールアドレスをこの新しいテーブルに入れない。** 入れた瞬間に匿名でなくなる
- 帯の見た目（T-061 で確定）を触らない

## 完了条件

**A 群 — 保存の経路**

- A-1 **メールアドレスを入れずにアンケートを送っても、回答が1行残る。** これを消すと落ちるテストがある
- A-2 回答に**メールアドレスが含まれない**。テーブルの列にも本文にも無い
- A-3 同じブラウザが**同じ識別子で上書きされる**（何度答えても行が増えない）。
  鍵は T-062 の `pergram.request_signal_id`（`request_signal.id` と同じ値）
- A-4 **メールアドレスを入れた場合の既存の経路が壊れていない。** `waitlist` への保存は今までどおり
- A-5 受け口が**キー集合ちょうど一致**で検証する（`request_signal.js` と同じ厳しさ）
- A-6 自由記述は `src/lib/waitlist_fields.js` の上限で**切る**。弾かない

**B 群 — スキーマと移行**

- B-1 `worker/schema.sql` の定義と `worker/migrations/` の移行SQL が**一致する**
- B-2 **既存データを壊さない。** `waitlist` と `request_signal` の定義は1文字も変わらない
- B-3 `docs/ops/deploy.md` に、移行SQL と **「テーブルを作る前にコードをデプロイすると
  書き込みが全部失敗する」** というデプロイ順序の危険が書かれている

**C 群 — 回帰**

- C-1 `npm test` が全件通る（**ただし `tests/hero_ranking.test.js` の2件は着手時点で既に赤**。下記の申し送り参照）
- C-2 `npm run build` が成功する
- C-3 `npm test -- tests/worker.test.js` が通る（既存の受け口の検証を緩めていない）

## 判断してよい範囲

- **新しいテーブルの名前と列の構成**（`request_survey` を想定。列は 識別子 / 成分 / 経路 / その他 / 自由記述 / 日時）
- 受け口のパス（`/api/request-survey` を想定）
- 上書きの実現方法（`INSERT OR REPLACE` か `ON CONFLICT DO UPDATE` か）
- テストの置き場

## テスト方法

- 単体・受け入れ: `npm test`。受け口は `tests/worker.test.js` と同じ作りで叩く
- 実環境確認: **必要。ただし PO が行う**（`npx wrangler d1 execute` は deny）

## 停止条件

- **S-7（このタスク固有）**: 「匿名のまま保存する」と「同じブラウザで上書きする」が
  両立しないと判断した場合。**両立する形は在る**（識別子を主キーにする）ので、
  両立しないという結論に至ったら止めて報告すること
- 共通の停止条件（`.claude/rules/org-escalation.md`）

## 参照すべき成果物

- `worker/request_signal.js` — **正典。**この受け口の作り（検証の厳しさ、握り潰し方）をそのまま踏襲する
- `worker/waitlist.js` / `worker/schema.sql` / `src/lib/waitlist_fields.js`
- `src/assets/request.js` の `data-request-survey` のフォーム送信まわり
- `.claude/rules/pergram-compliance.md`「データ保護」— **この一覧を更新するのもこのタスクの仕事**
- `docs/tasks/T-058.md` の決定ログ — デプロイ順序の危険を最初に踏んだ記録
- `docs/tasks/T-062.md` — `pergram.request_signal_id` / `_ack` の仕組み

## 連絡してよい相手

**無し（すべてオーケストレーター経由）。**

⚠️ 当初はテスト → 実装 / 実装 → テスト / テスト ⇄ レビューの宛先を書いていたが、**この実行環境の `Agent` ツールには担当エージェントへ名前を付けるパラメータが無く、`org-xxx-T-070` 形式の宛先は解決できない**（送信が「到達できない」で失敗する）。直接通信の4経路はこの環境では成立しないため、すべてオーケストレーター経由とする。

## 申し送り

- **⚠️ 本番 D1 のマイグレーションは PO しか流せない**（`npx wrangler d1 execute` が deny）。
  **かつ順序が重要である —— テーブルを作る前にコードをデプロイすると書き込みが全部失敗する。**
  T-058 で同じ危険を踏んでおり、そのときは PO が手元で移行を先に流して解決した
- **デプロイは `main` への push で自動的に走る**（Cloudflare Workers Builds、Production branch = `main`、
  `docs/ops/deploy.md` §4）。**つまり `main` へ merge した時点で本番へ出る。**
  移行が済むまで `main` へ入れないこと
- **着手時点で `tests/hero_ranking.test.js` の2件が既に赤である。** ヒーロー3件の手動指定
  （`HERO_PRODUCT_IDS`）が毎朝の価格更新に追随していないためで、**このタスクとは無関係**。
  T-049 が扱う。C-1 の判定でこれを自分の失敗と読まないこと
- 自由記述の防波堤は**フォーム側の注記（`lp.form.freeTextNote`）と文字数上限**であって、
  サーバ側の検閲ではない。この設計は変えない


### オーケストレーターからの申し送り（2026-09-08 テスト作成後）

**受け入れテスト `tests/request_survey.test.js`（コミット `5f05daa`）が先に書かれている。実装はこの契約に合わせること。**

| 項目 | テスト側で確定した形 |
| --- | --- |
| 受け口 | `POST /api/request-survey`。成功は **204 No Content**（`worker/request_signal.js` と同じ） |
| テーブル | `request_survey`。`id` / `nutrients` / `channel` / `nutrients_other` / `requests` / `created_at` の6列。`id TEXT PRIMARY KEY`、`created_at TEXT NOT NULL` |
| 送信本文 | `{ id, nutrients, channel, nutrients_other, requests }` の**キー集合ちょうど一致**。⚠️ `page` を含めない。空の自由記述は `null` |
| 識別子 | localStorage の `pergram.request_signal_id`（T-062） |
| 上書きの実現方法 | **縛られていない。** `INSERT OR REPLACE` / `ON CONFLICT DO UPDATE` のどちらでもよい |
| 何も選ばずに送信 | **1行残す。**「答えない」は skip ボタンが担うため、送信ボタンを押した以上は握り潰さない |

- テストが検査していない点が2つある。**自分で決めてよいが、決めたら決定ログへ書くこと。**
  (1) 上書き時に「空なら今の値を残す」（`worker/waitlist.js` の COALESCE 方式）は要求されていない
  (2) `nutrients` / `channel` を配列以外で受けたときの扱い
- C-1〜C-3（`npm test` / `npm run build`）はテストコードになっていない。**ステップ4で org-test が実行して判定する。**

## 成果物

- `tests/request_survey.test.js` — 受け入れテスト36件（A群 / B群）。実装前は28件が赤
- `tests/request_survey_regression.test.js` — 回帰・シナリオテスト14件（判定時に追加）
- `worker/request_survey.js` — 匿名のアンケート回答の受け口（新規）
- `worker/migrations/2026-09-08_request_survey.sql` — 移行SQL（新規。**未実行。PO が流す**）
- `worker/schema.sql` — `request_survey` の定義を追記（既存2表は無変更）
- `worker/index.js` — `/api/request-survey` のルートを1行追加
- `src/assets/request.js` — アンケートの段の送信を追加。押下と同じ識別子を使い回す
- `tests/request_survey_upsert.test.js` — 単体テスト5件（新規。受け入れテストが縛っていない2つの境界）
- `tests/request_survey_outbox.test.js` — 単体テスト10件（新規。R-1 の出荷箱：控える / 送り直す /
  受領で消す / 5xx・通信断では消さない / 壊れた控えの扱い / 控えにメールアドレスを入れない）
- `tests/mini_dom.js` — localStorage の 🔒 の文言を規約に合わせて修正（R-1 付随。例外的に許可された変更）
- `.claude/rules/pergram-compliance.md` — 「サーバが保持してよいもの」を3つ→4つへ
- `docs/ops/deploy.md` — §3 に「移行が先、`main` への merge が後」を追記

## ブロッカー

⚠️ **`main` へのマージが1点だけ止まっている。** PO が `worker/migrations/2026-09-08_request_survey.sql` を本番 D1 へ流すまで merge しない（先に merge すると自動デプロイでテーブル未作成のまま出て、書き込みが全部 503 になる）。手順は `docs/ops/deploy.md` §3。

## 決定ログ

### 2026-09-08 再レビューの新規指摘3件（N-1 / N-2 / N-3）は、直さずに残す
- 対象: アンケートの保存 / 送信の信頼性
- 決定: **3件とも Minor（任意対応）であり、レビューは通過している。コードを変えずに記録として残す。**
- **N-1 控えの消去が「送った本文」と紐付いていない**（`src/assets/request.js:452,454`）——
  ページ読み込み時の送り直しの応答が遅れて 2xx で返り、その間に同じページで答え直した控えがあると、
  新しいほうの控えが消える。**成立条件が重なるうえ、サーバ側は同じ `id` への COALESCE upsert なので
  最初の回答は残る。**失われうるのは「答え直した内容」だけである。
  整えるなら、送った本文と現在の控えを突き合わせてから消す形がある
- **N-2 控えの滞留に期限も試行回数の上限も無い** —— 受け口が受け取らない限り自由記述が残る。
  ⚠️ **上限を入れると R-1 と同じ「黙って捨てる」性質が形を変えて戻る。**
  規約上 localStorage は指定された置き場（`.claude/rules/pergram-compliance.md:54`）なので違反ではない。
  **入れないほうが良い可能性のほうが高いと判断した**
- **N-3 `[data-request-flow]` が無いページでは送り直しが走らない** ——
  早期 return がスクリプト冒頭にあり、送り直しは末尾にある。
  現状 `request.js` を読み込むページには必ず要望の器があるので実経路は無い
- 却下案: 3件目まで直してから閉じる → **レビューの往復が上限（2往復）に達しており、
  直せば再レビュー無しで出ることになる。**Minor のために無検証の変更を足すのは割に合わない
- 出典: T-070 再レビュー報告（`b6a6f6f` / `95c4bb9` の差分レビュー）


### 2026-09-08 送信に失敗したアンケート回答は、受領されるまで手元に控えて送り直す

- 対象: アンケートの保存 / 送信の信頼性
- 決定: **失敗した回答を localStorage へ控え、次の機会に送り直す**（受領された時点で消す「出荷箱」方式）。
  T-062 の `pergram.request_signal_ack` と同じ形。**永久に残る記録ではなく、受領で消える一時的な控えである。**
- 経緯: **テストとレビューの判定が正面から食い違ったため、オーケストレーターが裁定した。**
  - レビュー（R-1 / Major）: 撃ちっぱなしで失敗すると、段が `settled` になり**そのページ内に再送の機会が一切ない**。
    T-062 が直したのと同じ壊れ方で、**今回失われるのは押下の数ではなく回答そのもの**である
  - テスト: 再送には回答本文を localStorage へ置く必要があり、`tests/mini_dom.js:607` の
    🔒「ここに置いてよいのは表示の状態と匿名の識別子だけ」に反するので**現状の設計が正しい**
- 裁定の理由: **`tests/mini_dom.js:607` の 🔒 の文言が、`.claude/rules/pergram-compliance.md:54` と逆を向いている。**
  規約が言っているのは「年齢・性別・服用中サプリは **localStorage / IndexedDB のみ**。サーバへ送信しない」であり、
  **localStorage は禁じられた置き場ではなく、指定された置き場である。** 禁じられているのはサーバ側である。
  加えて、この回答はもともとサーバへ送る前提のもの（それがこのタスクの目的）であって、
  手元に一時的に控えることが送信より機微になることはない。
  **「回答が捨てられている」を直すタスクが、残った経路で同じことを起こしてはならない。**
- 却下案: 何もしない（テストの主張）→ 上記のとおり前提としている 🔒 の読みが規約と逆である
- 却下案: メモリ内での再試行だけにする → ページ遷移で消える。移行前に `main` へ入った場合の
  503 の窓（この構成が構造的に持つ窓）を救えない
- 付帯条件: **受領できたら必ず消す。** 控えを永続的な記録にしない。自由記述の中身をコードで解釈しない
- 出典: T-070 のレビュー報告（R-1）とテスト判定報告の突き合わせ。セッション内の裁定

### 2026-09-08 `tests/mini_dom.js` の localStorage の 🔒 は、規約と逆を向いていたので直す
- 対象: データ保護 / テストの防波堤
- 決定: 🔒 の文言を、実際の不変条件（**サーバへ送らないこと**が守るべき線であり、
  localStorage はむしろ指定された置き場であること）に合わせて書き直す。防波堤としての検査自体は残す
- 却下案: 文言を放置する → **この文言は既に1回、誤った設計判断を導いている**（上の R-1 の食い違い）。
  理由を知らない担当が次も同じ読み違いをする
- 出典: `.claude/rules/pergram-compliance.md:54` と `tests/mini_dom.js:607` の突き合わせ


### 2026-09-08 出荷箱の鍵・消す条件・再送の契機（R-1 の裁定を実装に落とした形）

- 対象: アンケートの保存 / 送信の信頼性
- 決定: 裁定（上の「受領されるまで手元に控えて送り直す」）を、次の3点で実装した。
  - **鍵**: `pergram.request_survey_outbox`。入るのは**受け口へ送る本文そのもの**
    （`{ id, nutrients, channel, nutrients_other, requests }`）1件だけ。**メールアドレスは入らない**
  - **消す条件**: **2xx（受領された）と 4xx（本文が受け付けられない）。**
    通信断と 5xx では消さない。消す箇所は `postRequestSurvey` の1つに閉じてある
  - **再送の契機**: **ページの読み込み**（`flushRequestSurveyOutbox()`）。
    アンケートの段は送信後に `settled` になり開き直せない（T-053 B-5b）ので、
    同じページ内には契機を作れない。控えが無ければ送信も走らない
- 却下案: 受領（2xx）でのみ消す → **同じ本文を何度投げても通らない 4xx を抱え続け、
  訪問のたびに 400 を貰いに行くだけの送信が永久に走る。** 回答は1行も増えない
- 却下案: すべての応答で消す（「返ってきたら消す」）→ **503 で消える。**
  移行前にコードが出た窓（`docs/ops/deploy.md` §3）が、まさに 503 が返る窓である
- 却下案: メモリ内でのみ再試行する → ページ遷移で消える。裁定で既に却下されている
- 付帯条件: 控えの**中身をコードで解釈しない**（N-01 / N-05）。
  送り直しの前に整えるのは**キー集合だけ**（受け口がちょうど一致でしか受け取らないため）。
  目印は `tests/request_survey_outbox.test.js`（10件）。
  実装前の版に対して同じテストを走らせ、**6件が赤になること**を確認済み
- 出典: `src/assets/request.js` の `SURVEY_OUTBOX_KEY` / `postRequestSurvey` /
  `flushRequestSurveyOutbox`

### 2026-09-08 壊れた型の選択肢が来たときに前の回答が残るのは、承知のうえで残す

- 対象: アンケートの保存 / 受け口の検証
- 決定: `nutrients` / `channel` が数値やオブジェクトで届くと、許可リストで空になり →
  `NULLIF` で NULL → COALESCE が**前の回答を残したまま 204 を返す**。
  **この挙動を承知のうえで残す。コードは変えない**（レビュー R-6 / Minor。記録のみと裁定）
- 経緯: 自分で決めた2つの方針が重なった結果である。どちらも単体では妥当であり、
  どちらを崩しても別の害が出る。
  - 「配列以外も受ける」を崩す（400 にする）→ **同じ送信に載っている自由記述まで捨てる。**
    このタスクは「回答が捨てられている」ことを直すために在る
  - 「空で上書きしない」を崩す → **空の再送信で前の回答が消える**（COALESCE を採った理由そのもの）
- 却下案: 空になった選択肢だけ 400 にする → 「何も選ばずに送っても1行残す」（テスト側の契約）と
  正面から衝突する。**空の回答と壊れた型を、受け口の側から区別できない**
- 付帯条件: **実経路が無い。** 現行のブラウザ側は必ず配列を送る（`checkedValues` の戻り値）。
  型が崩れうるのは手で叩いたときだけで、そのとき失われるものも無い（前の回答は残る）
- 出典: `worker/request_survey.js` の `allowedValues` と SQL。レビュー指摘 R-6

### 2026-09-08 匿名のアンケート回答は新しいテーブルへ入れる。既存2つには列を足さない
- 対象: アンケートの保存 / データ保護
- 決定: 新しいテーブル（`request_survey` を想定）を1つ足し、**T-062 の匿名の識別子を主キー**にして
  成分・経路・自由記述を持つ。`waitlist` と `request_signal` の定義は変えない
- 却下案: `request_signal` に列を足す（3列 → 7列）→ **その行の存在理由を壊す。**
  「個人を識別できるものを何も持たないこと」が存在理由であり、自由記述が混ざれば成り立たない
- 却下案: `waitlist` に匿名の行を入れる → **不可能。** `email` が主キーで、SQLite では後から変えられない
- 却下案: メールアドレスを必須にして今の経路に寄せる → 1クリックまで対価を下げた T-050 / T-051 の
  設計を丸ごと巻き戻す
- 出典: セッション内の PO 報告と、`.claude/rules/pergram-compliance.md`「データ保護」

### 2026-09-08 受け口・テーブル・送信本文の形をテスト側で先に決めた

- 対象: アンケートの保存 / 受け口の契約
- 決定: TDD の先行テストを書くにあたり、`## 判断してよい範囲` にある未決の4点を
  **テスト側で確定した**（実装はこれに合わせる）。
  - 受け口: `POST /api/request-survey`。成功は **204 No Content**（`worker/request_signal.js` と同じ）
  - テーブル: `request_survey`。列は `id` / `nutrients` / `channel` / `nutrients_other` /
    `requests` / `created_at` の6つ。`id TEXT PRIMARY KEY`（＝上書きの鍵）、
    `created_at TEXT NOT NULL`。**`waitlist` から `email` を `id` に差し替えた形**
  - 送信本文: `{ id, nutrients, channel, nutrients_other, requests }` **ちょうど一致**。
    `page` を含めない（位置も経路も持たない）
  - 上書き: **実現方法は縛らない。** 検査するのは挙動だけ（同じ id で2度送っても行は1つ、
    後から来た回答が勝つ）。`INSERT OR REPLACE` でも `ON CONFLICT DO UPDATE` でも通る
  - テストの置き場: `tests/request_survey.test.js` 1本（受け口・ブラウザ側・スキーマ・手順書）
- 却下案: 上書き時に「送られてきた値が空なら今ある値を残す」（`worker/waitlist.js` の
  COALESCE 方式）を要求する → **`INSERT OR REPLACE` では実現できない。**
  `## 判断してよい範囲` が両方を許している以上、テストで片方に縛るのは越権
- 却下案: 送信本文に `page` を入れる → `## 判断してよい範囲` の列の構成に無い。
  「どのページか」は `request_signal` が既に持っており、同じ id で突き合わせられる
- 却下案: 自由記述が空のときにキーごと落とす → キー集合ちょうど一致（A-5）と両立しない。
  空は `null` で送る
- 出典: セッション内のテスト設計。成果物は `tests/request_survey.test.js`

### 2026-09-08 2度目の回答が空でも、前の回答を消さない（COALESCE 方式を採る）

- 対象: アンケートの保存 / 上書きの実現方法
- 決定: 上書きは `INSERT ... ON CONFLICT(id) DO UPDATE` で行い、
  **送られてきた値が空なら今ある値を残す**（`worker/waitlist.js` と同じ COALESCE 方式）。
  `created_at` も更新せず、**最初に答えた日時**のまま置く
- 却下案: `INSERT OR REPLACE` → **2度目の送信が空だと、1度目に集めた回答が消える。**
  アンケートの器は別のページを開けばまた開き、そのまま送信ボタンを押せる。
  回答を消す UI は無いので、COALESCE で失われる操作は存在しない。
  `created_at` も replace では書き換わり、「いつ答えたか」が最後の送信時刻に化ける
- 付帯条件: 受け入れテストはこの点を縛っていない（`## 申し送り` が名指しした未検査点(1)）。
  **目印を自分で置いた** — `tests/request_survey_upsert.test.js` の
  「2度目が空でも1度目の回答を消さない」「created_at は最初に答えた日時のまま動かない」
- 出典: `worker/request_survey.js` の SQL とその上のコメント

### 2026-09-08 選択肢が配列でなくても本文ごと捨てない

- 対象: アンケートの保存 / 受け口の検証
- 決定: `nutrients` / `channel` が配列でなくても 400 にしない。
  `worker/waitlist.js` の `allowedValues` と同じく**単一の文字列も受け**、
  許可リストに無い値だけを落とす。数値やオブジェクトで来た回は空になるが、
  **行は残り、同じ送信に載っている自由記述も残る**
- 却下案: 配列でなければ 400 → **一緒に送られてきた自由記述まで巻き添えで捨てる。**
  このタスクは「回答が捨てられている」ことを直すために在るので、逆を向く。
  キー集合ちょうど一致（A-5）で構造は既に守られており、値の側は許可リストが守る
- 付帯条件: 受け入れテストはこの点を縛っていない（未検査点(2)）。目印は
  `tests/request_survey_upsert.test.js` の
  「単一の文字列で送られた成分・購入先も保存する」「選択肢が数値やオブジェクトでも 204」
- 出典: `worker/request_survey.js` の `allowedValues`

### 2026-09-08 検証の道具を worker/waitlist.js と共有しない（意図的な重複）

- 対象: アンケートの保存 / 受け口の実装
- 決定: `allowedValues` / `freeText` / `UUID_V4` を共通モジュールへ括り出さず、
  `worker/request_survey.js` に**同じものをもう一度書く**
- 却下案: `worker/` に共有モジュールを作って3つの受け口から読む → (1) `worker/waitlist.js` は
  このタスクの `## 変更範囲` に入っていない、(2) **メールアドレスを預かっている稼働中の
  受け口の挙動が、匿名の受け口の都合で動く形になる。** 片方の要件変更が他方へ漏れる
- 付帯条件: **「DRY でない」と見て整理されうる箇所である。** そう見えたときに読む先を
  `worker/request_survey.js` の冒頭コメントへ書いた
- 出典: `worker/request_survey.js` 冒頭の「検証の道具を worker/waitlist.js と共有していないのは意図的である」

### 2026-09-08 何も選ばずにアンケートを送っても1行残す

- 対象: アンケートの保存
- 決定: 成分も経路も選ばず自由記述も空のまま送信した場合も、**行を1つ残す。**
  空を理由にサーバもブラウザも送信を止めない
- 却下案: 空の回答は送らない → **「答えない」の意思表示は skip ボタン（`data-request-skip`）
  が既に担っている。** 送信ボタンを押したのは「送る」という操作であり、
  それを空だからと握り潰すと、分母（`request_survey_view`）と分子の対応が崩れる
- 出典: `src/assets/request.js` の skip の実装。目印は
  `tests/request_survey.test.js`「A-6 何も選ばずに送っても1行残る」

## 作業ログ

- 2026-09-08 オーケストレーターが PO 報告を受けて起票。**PO 判断で次セッションへ回した**（当セッションのコストが $96 に達したため）
- 2026-09-08 `org-test-T-070` が受け入れテストを作成。`tests/request_survey.test.js`（36件）。
  **着手前の `git status` は clean、ブランチは `org/request-survey`。**
- 2026-09-08 実装前の実行結果 = **28件が赤**（想定どおり）。緑の8件はいずれも
  「既存の挙動が壊れていないこと」を見る回帰・否定形の検査で、実装前に緑であるのが正しい
  （待機リストへの送信、GA4 に自由記述と個人識別情報を載せない、画面に件数を出さない、
  既存2テーブルの定義が変わっていない、移行 SQL が既存表に列を足していない）
- 2026-09-08 `npm test` 全体は 535件中 30件が赤。内訳は **本タスクの 28件 + `tests/hero_ranking.test.js` の 2件**
  （後者は着手時点で既に赤。T-049 の担当分であり、本タスクとは無関係）。
  **他のテストファイルは1件も壊していない**
- 2026-09-08 `org-test-T-070` が実装（`8cc78cb` / `b6470ae` / `b236af5`）を判定。
  **実装の報告を鵜呑みにせず、C-1 / C-2 / C-3 を自分で実行した。**
  結果は合格（Blocker 0 / Major 0）。証拠は `## 証拠` に置いた
- 2026-09-08 判定に足りない観点として回帰・シナリオテストを追加（`tests/request_survey_regression.test.js`、14件）。
  A-4 の通し確認、2つの表に同じ人が残ったときの数え方、T-062 の再送の仕組みの回帰、
  重複させた検証の道具のずれの検出。**変更は `tests/` のみ**
- 2026-09-08 回帰テストが空振りでないことを**変異で確認した**。
  `src/assets/request.js` の写しに「`currentSignalId()` のフォールバックを削り、
  早期 return を元の位置へ戻す」変異を入れると、受領済みブラウザのアンケート送信が
  **1件 → 0件**に落ち、狙いどおりテストが落ちる側へ倒れる（写しは作業ツリー外で実行。`src/` は触っていない）
- 2026-09-08 `org-implementation-T-070` が着手。着手前の `git status` は clean、
  ブランチは `org/request-survey`
- 2026-09-08 受け入れテストが縛っていない2点を先に単体テストへ固定（RED: 5件失敗を確認）→
  `worker/schema.sql` / 移行SQL / `worker/request_survey.js` / `worker/index.js` を実装 →
  受け口側の受け入れテストが緑（残り6件は赤）
- 2026-09-08 `src/assets/request.js` にアンケートの送信を追加。押下時に決まった識別子を
  `signalId` に控え、アンケートでも同じ値を使う。受け入れテスト 36件が緑
- 2026-09-08 `docs/ops/deploy.md` §3 に「移行が先、`main` への merge が後」を追記（B-3 が緑に）。
  `.claude/rules/pergram-compliance.md` の「サーバが保持してよいもの」を3つ→4つへ更新
- 2026-09-08 検証: `npm test` 540件中 538件が緑（赤2件は `tests/hero_ranking.test.js` の既知分）、
  `npm run build` は exit 0
- 2026-09-08 **レビューの修正（往復1回目）**。R-1 / R-1付随 / R-3 / R-4 / R-5 / R-6 の6件に対応。
  R-2（レート制限）は T-026 へ寄せられたため触っていない
  - R-1: 出荷箱方式を実装（`SURVEY_OUTBOX_KEY` / `postRequestSurvey` /
    `flushRequestSurveyOutbox`）。単体テスト `tests/request_survey_outbox.test.js` 10件を追加。
    **実装前の版（`git show HEAD:src/assets/request.js`）へ同じテストを当てて6件が赤になることを確認**
  - R-1付随: `tests/mini_dom.js` の localStorage の 🔒 を、規約（サーバへ送らないことが線）に
    合わせて書き直した。防波堤としての検査は残している
  - R-3 / R-4: `docs/ops/deploy.md` の表の数を4表へ、
    `.claude/rules/pergram-compliance.md` の「揃えるテスト」の案内を表ごとの正典へ
  - R-5 / R-6: 決定ログを2件追記
  - **テスト側の `tests/request_survey.test.js` と `tests/request_survey_regression.test.js` は
    1文字も触っていない。再送を足しても両者の期待は変わらなかった**
- 2026-09-08 修正後の検証: `npm test` 564件中 562件が緑（赤2件は既知の `hero_ranking`）、
  `npm run build` は exit 0

## 証拠

実装後（2026-09-08、ブランチ `org/request-survey`）。

```
$ node --test tests/request_survey_upsert.test.js     # 実装前（RED の確認）
# tests 5 / # pass 0 / # fail 5

$ node --test tests/request_survey.test.js tests/request_survey_upsert.test.js
# pass 41 / # fail 0

$ npm test
# tests 540 / # pass 538 / # fail 2
not ok 115 - 完了条件2: ヒーローの各行が、その製品の実データの単価と袋価格を出している
not ok 118 - T-009 が記録した順位・単価が、いまの実データと一致する
  → どちらも tests/hero_ranking.test.js。着手時点で既に赤（T-049 の担当分）

$ node --test tests/worker.test.js
# pass 28 / # fail 0

$ npm run build
build exit=0

$ grep -o "api/request-survey" dist/assets/request.js | wc -l
2        # ビルド成果物にも受け口が載っている
```

**未検証（PO が行う）**: 本番 D1 への移行SQL の実行と、実環境での書き込み確認。
`npx wrangler d1 execute` は deny のため実装側では実行できない。

## 組織への指摘

- [規約] 直接通信の宛先（`org-implementation-T-070` / `org-review-T-070`）が作業指示に
  書かれていたが、**この実行環境の `Agent` ツールに名前を付けるパラメータが無く、解決できなかった。**
  `CLAUDE.md`「エージェント間の直接通信」は `name` パラメータがある前提で書かれている。
  結果として4経路すべてが使えず、往復はオーケストレーター経由になった
- [道具] `superpowers:systematic-debugging` は今回不要だった（テストが赤いのは想定どおりのため）。
  `ecc:e2e-testing` は Playwright 前提の手順で、このリポジトリの検証手段
  （`node:test` + `tests/mini_dom.js` の自作 DOM + `node:sqlite`）とは前提が合わず使えなかった。
  ブラウザ相当の検証をこの構成でどう書くかは、既存のテストファイルを読んで倣うほかない
- [規約] （実装）完了条件 A-1〜A-6 のうち実環境での確認を要する部分（本番 D1 への移行SQL の
  実行と書き込みの確認）は、実施者が PO と明記されているため止まらずに済んだ。
  一方で **`## 変更範囲` に `worker/waitlist.js` が無いため、3つの受け口で同じ検証関数を
  重複させる形になった**。範囲の切り方が「触ってよいファイル」だけで示されており、
  「既存ファイルへ影響を出さずに共通化する」選択肢が最初から取れない
- [道具] `ecc:database-migrations` は PostgreSQL / MySQL と ORM（Prisma, Drizzle 等）向けの
  手順で、Cloudflare D1（SQLite・`wrangler d1 execute` で手流し・移行の適用記録を持たない）
  とは前提が合わなかった。順序の危険（テーブルを作る前にコードが出る）は
  `docs/tasks/T-058.md` の記録から拾った
