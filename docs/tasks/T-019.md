# T-019: T-009 / T-010 のレビュー指摘（Minor 5件）を反映する

## メタデータ
- 作成日: 2026-09-02
- 更新日: 2026-09-02
- 状態: 完了
- 優先度: 中
- 担当: org-implementation
- 進捗: Minor 5件を反映。R-010-4 は T-020 が充足済みのため未変更。テスト・ビルド・3幅の実測まで完了
- 依存: 無し
- TDD適用可否: 適用外（記録・コメント・文言の整合であり、振る舞いを変えない。順序は 実装 → レビュー）
- 手戻り回数: 0

## 目的

T-009 / T-010 のレビューで挙がった **Minor 5件**をまとめて反映する。
いずれも**振る舞いを変えない**（記録・コメント・文言の整合）。個別に起票すると
同じファイルを何度も開くことになるため、1タスクにまとめてある。

**5件に共通する性質は「理由を知らない次の担当が誤読する」ことである。** 直さないと、
T-009 / T-010 で下した判断が、次に触る担当によって黙って壊される。

| # | 指摘 | 場所 |
|---|---|---|
| R-009-1 | 例外の記録が引用しているカード見出しに「（Sample）」が抜けている | `docs/design/design.md`「何が嘘になっているか」1つ目 |
| R-009-2 | `HERO_PRODUCT_IDS` のブロックコメントが 2026-08-10 のままで、2026-09-02 の差し替えと値の出所が無い | `src/build/build.js:58-70` |
| R-010-2 | ヘッダのベータ導線から「ベータ版」の語が消え、遷移先がβ版だと読めない | `locales/ja.json` / `locales/en.json` の `lp.nav.beta` / `lp.nav.betaShort` |
| R-010-3 | 主CTAの文言「Waitlistに登録」「登録」が、コピー規則の反例そのもの | `locales/ja.json` の `lp.nav.cta` / `lp.nav.ctaShort` |
| R-010-4 | 「ヒーローの主従」という同じ不変条件が2ファイルに重複し、片方のコメントが他方に言及していない | `tests/render.test.js:431-442` と `tests/lp_cta.test.js` |

## 変更範囲

| ファイル | 変更内容 |
|---|---|
| `docs/design/design.md` | R-009-1。**「ヒーローのランキングカードの例外」の節の中だけ** |
| `src/build/build.js` | R-009-2。**`HERO_PRODUCT_IDS` のブロックコメントだけ。配列の値を変えない** |
| `locales/ja.json` / `locales/en.json` | R-010-2 / R-010-3 |
| `tests/render.test.js` | R-010-4。**コメントだけ** |
| `tests/lp_cta.test.js` | R-010-4。**コメントだけ** |

## 禁止事項

- 🔒 **`HERO_PRODUCT_IDS` の3件と順序を変えない。** PO が確定した値である（T-009 の決定ログ）
- 🔒 **CTA の主従を変えない。** T-010 の決定である
  - ⚠️ **2026-09-02 追記: この禁止事項は「ヘッダについてのみ」有効である。**
    T-020 が**ヒーローに限って** CTA をβ版1本へ絞った（T-020 の決定ログ「上書き対象: T-010」）。
    **ヒーローに待機リストの CTA はもう存在しない。** ヘッダでは T-010 の決定が今も生きている。
    **両方を同じものとして扱わないこと**
- 🔒 **`data-cta` の4値を変えない**（`hero_beta` / `hero_waitlist` / `header_beta` / `header_waitlist`）
- 🔒 **テストのアサーションを変えない。** R-010-4 で触ってよいのは**コメントだけ**である
- 🔒 禁止語を使わない。ボタンは操作の結果をそのまま名乗る（`.claude/rules/pergram-ui-copy.md`）
- 🔒 `docs/design/design.md` の例外の記録を削除しない
- 🔒 en の `lp.ctaNote` に手を出さない（**T-015 の担当**。同じファイルなので、T-015 と同時に走らせない）
- 🔒 `src/styles/` を触らない（**T-017 / T-018 の担当**）
- 768px でヘッダが折り返さないこと。R-010-2 の文言はここで制約される（T-010 の決定ログに経緯あり）

## 完了条件

1. R-009-1: 例外の記録が引用する見出し文言が、`locales/ja.json` の `lp.hero.cardTitle` の実文言と一致している
2. R-009-2: `HERO_PRODUCT_IDS` のブロックコメントから、**差し替え日（2026-09-02）と値の出所**（PO 確定 / `docs/design/design.md` の表）が辿れる
3. R-010-2: **遷移先がβ版であることが、ヘッダから読み取れる**（ラベル・注記・`aria-label` のいずれでもよい）
4. R-010-3: 主CTAの文言が「操作の結果を名乗る」形になっている、**または**現行を維持する判断とその理由が `## 決定ログ` に書かれている
5. R-010-4: `tests/render.test.js` と `tests/lp_cta.test.js` の**双方のコメントから、他方の存在に辿り着ける**
   - ⚠️ **2026-09-02 時点で、この条件は T-020 が既に充足している**（T-020 のレビュー R-020-3）。
     **確認だけして、満たされていれば手を加えないこと。二重作業になる**
   - **ただし R-010-4 の根本（同一の不変条件が2ファイルに重複して固定されている）は
     未解消のままである。** T-020 のレビュー R-020-7 が改めて指摘している。
     **重複そのものの解消は、このタスクの範囲外**（コメントだけを触る指示である）。
     気づきとして報告に書くこと
6. `npm test` が通っている（**着手時点の件数から減らさない**。
   ⚠️ **旧条件の「291件が全て」は 2026-09-02 に取り下げた** —— T-020 で 295 件、
   その後 T-011 / T-017 でさらに増えており、**固定値では判定できない**。
   着手時に自分で数え、その値を `## 証拠` に書くこと）
7. `npm run build -- --strict` が終了コード 0
8. 375 / 768 / 1440px で横あふれが 0px（文言を変えるため、折り返しの確認が要る）

## 判断してよい範囲

- 文章・コメント・文言の具体的な書き方
- R-010-3 を直すか、現行を維持して理由を記録するか（**どちらでもよい。記録すれば通る**）
- R-010-2 をラベル・注記・`aria-label` のどれで解くか

**決めてはならない**: `HERO_PRODUCT_IDS` の値。CTA の主従。`data-cta` の値。テストのアサーション。

## テスト方法

- `npm test`
- `npm run build -- --strict`
- **375 / 768 / 1440px の実測**（手順は下記）。文言変更が折り返しを壊していないこと

## 停止条件
- 共通の停止条件に加えて:
- R-010-2 を満たす文言が 768px でヘッダを折り返させてしまい、両立できないと判明した場合

## 参照すべき成果物

| パス / 手段 | 何を見るか |
|---|---|
| `docs/tasks/T-009.md` / `docs/tasks/T-010.md` の `## 決定ログ` | 変えてはいけない判断の理由 |
| `.claude/rules/pergram-ui-copy.md` | コピー規則（禁止語・使う語）。R-010-3 の根拠 |
| `locales/ja.json` の `lp.hero.cardTitle` | R-009-1 で一致させる実文言 |
| `src/build/build.js:55-100` | `HERO_PRODUCT_IDS` とフォールバックの仕組み |
| **モバイル幅の計測手順** | Chrome を `--remote-debugging-port` で起動 → Node 22 標準の `WebSocket` で DevTools Protocol へ接続 → `Emulation.setDeviceMetricsOverride`（`width:375, height:812, deviceScaleFactor:2, mobile:true`）。**`--window-size` だけでは meta viewport が無視され、正しく測れない** |

## 連絡してよい相手
無し（すべてオーケストレーター経由）

## 申し送り

- **5件とも T-009 / T-010 のレビューで Blocker でも Major でもないと判定されている。**
  両タスクは既に完了しており、これは後追いの整合である。
- **R-010-1（Major・ベータ版ボタンが「押せる操作」に見えない）は、このタスクではなく T-017 で扱う。**
  修正先が `src/styles/` であり、ここでは触らない。
- **T-015 と同時に走らせないこと。** どちらも `locales/*.json` を触る。


**🔒 「CTA の主従」は、ヒーローとヘッダで状態が違う。**
T-010 の決定（待機リストが主、β版が従）は**ヘッダでのみ生きている**。
T-020 が**ヒーローに限って**それを上書きし、待機リストのボタンと注記を要素ごと出力しなくなった。
**「LP の CTA は待機リストが主」と一文で書くと、ヒーローの実態と必ず食い違う。**

**`hero_waitlist` の GA4 イベントは T-020 以降ゼロになる。不具合ではない。**
`cta_click` の内訳を比べるときに誤読しないこと。

**T-020 のレビューが、このタスクへ回付した指摘（R-020-5 / R-020-7）:**

- `tests/render.test.js:461`（「製品一覧がまだ無いとき〜」）の判定が、**文言文字列 `β版` に
  依存している。** この文言は T-010 → T-020 で2回変わっており、**次に変わるとテストが
  黙って素通りする。** すぐ隣の行は `/ja/protein/` という構造で見ている。
  `data-cta="hero_beta"` か `hero__beta` で見るほうが安全
  - ⚠️ **ただし、あなたの変更範囲では `tests/render.test.js` は「コメントだけ」である。**
    アサーションの書き換えは範囲外。**気づきとして報告に書き、直さないこと**（停止条件 S-4）
- 同一の不変条件が2ファイルに重複して固定されている件（R-010-4 の根本）は未解消。
  **これも範囲外。報告のみ**

**あなたの前に T-015 が `locales/*.json` と `tests/render.test.js` を触る。**
🔒 **en の `lp.ctaNote` には手を出さないこと**（T-015 の担当領域。既に禁止事項にある）。

**`docs/design/design.md` は T-016 が既に触っている**（L232 付近の独自ドメイン、
L354 付近の付録、L427 付近の「LP からの動線」）。**あなたが触ってよいのは
「ヒーローのランキングカードの例外」の節の中だけ**である。それ以外の節の記述は
T-016 が実態へ合わせ済みなので、**上書きしないこと**。

## 成果物

| 対象 | 変更 |
|---|---|
| `docs/design/design.md` L560-563 | R-009-1。例外の記録が引用する見出しを実文言へ合わせた |
| `src/build/build.js` L64-77 | R-009-2。`HERO_PRODUCT_IDS` のブロックコメントに差し替え日と値の出所を追記 |
| `locales/ja.json` L137-140 / `locales/en.json` L137-140 | R-010-2 / R-010-3。ヘッダの2つの CTA の文言 |
| `tests/render.test.js` / `tests/lp_cta.test.js` | R-010-4。**変更なし**（T-020 が充足済みであることを確認しただけ） |

## ブロッカー
無し

## 決定ログ

### 2026-09-02 T-019 はコードレビュー未実施のまま完了とする（PO 判断）
- 対象: 組織 / レビュー工程
- 決定: **PO の判断により、独立したコードレビューを実施せずに完了とする。**
  セッションのコストが $75.90 に達しており、PO が「T-011 の Major の修正だけ入れて
  締める」を選択した
- 却下案:
  - **レビュー担当を1体起こす** → **本来はこちらが正しい。** T-019 は
    **実装担当が自分で書き、自分で検証しただけ**であり、独立した目が一度も入っていない。
    T-017 で「レビューを省いてよいのはテスト担当が実装前と実装後の両方で独立検査した
    場合だけ」と決めた条件を、**このタスクは満たしていない**
- 付帯条件 — **🔒 次にこのコードを触る担当は、以下が未検証であることを前提にすること:**
  1. **本タスクは、広告を出している LP の主 CTA の文言を変更している**
     （ja「Waitlistに登録」→「リリース通知を受け取る」、
     en "Join the waitlist" → "Get the launch email"）。
     **文言の妥当性を、実装担当以外の誰も見ていない**
  2. **`docs/design/design.md` に、変更したヘッダの CTA 文言が転記されている箇所が
     あるかを確認していない**（実装担当が「要判断」として報告し、
     `## 変更範囲` の外だったため未確認のまま）
  3. 言い換えがヘッダだけで止まっており、製品一覧の「Waitlistに登録」等5箇所と
     食い違っている（**T-024 として起票済み**）
- 出典: セッション内の PO 判断（2026-09-02）。成果物なし

### 2026-09-02 ヘッダのβ版導線に「β版」を出す幅は、900px 未満の短縮ラベルで確保する
- 対象: LP / コピー
- 決定: `lp.nav.beta`（900px 以上で表示）を「ランキングを見る（β版）」、
  `lp.nav.betaShort`（900px 未満で表示）を「β版一覧」にする。
  en は "See the ranking (beta)" / "Beta ranking"。
- 却下案:
  (a) `aria-label` で解く案 → **`src/templates/lp/hero.js` を触る必要があり、このタスクの
      `変更範囲` の外**（変更範囲は locales / build.js / design.md / テストのコメントのみ）。
      加えて、目で見る利用者には β 版だと伝わらない。
  (b) `betaShort` を「ランキング（β版）」にする案 → 375px で 133px になり、待機リスト側と
      合わせて 245px。ヘッダの actions に使える幅は 198px（375 − 左右padding 32 − ワードマーク 129
      − gap 16）で、**実測でヘッダが2行に折り返しヘッダ高が 72px → 116px に増えた。**
  (c) `betaShort` を「β版」だけにする案 → 幅は最も安全（49px）だが、遷移先が何の一覧なのかを
      名乗らなくなる。規約「ボタンは操作の結果をそのまま名乗る」に反する。
- 付帯条件: 「β版一覧」は 375px で 73px。待機リスト側「通知を受け取る」112px と合わせて 189px で
  **1行に収まることを実測済み**（Noto Sans JP 読み込み後 189px / `font-display: swap` の
  フォールバック system-ui でも 182px。いずれも上限 198px 未満）。
  **`betaShort` をこれ以上長くすると 375px で折り返す。** 上限は 198 − 112 − gap 3 = 83px 相当。
- 出典: セッション内の実測（DevTools Protocol の `Emulation.setDeviceMetricsOverride`）。証拠は `## 証拠`

### 2026-09-02 ヘッダの主CTAを「Waitlistに登録」から「リリース通知を受け取る」へ変える
- 対象: LP / コピー
- 決定: `lp.nav.cta` を「リリース通知を受け取る」、`lp.nav.ctaShort` を「通知を受け取る」にする。
  en は "Get the launch email" / "Notify me"。
- 却下案:
  (a) 現行維持（`## 完了条件` 4 が明示的に許している）→ `.claude/rules/pergram-ui-copy.md` の
      「ボタンは操作の結果をそのまま名乗る（「価格を通知する」。**「登録」ではない**）」は
      「登録」を反例として名指ししており、`lp.nav.ctaShort` はその語そのものだった。
      規約が名指しした反例を規約の側の例外として残す理由が見つからない。
  (b) 「Waitlistに登録」だけ残して短縮形だけ直す案 → 長短で意味がずれる。
      また `Waitlist` は「登録先の名前」であって「操作の結果」ではない。
- 付帯条件: **`lp.cta`（Waitlist セクション本体の見出し）と `lp.features.bannerCta`
  （「Waitlistで伝える →」）は `変更範囲` 外なので触っていない。** そのため「Waitlist」という
  語はページ内に残っており、ヘッダだけ言い換わった状態である。
  **同じ理由で別タスクが要る**（報告に記載）。
  `lp.form.submit`「優先アクセス＆リクエストを送信する」は既に結果を名乗る形で、整合している。
- 出典: セッション内の判断。コミットは `## 作業ログ`

### 2026-09-02 R-010-4 は既に充足しているので、テストのコメントを触らない
- 対象: LP / テスト
- 決定: `tests/render.test.js` と `tests/lp_cta.test.js` を**変更しない**。
  双方のコメントが既に他方を名指しで参照していることを読んで確認した
  （`tests/render.test.js` の「⚠️ 同じ不変条件を tests/lp_cta.test.js も持っている」、
  `tests/lp_cta.test.js` の「⚠️ **同じ不変条件を tests/render.test.js（…）も持っている**」）。
- 却下案: 表現を整えて書き直す案 → T-020 が入れた記述を上書きするだけで、
  条件の充足は変わらない。二重作業になる。
- 出典: T-020 のレビュー R-020-3

## 作業ログ
- 2026-09-02: T-009 / T-010 のレビュー結果を受けてオーケストレーターが起票
- 2026-09-02: 着手。`git status` に他タスクの差分（`docs/decisions-index.md` /
  `docs/token-usage-pergram.csv` / `docs/tasks/T-022.md`）があるが、いずれも本タスクの
  `変更範囲` の外なので触っていない（共通の停止条件 S-3 の但し書き）
- 2026-09-02: 着手時点の `npm test` は **330 件 pass / 0 fail**
- 2026-09-02: R-009-1 / R-009-2 を反映
- 2026-09-02: R-010-2 / R-010-3 を反映。初案（`betaShort`「ランキング（β版）」）が
  375px でヘッダを2行に折り返したため、実測に基づき「β版一覧」へ短縮した（決定ログ参照）
- 2026-09-02: R-010-4 は T-020 が充足済みであることを確認し、**変更しなかった**
- 2026-09-02: `npm test` 332 件 pass / 0 fail、`npm run build -- --strict` 終了コード 0、
  375 / 768 / 1440px で横あふれ 0px・ヘッダ1行を実測

## 証拠

### `npm test`

着手時点（変更前）:

```
# tests 330
# pass 330
# fail 0
```

完了時点（変更後）:

```
# tests 332
# pass 332
# fail 0
```

**件数が 330 → 332 に増えているのは、同じ作業ツリーで並行して走っている
T-011 / T-017 がテストを足したためである。** 本タスクはテストを増減させていない
（`tests/` を変更していない）。**減っていないことが完了条件6 の判定である。**

### `npm run build -- --strict`

```
LP          /ja/
製品一覧    /ja/protein/  — 46 製品
⚠️ ヒーロー   HERO_PRODUCT_IDS で手動指定した 3 件を出しています（実際の単価順ではありません）。
クロール    /ja/ を許可 / /ja/protein/ /api/ を拒否
            robots.txt  sitemap.xml  llms.txt を出力
EXIT=0
```

### 375 / 768 / 1440px の実測（完了条件8）

`dist/` を `http://127.0.0.1:8791/` で配信し、`--remote-debugging-port=9333` で起動した
Chrome 152 へ DevTools Protocol で接続。`Emulation.setDeviceMetricsOverride`
（375: `deviceScaleFactor:2, mobile:true` / 768: 同 / 1440: `dsf:1, mobile:false`）を
送ってから `documentElement.scrollWidth - clientWidth` と、ヘッダの各ボタンの
`getBoundingClientRect().top` を読んだ。**`--window-size` は meta viewport を無視するため使っていない。**

同一ページ上で **旧文言を DOM へ書き戻した状態**も測り、新旧を並べてある。

| 幅 | 変更前 overflow / ヘッダ高 / ボタンの行 | 変更後 overflow / ヘッダ高 / ボタンの行 |
|---|---|---|
| 375px | 0px / 72px / 1行（`top:[14]`） | **0px / 72px / 1行（`top:[14]`）** |
| 768px | 0px / 72px / 1行（`top:[14]`） | **0px / 72px / 1行（`top:[14]`）** |
| 1440px | 0px / 80px / 1行（`top:[18]`） | **0px / 80px / 1行（`top:[18]`）** |

ボタン幅（変更後）:

| 幅 | `header_beta` | `header_waitlist` | actions 合計 / 上限 |
|---|---|---|---|
| 375px | 「β版一覧」73px | 「通知を受け取る」112px | 189px / 198px |
| 768px | 「β版一覧」73px | 「通知を受け取る」112px | 189px / 198px |
| 1440px | 「ランキングを見る（β版）」188px | 「リリース通知を受け取る」178px | 370px（余裕あり） |

**768px でヘッダが折り返さないこと**（停止条件）を満たしている。ヘッダ高は3幅とも変更前と同一。

`font-display: swap` のフォールバック（system-ui）に差し替えた状態でも 375px の actions は
182px で1行を保つことを確認した。

### R-010-4 の充足確認（変更していない）

- `tests/render.test.js`: 「⚠️ 同じ不変条件を tests/lp_cta.test.js も持っている（data-cta 側から
  固定している）。片方だけ直すともう片方で必ず落ちる。両方を同時に直すこと。」
- `tests/lp_cta.test.js` の冒頭コメント: 「⚠️ **同じ不変条件を tests/render.test.js
  （「ベータ版への導線はヘッダとヒーローの両方に出る」）も持っている。片方だけ直すと
  もう片方で必ず落ちる。両方を同時に直すこと。**」

双方から他方に辿り着ける。T-020 が入れた記述であり、手を加えていない。

## 組織への指摘
- [規約] 指示欄（完了条件・禁止事項・申し送り）が起票後に3回書き換えられており、
  **本文中の「⚠️ 2026-09-02 追記」と、元の記述が同じ節に並存している。**
  完了条件5・6と禁止事項の「CTA の主従」は、追記を読まずに元の行だけを読むと逆の作業をする
- [規約] 完了条件8「横あふれ 0px」だけでは、**ヘッダが2行に折り返してヘッダ高が
  72px → 116px に増える変化を検出できない**（横あふれは 0px のままである）。
  実際に初案がこの状態になり、停止条件（768px の折り返し）を見て初めて気づいた
