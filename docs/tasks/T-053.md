# T-053: 製品一覧の面の統一と、要望フォームの文言修正

## メタデータ
- 作成日: 2026-09-08
- 更新日: 2026-09-08
- 状態: レビュー中
- 優先度: 高
- 担当: org-review-T-053
- 進捗: 実装完了。npm test 450/450 通過。オーケストレーターが実画面で B 群を確認済み。レビュー中
- 依存: T-051（ブロッカー: T-051 が作った `.request-*` の面そのものが対象のため）
- TDD適用可否: **部分適用**。文言（完了条件 A 群）は先にテストを書ける。見た目（B 群）は
  レンダリング結果の機械的検査＋スクリーンショットの目視で判定する
- 手戻り回数: 0

## 目的

**PO がプレビューを見て「デザインがバラバラ」と指摘した。** 主観の問題ではなく、
製品一覧の縦に並ぶ4つのセクションが**別々のデザイン語彙**で書かれている。

| セクション | クラス | 背景 | 枠線 | 角丸 | パディング |
|---|---|---|---|---|---|
| 単価の出し方 | `.explainer` | `--surface`（白） | `--line`（薄グレー） | `--radius` 14px | `--space-4` 16px |
| 注意事項・広告表示 | `.notice` | `--fill-panel`（グレー） | **無し** | `--radius-interactive` 8px | `--space-3` 12px |
| 要望の帯 | `.request-band__inner` | `--signal-band`（橙） | `--signal-edge` | `--radius-lg` 16px | `--space-6` 24px |
| 段のカード | `.request-flow__step` | `--surface`（白） | `--signal-edge` | `--radius-lg` 16px | `--space-6` 24px |

**角丸3種・背景3種・枠線3種・余白3種が縦に並んでいる。** 同じ階層の情報が
4通りの見た目を持つと、読み手は「これは別の種類の情報だ」と誤って受け取る。

加えて、実画面の確認で**PO の指摘以外に2件の不具合**が見つかっている（後述の完了条件 B-5 / B-6）。

文言側の問題は、**要望ボタンを押した直後に出る文が、押した人の行動を否定していること**である。
現在の `request.surveyLede` は「回答は任意です。リクエストはすでに受け取りました。」で、
**礼を言う前に免責から入っている。** PO の指示は、礼と依頼を先に置き、この免責文を消すこと。

## 変更範囲

- `src/styles/site.css` — `.request-*` 一式
- `src/styles/products.css` — `.explainer` / `.notice`
- `src/styles/tokens.css` — **面のトークンを足す場合のみ**（既存トークンの値は変えない）
- `src/templates/request.js` — 段の構造（文言の挿入位置、空の段が残らない形）
- `src/templates/products/head.js` — `.explainer` / `.notice` のマークアップ
- `src/assets/request.js` — 段の開閉で空の器が残らないようにする処理と、
  **段を開いたときのスクロール位置**（2026-09-08 にオーケストレーターが範囲を拡張。
  理由は決定ログ「礼の文が読まれるところまでを完了とする」）
- `.claude/rules/pergram-ui-copy.md` — 角丸の記述（「カード 14px / 面 16px」）を
  T-053 の整理後の実態へ合わせる1行だけ
- `locales/ja.json` / `locales/en.json` — 文言
- `tests/` — 対応する検査
- `docs/design/design.md` — 面の体系を記録する

## 禁止事項

- **`tokens.css` の既存トークンの値を変えない。** `--signal` / `--signal-hover` /
  `--line-control` は WCAG 2.2 AA のコントラスト比を根拠に固定されている（🔒 コメントあり）
- **`.check-other` を改名しない。** `tests/render.test.js` の検査がこの綴りを見ている
- **`.request-flow__heading` のクラス名を変えない。** `src/assets/request.js` が
  フォーカス移動に使っている
- **段の順序を変えない**（アンケート → メール → 支援）
- **`data-cta` / `data-request-*` / `aria-controls` の値を変えない。** GA4 の前後比較が切れる
- **押下数を画面に出さない**（景表法）。**リクエスト数を並び順に使わない**（N-03）
- **成分の効能・効果に触れる文章を書かない**（N-02）。文言は既存の範囲内で言い換えるだけ
- LP（`src/templates/lp/`）の構造そのものは変えない。`.request-*` は共用なので
  **LP 側の見た目が壊れていないことは確認する**が、LP 固有の改造はしない

## 完了条件

**A 群 — 文言（先にテストを書ける）**

- A-1. `locales/ja.json` に、アンケートの段の冒頭へ出す礼の文言のキーが存在し、その値が
  `リクエストありがとうございます。` と `もしよければ任意で詳しくご要望をお聞かせください。`
  の2文を含む
- A-2. `npm run build` の出力（`dist/ja/protein/index.html`）で、
  **`リクエストありがとうございます。` が `どの成分・製品を追加しますか` より前の位置に出現する**
  （文字列インデックスの比較で判定する）
- A-3. `dist/` 全体で `回答は任意です。リクエストはすでに受け取りました。` の出現が **0回**
- A-4. `locales/en.json` に A-1 に対応するキーが存在し、`locales/ja.json` とキー集合が一致する
  （既存のキー集合一致の検査が通る）
- A-5. 上記を検査するテストが `tests/` にあり、**文言を元へ戻すと落ちる**

**B 群 — 面の統一（レンダリング結果と実画面で判定）**

- B-1. **面の役割が3つ以下に整理され、CSS 上で1組のトークン（または1つの基底クラス）から
  派生している。** 「同じ役割の面が別々の角丸・背景・枠線・余白を直書きしている」箇所が
  `src/styles/` に残っていない
- B-2. `.explainer` / `.notice` / `.request-band__inner` / `.request-flow__step` の
  **角丸・パディングが、役割が同じものどうしで一致している**（grep で値を突き合わせて確認できること）
- B-3. **要望ボタンが帯の中で左端に寄っていない。** 帯の中の配置が意図されたものになっている
  （中央・全幅・右など、デザイン担当が決めた配置で、1280px と 390px の両方で成立する）
- B-4. アンケートの段・メールの段・完了表示の**見出しと本文の字送り（サイズ・太さ・色）が、
  同じ役割どうしで一致している**
- B-5. **空の器が画面に残らない。** アンケートを「回答せずに進む」で飛ばしたとき、
  1280px の実画面に**中身の無い枠だけのカードが出ない**（現状は出る。スクリーンショットで確認済み）
- B-6. 段の見出しへプログラム的にフォーカスを移したとき、**見出しを囲む枠が
  「壊れた要素」に見えない**（現状は `tabindex="-1"` へのフォーカスリングが横幅いっぱいの
  枠として出る。スクリーンショットで確認済み）
- B-6b. **礼の文が画面内に見える。** 要望ボタンを押した直後、1280px と 390px の実画面で
  `リクエストありがとうございます。` が見出しと**同時に**見えている
  （現在の `bringIntoView(heading)` は見出しを画面上端へ送るため、その前に置いた礼の文が
  画面外へ出る。A-2 の文字列検査だけでは検出できない）
- B-7. 1280px / 390px の両方で `document.documentElement.scrollWidth == clientWidth`
  （横溢れ 0）。初期表示・アンケート展開・メール展開の3状態すべてで
- B-8. LP（`/ja/`）でも `.request-*` の見た目が崩れていない（同じく横溢れ 0、目視で確認）

**C 群 — 回帰**

- C-1. `npm test` が全件通る
- C-2. `npm run build` が成功する
- C-3. `node scripts/preview.js --build-only` が成功し、`.preview/assets/request.js` が存在する

## 判断してよい範囲

**デザイン担当（org-design-T-053）:**
- 面の体系そのもの（何種類にするか、どの役割にどの面を割り当てるか、角丸・余白・背景の値）
- 新しいトークン名を `tokens.css` へ足すか、基底クラスを `site.css` へ作るか
- ボタンの配置（中央/全幅/右/左のまま + 余白調整）
- 見出しの字送りの階層
- フォーカスリングの見せ方
- 空の器を消す手段（CSS の `:empty` 系か、DOM から除くか、段ごと畳むか）の**推奨案**

**実装担当:**
- 設計書のとおりに書くための具体的な CSS の書き方・セレクタ・ファイル内の置き場
- テストの書き方

**エスカレーションするもの:**
- 既存トークンの値を変えたくなったとき（禁止事項に当たる）
- 文言を PO 指示以外に変えたくなったとき
- LP の構造変更が必要になったとき

## テスト方法

- **A 群**: `org-test` が先に受け入れテストを書く（この時点で落ちる）。`tests/` に置く
- **B 群**: レンダリング結果と CSS の機械的検査で書けるものはテスト化する。
  書けないもの（配置の良し悪し）は**スクリーンショットで判定する**。
  実施者はオーケストレーター（`.preview` を 4173 で配信し、CDP でモバイル/デスクトップを撮る）
- **C 群**: 実装担当が `npm test` / `npm run build` を実行する

## 停止条件

共通の停止条件に加えて:
- **既存トークンの値を変えないと成立しない設計になったら止める。** WCAG の根拠が
  トークンのコメントに書かれており、勝手に動かすとアクセシビリティの後退になる

## 参照すべき成果物

- `docs/design/design.md` — デザイン要件書
- `src/styles/tokens.css` — トークンの 🔒 コメント（変えてよい/いけないの根拠）
- `src/templates/request.js` — 段の構造と 🔒 コメント（T-050 / T-051 の決定が書いてある）
- `docs/decisions-index.md` — 過去の決定。特に「要望導線」「待機リスト」の対象
- `docs/tasks/T-050.md` / `docs/tasks/T-051.md` — 直前の2タスク
- `.claude/rules/pergram-ui-copy.md` — コピーの規則（禁止語・使う語）
- スクリーンショット（現状）: セッションの scratchpad `before/` に5枚

## 連絡してよい相手
無し（すべてオーケストレーター経由）

## 申し送り

**PO の指示の原文（要旨）:**
1. 単価の出し方・注意事項・リクエストの各セクションのデザインがバラバラ。統一感を出すこと
2. ボタンが左端に寄っていてダサい
3. 「どの成分・製品を追加しますか」の**前**に
   「リクエストありがとうございます。／もしよければ任意で詳しくご要望をお聞かせください。」を入れる
4. 「回答は任意です。リクエストは既に受け取りました」は削除
5. 成分回答した段・リクエストを受け取った表示・メールで知らせる段で、
   フォント・文字色・サイズが違う
6. **デザイナーロールをアサインし、そのエージェントに徹底的にデザインさせること**

**オーケストレーターが実画面で追加で観測した事実（PO 指摘外）:**
- アンケートを飛ばすと、**中身の無い白い枠だけのカードが残る**（B-5）
- 段の見出しにフォーカスが移ると、**横幅いっぱいの枠**が出て壊れて見える（B-6）
- 要望の帯が上下2箇所にあり、**両方とも橙の面**なので画面内で最も強い面が2回出る

**この組織にデザイナーの役は無い。** PO 指示に従い `org-design` をデザイン担当として
アサインする。役の不足そのものは組織への指摘として記録する。

## 成果物

| コミット | 中身 |
|---|---|
| `a48b8f8` | `src/styles/{tokens,site,products,lp}.css` — 面の役割3つ・`--panel-*`・字送り T1〜T6・ボタン配置・フォーカスリング・段の余白 |
| `36f0ecf` | `locales/{ja,en}.json` / `src/templates/request.js` — `request.surveyThanks` 追加、`request.surveyLede` 削除、礼の文を見出しより前へ |
| `fc97103` | `src/assets/request.js` — 飛ばした段を器ごと畳む / 画面へ寄せる対象を段そのものへ |
| `c2f362d` | `.claude/rules/pergram-ui-copy.md` — 角丸の記述を「カード・パネル 14px / 全画面シート 16px」へ |

**未実施（範囲外として報告のみ）**: `src/styles/tokens.css` の角丸ブロックの
コメント「カード 14px / 面 16px / 操作 8-10px」も同じ理由で古くなったが、
指示が規約ファイルの1行に限定されていたため触っていない。

## ブロッカー
無し

## 決定ログ

### 2026-09-08 デザイナー役は置かず、org-design をデザイン担当としてアサインする
- 対象: 組織 / 役の割り当て
- 決定: PO が「デザイナーロールをアサインしろ」と指示したが、この組織の役は5つで固定されており
  デザイナーは無い。**新しい役を作らず、`org-design` へデザインの責務を負わせる**。
  設計書（面の体系）を書かせ、実装は `org-implementation` が行う既存の流れに載せる
- 却下案: 新しく `org-designer` を定義する → 役を増やすのはこの組織で最も高い変更であり
  （`org-improvement-feedback.md` の7段のはしご）、1タスクの必要から飛びつく段ではない。
  まず既存の役に skill（`frontend-design` / `ecc:design-system`）を持たせて足りるかを見る
- 付帯条件: 足りなければ組織への指摘として記録し、改善の場で役の追加を検討する
- 出典: セッション内の判断、成果物なし

### 2026-09-08 面の統一は「見た目を揃える」ではなく「役割の数を減らす」で解く
- 対象: 製品一覧 / 面のデザイン
- 決定: 4つのセクションの値を1つずつ手で揃えるのではなく、**面の役割を3つ以下に定義し、
  1組のトークンまたは基底クラスから派生させる**（完了条件 B-1）
- 却下案: 各セクションの角丸・余白を目視で合わせる → **次にセクションが増えたとき、
  また別の値が直書きされる。** 今回の食い違いはまさにそうやって生まれた
- 出典: セッション内の判断、成果物なし

### 2026-09-08 面の役割を3つに固定し、値の出所を `--panel-*` トークンへ寄せる
- 対象: 製品一覧 / 面のデザイン
- 決定: 縦に積まれる箱の役割を **A 読む面（カード）/ B 呼びかけの面（帯）/ C 注記（面を持たない）**
  の3つに固定する。**A と B は角丸・余白・枠線の太さを完全に共有し、色だけが違う**。
  値は `tokens.css` へ足す `--panel-bg` / `--panel-border` / `--panel-radius` /
  `--panel-pad` / `--panel-pad-lg` / `--panel-call-bg` / `--panel-call-border` から引き、
  コンポーネント側に直書きしない。**既存トークンの値は1つも変えていない**
  （新トークンはすべて既存トークンの別名で、新しい色も寸法も足していない）
- 却下案:
  (1) 各セクションの角丸・余白を目視で合わせる → 値を揃えても**出所**が揃わないので、
      次にセクションが増えたときまた別の値が直書きされる。今回の食い違いはそうやって生まれた
  (2) `.surface` のような基底クラスを作ってテンプレートへ付与する →
      `.p-item` は T-053 の変更範囲外でクラスを足せない。トークンなら
      テンプレートを触らずに出所を1箇所にできる
  (3) 面の役割を4つにし「注記の面」を灰色の箱として残す → 3つに収まるものを4つにすると、
      次の1つを足す言い訳になる
- 出典: docs/design/design.md「面の体系と字送りの階層（2026-09-08 / T-053）」§1〜§2

### 2026-09-08 面の角丸は 16px ではなく 14px（`--radius`）へ揃える
- 対象: 製品一覧 / 面のデザイン
- 決定: `.request-band__inner` と `.request-flow__step` の `--radius-lg`（16px）をやめ、
  `--radius`（14px）へ寄せる。`--radius-lg` は今後**モバイルの絞り込みシート**
  （画面いっぱいに出るもの）専用に残す
- 却下案: 16px へ寄せる → 揃える先として少数派。このページで最も多く出る箱
  （製品カード6枚 + 解説）が 14px であり、2箇所のために8箇所を動かすことになる
- 付帯条件: `.claude/rules/pergram-ui-copy.md` の「カード 14px / 面 16px」という記述は、
  この整理の後は「カード・パネル 14px / 全画面シート 16px」が正しい。
  規約ファイルの更新は T-053 の変更範囲外であり、オーケストレーターの判断に委ねる
- 出典: docs/design/design.md 同節 §2.2

### 2026-09-08 要望ボタンは 900px 以上で「左に文・右にボタン」の2列にする
- 対象: 要望導線 / 要望ボタンの配置
- 決定: `.request-band__inner` の `justify-items: start` をやめ、**900px 未満（および LP の
  全幅）は1列でボタンを帯の幅いっぱい**、**900px 以上の製品一覧だけ2列**にして
  左にリード文、右にボタン（240–320px）と注記を置く。注記はボタンの直下に保つ
- 却下案:
  (1) 全幅の橙のボタン（900px 以上）→ 888px の棒は帯そのものより強い面になり、
      「帯 > ボタン」の階層が逆転する
  (2) 中央寄せの積み重ね（LP と同じ形）→ 左揃えのデータ一覧に中央寄せの箱が入ると
      視線の軸が2本になる。データを読む画面にランディングページの構図を持ち込まない
  (3) 左寄せのまま余白調整 → 帯の右 2/3 が意味を持たない空白のままで、「置き忘れ」に見える
- 出典: docs/design/design.md 同節 §4

### 2026-09-08 上下2箇所の要望の帯は、見た目を変えず同一に保つ
- 対象: 要望導線 / 面のデザイン
- 決定: 製品一覧のリストの前と後ろに出る2つの帯を、**同じ見た目のまま**にする。
  片方を弱めない
- 却下案: 上の帯を弱い面にする → **同じ部品が場所によって違って見えるのは、
  今回直している問題そのもの**。どちらから押されたかは `data-cta` が分けて数えており、
  画面で区別する必要がない。帯が2列になって背が低くなるため圧迫感は現状より下がる
- 出典: docs/design/design.md 同節 §4 末尾

### 2026-09-08 空の器は「段そのものを畳む」で消す。フォームの hidden は維持する
- 対象: 要望導線 / 段の開閉
- 決定: `collapseStep()` でフォームを隠すのに加えて**段そのものを `hidden` にする**。
  `finishStep()` は完了文言を出すので段を戻す。あわせて
  `.request-flow__step:first-child { margin-top }` を
  `.request-flow:has(> .request-flow__step:not([hidden]))` の側へ移す
  （先頭の段を畳むと2番目の段が余白を失うため）
- 却下案:
  (1) CSS の `:has()` だけで消す → 二重否定の `:has()` は読めず、次に触る担当が意味を復元できない
  (2) 飛ばしたときにフォームを元へ戻して段を初期状態へ返す → 既存テスト
      「飛ばした段でもフォームは畳まれ、次の段が開く」が `form.hidden === true` を検査している。
      **視覚の修正にテストの書き換えを混ぜない**
- 付帯条件: 段を畳んでも要望ボタンの `aria-expanded` は `true` のまま残る（WCAG 4.1.2）。
  `false` へ戻すと「押し直すと空の段が開く」という別の不整合が出るため、
  **状態機械の設計として別タスクへ送る**。現状（空の器が実際に見えている）より悪化はしない
- 出典: docs/design/design.md 同節 §5

### 2026-09-08 見出しのフォーカスリングは消さず、`width: fit-content` で文字に沿わせる
- 対象: 要望導線 / アクセシビリティ
- 決定: `.request-flow__heading` に `width: fit-content; max-width: 100%` を与え、
  `:focus-visible` の輪郭を 2px `--signal` のまま `outline-offset: 4px` +
  `border-radius: var(--radius-interactive)` にする。**輪郭を消さない**
  （WCAG 2.4.7 / 2.4.13）。`--signal` は白い面に対して 5.18:1 で非テキスト 3:1 を満たす。
  あわせて `scroll-margin-top`（`--head-offset` / `--head-offset-lg`）を足し、
  フォーカスした見出しがスティッキーヘッダの下に潜るのを防ぐ（WCAG 2.4.11）
- 却下案:
  (1) フォーカスの対象を段の器へ移す → 枠がさらに大きくなり逆効果
  (2) 輪郭をやめて背景の色替えにする → 装飾に見えるうえ、面の色を増やすことになる
- 出典: docs/design/design.md 同節 §6

### 2026-09-08 礼の文は新キー `request.surveyThanks` を見出しより前に置き、`surveyLede` は削除する
- 対象: 要望導線 / 文言
- 決定: `request.surveyThanks` を追加し（ja「リクエストありがとうございます。もしよければ
  任意で詳しくご要望をお聞かせください。」/ en「Thanks for your request. Tell us more if
  you like — it is all optional.」）、`surveyStep()` の `<form>` 直後・見出しより前に
  `<p class="request-flow__thanks">` として置く。`request.surveyLede` は ja / en とも
  **キーごと削除**する。字送りは既存の T3（本文・リード）を流用し、**新しい字送りの役割を作らない**
- 却下案: 既存の `request.surveyLede` の値だけを書き換える → PO の指示は
  「免責文の削除」であり、キーが残ると別の担当が同じ位置へ免責文を書き戻す余地が残る
- 付帯条件: 段を開くとき `bringIntoView(heading)` が見出しを画面上端へ送るため、
  **見出しより前に置いた礼の文が画面の外へ出る**。`heading.focus({ preventScroll: true })` +
  `bringIntoView(step)` への変更が要る。これは T-053 の変更範囲
  （`src/assets/request.js` は「空の器が残らないようにする場合のみ」）を超えるため、
  **オーケストレーターの承認が要る**
- 出典: docs/design/design.md 同節 §7


### 2026-09-08 礼の文が読まれるところまでを完了とする（変更範囲の拡張）
- 対象: 要望導線 / 段のスクロール
- 決定: `src/assets/request.js` の変更範囲を「空の器」だけでなく**段を開いたときの
  スクロール位置**まで広げる。`heading.focus({ preventScroll: true })` にしたうえで、
  画面へ寄せる対象を見出しから**段そのもの**へ変える
- 却下案: 変更範囲を守って文字列の位置だけ直す → **完了条件 A-2（文字列の前後関係）は
  通るが、実画面では礼の文が画面外へ出る。** PO の指示は「文言をちゃんと書いておけ」で
  あって「HTML に存在させろ」ではない。条件を満たして目的を外す典型なので、
  完了条件へ B-6b を足して観測できる形にした
- 付帯条件: フォーカスは見出しへ移したままにする（支援技術の読み上げ位置を変えない）
- 出典: org-design-T-053 の完了報告 §7 末尾

### 2026-09-08 範囲外の指摘は消さずに別タスクへ切り出す
- 対象: 要望導線 / ドキュメント
- 決定: デザイン担当が挙げた範囲外の2件を T-054（`aria-expanded` の不整合）と
  T-055（design.md §9 の配色が実装と食い違う）として起票する。
  `.claude/rules/pergram-ui-copy.md` の角丸の記述だけは、**T-053 が原因で古くなる**ため
  T-053 の変更範囲に含める
- 却下案: T-053 の中で全部直す → 変更範囲が膨らみ、レビューが「今回の変更か既存の問題か」を
  分けられなくなる
- 出典: org-design-T-053 の完了報告「オーケストレーターへ上げたいこと」

### 2026-09-08 面の統一の検査は「トークンが在る」と「直書きが消えた」の両方を見る
- 対象: 製品一覧 / 面のデザイン / 受け入れテスト
- 決定: `tests/panel_unify.test.js` の B-1 / B-2 を、**`--panel-*` が tokens.css に
  定義されていること**と、**`.explainer` / `.request-band__inner` / `.request-flow__step` の
  角丸・背景・枠線・余白が `--panel-*` 以外の値を持たないこと**の2本立てにした。
  さらに「消しただけにしない」ため、4プロパティが実際に `--panel-*` から来ていることも見る
- 却下案: トークンの存在だけを見る → **コンポーネント側の直書きが残っていても通る。**
  今回の食い違い（角丸3種・背景3種）はまさに直書きで生まれており、
  出所を1つにしたことを検査できていない
- 付帯条件: セレクタは**完全一致でしか見ない**。`.waitlist-band .request-band__inner`
  のような文脈付きの上書き（LP で器の面を白へ抜く指定。lp.css に 🔒 の理由がある）は
  設計書が「支配しないもの」と書いた範囲なので、巻き込まない
- 出典: tests/panel_unify.test.js / docs/design/design.md 同節 §2.4

### 2026-09-08 実装前から通る2件を、あえて受け入れテストに残す
- 対象: 受け入れテスト / 回帰
- 決定: 17件のうち **B-4 T1（段の見出しは常に `--size-md`）** と
  **B-5（送った段は器が残り完了文言が読める）** の2件は、着手時点で既に通る。
  **消さずに残す。** どちらも T-053 の変更で**壊れる側**の不変条件だからである
  （T1 は字送りを触るときに、段の器は `collapseStep` を触るときに壊れうる）
- 却下案: 落ちないテストは受け入れテストではないので消す → 消すと、
  「空の器を畳む」実装が `finishStep` の段まで畳んでしまう事故を誰も止められない
- 付帯条件: この2件は**完了条件の充足の証拠にはならない**。オーケストレーターへ
  「実装前に通っている」と報告済み
- 出典: tests/panel_unify.test.js


### 2026-09-08 LP の先頭の段の余白は `:has()` へ移さず、規則ごと削除する
- 対象: 要望導線 / 段の余白
- 決定: 設計書 §5 は `.waitlist-band .request-flow__step:first-child` を
  `.waitlist-band .request-flow:has(...)` へ**移す**と書いているが、**移さずに削除した**。
  移した後の値（`--space-6`）は site.css の `.request-flow:has(...)` と**同値**であり、
  lp.css に写すと同じ値の出所が2つになる。lp.css 冒頭の 🔒 が
  「段そのものは site.css が唯一の出所。ここへ写して上書きしない。2つ持つと片方だけ
  直った状態が生まれる（それが T-051 の発端である）」と明記している
- 却下案: 設計書の字面どおり `.waitlist-band .request-flow:has(...) { margin-top: var(--space-6) }`
  を置く → 見た目は同じだが、次に製品一覧側の余白を変える担当が LP 側の存在に気付かない。
  **今回直している「出所が2つある」問題を、直した直後に1つ作ることになる**
- 付帯条件: 実画面（LP / 1280px・390px）で段の上の余白が従来どおり出ることを確認済み
- 出典: コミット a48b8f8 / src/styles/lp.css の 🔒

### 2026-09-08 `.notice__beta` も T5 へ揃える（設計書の変更点表には無い）
- 対象: 製品一覧 / 字送り
- 決定: 設計書 §3 の階層表は `.notice__beta` を **T5（`--size-xs` / `--muted-weak`）** に
  割り当てているが、同節の「変更点」表には載っていない。**階層表の側を正として揃えた**
  （`font-size: var(--size-xs)` / `line-height: 1.7` / `color: var(--muted-weak)`）。
  親の `.notice` を T3 へ上げた結果、β版の注記だけが本文と同じ大きさのまま残り、
  「広告表示より弱く、しかし読める強さ」という元の意図が崩れるため
- 却下案: 変更点表に無いので触らない → `.notice` の色を `--muted` から `--muted-strong` へ
  上げたことで、より弱いはずの `.notice__beta`（`--muted-faint`）との差だけが残り、
  T5 が階層表のとおりに存在しない状態になる。なお `--muted-weak` は `--muted-faint` より
  濃いので、コントラストは下がっていない
- 出典: docs/design/design.md 同節 §3 / コミット a48b8f8


## 作業ログ
- 2026-09-08 PO 指摘を受けて起票。現状のスクリーンショット5枚を取得し、
  CSS の値を突き合わせて食い違いを表にした
- 2026-09-08 (org-design-T-053) `frontend-design` / `ecc:design-system` /
  `ecc:frontend-design-direction` の3スキルを読み、現状のスクリーンショット5枚と
  `src/styles/{tokens,site,products,lp}.css` / `src/templates/request.js` /
  `src/assets/request.js` / `locales/{ja,en}.json` / 既存テストを突き合わせた。
  `docs/design/design.md` へ「面の体系と字送りの階層（2026-09-08 / T-053）」を追記（§1〜§10）。
  実装コード・文言ファイルには一切触れていない
- 2026-09-08 (org-test-T-053) 受け入れテスト `tests/panel_unify.test.js` を新規作成（17件）。
  着手時の `npm test` は 433件全通過。追加後は 450件中 435通過 / 15失敗で、
  **失敗はすべて新規の15件**（既存テストの新たな失敗は0件）。
  `src/` `locales/` には触れていない。B-3 / B-6 / B-6b / B-7 / B-8 は
  スクリーンショット判定のためテスト化していない
- 2026-09-08 (org-implementation-T-053) 設計書 §1〜§10 のとおりに実装。
  `tokens.css` へ `--panel-*` / `--head-offset*` を追加（既存トークンの値は不変）、
  `site.css` に面の幾何を決める規則を1本置き、`products.css` の `.explainer` / `.notice` の
  直書きを削除。字送りを T1〜T6 へ統一、要望ボタンを 900px 以上で2列に、
  `.request-flow__heading` のフォーカスリングを文字へ沿わせ、
  `src/assets/request.js` で段ごと畳む／段そのものを画面へ寄せる形へ変更。
  文言は `request.surveyThanks` を追加し `request.surveyLede` をキーごと削除。
  `npm test` 450件全通過（新規の失敗 0）。`npm run build` / `preview --build-only` 成功。
  さらに Chrome ヘッドレス + CDP で 1280px / 390px × 製品一覧 / LP × 初期・展開・飛ばしの
  各状態を実測（横溢れ 0、礼の文と見出しが同時に可視、飛ばした段の空の器が消えた）

## 証拠

**C-1 `npm test`**（受け入れテスト15件を含む全件）
```
1..450
# tests 450   # pass 450   # fail 0   # duration_ms 1790.6
```

**C-2 `npm run build`** — 成功（`製品一覧 /ja/protein/ — 46 製品`）。
**C-3 `node scripts/preview.js --build-only`** — 成功。`.preview/assets/request.js` 17912 バイト。

**A-2 文字列の前後関係**（`dist/`、`node -e` で実測）
```
dist/ja/protein/index.html  thanks 221834 < heading 221934
dist/ja/index.html          thanks  15121 < heading  15221
```

**A-3 免責の一文**: `dist/` 配下の全ファイルを走査して出現 **0 回**。

**B-2 面の実測値**（Chrome ヘッドレス + CDP、`getComputedStyle`）

| 要素 | 角丸 | パディング(1280 / 390) | 背景 |
|---|---|---|---|
| `.explainer` | 14px | 24px / 16px | `rgb(255,255,255)` |
| `.request-band__inner` | 14px | 24px / 16px | `rgb(255,247,237)` |
| `.request-flow__step` | 14px | 24px / 16px | `rgb(255,255,255)` |
| `.notice` | 0px | 0px | `rgba(0,0,0,0)`（面なし） |

**B-3 ボタンの配置**: 1280px の製品一覧で帯の内寸 921px に対しボタンは
左オフセット 576px・幅 320px（右列）、リード文は幅 527px（左列）。
390px では幅 324px = 内寸いっぱいの1列。LP は 1280px でも 590px の全幅1列。

**B-5 空の器**: アンケートを飛ばした後に `hidden` でない段は `email` のみ
（1280px / 390px、製品一覧 / LP の4通りすべて）。飛ばした `survey` の段は器ごと消えている。

**B-6 フォーカスリング**: `.request-flow__heading` の実測幅 247px に対し段の内寸は 871px。
`outline: rgb(194,65,12) solid 2px` / `outline-offset: 4px` / `border-radius: 8px` /
`scroll-margin-top: 88px`。輪郭は消していない。

**B-6b 礼の文が画面内に見える**（要望ボタン押下直後）

| 幅 / ページ | 礼の文 top–bottom | 見出し top–bottom | 画面高 | 同時に可視 | フォーカス |
|---|---|---|---|---|---|
| 1280 / 製品一覧 | 137–159 | 171–198 | 900 | ○ | `request-flow__heading` |
| 1280 / LP | 113–136 | 148–175 | 900 | ○ | 同 |
| 390 / 製品一覧 | 117–162 | 174–201 | 844 | ○ | 同 |
| 390 / LP | 93–138 | 150–177 | 844 | ○ | 同 |

**B-7 / B-8 横溢れ**: `document.documentElement.scrollWidth == clientWidth` が
**初期表示・アンケート展開・アンケートを飛ばした後**の3状態すべてで真。
1280px は 1265 == 1265、390px は 390 == 390（製品一覧 / LP の両方）。

## 組織への指摘
- [ロール] PO が「デザイナーロールをアサインしろ」と指示したが、組織の5つの役にデザイナーが無く、設計の役へ兼務させる形にした
- [反復] 見た目の不具合（空の器・フォーカスリング）が、レビューとテストを通過した後に PO の目視で初めて見つかった。T-050 でも同じ経路で発覚している
- [ロール] 視覚の設計を担う役が無いため、設計の役が「方式設計」と「視覚デザイン」という性質の違う作業を同じタスクの中で抱えた（引き金2）
- [道具] 画面の見た目を判定する手段がスクリーンショットの目視しかなく、CSS の食い違い（角丸3種・背景3種）は人が値を表に書き出すまで検出されなかった
- [道具] CSS の規則を読む道具（セレクタの一致判定・`var()` の解決・トークンの読み出し）がテスト間で共有されておらず、`tests/styles.test.js` の同等の関数を新しいテストファイルへ書き写した
- [規約] 完了条件 B-3 / B-6 / B-6b は二値で判定できずテストへ変換できないまま「スクリーンショットで判定」と指示された。テストへ変換できない完了条件は差し戻すという規約と、視覚を扱うタスクの実態が噛み合っていない
- [道具] 「スクリーンショットで目視」とされた完了条件（B-3 / B-5 / B-6 / B-6b / B-7）は、Chrome ヘッドレス + CDP で座標・可視判定・scrollWidth を数値で取れば実装担当が自分で確定できた。この手順がスキルとして共有されておらず、毎回その場で書き起こしている（同じ手順を書くのはこのリポジトリで少なくとも2度目）
- [規約] 設計書の指示（LP の余白を `:has()` へ「移す」）と、対象ファイルに書かれた 🔒（lp.css へ写さない）が正面から食い違い、実装担当が設計側の判断を上書きする形になった。設計成果物と対象ファイルの 🔒 の突き合わせは、設計の段では行われない
