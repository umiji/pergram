# T-050: β版ページに要望ボタンを置き、待機リストを段階化する

## メタデータ
- 作成日: 2026-09-07
- 更新日: 2026-09-07
- 状態: 未着手
- 優先度: 高
- 担当: 未割当
- 進捗: 起票のみ
- 依存: 無し
- TDD適用可否: 適用
- 手戻り回数: 0

## 目的

**8/31〜9/6 の Google 広告（110クリック / ¥2,316）で待機リスト登録が 0 件だった。**
D1 を直接確認して 0 件を確定済みであり、送信経路の不具合ではない。

原因の最有力は「需要が無い」ではなく **測っていた対象と、ページが誘導していた先が食い違っていた**ことである。

- LP のヒーローの CTA は **β版（製品一覧）への1本だけ**で、待機リストのボタンは出していない
  （`src/templates/lp/hero.js` の 🔒 コメント。2026-09-02 T-020、PO 指示の実験）
- 製品一覧側の待機リストの案内は **リストの後ろ**にあり、しかも**その場のフォームではなく別ページへのリンク**
  （`src/templates/products.js` の `waitlistBanner`）
- つまり広告で連れてきた人をβ版へ送り、そのうえで待機リスト登録を KPI にしていた

**このタスクは「メールアドレスという重い対価を、意思表示1クリックまで下げる」ことを行う。**
知りたいのは「このサービスの拡充を望む人がどれだけ居るか」であり、メールアドレスはその後段でよい。

## 変更範囲

| ファイル | 変更内容 |
|---|---|
| `locales/ja.json` / `locales/en.json` | ヒーローのラベル文言、要望ボタンとその注記、段階ごとの見出し |
| `src/templates/products.js` | 待機リストの案内の位置、要望ボタンの設置 |
| `src/templates/products/filters.js` | 絞り込みブロック周辺への設置（必要な場合のみ） |
| `src/assets/products.js` | 要望ボタンの挙動（段階の開閉）、GA4 イベント送信 |
| `src/styles/` | 上記に必要な範囲のみ |
| `tests/` | 下記「完了条件」を検査するテスト |

## 禁止事項

- 🔒 **要望ボタンの押下数を画面に出さない。** 出すと「人気」の表示になり、`.claude/rules/pergram-ui-copy.md` の
  禁止語と景表法に触れる。押した本人へのフィードバック（ボタンの状態変化）だけにする
- 🔒 **要望の数を製品の並び順に一切使わない（N-03）。** 並び順を決めるのは常に有効成分1単位あたりの価格
- 🔒 **β版（製品一覧）へのリンクを LP から削除しない。** 2026-09-07 の PO 判断（下記 決定ログ）
- 🔒 **成分の効能・効果に触れる文言を書かない（N-02）。** ボタンも注記も「追加してほしいか」だけを問う
- 🔒 サーバへ新しい列を足さない。第1段階の計測は **GA4 イベントのみ**で行う。
  D1 のスキーマを変える必要が出たら、着手せず報告する（共通の停止条件 S-2）
- 🔒 自由記述の**本文**を GA4 へ送らない。書かれたかどうか（0/1）だけ（`.claude/rules/pergram-compliance.md`）
- 製品一覧の初期表示件数を変えない（**10件のまま**。2026-09-07 PO 判断）

## 完了条件

1. **ヒーローのラベルが差し替わっている。**
   `locales/ja.json` の `lp.hero.cardTitle` / `lp.hero.cardTitleShort` が `（Sample）` を含まず、
   **「価格情報、順位はサンプルです」の旨**を含む。`locales/en.json` も対応する文言になっている。
   旧文言 `（Sample）` へ戻すと `npm test -- tests/<このタスクで足したテスト>` が落ちる
2. **待機リストの案内が、製品リストより前に出力されている。**
   ビルド後の製品一覧 HTML において、待機リストの案内ブロックの出現位置が、
   製品リスト（`rank-list` 相当）の出現位置より前である。位置を元に戻すとテストが落ちる
3. **要望ボタンが2箇所に出力されている。**
   絞り込みブロックの近傍と、製品リストの末尾。それぞれ `data-cta` 属性を持ち、値が互いに異なる
   （どの位置が押されたかを GA4 の `location` で分離できる）
4. **ボタンの文言と注記が出ている。**
   ボタンに「他の成分・製品追加をリクエスト」の旨、その近傍に**登録が不要である旨の注記**が出力される
5. **段階が正しい順で開く。**
   要望ボタンを押すまでアンケート・メール入力欄・支援の案内は表示されない。
   押した後に「アンケート（任意）→ メールアドレス（任意）→ 支援の案内」の順で到達できる。
   この順序と初期非表示を検査するテストがある
6. **押下数が画面のどこにも描画されていない**ことを検査するテストがある
7. `npm test` のうち、**このタスクが触った領域のテストが通っている**
   （`tests/hero_ranking.test.js` の2件は着手前から落ちている。下記「申し送り」）
8. `npm run build` が成功する

## 判断してよい範囲

- **決めてよい**: 段階の実装方式（既存の待機リストフォームを製品一覧へ持ち込むか、
  遷移させたうえで段階を開くか）、DOM 構造、CSS、テストの置き場と粒度、
  GA4 イベント名とパラメータ名、注記の具体的な言い回し（禁止語に触れない範囲で）、
  要望ボタンの見た目と押下後のフィードバック表現
- **決めてはいけない**:
  - ボタンの趣旨（「他の成分・製品追加をリクエスト」から変えない）
  - 段階の順序（要望 → アンケート → メール → 支援）
  - 製品一覧の初期表示件数（10件）
  - β版リンクを LP から消すこと
  - D1 のスキーマ、サーバが保持する列
  - 「サンプル」表記をやめること（PO 判断済み。下記 決定ログ）

## テスト方法

- 単体・結合: 実装担当が書く（TDD 適用。先に落ちるテストを書いてから実装する）
- 回帰: `npm test`。ただし `tests/hero_ranking.test.js` の2件は着手前から落ちているため、
  **このタスクの判定にはスコープを絞ったコマンドを使う**
- 実環境確認: **不要**。`npm run build` の出力と `npm run preview` の描画で判定する。
  ブラウザでの目視は、オーケストレーターがプレビューを立てて PO へ見せる

## 停止条件

共通の停止条件に加えて:

- 支援ウィジェット（Codoc）が、この市場の設定に存在しない、または製品一覧のページから
  呼び出せないことが分かった場合、**その段だけを未実装として報告する**（他の段は完了させる）
- 段階の実装に D1 のスキーマ変更が必要だと判明した場合、着手せず報告する
- 要望ボタンの計測を GA4 だけでは成立させられないと判明した場合、報告する

## 参照すべき成果物

- `docs/research/validation-plan.md` §5〜§7（判定基準、待機リストで取るデータ、GA4 の計測設計）
- `.claude/rules/pergram-compliance.md`（N-02 / N-03 / データ保護 / 自由記述の扱い）
- `.claude/rules/pergram-ui-copy.md`（ボタンは操作の結果を名乗る。禁止語）
- `src/templates/lp/hero.js` の 🔒 コメント（T-020 でヒーローの CTA をβ版1本にした経緯）
- `src/templates/products.js` の `waitlistBanner`（現在の位置と、別ページ送りになっていること）
- `src/assets/lp.js:73`（既存の `data-cta` → GA4 送信。同じ仕組みに乗せる）
- `docs/decisions-index.md` の `対象: LP` / `待機リスト` / `製品一覧` の行
- `docs/tasks/T-024.md`（`products.waitlistCta` が「Waitlistに登録」のままである件。本タスクで直してよい）

## 連絡してよい相手
無し（すべてオーケストレーター経由）

## 申し送り

- **`tests/hero_ranking.test.js` の2件は、このタスクの着手前から落ちている**（T-049）。
  原因は価格スナップショットの日次更新でヒーロー3件の実データが動き、
  `HERO_PRODUCT_IDS` の記録値が追随していないこと。**このタスクとは無関係であり、直さない。**
  完了判定には範囲を絞ったテストコマンドを使うこと
- **GA4 は 2026-09-07 に復旧したばかり**である（T-047 / T-048 で CSP を修正）。
  8/31〜9/6 の配信期間中はビーコンが全滅していたため、当時のファネルの記録は存在しない
- 待機リストの D1 は 8/31〜9/6 で 0 件。PO が管理画面で直接確認済み

## 成果物
<!-- 完了時に何ができたか。ファイルパスとコミット -->

## ブロッカー
無し

## 決定ログ

### 2026-09-07 ヒーローのランキングカードは「価格情報・順位ともサンプル」と名乗る
- 対象: LP / ヒーロー
- 決定: `lp.hero.cardTitle` の `（Sample）` を「価格情報、順位はサンプルです」の旨へ差し替える
- 却下案: (a) 46製品から計算した上位3件にして `（Sample）` を外し「46製品中の上位3件・日付」と
  名乗る案 → PO が採らなかった。(b) 現状維持 → 何がサンプルなのかが伝わらないため却下
- 付帯条件: **オーケストレーターは、この表記が実態より控えめである旨を PO へ申し送った。**
  ヒーローに出る価格は日次更新される実データであり（`tests/hero_ranking.test.js` が
  実データとの一致を検査している）、サンプルなのは3件の選び方が手動である点だけである。
  PO は内容を理解したうえで「変更を許可する」と明示した
- 出典: セッション内の PO 判断、成果物なし

### 2026-09-07 意思表示は成分単位ではなくサービス単位で問う
- 対象: 待機リスト / 製品一覧
- 決定: 第1段階のボタンは「他の成分・製品追加をリクエスト」とし、
  **サービス全体への要望**を1クリックで受ける。成分ごとの意思表示ボタンは作らない。
  成分の内訳は、押した人にだけ開く第2段階のアンケート（任意）で取る
- 却下案: 成分チップごとに意思表示ボタンを置く案 → PO の意図は
  「このサービスの機能拡充を望むか」であり、個別成分への評価ではないため却下。
  加えて製品単位のボタンは N-03（独自ランキング）と景表法の「人気」表示に触れやすい
- 出典: セッション内の PO 判断、成果物なし

### 2026-09-07 β版へのリンクは LP から削除しない
- 対象: LP / 導線
- 決定: 待機リスト登録を増やす目的で、LP からβ版（製品一覧）への導線を削除しない
- 却下案: LP からβ版リンクを削除して待機リストへ一本化する案 → β版は現時点で
  唯一ユーザーへ価値を届けている機能であり、計器（待機リスト）の数字のために
  製品を隠すことになるため却下。加えて広告の着地ページとして、
  広告文が約束した単価比較を提供できなくなる
- 出典: セッション内の PO 判断、成果物なし

## 作業ログ

### 2026-09-07 受け入れテストの先行作成（org-test）

1. T-050 の指示書、`CLAUDE.md`、`.claude/rules/pergram-compliance.md`、
   `.claude/rules/pergram-ui-copy.md` を読み、完了条件1〜6を二値化する方法を決めた
   （7・8 はコマンドの実行そのものなのでテスト化しない）
2. 既存のテストの作法を確認した（`tests/render.test.js` / `tests/lp_cta.test.js` /
   `tests/waitlist_step2.test.js` / `tests/mini_dom.js`）。依存パッケージを増やさず、
   テンプレートを描画して `tests/mini_dom.js` の DOM で読む方式に合わせた
3. `tests/request_flow.test.js` を新規作成した（16 件）。
   `tests/hero_ranking.test.js` と混ざらないよう独立したファイルにした
4. 実行して 16 件中 15 件が落ちることを確認した（下記 ## 証拠）
5. `npm test` 全体でも、落ちているのはこの15件だけであることを確認した。
   **申し送りにある `tests/hero_ranking.test.js` の2件は、現時点では落ちていない**
   （10件すべて通る）。実装側は「新しく落ちたものは自分の変更が原因」と判断してよい

## 証拠

### 受け入れテストが落ちること（実装前なので落ちるのが正しい）

```
$ node --test tests/request_flow.test.js
not ok 1 - 完了条件1 ja のヒーローのラベルが「価格情報・順位はサンプル」と名乗る
not ok 2 - 完了条件1 en のヒーローのラベルも対応する文言になっている
not ok 3 - 完了条件1 描画された LP に （Sample） が出てこない
not ok 4 - 完了条件2 待機リストの案内が製品リストより前に出力されている
not ok 5 - 完了条件3 要望ボタンが2箇所（リストの前と後ろ）に出力されている
not ok 6 - 完了条件3 要望ボタンの data-cta の値が互いに異なる
not ok 7 - 完了条件4 要望ボタンが「他の成分・製品追加をリクエスト」の旨を名乗る
not ok 8 - 完了条件4 要望ボタンの近傍に、登録が不要である旨の注記が出ている
not ok 9 - 完了条件4 en の製品一覧にも要望ボタンが2つ出る
not ok 10 - 完了条件5 アンケートとメールの段が、初期状態では表示されない
not ok 11 - 完了条件5 段の順序が アンケート → メール → 支援 である
not ok 12 - 完了条件5 支援の案内の段が、初期状態では表示されない
not ok 13 - 完了条件5 製品一覧のメール入力欄は、段の中にあり初期状態では隠れている
not ok 14 - 完了条件5 要望ボタン自身は初期状態から押せる
not ok 15 - 完了条件6 要望ボタンの周辺に押下数が描画されていない
ok 16 - 完了条件6 押下数を差し込むための文言が locale に存在しない
# tests 16
# pass 1
# fail 15
```

落ちている理由（抜粋。いずれも「未実装だから」であって、テストの書き損じではない）:

```
error: 'lp.hero.cardTitle に英字の Sample が残っている: {nutrient} 1{unit}あたり最安ランキング（Sample）'
error: 'lp.hero.cardTitle に (Sample) が残っている: Lowest price per {unit} ({nutrient}) (Sample)'
error: '待機リストの案内が製品リストより後ろに出力されている'
error: '要望ボタン（data-request-cta）が2つでない: 0個'
error: 'data-request-step を持つ要素が製品一覧ページにも LP にも無い。段階（アンケート / メール / 支援）を機械可読な目印付きで出力すること'
```

**16 番だけは実装前から通る。** 「押下数を差し込む文言が locale に無い」という禁止の
検査であり、実装前に通るのは正しい（禁止事項は破られていない状態が初期値）。
実装後に破られたら落ちる回帰テストとして機能する。

### 着手前の基準線（このタスクと無関係な失敗が無いこと）

```
$ npm test
# tests 356
# pass 341
# fail 15      ← 15件すべて tests/request_flow.test.js（上記）

$ node --test tests/hero_ranking.test.js
# pass 10
# fail 0
```

**申し送りの「`tests/hero_ranking.test.js` の2件は着手前から落ちている」は、
2026-09-07 のこの時点では成り立っていない**（10件すべて通る）。
基準線は「`tests/request_flow.test.js` の15件だけが落ちている」である。

## 組織への指摘
- [規約] 受け入れテストを実装より先に書く規約だが、完了条件が「要望ボタン」「段階」のような
  画面の言葉で書かれているため、機械が名指しするための目印（属性名）をテスト側が先に決めることになる。
  一方でタスクの `## 判断してよい範囲` は DOM 構造を実装の裁量としており、
  どちらが目印を決めるのかが規約からは読み取れない（今回はテスト側で決め、ファイル冒頭に明記した）
