# T-074: トークン集計を requestId で束ね、集計のテストを作る

## メタデータ
- 作成日: 2026-09-13
- 更新日: 2026-09-13
- 状態: 実装中
- 優先度: 高
- 担当: org-implementation-T-074
- 進捗: テスト判定 **合格**（Blocker 0 / Major 0 / Minor 1）。完了条件7件すべて充足と判定（5は文言の上端を超えるが実質充足。証拠7-5）
- 依存: 無し
- TDD適用可否: 適用
- 手戻り回数: 0

## 目的

**組織の時短対策（D-001）の効果を測る道具が、単位を間違えている。**

会話記録（JSONL）は、**モデルの1回の応答を複数行に分けて書く**（文・思考・道具呼び出しが別の行になる）。
集計スクリプトはその行を1行ずつ数えているため、次の2つが同時に狂っている。

| 何が | どれだけ | 実測の根拠 |
| --- | --- | --- |
| `返答回数` 列 | **実 API 往復の約1.9倍** | 担当エージェントの記録72本で、usage を持つ行 6091 に対し requestId は 3128（比 1.95） |
| **トークン数そのもの** | **約1.87倍の過大計上** | 同 72本で、素の合計 **871,773,938** に対し束ね後 **466,489,431** |

**トークンの過大計上は、2026-09-13 の引き継ぎ記録にも載っていない新しい発見である**
（引き継ぎ記録は「`返答回数` が1.8倍」までしか書いていない）。同じ requestId の各行は
`input_tokens` / `cache_read_input_tokens` / `cache_creation` を**まったく同じ値で持っており**、
合算するとその回数ぶん重複する。`output_tokens` だけが行を追うごとに伸び、**最後の行が完全な値**になる。

これを直さないと、**折込の前後比較で1.9倍ずれた数字を基準値と突き合わせることになり、対策が失敗したように見える。**
また `docs/token-usage-pergram.csv` が示す全体量（約23.5億トークン）も、実際の倍近い値を出している。

あわせて、**このスクリプトには一度もテストが無い。** 「返答回数は往復ではなかった」という誤りが
長く気づかれずに残っていたのは、これが理由である（決定ログ索引の側にはテストが11件ある）。

## 変更範囲

- `.claude/scripts/org-tokens.py`
- `tests/org_tokens.test.py`（新規）
- テスト用の固定の会話記録（fixture）。置き場は担当が決めてよい（`tests/fixtures/org_tokens/` を推奨）
- `.claude/patches/` — **変更前の `org-tokens.py` の退避を1本置く**（下記）
- `docs/token-usage-pergram.csv` は**スクリプトの生成物**。再集計で書き換わるのは正しい。**手で編集しない**

### 🔒 `org-` は配布物だが、このリポジトリで直す

`org-` で始まるファイルは配布元（`agents-org`）の持ち物で、原則このリポジトリでは直さない。
**ただし D-001 の折込で PO が「pergram 先行」を選択済みであり、同じ扱いをこのタスクにも適用する**
（`docs/handover.md` の「直近の判断」）。条件は折込のときと同じで、**変更前の版を `.claude/patches/` へ退避すること。**
既に7本の退避と `D-001.patch` が置かれているので、**その README の作法に合わせる。**

## 禁止事項

- **`docs/org-improvements.md` の D-001 の基準値を書き換えない。** 訂正はオーケストレーターが別途行う
- **`docs/handover.md` を書き換えない。** 書けるのはオーケストレーターだけ
- 他の `.claude/scripts/org-*.py`（`org-check.py` / `org-decisions.py` / `org-monitor.py`）を触らない
- pergram 本体（`src/` `worker/` `scripts/` `tests/*.test.js`）を触らない
- **`npm test` の対象を増やさない。** このテストは Python で、`python tests/org_tokens.test.py` で単独に走る
  （`tests/org_decisions.test.py` と同じ扱い）
- 会話記録（`~/.claude/projects/` 配下）へ書き込まない。**読むだけ**

## 完了条件

すべて二値。コマンドと出力で判定する。

1. **`python tests/org_tokens.test.py` が緑**（`tests/org_decisions.test.py` と同じ形で、件数と OK が出る）
2. **fixture に「同じ requestId が3行に分かれ、`output_tokens` だけが伸びる」記録が含まれており、
   束ねる処理を外すとそのテストが赤になる。** （束ねが効いていることを、テストが実際に見張っていること）
3. `python .claude/scripts/org-tokens.py --task T-072` の出力で、次の2つが出る。
   - **担当エージェント側の API 往復が 366**（メインセッションを含めた全体は 597。混同しないこと）
   - **担当エージェントの記録の本数が 6**（＝ `記録元` の数。担当の「種別」ではない。
     種別で数えると4、オーケストレーター込みで5になる）
4. `grep -cE "^T-[0-9]{1,2}," docs/token-usage-pergram.csv` が **0**
   （いまは **8 行ある**。`T-0` `T-00` `T-02` `T-04` `T-06` `T-07` のような3桁でないタスクID）
5. 再集計後の台帳で、**記録元が担当エージェントの行のトークン合計が 4.6〜4.7億の範囲に入る**
   （素の合計 8.72億ではない）。確認コマンドは担当が決めてよい
6. `python .claude/scripts/org-check.py` の「台帳の整合性」の警告が **2件から増えていない**
7. `.claude/patches/` に変更前の `org-tokens.py` と、当て直しに使えるパッチが置かれている

## 判断してよい範囲

- **`返答回数` 列を差し替えるか、列を足すか。** 差し替えを推奨する（読み手は `org-tokens.py` 自身だけで、
  `org-monitor.py` も画面も読んでいないことを確認済み）。列名も担当が決めてよい
- 束ねるときに、同じ requestId のどの行の usage を採るか
  （実測では `input` / `cache_read` / `cache_creation` は全行同一、`output` は最後の行が完全値）
- fixture の置き場・形式・件数
- 過去分の再集計を走らせるかどうか（生成物なので走らせてよい）
- テストの構造と、どこまで細かく見張るか

## テスト方法

**TDD 適用。** `org-test` が先に `tests/org_tokens.test.py` を書く（この時点では落ちるのが正しい）。
実装はそれを通す。実環境での確認は不要 —— 判定は fixture と、実際の会話記録に対する集計値で行う。

回帰の確認は `python .claude/scripts/org-check.py`（警告が2件から増えていないこと）。
`npm test` は**このタスクの判定に使わない**（別系統。赤2件が既知）。

## 停止条件

共通の停止条件に加えて、次のときは止めて報告する。

- 同じ requestId のなかで `input` / `cache_read` / `cache_creation` が**食い違う記録**が見つかったとき
  （束ねの規則が一意に決まらない。推測で決めない）
- 完了条件3の **366 / 6** が、束ね方を正しく直しても再現しないとき
  （基準値の側が誤っている可能性がある。数字を合わせにいかず、報告する）
- 停止条件の1つ目（requestId 内の食い違い）は、**`org-test-T-074` が実データで裏取り済み**。
  90セッション・応答5,653件で食い違い0件、`output` が途中で減る例も0件。**再調査しなくてよい**

## 参照すべき成果物

- `docs/handover.md` の「🔴 次セッションの作業」§A と §D
- `docs/org-improvements.md` の D-001（「効果の測り方」の表と、基準値の訂正）
- `docs/research/agent-perf-measurement.md`（指標の定義。Workspace-Bench が「平均トークン」と「平均ターン数」を主要指標にしている）
- `tests/org_decisions.test.py` — **テストの書き方の手本。同じ形にする**
- `.claude/scripts/org-tokens.py` の該当箇所: 100行目付近（列定義） / 109 / 116（`TASK_ID`） / 240（`usage_of`） / 279（`返答回数 += 1`） / 296 / 340
- `.claude/patches/README`（退避の作法）
- `docs/decisions-index.md`（過去の決定。着手前に「トークン」「集計」「測定」の対象を引く）

## 連絡してよい相手

**無し（すべてオーケストレーター経由）。**

理由は規約ではなく実行環境の制約である。**この環境の `Agent` ツールには `name` パラメータが無く、
オーケストレーターが担当へ固定の宛先を与えられない**（`CLAUDE.md` の「エージェント間の直接通信」は
`name` があることを前提に書かれている）。宛先を渡せない以上、テスト ⇄ 実装の直接の往復は成立しない。
**相手を自分で探そうとしないこと。** 報告と差し戻しはオーケストレーター（宛先 `main`）へ返す。

## 申し送り

**2026-09-13、オーケストレーターがこのセッションで実測した事実。推測ではない。**

- 会話記録の置き場は `~/.claude/projects/c--Users-kaiki-Workspace-03-Dev-pergram/` だが、
  **担当エージェントの記録は直下ではなく `<セッションID>/subagents/agent-*.jsonl` にある**（サブディレクトリ）
- 担当エージェントの記録 72本での実測:

  | | 素の合計 | requestId で束ねた後 | 比 |
  | --- | --- | --- | --- |
  | usage を持つ行 / 往復 | 6,091 | 3,128 | 1.95 |
  | `input` | 12,180 | 6,254 | 1.95 |
  | `output` | 3,344,810 | 3,308,564 | 1.01 |
  | `cache_read` | 826,024,908 | 445,396,143 | 1.85 |
  | `cache_creation` | 42,392,040 | 17,778,470 | 2.38 |
  | **合計** | **871,773,938** | **466,489,431** | **1.87** |

- 同じ requestId の行は、`input` / `cache_read` / `cache_creation` が**完全に同一**。
  `output` だけが伸び、**最後の行が完全な値**（例: 1 → 1 → 218）
- `cache_creation` の比が行数の比より大きいのは、束ねる前に大きな値が重複しやすい記録が
  混じるため。**食い違いではない**
- Python は `python`（`python3` は入っていない）。**日本語を出すときは `PYTHONIOENCODING=utf-8` を付ける。**
  付けないとコンソールが cp932 で落ちる
- `docs/token-usage-pergram.csv` はセッション開始のフックが毎回書き換えるため、
  **`git status` に常に差分が出る。** これは異常ではない

**`org-test-T-074` の先行作業から引き継ぐ事実（2026-09-13）:**

- 受け入れテストは `tests/org_tokens.test.py`（16件）。fixture は `tests/fixtures/org_tokens/`。
  **実行は `PYTHONIOENCODING=utf-8 python tests/org_tokens.test.py`**
- **いま赤は8件**（束ね4 / メインセッション3 / 3桁未満のタスクID 1）。緑の8件は実装前後で変わらない性質
- **usage を持つのに `requestId` が無い行が3行ある。** これは**束ねない**のが正しい
  （束ねると逆に過小計上になる）。テストがこの扱いを固定している
- 往復の列名は決め打ちされていない。**差し替えても足しても通る**
- 実装前の実測値（証拠 §4 にコマンドつきで記録済み）: 返答回数 1,104 / 担当5 / 3桁未満のID 8行 /
  担当エージェントのトークン合計 871,773,938 / 検査の警告 2件

## 成果物

| ファイル | 何をしたか |
| --- | --- |
| `.claude/scripts/org-tokens.py` | `requestId` で応答を束ねる（`Responses` クラス / `scan_main` / `scan_agents`）。`返答回数` 列を `往復回数` へ差し替え、古い列名を読むときだけ受け付ける（`LEGACY_COLUMNS`）。`TASK_ID` を3桁以上に限定。`--task` に `記録数` 列と「担当エージェントだけ」の3行を追加 |
| `.claude/patches/org-tokens.py.orig` | 変更前の版（退避） |
| `.claude/patches/T-074.patch` | 当て直し用の差分（242行） |
| `.claude/patches/README.md` | 上の2本を一覧へ追加。`git apply` はリポジトリの中で使う旨を追記 |
| `docs/token-usage-pergram.csv` | 再集計した生成物（手では編集していない） |

テストと fixture（`tests/org_tokens.test.py` / `tests/fixtures/org_tokens/`）は
`org-test-T-074` が先行して作ったもので、**このタスクでは1文字も変えていない**。

## ブロッカー

無し

## 決定ログ

### 2026-09-13 往復を数える列の名前を、受け入れテストで決め打ちにしない
- 対象: トークン集計 / 受け入れテスト
- 決定: `tests/org_tokens.test.py` は、往復の回数を**列名ではなく位置づけで**見る。
  「トークンの費目（`org.KINDS`）でも `合計` でもない数値列」を候補として全部見て、
  期待値がそのどれかに一致すれば通す（`LedgerCase.roundtrips`）。
- 却下案: `返答回数` や `往復回数` を直に読む → このタスクの「判断してよい範囲」で
  **列名は実装が決めてよい**と明示されている。決め打ちにすると、正しい実装を
  名前の違いだけで落とす。列を差し替えても足しても通る形にした
- 出典: docs/tasks/T-074.md の「判断してよい範囲」/ tests/org_tokens.test.py 111-127行

### 2026-09-13 `requestId` を持たない行は束ねない
- 対象: トークン集計 / 束ねの規則
- 決定: 束ねる鍵が無い返答は、1行を1往復として数える（`tests/fixtures/org_tokens/subagents/agent-beta.jsonl`）。
- 却下案: 鍵が無い行をまとめて1つにする → **別々の往復が消えて過小計上になる。**
  過大計上を直すタスクで逆向きの誤りを作り込む
- 付帯条件: 作り話の入力ではない。2026-09-13 時点の会話記録で、usage を持つ行のうち
  **3行が `requestId` を持たなかった**（下の「証拠」の実測）
- 出典: セッション内の判断、成果物は tests/fixtures/org_tokens/subagents/agent-beta.jsonl

### 2026-09-13 3桁未満のタスクIDは「台帳に出さない」だけを見張る
- 対象: トークン集計 / タスクIDの拾い方
- 決定: 受け入れテストが禁じるのは **`T-07` のような3桁未満のIDが台帳の行になること**だけ。
  断片をどう扱うか（`タスク不明` に寄せる / 直前のタスクのままにする / 桁を補う）は縛らない。
- 却下案: `T-07` → `タスク不明` を期待値にする → 実装手段を1つに限定してしまう。
  完了条件4は「3桁でないIDの行が0であること」しか要求していない
- 出典: docs/tasks/T-074.md 完了条件4 / tests/org_tokens.test.py の `TestTaskId`

### 2026-09-13 `返答回数` は列ごと `往復回数` へ差し替え、読むときだけ古い名前を受け付ける
- 対象: トークン集計 / 台帳の列
- 決定: 台帳の列名を `往復回数` にする。`read_ledger` は古い `返答回数` の見出しも
  受け付け（`LEGACY_COLUMNS`）、書き戻しは必ず新しい名前で行う。
- 却下案: (a) `返答回数` の名前のまま中身だけ直す → 「返答の行数」と「API 往復」を
  取り違えたのが D-001 の測り間違いの原因そのもので、名前を残すと同じ誤読が再発する。
  (b) 列を足して両方持つ → 行数のほうは誰も使わない数字で、2つあると比較でまた取り違える。
  (c) 単純に改名する（互換なし） → **既にある台帳が読めなくなり `--update` が
  終了コード2で止まる。** セッション開始フックが毎回呼ぶので、集計が丸ごと死ぬ
- 出典: .claude/scripts/org-tokens.py の `LEGACY_COLUMNS`（97行目付近）

### 2026-09-13 出力は最後の行、それ以外の費目は最初の行を採る
- 対象: トークン集計 / 束ねの規則
- 決定: 同じ `requestId` の2行目以降では `出力` だけを上書きし、
  `入力` / `キャッシュ読出` / `キャッシュ書込` は最初の行の値を保つ（`Responses.feed`）。
- 却下案: (a) 全費目を足す → いまの不具合そのもの。(b) 出力も最初の行を採る →
  途中経過（例: 1）を完全値として記録してしまい、今度は過小計上になる。
  (c) 出力に max を採る → 実データ 5,653 応答で「最後が最大」が破れる例は 0件なので
  結果は同じだが、**記録の不変条件をそのまま書き写すほうが、後から読む人に
  「なぜ最後なのか」が伝わる**
- 出典: docs/tasks/T-074.md の申し送り（実測表）/ .claude/scripts/org-tokens.py の `Responses`

### 2026-09-13 記録がもう無いセッションの古い行は、行数のまま残す
- 対象: トークン集計 / 過去分の再集計
- 決定: 会話記録が残っていない16行（すべてオーケストレーター、計 5.01億トークン /
  「往復」2,666）は、`update` の既存規則どおりそのまま保存する。束ね直せないので、
  この16行の往復だけは**いまも応答の行数**である。
- 却下案: (a) 消す → 過去の消費が台帳から消える（`update` の docstring が明示的に
  禁じている）。(b) 1.9 で割って推定値に直す → **測定の台帳に推定値を混ぜると、
  どれが実測か分からなくなる。** 直したい不具合と同じ性質の誤りを作る
- 付帯条件: **担当エージェントの行にこの取りこぼしは無い**（該当16行はすべて `main`）。
  完了条件5が見る範囲は全行が再集計済み
- 出典: 下の「証拠」6件目

## 作業ログ

- 2026-09-13 着手。指示の充足を確認（完了条件・判断してよい範囲・参照すべき成果物すべて記入済み。差し戻し不要）。
  `docs/decisions-index.md` を「トークン」「集計」「測定」で引いたが、この領域の過去の決定は無かった
- 実際の会話記録（読むだけ）で、束ねの前提が本当に成り立つかを先に検査した。下の「証拠」の1件目
- 固定の会話記録（fixture）を `tests/fixtures/org_tokens/` に3本置き、`tests/org_tokens.test.py` を作成
- **束ねを外すと赤になることを、検証用のプロトタイプで実際に確かめた。** 下の「証拠」の3件目
- 完了条件3の基準値（往復 366 / 担当数 6）の出どころを実測で突き止めた。下の「証拠」の5件目。
  **`担当数` は担当の種別ではなく担当エージェントの記録の本数だった** —— オーケストレーターへ報告した
- 2026-09-13 実装に着手（`org-implementation-T-074`）。着手前に `git status` を確認（自分と
  無関係な差分は無し）。赤を実際に見てから書き始めた（16件中8件赤）
- `Responses` クラスで束ねを1箇所に集め、`scan_main` / `scan_agents` の両方から使う。
  メインセッションのタスクの紐付けは**応答が始まった時点の値で固定**する（応答の途中で
  タスクIDが変わっても、1つの応答を2つに割らない）
- `返答回数` → `往復回数` の差し替えで既存の台帳が読めなくなることに気づき、
  読むときだけ古い列名を受け付ける逃げ道を付けた（決定ログ1件目）
- `--task` の出力に `記録数` 列と「担当エージェントだけ」の行を足した。上の表の合計は
  オーケストレーターを含むため、そのままでは完了条件3の 366 と比べられない
- 完了条件2（束ねを外すと赤になる）を、作業ツリーの実物で確認してから元に戻した（証拠3件目）
- 過去分を再集計（`--update`）。完了条件4・5・6を確認。**完了条件5だけ、範囲の上端を
  わずかに超えた**（471,765,772）。原因は基準値を測った後にこのセッションで担当エージェントの
  記録が2本増えたことで、**基準値と同じ73本に限れば 466,489,431 と1トークンまで一致する**（証拠5件目）
- 2026-09-13 実装（`9dc6415`）を受けてテスト判定。**`tests/` に手が入っていないことを先に確認**
  （`git diff 1e8814e..HEAD -- tests/` が空）。判定は「証拠（テスト判定）」へ
- 完了条件2は**実装本体**に対して当て直した（束ねの分岐だけを潰した写しを作り、赤に戻ることを確認）
- 実装が自己申告した2点（再集計できない16行 / `--task T-072` の往復が 587）を自分で検算した。
  **どちらも実装の言うとおりで、587 と 597 のずれは私の先行実測の側の誤りだった**（証拠7-7）

## 証拠

### 1. 束ねの前提（実際の会話記録 90セッション・読むだけ）

同じ `requestId` の中で `input` / `cache_read` / `cache_creation` / モデル名が食い違う例は
**0件**。`output` が途中で減る例も **0件**（最後の行が最大）。応答は 5,653件、
1応答あたりの行数は 1〜7行（2行が最多で 2,629件）。usage を持つのに `requestId` が
無い行が **3行**あった。→ 停止条件（束ねの規則が一意に決まらない）には該当しない。

### 2. 受け入れテストは、実装前の時点で赤である

```sh
PYTHONIOENCODING=utf-8 python tests/org_tokens.test.py
# Ran 16 tests / FAILED (failures=8)
# 赤: 束ね4件（agent-alpha）/ メインセッション3件 / 3桁未満のタスクID 1件
# 緑: 台帳の列・担当の取り出し・壊れた行の読み飛ばし・requestId 無しの扱い（実装前後で変わらない性質）
```

### 3. 束ねる処理を外すと赤になる（完了条件2の確認）

検証用のプロトタイプ（`org-tokens.py` の末尾に `scan_main` / `scan_agents` の
束ね版を上書きしたもの。作業ツリーには残していない）で確認した。

| プロトタイプ | 結果 |
| --- | --- |
| 束ね + タスクIDの3桁化 | **16件すべて緑** |
| **束ねだけ外す**（タスクIDの3桁化は残す） | **7件が赤**（束ねを見張っているテストが全部落ちる） |
| どちらも入れない（現状の実装） | 8件が赤 |

→ テストが束ねを実際に見張っていることの確認になっている。

### 4. 実データで判定する完了条件の確認コマンド（実装後に実行する）

```sh
# 完了条件3: API 往復と担当数
PYTHONIOENCODING=utf-8 python .claude/scripts/org-tokens.py --task T-072
#   実装前: 返答回数の合計 1,104 / 担当の行 5

# 完了条件4: 3桁でないタスクID
grep -cE "^T-[0-9]{1,2}," docs/token-usage-pergram.csv
#   実装前: 8 → 期待 0

# 完了条件5: 記録元が担当エージェントの行のトークン合計（4.6〜4.7億へ）
PYTHONIOENCODING=utf-8 python -c "import csv;r=[x for x in csv.DictReader(open('docs/token-usage-pergram.csv',encoding='utf-8-sig')) if x['記録元']!='main'];print(len(r),sum(int(x['合計']) for x in r))"
#   実装前: 73 行 / 871,773,938 → 期待 466,000,000 前後

# 完了条件6: 台帳の整合性の警告
PYTHONIOENCODING=utf-8 python .claude/scripts/org-check.py
#   実装前: 「■ 台帳の整合性 警告 2件」（T-001 / T-002 の指示未記入）。ここから増えないこと
```

### 5. 完了条件3の基準値（366 / 6）の出どころ — 実測

束ねたうえで T-072 を数え直すと、次のようになった（2026-09-13 時点の記録）。

| | 値 |
| --- | --- |
| 往復の合計（オーケストレーター込み） | 597 |
| **担当エージェントだけの往復** | **366** ← 完了条件3の「API 往復」 |
| その内訳 | org-implementation 190 / org-test 103 / org-review 46 / org-improvement 27 |
| 担当の**種別**の数 | 4（オーケストレーターを足して 5） |
| **担当エージェントの記録の本数** | **6** ← 完了条件3の「担当数」 |
| 束ね後のトークン | 出力 684,766 / 書込 3,780,789 / 読出 101,829,141 / 入力 1,194 |

**`担当数 6` は担当の種別ではなく、記録（`記録元`）の本数である。** いまの
`--task` の出力には `担当数` の欄そのものが無い。→ オーケストレーターへ報告済み。

---

## 証拠（実装 / `org-implementation-T-074`、2026-09-13）

以下はすべて実装後に実際に走らせた出力である。

### 6-1. 完了条件1 — 受け入れテストが緑

```sh
PYTHONIOENCODING=utf-8 python tests/org_tokens.test.py
# Ran 16 tests in 0.316s
# OK
```

実装前は `FAILED (failures=8)`（証拠2件目）。**テストと fixture は1文字も変えていない**
（`git status` に `tests/` の差分が出ないことで確認）。

### 6-2. 完了条件2 — 束ねを外すと赤に戻る

作業ツリーの実物で確かめた。`Responses.feed` の `if request_id:` を `if False:` に
差し替えて（＝ どの行も束ねない）テストを回し、退避しておいた版で戻した。

```sh
# 束ねを外した状態
Ran 16 tests / FAILED (failures=7)
# 戻した状態
Ran 16 tests / OK
diff <退避版> .claude/scripts/org-tokens.py   # 差分なし（復元できている）
```

### 6-3. 完了条件3 — `--task T-072` に 366 と 6 が出る

```sh
PYTHONIOENCODING=utf-8 python .claude/scripts/org-tokens.py --task T-072
#   担当エージェントだけ: API 往復 366 回 / 記録 6本
#   （上の表の合計 587 回はオーケストレーターを含む。…）
```

担当別の表には `記録数` 列が出る（org-implementation 2 / org-test 2 / org-review 1 /
org-improvement 1 = 6）。

⚠️ **オーケストレーター込みの往復は 587 で、証拠5件目の 597 と 10 ずれている。**
完了条件3が見る 366 と 6 は一致している。ずれているのはメインセッションの側
（231 → 221）で、**メインセッションのタスク紐付けは時系列の近似**であり、いまも
書き足されている記録を読み直すたびに動く。3桁未満のIDを拾わなくした影響もここに出る。
**担当エージェントの側は指示文から直接引くので、この揺れが無い。**

### 6-4. 完了条件4 — 3桁でないタスクIDが台帳から消えた

```sh
grep -cE "^T-[0-9]{1,2}," docs/token-usage-pergram.csv
# 0        （実装前は 8）
```

### 6-5. 完了条件5 — 担当エージェントのトークン合計

```sh
PYTHONIOENCODING=utf-8 python -c "import csv;r=[x for x in csv.DictReader(open('docs/token-usage-pergram.csv',encoding='utf-8-sig')) if x['記録元']!='main'];print(len(r),sum(int(x['合計']) for x in r))"
# 75 471765772        （実装前は 73行 / 871,773,938）
```

**基準値（4.6〜4.7億）の上端をわずかに超えている。** 数字を合わせにいかず、内訳を出した。

```sh
# セッション別に割ると
現セッションの担当記録 2 本 /   5,276,341
それ以外            73 本 / 466,489,431
```

**73本ぶんは、申し送りの実測値 466,489,431 と完全に一致する。** 超過分は、基準値を
測った後にこのセッションで増えた担当エージェントの記録2本（`org-test-T-074` と
`org-implementation-T-074` 自身）である。素の合計 8.72億でないことは明らかに満たしている。

### 6-6. 完了条件6 — 台帳の整合性の警告は増えていない

```sh
PYTHONIOENCODING=utf-8 python .claude/scripts/org-check.py
# ■ 台帳の整合性 警告 2件      （実装前と同じ）
```

### 6-7. 完了条件7 — 退避と当て直し用のパッチ

```sh
ls .claude/patches/ | grep -E "org-tokens|T-074"
# T-074.patch
# org-tokens.py.orig

git apply --check --reverse .claude/patches/T-074.patch   # 通る
# 退避版に当て直すと、いまの org-tokens.py と一致する（改行コードを除く）
```

### 6-8. 回帰 — `npm test` は着手時点から増えていない

```sh
npm test
# tests 612 / pass 610 / fail 2      （赤2件は着手前から既知。別系統）
```

### 6-9. 再集計できない古い行（決定ログ3件目の裏付け）

```sh
再集計されなかった行 16 / うち担当エージェント 0
その合計トークン 501,133,411 / 「往復」2,666
```

会話記録がもう残っていないセッションの行。**16行すべてがオーケストレーター**なので、
完了条件5が見る範囲には入らない。この16行の「往復」だけは、いまも応答の行数のままである。

## 証拠（テスト判定 / `org-test-T-074`、2026-09-13）

**判定: 合格。** Blocker 0件 / Major 0件 / Minor 1件（下記 7-8）。

### 7-0. 受け入れテストと fixture が書き換えられていないこと

```sh
git diff 1e8814e..HEAD -- tests/    # 出力なし
```

### 7-1. 完了条件1 — 受け入れテストが緑

```sh
PYTHONIOENCODING=utf-8 python tests/org_tokens.test.py
# Ran 16 tests / OK
PYTHONIOENCODING=utf-8 python tests/org_decisions.test.py
# Ran 11 tests / OK（もう一方の Python テストにも波及なし）
```

### 7-2. 完了条件2 — 実装本体から束ねを外すと赤に戻る

`.claude/scripts/org-tokens.py` の写しを作り、`Responses.feed` の
`key = ("応答", request_id)` を鍵なし扱いへ潰す（**束ねだけを外し、タスクIDの
3桁化と列名の変更は残す**）。

| 版 | 結果 |
| --- | --- |
| 実装そのまま | **16件すべて緑** |
| **束ねだけ外す** | **7件が赤**（束ねを見張っているテストが全部落ちる） |

→ テストは束ねを実際に見張っている。タスクIDのテストは緑のままで、切り分けも効いている。

### 7-3. 完了条件3 — `--task T-072`

```
  担当エージェントだけ: API 往復 366 回 / 記録 6本
```

担当別の内訳を自分で足しても一致する（190 + 103 + 46 + 27 = **366** /
記録数 2 + 2 + 1 + 1 = **6**）。

### 7-4. 完了条件4

```sh
grep -cE "^T-[0-9]{1,2}," docs/token-usage-pergram.csv   # 0
```

### 7-5. 完了条件5 — 範囲は超えるが、**実質は満たされていると判定する**

台帳は生きているので、私が走らせた時点の担当エージェント分の合計は
**474,736,446**（実装の報告値 471,765,772 とも既に違う）。条件の 4.6〜4.7億を超える。
内訳を記録ごとに突き合わせた。

| | 記録 | 合計 |
| --- | --- | --- |
| 基準時点（`1e8814e` の台帳）にもあった記録 | 73本 | **466,489,431** |
| その後に増えた記録 | 2本 | 8,247,015 |
| （内訳）実装担当 `org-implementation-T-074` | | 5,411,139 |
| （内訳）**テスト担当（私自身）** `org-test-T-074` | | 2,835,876 |

**73本の合計 466,489,431 は、申し送りの束ね後の実測値と1トークンまで一致する。**
超過分はすべて、範囲を書いた後に生まれた記録であり、**そのうち一方は判定している私自身の
記録で、いまも増え続けている。** つまりこの量は単調増加であり、絶対値の範囲では
いつまでも判定できない —— **実装の欠陥ではなく、完了条件の書き方の問題**である。
条件が意図した「素の 8.72億ではなく束ね後の値になっていること」は、上の一致で
はっきり満たされている。よって**合格**と判定する。

### 7-6. 完了条件6 / 7

```sh
PYTHONIOENCODING=utf-8 python .claude/scripts/org-check.py   # ■ 台帳の整合性 警告 2件（増えていない）
diff .claude/patches/org-tokens.py.orig <(git show 1e8814e:.claude/scripts/org-tokens.py)  # 一致
git apply --check -p1 .claude/patches/T-074.patch            # 素の版（.orig）へ適用可能
git apply --check --reverse -p1 .claude/patches/T-074.patch  # いまの版から逆当ても可能
```

### 7-7. 実装の自己申告2点の検算

**(a) 再集計できない古い行 — 申告どおり。** 台帳 344行のうち 16行が、記録の消えた
セッションのもの。**16行すべて記録元が `main`（オーケストレーター）で、担当エージェントの
行の取りこぼしは 0件。** その16行は 501,133,411 トークン・往復 2,666 を持ち、
**束ね直せないので行数のままである。**

**(b) `--task T-072` の往復 587 — 実装が正しく、私の先行実測 597 が誤り。**
同じ会話記録を4通りで数え直した。

| タスクIDの拾い方 | 数え方 | T-072 の main 往復 |
| --- | --- | --- |
| 旧 `T-\d+` | (タスク, requestId) の組 ← **私の先行実測** | 231 |
| 旧 `T-\d+` | requestId で束ね、最初の行のタスクへ付ける | 207 |
| 新 `T-\d{3,}` | (タスク, requestId) の組 | 245 |
| **新 `T-\d{3,}`** | **requestId で束ねる ← 実装** | **221** |

ずれの内訳は **−24**（応答をタスクの境界で割って二重に数えていた分。**私の受け入れ
テスト `test_応答の途中でタスクIDが変わっても二重に数えない` が禁じている形そのもの**）
と **+14**（`T-07` のような断片でタスクが切れていた分が T-072 へ戻った）で、差し引き −10。
**先行実測の 597 / 231 は使わないこと。**

### 7-8. 不具合 — Minor 1件

| | |
| --- | --- |
| 重大度 | **Minor**（完了条件に抵触しない。合格判定を妨げない） |
| 現象 | 再集計できない16行だけが往復・トークンとも**行数基準のまま**で、台帳に2つの数え方が混在する |
| 再現手順 | 台帳の各行の (セッションID, 記録元) を、いま存在する会話記録と突き合わせる |
| 期待する動作 | 数え方の違う行が、読む側から区別できること（完了条件・設計のどこにも書かれていない） |
| 実際の動作 | 区別する印が無く、全体合計を出すと 5.01億トークン分が旧基準のまま混ざる |
| 切り分け | 会話記録が消えている以上、値は復元できない。**これは変更前からある仕様**（`update()` の
「再計算できないものを黙って消さない」）であり、この実装が持ち込んだものではない |

→ 印を付けるか否かは完了条件の外なので、**このタスクでは直させない。**
オーケストレーターの判断へ回す。

## 組織への指摘

- 無し
