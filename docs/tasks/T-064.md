# T-064: 既存のmedia成果物を退避し作り直しの土台を作る

## メタデータ
- 作成日: 2026-09-08
- 更新日: 2026-09-08
- 状態: 未着手
- 優先度: 高
- 担当: 未割当
- 進捗: 未着手
- 依存: 無し
- TDD適用可否: 適用外（ファイル操作と設定）
- 手戻り回数: 0

## 目的
PO 判断により、`.media-agent/x/pergram-jp/` の既存成果物は全て無効とする。戦略・ペルソナ・playbook のいずれも、価格一色の前提と誤った観測の取り方の上に積まれている。部分的に直すと**どこまでが旧前提かが分からなくなる**ので、作り直しの起点を1つに定める。

## 変更範囲
- `.media-agent/x/pergram-jp/` 配下（下記の禁止事項を除く）

## 禁止事項
- **削除しない。移動のみ。** 退避先は `.media-agent/x/pergram-jp/_archive/2026-09-08/`
- `config.yaml` の `x_reader` 節を旧値へ戻さない（`tab: top` と `max_requests_per_day: 60` は 2026-09-08 の PO 判断で入った。戻すと同じ失敗を繰り返す）
- `data/media-agent.db` を消さない（`.gitignore` 済み。観測は残す）
- `.claude/commands/` を触らない（T-063 の範囲）

## 完了条件
1. `ls .media-agent/x/pergram-jp/_archive/2026-09-08/` に `strategy.md` `rules.md` `personas/` `playbooks/` `method-log.md` が存在する
2. `ls .media-agent/x/pergram-jp/personas/ .media-agent/x/pergram-jp/playbooks/` がどちらも空を返す
3. `grep -E "tab:|max_requests_per_day" .media-agent/x/pergram-jp/config.yaml` が `top` と `60` を返す
4. `media-agent doctor --account x/pergram-jp` が、成果物が空であること以外の新しい失敗を出さない
5. 退避したものの一覧と、なぜ無効としたかが `.media-agent/x/pergram-jp/REBUILD.md` に1件1行で書かれている

## 判断してよい範囲
- 退避先ディレクトリの内部構成
- `config.yaml` の `content.personas.schedule` と `content.playbooks.active` を、参照先が空になることへどう合わせるか（コメントアウトか空値か）

## テスト方法
上記コマンドの出力による確認のみ。実環境での確認は不要。

## 停止条件
- **削除（`rm`）が必要になったら止めて報告する。** 移動で済まない事情が出た時点で想定外である
- `media-agent` が退避後に起動しなくなった場合、戻さずに報告する

## 参照すべき成果物
- `.media-agent/x/pergram-jp/method-log.md`（退避対象。なぜ無効かの根拠がここにある）
- `.claude/rules/pergram-compliance.md`（`rules.md` を作り直すときの上位規約）

## 連絡してよい相手
無し（すべてオーケストレーター経由）

## 申し送り
無し

## 成果物
未着手

## ブロッカー
無し

## 決定ログ
特記なし

## 作業ログ
未着手

## 証拠
未着手

## 組織への指摘
無し
