# T-020: ヒーローのCTAをβ版1本に絞る（PO指示の実験）

## メタデータ
- 作成日: 2026-09-02
- 更新日: 2026-09-02
- 状態: 完了
- 優先度: 高
- 担当: 未割当
- 進捗: 完了条件9件充足・main へマージ済み。レビュー工程はPO指示により未実施
- 依存: 無し（T-010 は完了済み。本タスクはその一部を上書きする）
- TDD適用可否: 適用外（PO が見た目の効果を試す実験であり、事前にテストへ変換できる完了条件が
  「何が表示されるか」に限られる。順序は 実装 → レビュー）
- 手戻り回数: 0

## 目的

**PO の直接指示による、ヒーローの見せ方の実験である。** 3つある。

1. **ヒーローのβ版ボタンの文言が、PO の提示したものと違う。**
   現在 `lp.hero.beta` = `{nutrient}の単価ランキングを見る（ベータ版）`（描画すると
   「タンパク質の単価ランキングを見る（ベータ版）」）。
   **「プロテインの最安ランキングを見る（β版）」にする。**
2. **ヒーローの「Waitlistに登録」ボタンを非表示にする。**
3. **ヒーローのβ版ボタンだけ、色のトーンを変えて目立たせる。**

**これは T-010（CTA導線を待機リストに一本化する）の判断を、ヒーローに限って上書きする。**
T-010 はヒーローで待機リストを主・β版を従にしたが、PO は**ヒーローではβ版だけを見せる**形を試す。
**ヘッダの2つのCTAは触らない。** 待機リストへの導線はヘッダとページ下部のフォームに残る。

## 変更範囲

| ファイル | 変更内容 |
|---|---|
| `src/templates/lp/hero.js` | ヒーローの待機リストボタンを出さない。β版ボタンのクラス |
| `locales/ja.json` / `locales/en.json` | `lp.hero.beta` の文言。ja / en の両方 |
| `src/styles/lp.css` または `src/styles/tokens.css` | β版ボタンの色のトーン（🔒 色の出所は `tokens.css`） |
| `tests/lp_cta.test.js` | **本タスクが上書きした4件のアサーションの更新**（下記） |
| `tests/render.test.js` | 同上（431〜442行） |

## 禁止事項

- 🔒 **ヘッダの2つのCTAを触らない。** `header_beta` / `header_waitlist` は現状のまま
- 🔒 **`data-cta` の値そのものを変えない。** `hero_beta` は残る。`hero_waitlist` は
  **要素ごと出さなくなる**が、**値の綴りを変えたり別の値へ付け替えたりしない**
- 🔒 **ページ下部の待機リストフォームを触らない。** 登録の受け皿は消さない
- 🔒 色をコンポーネントへ直書きしない。`src/styles/tokens.css` が唯一の出所
- 🔒 タップ領域を 44px 未満にしない
- 🔒 禁止語を使わない（`おすすめ` `安心` `話題の` 等）
- 🔒 ヒーローのランキングカード（T-009 の3件）を触らない
- 🔒 LP に購入リンクを置かない、`ItemList` を付けない
- `src/build/build.js` `src/templates/lp/form.js` `worker/` を触らない（**T-011 が同時に走る**）

## 完了条件

1. ヒーローのβ版ボタンの文言が、ja で **「プロテインの最安ランキングを見る（β版）」** である
2. en にも対応する文言があり、`npm run build -- --strict` が終了コード 0（未定義キーが無い）
3. ヒーローに `data-cta="hero_waitlist"` の要素が**出力されない**
4. ヒーローの `data-cta="hero_beta"` の要素が**残っており、押せる**
5. ヘッダの `data-cta="header_beta"` と `data-cta="header_waitlist"` が**変更前と同一**である
6. ページ下部の待機リストフォーム（`<button type="submit">`）が残っている
7. ヒーローのβ版ボタンが、ヘッダのβ版ボタンとは**異なる見た目**になっている
   （クラスまたはトークンの差として検証できること）
8. `npm test` が通っている
9. 375 / 768 / 1440px で横あふれが 0px、タップ領域が 44px 以上

## 判断してよい範囲

- **β版ボタンの具体的な色。** 「目立つ」の実現方法（塗り／濃い境界／別トーンの塗り）は任せる。
  ただし `tokens.css` 経由で、既存のブランドの系統から外れないこと
- 「プロテイン」をどう出すか。**`{nutrient}` の置換では「タンパク質」にしかならない。**
  カテゴリの表示名を使うか、この文言だけ literal にするかは任せる。
  **ただしカテゴリ固有の分岐をコードに書かないこと**（🔒 不可逆な設計判断「出し分けは条件分岐ではなくデータで」）
- 非表示の実現方法（要素を出さない／条件分岐）。**ただし CSS の `display:none` で隠すのではなく、
  出力しないこと**（読み上げに残ると「見えないボタン」になる）
- en の文言

**決めてはならない**: ヘッダのCTA。フォーム。ランキングカードの中身。`data-cta` の綴り。

## テスト方法

- `npm test`
- `npm run build -- --strict`
- **375 / 768 / 1440px の実測。** Chrome を `--remote-debugging-port` で起動 → Node 22 標準の
  `WebSocket` で DevTools Protocol へ接続 → `Emulation.setDeviceMetricsOverride`
  （`width:375, height:812, deviceScaleFactor:2, mobile:true`）→ `Page.captureScreenshot`。
  **`--window-size` だけでは meta viewport が無視され、正しく測れない**

## 停止条件

- 共通の停止条件に加えて:
- 待機リストへの導線が、ヘッダとページ下部フォームの**両方とも**失われる形になった場合
  （登録の受け皿が消えるため）

## 参照すべき成果物

| パス | 何を見るか |
|---|---|
| `src/templates/lp/hero.js` | ヒーローとヘッダのボタン描画 |
| `locales/ja.json` の `lp.hero.beta` / `lp.hero.cardTitle` | 現行文言。カードは「（Sample）」付き |
| `src/styles/lp.css` の `btn--signal` / `btn--quiet` | 既存のボタンの見た目 |
| `src/styles/tokens.css` | 色の唯一の出所 |
| `docs/tasks/T-010.md` の `## 決定ログ` と `## 申し送り` | 上書きする判断の元の理由 |
| `tests/lp_cta.test.js` / `tests/render.test.js:431-442` | 上書き対象のアサーション |
| `.claude/rules/pergram-ui-copy.md` | コピー規則 |

## 連絡してよい相手
無し（すべてオーケストレーター経由）

## 申し送り

### テストのアサーションを更新してよい（例外的な許可）

**通常、実装担当は `tests/` を触らない。本タスクは例外である。**

T-010 で固定した「ヒーローの主CTAは待機リスト」という不変条件を、**PO が明示的に覆した**。
そのため次の箇所は失効している。**更新すること。**

- `tests/lp_cta.test.js` — ヒーローの `btn--signal` が待機リストに付くことを要求する4件
- `tests/render.test.js` 431〜442行 — 同じ不変条件を二重に固定している箇所

**更新の作法は3つ。**

1. **アサーションを削除して弱めない。** 「ヒーローに待機リストボタンが**無い**こと」と
   「β版ボタンが**ある**こと」を、新しい不変条件として固定し直す
2. **コメントに、いつ・誰が・なぜ覆したかを書く。** 現在のコメントは T-010 の理由を説明しており、
   そのままだと次に読む担当が混乱する。**「2026-09-02 に PO の指示でヒーローはβ版1本にした」**と、
   ヘッダとフォームに待機リスト導線が残っていることを書く
3. **2ファイルが同じ不変条件を持っていることを、双方のコメントに書く**
   （T-010 のレビュー R-010-4 の指摘。片方だけ直すと必ずもう片方で落ちる）

### 記録しておくべき副作用

- **`hero_waitlist` の GA4 イベントは、これ以降ゼロになる。** 要素が出なくなるためである。
  **これは不具合ではなく、この実験の当然の結果である。** `## 決定ログ` に明記すること。
  変更前後で `cta_click` の内訳を比べるときに、ここを知らないと「計測が壊れた」と誤読される
- 本タスクは T-010 の完了条件1（「主CTAが待機リストであることが一目で分かる」）を
  **ヒーローに限って無効化する。** ヘッダでは引き続き待機リストが主である

### PO へ伝えてある懸念（判断済み・作業は止めない）

β版の遷移先 `/ja/protein/` は、楽天以外の価格欄にプレースホルダを出しており、
robots.txt でも塞いである。**T-010 が待機リストを主にした理由がこれだった。**
ヒーローをβ版1本にすると、その面へ人を送ることになる。
**PO はこれを承知のうえで実験を指示している。作業を止めないこと。**

### 同時に走っているもの

- **T-011（待機リストフォームの2段階化）が並行する。**
  `src/templates/lp/form.js` / `src/assets/lp.js` / `worker/` / `tests/worker.test.js` は
  **T-011 の持ち物である。触らないこと。**
- `locales/*.json` は**両方が触る。** 自分が足す・変えるキーだけを操作し、
  他のキーの行を並べ替えたり整形したりしないこと

## 成果物

| パス | 変更内容 |
|---|---|
| `src/templates/lp/hero.js` | ヒーローの待機リストボタン（`hero_waitlist`）とその注記（`hero__note`）を**出力しない**。β版ボタンを `btn--quiet` → `btn--dark` へ |
| `locales/ja.json` | `lp.hero.beta` = 「プロテインの最安ランキングを見る（β版）」（1行のみ） |
| `locales/en.json` | `lp.hero.beta` = "See the lowest-price protein ranking (beta)"（1行のみ） |
| `src/styles/lp.css` | `.hero__beta` の余白とコメント。死んだ `.hero__waitlist` / `.hero__note` を削除 |
| `tests/lp_cta.test.js` | 不変条件を張り直し（弱めていない。下の「証拠」参照） |
| `tests/render.test.js` | 同じ不変条件を持つ箇所を同時に更新 |

## ブロッカー

無し。**2026-09-02 の事後レビューで出た Blocker（R-020-1）は、Q-005 の PO 回答により解消。**
Major 3件はすべて他タスク（T-015 / T-017 / T-018 / T-019）へ回付済み。

## レビュー結果（2026-09-02、事後）

**判定: 差し戻し（Blocker 1 / Major 3 / Minor 6）。**
**完了条件9件はすべて充足しており、禁止事項の違反は1件も無い。**
差し戻しの理由は実装の欠陥ではなく、**PO 指定文言のコンプライアンス上の問題**と、
**下流タスクへの未申告の副作用**である。

| ID | 重大度 | 内容 | 回付先 |
|---|---|---|---|
| R-020-1 | **Blocker** | 「最安」の最上級表示から比較基準（1gあたり）と調査範囲が落ちている。景表法の有利誤認のリスク。同じヒーロー内のカードは「1{unit}あたり最安ランキング（Sample）」と基準付きで、**ボタンとカードで主張の強さが食い違っている** | **PO（Q-005）**。文言は PO 指定のため実装への差し戻しでは解けない |
| R-020-2 | Major | `lp.ctaNote` が全ロケールで未描画になり、**既存の「できないことを書かない」テストの守備範囲から外れた。** T-015 の完了条件が、描画ベースのままでは達成不能（旧文言を戻しても落ちない） | **T-015**（完了条件を差し替え済み） |
| R-020-3 | Major | T-017 / T-018 / T-019 の指示欄が陳腐化。**T-018 完了条件1 の対象要素が存在せず達成不能**、T-019 完了条件6 の件数が古い、T-019 完了条件5 は T-020 が充足済み、禁止事項「CTA の主従を変えない」がヒーローについて失効 | **T-017 / T-018 / T-019**（すべて更新済み） |
| R-020-4 | Major | 375px でヒーローの唯一の CTA がファーストビュー外にある可能性が高く、**実験の効果を測れない**（測れるのが目立たせ方ではなくスクロール率になる） | **T-018**（申し送り済み） |
| R-020-5 | Minor | `tests/render.test.js:461` の判定が文言文字列 `β版` に依存。文言が変わると**黙って素通りする** | T-019（報告のみ・範囲外） |
| R-020-6 | Minor | `tests/lp_cta.test.js` の「異なる面」判定が `btn--quiet` でないことしか禁じておらず、`btn--subtle` を当てても通る | 未回付 |
| R-020-7 | Minor | 同一不変条件の2ファイル重複そのものは未解消（相互参照コメントどまり） | T-019（報告のみ・範囲外） |
| R-020-8 | Minor | `src/styles/lp.css:123-128` の `.hero__beta { margin-top: 0 }` は実効の無い宣言。次の担当が「効いている」と誤読しうる | 未回付 |
| R-020-9 | Minor | `betaPath` が null のとき `<div class="hero__actions">` が空で出力される。実ビルドでは到達しないが `renderLp({ betaPath: null })` では発生 | 未回付 |
| R-020-10 | Minor | `lp.cta` / `lp.ctaNote` が未参照キーとして残る。未使用キーの検出手段が無い | T-015 と同根 |

**レビューが明示的に「妥当」と評価した点**（撤回すべき指摘は無い）:

- **注記（`lp.ctaNote`）も出さないという実装担当の独自判断は妥当。** ボタンが消えた状態で
  注記だけ残すと「ランキングを見る」という遷移の説明として読まれ、コピー規則
  「ボタンは操作の結果をそのまま名乗る」に**正面から反する**。**注記だけ残すほうが規約違反になる**
- **失効した回帰テストの張り直しでアサーションは弱められていない。むしろ純増している。**
  `RETIRED_CTAS` を新設し「要素が出ないこと」と「綴りを他へ付け替えないこと」を積極的に固定しており、
  削除による沈黙を作っていない

**レビューが既存事象として挙げ、指摘に数えなかったもの**（回付のみ）:

1. `.claude/rules/pergram-ui-copy.md:54-55` の色が実装と食い違う（規約は `--signal` = 青 / `--verified` = 緑と書くが、`tokens.css` は**どちらも `#ea580c`**）→ 未起票
2. `btn--signal` の 3.56:1（既知）。**T-020 でヒーローから消えた結果、ファーストビューに残る唯一の待機リスト導線がその `btn--signal` になったため、T-017 の重要度はむしろ上がった** → T-017
3. `.btn--dark:hover { background: #000 }` は色の直書き（`tokens.css` 由来でない）→ 未起票
4. `.btn--dark` が `lp.css:72` と `products.css:119` の2箇所で定義（DRY 違反）→ 未起票
5. `tests/render.test.js:456` のクラス文字列完全一致は **T-011 で高確率で落ちる** → T-011 へ申し送り済み
6. `src/styles/lp.css` が 1000 行超（共通規約の上限 800 行超過）→ 未起票

## 決定ログ

### 2026-09-02 ヒーローの CTA は「最安」の基準を添えずに現行文言を維持する
- 対象: LP / ヒーローの CTA 文言
- 決定: **PO の判断により、現行文言を維持する。** ja「プロテインの最安ランキングを見る（β版）」/
  en "See the lowest-price protein ranking (beta)"（`locales/ja.json:141` / `locales/en.json:141`）。
  比較基準（1gあたり）を文言へ戻さない
- 却下案:
  - **A案（比較基準を戻す。例「1gあたりの最安ランキングを見る（β版）」）** → 事後レビューと
    オーケストレーターの推奨案だった。カード（「1{unit}あたり最安ランキング（Sample）」）・
    `lp.metaTitle`（「成分1gあたり最安値」）・コピー規則「数値には必ず単位と基準を添える」の
    すべてと整合する。**PO は訴求の強さを優先して採らなかった**
  - **B案（調査範囲を添える。例「楽天市場の価格で比較（β版）」）** → 1gあたりという
    pergram の中核が伝わらない
- 付帯条件 — **🔒 この決定に伴って残るリスクを、事実として記録する。次にこの文言を触る担当は
  これを読んだうえで判断すること:**
  1. **景表法の最上級表示（最安）は合理的根拠と調査範囲・時点の明示を要するが、
     その根拠資料は未整備である。** 実装上、ランキングを並べているのは
     「有効成分1単位あたりの価格の昇順」であり、絶対価格の安さではない。
     **「最安」と「1gあたり最安」は別の概念である**
  2. **遷移先の `/ja/protein/` は、楽天市場以外の価格欄がプレースホルダである**
     （`products.betaNoData`）。この面へ広告流入を送っている
  3. **ヒーロー内でボタンとカードの主張の強さが食い違う。** カードは基準と
     「（Sample）」の但し書きを持ち、ボタンは持たない
  4. `.claude/rules/pergram-ui-copy.md` の「数値には必ず単位と基準を添える」
     「事実のみを書き、優劣を述べない」に対する**明示的な例外**である。
     **禁止語リストには該当しない**（問題は語彙ではなく基準の欠落）
  5. `CLAUDE.md`「迷ったらより保守的な方を選ぶ」に対する例外でもある。
     **PO が承知のうえで選んだ**
- 出典: `docs/po-queue.md` Q-005（PO の回答、2026-09-02）／
  T-020 事後レビュー R-020-1（`docs/tasks/T-020.md` の `## レビュー結果`）

### 2026-09-02 ヒーローのCTAをβ版1本にする（PO指示）
- 対象: LP / ヒーロー / CTA
- 決定: ヒーローから待機リストボタンを外し、β版ボタンだけを残して色のトーンで目立たせる。
  文言は「プロテインの最安ランキングを見る（β版）」
- 却下案: T-010 の形（ヒーローで待機リストを主・β版を従）を維持する案
  → PO が実験として別の形を試すと判断した
- 付帯条件: ヘッダの2つのCTAとページ下部のフォームは触らない。待機リストへの導線は残る。
  `hero_waitlist` の GA4 イベントはゼロになる
- 上書き対象: T-010 2026-09-02（主CTAを待機リスト側に寄せる決定のうち、**ヒーローに関する部分のみ**。
  ヘッダについては T-010 の決定が生きている）
- 出典: セッション内の PO 判断、成果物なし

### 2026-09-02 `hero_waitlist` の GA4 イベントはこれ以降ゼロになる
- 対象: LP / ヒーロー / CTA / GA4
- 決定: ヒーローの待機リストボタンを**要素ごと出力しない**ので、`cta_click` の内訳から
  `hero_waitlist` が消える。**これは不具合ではなく、この実験の当然の結果である。**
  変更前後で `cta_click` を比べるときは、`hero_waitlist` のゼロを「計測が壊れた」と読まないこと。
  待機リストの登録数を見るなら `header_waitlist` とフォームの送信を見る
- 却下案: `hidden` や `display:none` で残してイベントの系列を途切れさせない案
  → 読み上げには残るので「見えないボタン」になる。アクセシビリティを壊すと判断して採らない
- 付帯条件: `hero_waitlist` の**綴りは予約したままにする**。他の要素へ付け替えない
  （付け替えると過去の GA4 の履歴と突き合わせられなくなる）。`tests/lp_cta.test.js` の
  `RETIRED_CTAS` で固定してある
- 出典: セッション内の判断 / `tests/lp_cta.test.js`

### 2026-09-02 ヒーローのボタンの下の注記も一緒に出さない
- 対象: LP / ヒーロー / コピー
- 決定: 待機リストボタンの真下にあった注記（`lp.ctaNote` =
  「登録は無料。メールアドレスだけで、数分で完了します。」）もヒーローから出さない
- 却下案: 注記だけ残す案 → 「プロテインの最安ランキングを見る」の真下に「登録は無料」が残ると、
  ランキングを見ることが登録であるかのように読める。事実と違う案内になるので採らない
- 付帯条件: `lp.cta` / `lp.ctaNote` の翻訳キーは**削除していない**（未使用のまま残してある）。
  実験を戻すときに文言を書き直さずに済む。T-011 が同じ `locales/*.json` を触っているためでもある
- 出典: セッション内の判断

### 2026-09-02 「プロテイン」は locales 側で literal にする（コードに分岐を書かない）
- 対象: LP / ヒーロー / 文言 / i18n
- 決定: `lp.hero.beta` から `{nutrient}` を外し、「プロテイン」を文言に直接書く。
  `hero.js` 側は `t('lp.hero.beta', { nutrient: nutrientName })` の**引数を残したまま**にする
  （余分な引数は無視されるので壊れないし、`{nutrient}` を含む文言へ戻せば従来どおり置換される）
- 却下案: (a) `{nutrient}` のまま使う → `nutrientI18n` の名前は「タンパク質」で、PO の指定した
  「プロテイン」にならない /
  (b) ヒーローだけ別の表示名を `config/categories.json` から渡す → `build.js` を経由する必要があり、
  `build.js` は T-011 の持ち物で本タスクの禁止事項だった /
  (c) `hero.js` にカテゴリ別の分岐を書く → 不可逆な設計判断「出し分けは条件分岐ではなくデータで」に反する
- 付帯条件: **カテゴリを protein 以外へ広げるときにこの1行が残る。** その時点で
  カテゴリ別のキー（`config/categories.json` の `explainerKey` と同じやり方）へ移すこと。
  コードに分岐を足さない
- 出典: `src/templates/lp/hero.js` のコメント / `locales/ja.json`

### 2026-09-02 ヒーローのβ版ボタンの面は `btn--dark`（signal は使わない）
- 対象: LP / ヒーロー / CTA / デザイントークン
- 決定: 既存の `btn--dark`（`--ink` #0f172a の塗り + `--paper` の文字）を当てる。
  ヘッダのβ版は `btn--quiet`（透明の面 + 細い罫線）のままなので、クラスの差として検証できる
- 却下案: (a) `btn--signal`（オレンジ）を当てる → signal は待機リストの色であり、ヘッダの
  `header_waitlist` と同じ面になってファーストビューにオレンジのボタンが2つ並ぶ /
  (b) 新しい色トークンを足す → `tokens.css` に「色を増やさない」と明記されている
- 付帯条件: 色は `tokens.css` 経由のまま。コンポーネントに直書きしていない
- 出典: `src/styles/lp.css` `.btn--dark` / 実測値は下の ## 証拠

## 作業ログ
- 2026-09-02: PO の指示を受けてオーケストレーターが起票
- 2026-09-02: 先にテストを新しい不変条件へ書き換え、落ちること（7件）を確認してから実装した
- 2026-09-02: `locales/*.json`（1行ずつ）→ `hero.js` → `lp.css` の順で変更。`npm test` 295/295、
  `npm run build -- --strict` 終了コード 0
- 2026-09-02: Chrome を CDP で操作して 375 / 768 / 1440px を実測（`Emulation.setDeviceMetricsOverride`）

## 証拠

### テスト（赤 → 緑）

実装前にテストだけを新しい不変条件へ書き換えた時点（赤）:

```
$ npm test
# tests 295 / # pass 288 / # fail 7
not ok 131 - 🔒 data-cta の値が変更前と同一である（GA4 の cta_click の比較可能性）
not ok 133 - 🔒 ヒーローに hero_waitlist の要素が出力されない（CSS で隠すのではなく出さない）
not ok 137 - ヒーローのCTAはβ版の1つだけである
not ok 138 - ヒーローのβ版ボタンが、ヘッダのβ版ボタンとは異なる面（クラス）である
not ok 139 - ヒーローに btn--signal は無い（待機リストのボタンが無いため）
not ok 141 - ヒーローのβ版ボタンの文言が PO の指定どおりである（ja）
not ok 217 - ベータ版への導線はヘッダとヒーローの両方に出る
```

実装後（緑）:

```
$ npm test
# tests 295 / # pass 295 / # fail 0
$ npm run build -- --strict
終了コード 0（LP /ja/ と製品一覧 /ja/protein/ を出力）
```

### 出力 HTML（`dist/ja/index.html`）

```
$ grep -c 'data-cta="hero_waitlist"' dist/ja/index.html   → 0   （完了条件3）
$ grep -c 'data-cta="hero_beta"'     dist/ja/index.html   → 1   （完了条件4）
$ grep -c 'プロテインの最安ランキングを見る（β版）'      → 1   （完了条件1）
$ grep -c '<button type="submit"'    dist/ja/index.html   → 1   （完了条件6）
```

ヘッダ（完了条件5。`siteHeader()` は `git diff` 上で差分なし）:

```
53: <a class="btn btn--quiet"  href="/ja/protein/" data-cta="header_beta">
57: <a class="btn btn--signal" href="#waitlist"    data-cta="header_waitlist">
```

ヒーロー:

```
72: <div class="hero__actions">
73:   <a class="btn btn--dark btn--block hero__beta" href="/ja/protein/" data-cta="hero_beta">プロテインの最安ランキングを見る（β版）</a>
74: </div>
```

### 実測（完了条件7 と 9）

Chrome を `--remote-debugging-port` で起動し、Node 22 標準の `WebSocket` で DevTools Protocol へ接続。
`Emulation.setDeviceMetricsOverride`（375 は `deviceScaleFactor:2, mobile:true`）を送ってから測定。
`--window-size` だけでは meta viewport が無視される（実際、最初の試行で `innerWidth` が 981 になった）。

| 幅 | innerWidth | 横あふれ | 44px 未満のボタン | ヒーローβ版 | ヘッダβ版 |
|---|---|---|---|---|---|
| 375 | 375 | **0px** | なし | 343×55px `rgb(15,23,42)` 塗り | 90×44px 透明＋罫線 |
| 768 | 768 | **0px** | なし | 736×55px `rgb(15,23,42)` 塗り | 90×44px 透明＋罫線 |
| 1440 | 1440 | **0px** | なし | 349×55px `rgb(15,23,42)` 塗り | 142×44px 透明＋罫線 |

- どの幅でも `[data-cta="hero_waitlist"]` は DOM に存在しない（`heroWaitlistPresent: false`）
- どの幅でもページ下部の `button[type="submit"]` は 1 件存在する
- ヒーローβ版のコントラスト: `#0f172a` の地に `#fafaf9` の文字（WCAG AA を十分に満たす）
- スクリーンショットは scratchpad に保存（`lp-375.png` / `lp-768.png` / `lp-1440.png` と、
  β版ボタンを中央に寄せた `cta-*.png`）。リポジトリには入れていない

### 自分では確認できていないこと

- **GA4 の `cta_click` が実際にどう変わるかは未検証。** 本番に出してからしか見えない
- **実機での見え方は未検証。** 確かめたのはヘッドレス Chrome のエミュレーションまでである
- **「目立つ」が PO の意図どおりかは未検証。** 自動テストで固定したのは
  「ヘッダと面が違うこと」までで、色の選択そのものは PO / レビューの判断を仰ぐ

## 組織への指摘
- [道具] モバイル幅の実測に使う CDP の手順が毎回書き捨てになっており、今回もスクリプトを一から書いた
