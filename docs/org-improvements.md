# 組織改善の台帳

**書けるのはオーケストレーターだけ。** 担当エージェントは自分のタスクファイルの
`## 組織への指摘` に1行書く。**それをここへ吸い上げて起票するのがオーケストレーターの仕事である。**

担当エージェントがここへ直接書きに来ないのは、全タスクが触るファイルになると
(a) 同時に走る他タスクの書き込みが消え、(b) 並列に走らせられるタスクが無くなるためである。

**直す場所は、それを表現できる最も低い段を選ぶ**（`.claude/rules/org-improvement-feedback.md`）。

```
1. そのタスクの指示文を直す        ← いちばん安い。1タスクで閉じる
2. タスク別ファイルの雛形を直す
3. 層2の規約を直す
4. スキル（手順書）を足す・直す
5. 担当エージェントの定義を直す
6. 役を増やす・統合する            ← 1〜5 で解けないと分かったときだけ
7. 配布物そのものを直す            ← 全導入先へ波及する。いちばん高い
```

---

## 組織点検の記録

**組織点検を1回行ったら、ここへ1行足す。** 書式は次のとおり。

```
- YYYY-MM-DD 完了 N件時点 / 起票 N件 / エスカレート N件
```

`完了 N件時点` は、**その点検を行った時点で状態が `完了` になっていたタスクの数**である
（`python3 .claude/scripts/org-check.py --summary` の先頭に出る）。

セッション開始時に走る検査がこの数を読み、**「前回の組織点検から N 件完了」の1行を出す。**
次の点検の時期が、誰も覚えていなくても目に入るようにするための仕掛けである（目安は5件）。

**書けるのはオーケストレーターだけである。**

- 2026-09-08 完了 28件時点 / 起票 18件（I-007〜I-025）/ エスカレート 3件（Q-012 / Q-013 / Q-014）

---

## I-001 完了条件が二値で判定できない形で書かれる（I-016 を統合）

- 状態: **層1へ差し戻し済み**（Q-013 A案）
- 起票: 2026-09-02
- 出所: T-015 `[規約]` / T-017（テスト担当）`[規約]` / T-021 `[規約]` / T-019 `[規約]` / T-009 `[規約]` / T-010 `[規約]`×2 / T-047 `[規約]` / T-051 `[規約]` / T-057 `[規約]`
- 適用範囲: **配布物**
- 段の見立て: **2（タスク別ファイルの雛形）**

### 観測された事実（3件、いずれも別のタスク）

| タスク | 何が起きたか |
|---|---|
| **T-015** | 完了条件が「既存テストを ja / en の両方へ広げる」と**手段で**書かれていた。**そのとおり実施すると空振りのテストになる**状態だった（対象の文言が描画されなくなっており、旧文言を戻しても落ちない）。T-020 のレビュー指摘を経て条件が差し替えられて救われた |
| **T-021（T-006 由来）** | 完了条件が**生成物（`dist/`）だけ**を見ており、**配信の実態を見る条件が無かった。** 5件すべて充足していたが、本番では目的が達成されていなかった（Cloudflare が robots.txt を前置してAIクローラを全面ブロック） |
| **T-017** | 完了条件に `npm test` が通ることを置いたため、**同じ作業ブランチで並行している別タスクの未完成分で常に落ち**、このタスクの合否を自力で二値判定できなかった |

### 共通しているもの

**3件とも「何を確かめるか」ではなく「何をするか」「どこが緑か」で書かれている。**

- T-015 は**手段**を書いた（テストを広げる）
- T-021 は**代理指標**を書いた（生成物が正しい ≠ 配信が正しい）
- T-017 は**他人の状態に依存する指標**を書いた（`npm test` 全体）

T-015 の担当が的確に言語化している —— **「条件が二値の観測（旧文言を戻すと落ちる）で書かれていれば、差し替え自体が不要だった」。**

### 検討の材料

雛形（`.claude/templates/T-XXX.md`）の `## 完了条件` の案内文は現在こうなっている。

> 二値で判定できる形。コマンドと出力で確認できること。
> 「正しく動くこと」は完了条件ではない。テストへ変換できるかで判定する

**「二値で判定できる形」だけでは、上の3つの落とし方を防げていない。** 3件とも
「コマンドと出力で確認できる」形式は満たしていた。

**なお、このセッションでは T-015 / T-019 / T-022 の完了条件を
「着手時点の件数から減らさない」形へ改め、並行担当由来の失敗はファイル名を報告する
扱いへ変えた**（段1の対処）。**3回目なので段2以上が要ると見ている。**

### 4件目（同日、T-019）— 代理指標のもう一つの形

**完了条件8「375 / 768 / 1440px で横あふれが 0px」だけでは、
ヘッダが2行に折り返してヘッダ高が 72 → 116px に増える変化を検出できない**
（折り返しても横あふれは 0px のままであるため）。

T-019 の担当は**初案が実際にこの状態になり、実測で気づいた。**
条件どおりに判定していれば、**折り返したヘッダのまま合格していた。**

T-021 と同じ「代理指標」の型である —— **横あふれが 0 であること ≠ 崩れていないこと。**


### 対応（2026-09-06。組織の定義を持つリポジトリ側で直した）

**段2（タスク別ファイルの雛形）で直した。** `.claude/templates/T-XXX.md` の `## 完了条件` の案内文へ、守ること3つを実例つきで入れた——**①手段ではなく観測で書く ②範囲を限定する ③生成物ではなく届いた先で観測する。** それぞれ、この課題の出所である T-015 / T-017 / T-021 の実例をそのまま悪い例・良い例として書いてある。

**適用範囲は配布物。** すべての導入先に入る。**この雛形が効くのは、これから起票するタスクだけである**——既存のタスクファイルは書き換えていない。

**期待する効果:** `[規約]` 種別で「完了条件が二値で判定できない」という指摘が、次の組織点検で0件になる。
**悪化しうるもの:** 案内文が長くなり、雛形をコピーした直後のファイルが読みにくくなる。

### 経過

**2026-09-08（組織点検）— 対応後に起票されたタスクから、同じ型が3件出ている。**

出所へ T-009 / T-010（2件）/ T-019 / T-047 / T-051 / T-057 を追加した。**このうち
T-047・T-051（作成 2026-09-07）と T-057（作成 2026-09-08）は、雛形を直した 2026-09-06 より
後に起票されている。** 雛形（`.claude/templates/T-XXX.md`）にはこのリポジトリでも守ること3つが
入っていることを確認済みである。

| タスク | 破られた守ること |
|---|---|
| T-047 | **3（生成物ではなく届いた先で観測する）** — 「ホストを列挙して照合するテスト」で書かれ、1巡目・2巡目とも**テストは全件通るのに実環境では壊れたまま**完了報告に至った |
| T-051 | **1（観測で書く）の亜種** — 「文言が、どちらも `X`」が完全一致か部分一致か一意に定まらない |
| T-057 | **2（範囲を限定する）の裏返し** — 幅を2点書いたところ「2点だけ見ればよい」と読まれ、その間と外の幅は誰も見ていない |

### 再診断（2026-09-08。組織点検）

**判定: 効かなかった。** 適用後に起票された12タスク中**7タスクで再発**（T-047 / T-050 / T-051 /
T-053 / T-057 / T-061 / T-070）。

**原因の推定: 中身は正しい。届く経路が誤っている。** 直したのは雛形の `## 完了条件` の**HTML
コメント**である。ところが**完了条件を書くのはオーケストレーターであり、その手順書
（`.claude/skills/org-orchestrate/SKILL.md`）は今も「二値で判定できる形」の一行しか持たない。**
後発8タスクのファイルに HTML コメントの残骸は1つも無い（節を埋めた時点で消える）。
**書く側は、書く瞬間にその3つを一度も見ていない。**

**対応案（Q-013・段4・配布物・重）:** オーケストレーターの手順書へ守ること3つの**見出しだけを3行**
置き、実例は雛形を参照させる。

### 差し戻し（2026-09-08。PO 回答 Q-013 A案）

**内容は承認された。ただし `.claude/skills/org-orchestrate/SKILL.md` は `org-` 接頭辞の配布物であり、
このリポジトリでは編集しない。配布元（`agents-org`）へ差し戻す。**

**差し戻す内容:** `SKILL.md` の「3. 登録して割り当てる」の完了条件を書く箇所（81行目付近、
現在は「2. 完了条件 — 何が満たされたら完了か。**二値で判定できる形**」の一行）へ、次の3行を足す。

> ①手段ではなく観測で書く ②範囲を限定し、無関係な理由で落ちない形にする
> ③生成物ではなく届いた先で観測する。**実例は `.claude/templates/T-XXX.md` の `## 完了条件` にある**

**実例は複写せず参照にする。** 同じ内容が2箇所へ載ると、片方だけ直されて食い違う。

**配られてくるまで、オーケストレーターは起票のたびに雛形の案内文を自分で読むこと。**

**`撤回` にはしない。** 規約は「効かなかったときは `撤回` にして戻す」としているが、**戻すべき対象が
無い。** 雛形の案内文は誤っていない（新しい対応案が実例の参照先として使う）。**戻すと、次の対応が
指す先が消える。** ここは `承認待ち` へ進める。

**I-016（条件が解を構造的に排除する／停止条件が中間状態を想定していない）をここへ統合した。**
同じ節を同じ段で直すことになり、別レコードのままだと二重に手が入るためである。

**「適用済み」の判定は保留すべきである。** 直したもの（雛形の案内文）は届いているのに、
症状が止まっていない。**案内文は HTML コメントであり、節を埋めた時点で消える** ——
起票する側がその場で読む保証が無い、という経路の疑いがある。


---

## I-002 上流タスクの完了で下流の指示欄が失効するが、伝える手段が起動メッセージしかない

- 状態: **適用済み**
- 起票: 2026-09-02
- 出所: T-017（実装担当）`[規約]` / T-020 の事後レビュー R-020-3 / T-019 `[規約]` / T-050 `[規約]`
- 適用範囲: **配布物**
- 段の見立て: **未定（段1〜3のどこか）**

### 観測された事実

T-020（ヒーローの CTA をβ版1本に絞る）がマージされた結果、**未着手だった3タスクの指示欄が
同時に陳腐化した。**

| タスク | 何が失効したか |
|---|---|
| **T-018** | 完了条件1「待機リストの主CTAの上端が y<812」→ **その要素が存在しなくなり、達成不能になった** |
| **T-019** | 完了条件6「291件が全て」→ 件数が変わった。完了条件5 は T-020 が既に充足（二重作業になるところだった） |
| **T-017** | 完了条件1b の対象（`btn--quiet`）がヒーローから消えた。禁止事項「CTA の主従を変えない」が**ヒーローについてのみ失効**した |

**これらは T-020 の事後レビューが見つけた。** レビューを飛ばしていれば、
**達成不能な完了条件を持ったタスクへ担当を割り当てていた。**

T-017 の実装担当の指摘: **正しい読み替えは起動メッセージでのみ渡された。**
台帳の規約は「タスク別ファイルがそのまま作業指示」としているため、
**ファイルだけを読むと失効した禁止事項に従うことになる。**

### 検討の材料

- 台帳には `## 申し送り`（登録後に埋まる唯一の指示欄）がある。**今回もそこへ書いた。**
  ただし**書いたのは完了条件と禁止事項の本文のほうで、申し送りは補足だった** ——
  つまり運用としては機能した。問題は**気づく手段がレビューしかなかった**ことである
- 「上書き対象」の仕組みは**決定ログにはある**（`- 上書き対象: T-003 2026-05-10`）が、
  **指示欄（完了条件・禁止事項）にはない**
- 検査スクリプト（`org-check.py`）は指示欄が**空**であることは検出するが、
  **失効していること**は検出できない

### 続報（同日、T-019 の担当から）— 直し方の副作用

**オーケストレーターが指示欄を直した方法そのものにも問題があった。**

> `[規約]` 指示欄が起票後に3回書き換えられ、「⚠️ 2026-09-02 追記」と元の記述が
> 同じ節に並存している。**追記を読まずに元の行だけを読むと逆の作業をする**

**そのとおりである。** 失効した記述を残したまま「⚠️ 追記」を足す形にしたのは、
規約が「決定ログは書き換えず、上書きする新項目を追記する」としているのに
引きずられた判断だが、**決定ログ（履歴）と指示欄（作業指示）では要件が逆である** ——
履歴は過去を残すことに意味があり、**指示は今やるべきことが一意であることに意味がある。**

**この観測は I-001 とも繋がる**: 指示欄を後から直さざるを得なくなる原因の一つが、
最初に「何を確かめるか」ではなく「何をするか」で書いていることである。


### 対応（2026-09-06。組織の定義を持つリポジトリ側で直した）

**段3（オーケストレーターの手順）で直した。** 3つ入れてある。

1. **完了報告を受けたときの手順へ、番号付きで「下流タスクの完了条件と禁止事項が失効していないか見る」を追加した**（これまでは説明の節に書いてあるだけで、手順の並びに入っていなかった）
2. **見直す相手は「次の1タスク」ではなく、その変更が効く全ての未着手タスク**であることを、この課題の実例（1つのマージで3タスクが同時に失効した）とともに書いた
3. **「⚠️ 追記」で新旧を並存させることを禁じ、書き換えさせる形にした。** 決定ログ（履歴。消さない）と指示欄（作業指示。残さない）で要件が逆であることを表にした。続報で出た「追記を読まずに元の行だけを読むと逆の作業をする」への対応である

**検出は機械に持たせていない。** 指示欄が空であることは検査が言えるが、**書いてあるのに失効していること**は言えない。ここは人の目に残す。

**期待する効果:** 達成不能な完了条件を持ったタスクへ担当が割り当てられる事象が起きない。
**悪化しうるもの:** 完了報告のたびに見る対象が1つ増える。

---

## I-003 ブラウザでの目視確認の手順が、毎回書き捨てられている（I-008 と I-011 の一部を統合）

- 状態: **承認済み**（Q-014 A案 → T-071）
- 起票: 2026-09-02
- 出所: T-020 `[道具]` / T-011（テスト担当）`[道具]`×2 / T-009 `[道具]` / T-010 `[道具]` / T-053 `[道具]`×2 `[反復]` / T-057 `[道具]`×3 `[反復]` / T-061 `[道具]`×2
- 適用範囲: **このリポジトリのみ**
- 段の見立て: **4（スキルを足す）**

### 観測された事実

**同じ罠に、複数のセッション・複数の担当が繰り返し落ちている。**

| 罠 | 何が起きるか |
|---|---|
| ヘッドレス Chrome の `--window-size=375,812` でモバイル幅を見る | **端末エミュレーションを有効にしないと meta viewport が無視される。** デスクトップ幅のレイアウトが375pxの窓に切れて写るだけで、**「375px で右端が切れている」という誤った観測を生む。** 前セッションで実装担当2体が続けてここで詰まり、どちらも「375px は未検証」のまま完了報告した |
| `dist/` を `file://` で開く | **CSS を `/assets/...` の絶対パスで読むため、スタイルが当たらない。** エラーにならないまま**レイアウトの観測が誤る**（実際に `display:grid` が `block` に見えた）。ローカル HTTP 配信が要るが、**手順がリポジトリに無い** |

正しい手順（Chrome を `--remote-debugging-port` で起動 → Node 22 標準の `WebSocket` から
DevTools Protocol の `Emulation.setDeviceMetricsOverride` を叩く）は、
**毎回オーケストレーターが起動メッセージへ書き写している。** 依存パッケージの追加は不要。

### 検討の材料

- **この2つは「知らないと誤った観測をする」類であり、失敗が沈黙する。** テストが緑でも、
  目視の結論だけが間違う
- 前セッションの記録にも同じ内容がある。**セッションをまたいで再発している**
- スキル（呼ばれたときだけ読み込まれる手順書）にすれば、起動メッセージが短くなり、
  かつ担当が自分で呼べる


### 診断（2026-09-06）

**段は4（スキルを足す）で見立てどおり。ただし、作るスキルの中身はこのリポジトリ固有である**（ブラウザでの目視確認は Web の成果物にしかない）。**配布物の側では作れない。**

**配布物の側で直したのは、気づく仕組みのほうである。** オーケストレーターの手順の「層2を保守する」に、保守の引き金として **「同じ手順を、2回以上その場で書き起こした」** を足した。起動メッセージへ毎回書き写しているものがあれば、それはスキルへ出す合図である、と明示してある。

**残っているのはこのリポジトリ側の作業である** — `.claude/skills/` へ、ブラウザでの目視確認の手順（モバイル幅のエミュレーション、ローカル配信、`hidden` な要素の出し方）をスキルとして1本作る。**依存パッケージの追加は要らない**ことが観測で分かっている。

### 経過

**2026-09-08（組織点検）— 診断のまま放置され、以後3タスクで再発した。**

出所へ T-009 / T-010 / T-053 / T-057 / T-061 を追加した。**診断（2026-09-06）が「このリポジトリ側で
スキルを1本作る」と結論しているのに、作られていない。**

- **T-053** — 同じ測定台を3回書き足した（実装1往復目・レビュー・修正）
- **T-057** — 同じ道具が担当によって動いたり動かなかったりする（設計担当のシェルでは Chrome が
  終了コード 21 で落ちる）。さらに `document.fonts.ready` を待たないため文字幅が約16%細く出て、
  **その値で判断した結果、実際に一度誤った設計をした**
- **T-061** — 設計と実装が測定台を**それぞれ書いた**。設計が scratchpad に残した `cdp.js` は
  セッションの一時領域にあるだけで、次のタスクには残らない

**T-061 の担当が的確に書いている** —— 「タスクファイルの申し送りは『作り直さない』と書いているが、
**残っているのは注意書きだけで、コードはどこにも残っていない**」。

**対応案（Q-014・段4・このリポジトリのみ・中）:** `.claude/skills/browser-measure/` を新設し、**手順とスクリプト本体の両方**を置く（Chrome の起動オプション／依存なしの CDP クライアント／`Emulation.setDeviceMetricsOverride`／**`document.fonts.ready` の待機**／`.preview` のローカル HTTP 配信）。担当エージェント3体の「使うスキル」へ1行ずつ足す。**Q-011 のどの案を PO が選んでも必要になるため、Q-011 の回答を待たない。**

**I-008（検証の補助部品に置き場が無い）と、I-011 のうち「レビューが挙動の回帰を確かめる手段が無い」の1点を、ここへ統合した。** 3件とも症状が違うだけで根は1つ ——**検証用の道具に、リポジトリ内の恒久的な置き場が無い。**

### 承認と着手（2026-09-08。PO 回答 Q-014 A案）

**T-071 として起票した**（`docs/tasks/T-071.md`）。**`browser-measure` は `org-` を付けない＝
このリポジトリの持ち物**なので、ここで作ってよい。担当エージェント定義の「使うスキル」への追記は、
配布物の明示的な例外にあたる（`.claude/rules/org-improvement-feedback.md`「例外は2つだけ」）。

**放置の費用が測れる形で出た唯一の課題である。** 診断から2日で、少なくとも6回の書き起こしが
発生し、うち1回は誤った設計を生んだ。

---

## I-004 範囲外にしか修正先が無い指摘が、そのタスクの通過判定を詰ませる

- 状態: **効果確認済み**（後半「脆いテスト」は I-022 へ分割）
- 起票: 2026-09-02
- 出所: T-011（テスト担当）`[規約]`×2 / T-010 `[規約]` / T-050 `[規約]` / T-051 `[反復]`
- 適用範囲: **配布物**
- 段の見立て: **未定**

### 観測された事実

T-011 の実装担当は、`tests/render.test.js:696` の判定（支援ウィジェットが完了状態の中にあるかを
正規表現で切り出して見る）が、**連続した `</div>` を領域の終わりと誤読する**ため、
ステップ2を完了状態に置くと壊れる構造であることに気づいた。

`tests/render.test.js` は**変更範囲外**だったため、担当は**テストを触らず、製品側の
マークアップ**（成分チップの並べ枠クラスの置き場）**を変えて当たらないようにした。**
規約どおりの正しい振る舞いである。

**その結果、テスト担当が「その回避が妥当か」を独立に検証する費用が発生した** ——
正規表現に頼らず `div` の対応を数え、切り出し終端が真の構造上の終端と一致することを確かめた
（偶然通っているのではないことの確認）。

### 検討の材料

- **これは規約が正しく働いた結果であり、規約違反ではない。** 費用が発生したという事実だけが観測である
- 代替案はいずれも高い: (a) 実装に範囲外のテストを直させる → 変更範囲の意味が消える、
  (b) 検証を省く → 回避が不変条件を壊していても気づかない
- **脆いテスト（正規表現での切り出し、文言文字列への依存、クラス名の完全一致）そのものを
  減らすほうが根本に近い。** T-020 のレビューも同種の指摘を3件出している
  （R-020-5 / R-020-6 / 既存事象5）

### 対応（2026-09-06。組織の定義を持つリポジトリ側で直した）

**段3（レビューの規約）で直した。** レビュー担当の定義へ、**修正先が `## 変更範囲` の外にしか無い指摘には `[範囲外]` を付し、そのタスクの通過判定から外す**という節を足した。オーケストレーターが別タスクとして起票するまでは消えたことにならない、という条件も入れてある。

**これは、この課題の観測（回避の独立検証に費用が発生した）そのものへの対応ではない。** そちらは記録のとおり「規約が正しく働いた結果」であり、代替案はいずれも高い。**直したのは、同じ根から出ているもう一方の症状**——T-010 で観測された「範囲外にしか修正先が無い Major を挙げると、そのタスクの中では絶対に解消できないまま要修正判定になる」——である。

### 経過

**2026-09-08（組織点検）— 「脆いテスト」の側が再発した。**

出所へ T-010 / T-050 / T-051 を追加した。T-051 の担当の観測 —— LP の作り替えで既存テストが
落ちた2件が、どちらも**検査の中身ではなく「属性の並び」「CSS のクラス名」への結合**だった
（T-050 でも同種の指摘が出ている。`tests/lp_cta.test.js` の送信ボタンの正規表現）。
**構造を移す種類の変更のたびに、実装が「テストを直すか、実装を検査の書き方に合わせるか」の
判断を強いられている。**

さらに T-010 から、この課題の別の面が出ている —— **決定を覆すタスクで、その決定を固定している
既存テストを誰が更新するかが規約に無い。** 実装は `tests/` を触れず、テスト担当は実装前に着手を
終えている。**誰も更新しない窓が構造的に開いている。**

### 効果の照合（2026-09-08。組織点検）

**判定: 効いた。** `[範囲外]` は適用後の3タスクで実使用されている（`docs/tasks/T-051.md:861` / `T-057.md:301` / `T-058.md:263`）。**T-058 は範囲外 Major を2件抱えたまま「通過」しており、「直せず通過もできない」詰みが実際に解消した実例になっている。**

**後半（脆いテストそのものを減らす）は I-022 へ分割した。** 1つのレコードの中で片方が`効果確認済み`、もう片方が未着手という状態は、**未着手の側を対処済みとして読み飛ばさせる。**
 正規表現での切り出し、文言の完全一致、クラス名の完全一致に依存した判定を減らす話で、**これはこのリポジトリのテストの書き方の問題**であり、次の組織点検で改善エージェントが診断する。

---

---

## I-005 修正が既存の受け入れテストの前提を論理的に覆すとき、S-4 と「全件通過」が正面から衝突する

- 状態: **起票**
- 起票: 2026-09-08
- 出所: T-062（実装担当）`[規約]` / T-062（レビュー担当）`[規約]` — **2つの役が独立に同じ観測へ至った** / T-058 `[規約]`×2 / T-010 `[規約]`（I-018 から移設）
- 適用範囲: **配布物**
- 段の見立て: **未定**（2「タスク別ファイルの雛形」か 3「層2の規約」あたり）

### 観測された事実

T-062 は「一度送信に失敗したブラウザが永久に押下を記録しない」欠陥の修正である。
完了条件 A-1 は **「識別子はあるが受領の確認が無いブラウザは、次の押下で送り直す」**。

ところが T-051 の受け入れテスト `tests/request_unify.test.js` は、**まさにその逆**
——「localStorage に id があれば POST しない」——を固定していた。**両立しない。**

このファイルは T-062 の `## 変更範囲` に挙がっていなかった。担当は次の板挟みに置かれた。

| 選択肢 | 破るもの |
| --- | --- |
| テストを触らず止めて報告する | 完了条件 C-1（`npm test` 全件通過）に到達できない |
| テストの前提を更新する | 共通の停止条件 S-4（変更範囲を超える） |

担当は後者を選び、**決定ログ・完了報告・組織への指摘のすべてに開示した。**
レビューは差分を独立に検算し（新テスト16件を旧実装へ当て、落ちる7件が RED の出力と
番号まで一致することまで確認）**「緩めではなく追随」と判定**した。
オーケストレーターは事後に `## 変更範囲` へ追認した。

### I-004 との違い

**I-004 は「テストが脆くて当たった」場合である**（正規表現の切り出しが構造の変化を誤読する）。
回避する余地があり、実装は回避した。

**こちらは回避の余地が無い。** 旧テストが固定していた挙動そのものが、
新しい完了条件によって**誤りだと決まった**。逃げ道は「止める」か「直す」しかない。

### 検討の材料

- **起票の時点で防げた。** オーケストレーターが「この修正はどの既存テストの前提を覆すか」を
  調べずに `## 変更範囲` を書いた。`grep` 一発で分かる範囲だった
- 雛形に一行足すだけで拾える可能性がある —— `## 変更範囲` の下に
  **「この変更が前提を覆す既存テスト」**の欄を作る、など
- **S-4 の側を緩めてはならない。** 「完了条件のためなら範囲を超えてよい」と読める規約にすると、
  範囲そのものが意味を失う。**直すべきは起票の側である**
- 開示が効いた事例でもある。担当が黙って直していれば、レビューは「緩めた」と読んだ可能性が高い

---


## I-006 直接通信の4経路が、この実行環境では成立しない（宛先を付ける手段が無い）

- 状態: **起票**
- 起票: 2026-09-08
- 出所: T-070（テスト担当）`[規約]` / T-070（レビュー担当）`[規約]` — **2つの役が独立に同じ観測へ至った。加えてオーケストレーター自身が送信の失敗として踏んだ** / T-051 `[道具]`
- 適用範囲: **配布物**
- 段の見立て: **未定**（3「層2の規約」か 7「配布物そのもの」）

### 観測された事実

`CLAUDE.md`「エージェント間の直接通信」は、**担当エージェントを起こすときに `name` を付け、その名前が宛先になる**と書いている。規約はこの前提の上に、4つの経路・往復上限・「役割名だけを付けてはならない」という注意まで組み立てている。

**この実行環境の `Agent` ツールに `name` パラメータが無い。**

- オーケストレーターが規約どおり `org-test-T-070` を宛先にして送信 → **「到達できない」で失敗**
- 担当エージェント側は、そもそも誰が居るかを一覧する手段を持たない（規約が意図した設計）ため、**宛先が解決できないと経路そのものが存在しない**

### 実害が出た

T-070 で、**テスト判定とレビュー判定が正面衝突した**。

- レビュー: 送信に失敗した回答が再送されない（R-1 / Major）
- テスト: 再送は `tests/mini_dom.js` の 🔒 に反するので現状が正しい

規約が用意している「テスト ⇄ レビューの観点の受け渡し（1往復）」は、**まさにこの種の食い違いを早期に見つけるための経路**である。それが存在しなかったため、衝突はオーケストレーターの裁定まで持ち越された。**裁定の結果、テスト側が前提にしていた 🔒 の文言が規約と逆を向いていたことが判明した** —— 観点を1往復させていれば、より早く、より安く見つかった可能性が高い。

### 検討の材料

- **規約を「経路が使えるなら使う」と条件付きにするのは、安易だが弱い。** 経路が無い環境では、規約が想定した早期発見の機能そのものが失われる。「無くても回る」と書くと、失われたことに誰も気づかない
- **代替は在る。** オーケストレーターが両者の判定を突き合わせる（今回やったこと）。ただし裁定まで持ち越す分、発見が遅く、往復が増える
- **オーケストレーターは実際に `agentId` で送信できている。** 名前が無いだけで、送信の仕組み自体は動く。**問題は「担当エージェントへ、他の担当の宛先を教える手段」が無いこと**である
- 段7（配布物そのもの）へ行く前に、段3で「この環境では宛先を渡せないので、判定の突き合わせはオーケストレーターが行う」と明示する手がある。**ただしそれは機能の縮退を追認するだけで、失われたものは戻らない**

---

## I-007 見た目の不具合が、テストとレビューを通過したあとで PO の目視によって発覚する

- 状態: **未処理**
- 起票: 2026-09-08
- 更新: 2026-09-08
- 種別: `ロール` / `反復`
- 出所: T-050 / T-053（設計担当 `[ロール]`×2 `[反復]` `[道具]`×2 `[規約]`）/ T-057 `[反復]`
- 段の見立て: **未定**（4「スキル」か 6「役を増やす」）

### 観測された事実

**3回続けて、同じ経路で発覚している**（T-050 → T-053 → T-057）。判定工程（テスト・レビュー）を
どちらも通過したものが、PO が画面を見た瞬間に落ちている。

- **視覚の設計を担う役が無い。** PO が「デザイナーロールをアサインしろ」と指示したが、組織の5つの役に
  デザイナーが無く、設計の役へ兼務させた。結果として設計の役が「方式設計」と「視覚デザイン」という
  **性質の違う作業を同じタスクの中で抱えた**（引き金2）
- **判定する手段が目視しかない。** CSS の食い違い（角丸3種・背景3種）は、人が値を表に書き出すまで
  検出されなかった
- **完了条件が二値へ変換できない。** T-053 の B-3 / B-6 / B-6b は「スクリーンショットで判定」と
  指示された。**「テストへ変換できない完了条件は差し戻す」という規約と、視覚を扱うタスクの実態が
  噛み合っていない**

**I-003（測定台が残らない）と根が重なるが、同じではない。** あちらは道具が残らない話であり、
こちらは**視覚の合否を誰が・何を根拠に決めるのか**が決まっていない話である。

---

## I-008 検証の補助部品に置き場が無く、タスクごとに書き写されている

- 状態: **統合済み → I-003**
- 適用範囲: **このリポジトリのみ**
- 起票: 2026-09-08
- 更新: 2026-09-08
- 種別: `道具`
- 出所: T-011 `[道具]` / T-045 `[道具]` / T-051 `[道具]` / T-053 `[道具]`
- 段の見立て: **未定**（1「指示文」では閉じない。4「スキル」か、リポジトリ側の道具の整備）

### 観測された事実

- **`tests/mini_dom.js`** は T-011 の担当が自作した（依存パッケージ無しの方針で jsdom が入らない）。
  以後、LP のクライアント側を検証するタスクは毎回ここに当たる
- **その mini_dom が `:not()` を解釈できず、本番コードのセレクタを書き換えて回避した**（T-051）。
  **テストの道具の制約が、本番コードの書き方を決めている**
- **CSS の規則を読む処理**（セレクタの一致判定・`var()` の解決・トークンの読み出し）がテスト間で
  共有されておらず、`tests/styles.test.js` の同等の関数を新しいテストファイルへ書き写した（T-053）
- **Python スクリプト（`.claude/scripts/`）の単体テストの置き場と実行手段が無い。** `npm test` からも
  走らないため、`tests/org_decisions.test.py` を手で実行する形になった（T-045）

---

## I-009 設計成果物が実態からずれたまま残り、直す担当が決まっていない

- 状態: **未処理**
- 起票: 2026-09-08
- 更新: 2026-09-08
- 種別: `規約`
- 出所: T-053 `[規約]`×3 / T-061 `[規約]`
- 段の見立て: **未定**

### 観測された事実

- **実装が設計成果物から意図的に逸脱したとき、設計成果物そのものを実態へ直す担当が決まっていない。**
  実装は決定ログに書いて終わり、設計の担当は既に終了しており、🔒 付きの設計書が古いまま残る
  （T-053、`design.md` §5）
- **設計書が実装より先に書かれた結果、実装してみたら誤りだった記述が 🔒 付きで残った**
  （「`openStep()` は … 開き直しも壊れない」）。今回は実装担当がレビュー指摘を受けて直した
- **設計書の指示と、対象ファイルに書かれた 🔒 が正面から食い違った**（LP の余白を `:has()` へ「移す」
  vs `lp.css` へ写さない）。**設計成果物と対象ファイルの 🔒 の突き合わせは、設計の段では行われない**
- **設計成果物が指定した文言が、このリポジトリの既存の 🔒 テストに当たって落ちた**（T-061。押下数の
  表示を禁じる正規表現に「1件」が引っかかった）。**設計の段階でテストは走らないため、文言を決めた側は
  当たったことを知りえない**

---

## I-010 `npm test` 全件通過を完了条件に置くと、作業ツリーを共有する並列実行下では二値判定できない

- 状態: **未処理**
- 起票: 2026-09-08
- 更新: 2026-09-08
- 種別: `規約`
- 出所: T-014 `[規約]` / T-017 `[規約]` / T-050 `[規約]` / T-057 `[道具]`
- 段の見立て: **未定**（3「層2の規約」。**この組織は「1タスク1ブランチにはしない」を意図的に選んでいる**
  ので、並列実行の側を変える案は採れない）

### 観測された事実

| タスク | 何が起きたか |
|---|---|
| T-014 | 完了条件4「`npm test` が通っている」が、同時に走る T-009 / T-010 の受け入れテストが赤である間は**構造的に満たせない** |
| T-017 | 同じ作業ブランチで並行する T-011 が skip を外した受け入れテスト5件で常に落ち、**このタスクの合否を自力で二値判定できない** |
| T-050 | `## 申し送り` に「2件が着手前から落ちている」と書かれていたが**実際は落ちておらず**、起動時の指示で口頭に打ち消された |
| T-057 | **レビュー担当が完了条件「`npm test` 全件通過」を自分で再現できない。** 並行する別タスクの未コミット変更が混ざり、自分のタスクの回帰と切り分ける手段が「全件実行」しか無い（他タスクの worker 変更で3件落ちた） |

**I-001（完了条件が二値で判定できない）は T-017 の1件をこの型として拾っているが、原因が違う。**
I-001 は完了条件の**書き方**の問題であり、こちらは**同じ作業ツリーを共有するという構造**の問題である。
書き方を直しても、全件通過を見たい場面そのものは残る。

---

## I-011 レビュー担当の道具立てと報告の書式が、規約の課す仕事に足りていない

- 状態: **未処理**
- 起票: 2026-09-08
- 更新: 2026-09-08
- 種別: `道具` / `規約`
- 出所: T-047 `[規約]` / T-051 `[規約]` `[道具]` / T-053 `[道具]`
- 段の見立て: **未定**（5「担当エージェントの定義」の可能性が高い）

### 観測された事実

- **編集ツールが与えられていないため、規約が求める `## 組織への指摘` の自己記入ができない**（T-047）。
  完了報告経由の転記に依存しており、**オーケストレーターが転記を忘れると指摘が組織から消える**
- **レビュー対象の差分を1コマンドで取り出す手段が無い**（T-051）。変更が2タスクにまたがるとき
  `git diff main...HEAD` では無関係な過去タスクの変更 99 ファイルが混ざる。毎回コミット履歴から
  自分で組み立てている
- **挙動の回帰を確かめる手段が無い**（T-053）。テストの DOM ハーネスを scratchpad から絶対パスで
  呼び直す再現スクリプトを書き起こした。読み取り専用で既存ハーネスに1シナリオだけ流す手順が無い
- **往復2回目の「確認パス」に、1回目と同じ完了報告の書式が要求される**（T-051）。見るべきは
  「12件が解消したか」だけなのに、判定・充足確認・設計との整合性・指摘一覧・良い点を再び埋める形に
  なっており、**全文レビューと確認パスの区別がロールの定義にも報告の書式にも無い**

---

## I-012 レビューが本番反映の後に走り、門として機能しない実行順になった

- 状態: **未処理**
- 起票: 2026-09-08
- 更新: 2026-09-08
- 種別: `規約`
- 出所: T-048 `[規約]`
- 段の見立て: **未定**（1「指示文」で閉じる可能性がある）

### 観測された事実

T-048 でレビューがデプロイ後に実施され、**レビュー判定が本番反映前の門として機能しない実行順に
なった**（指摘は Minor のみで実害は出ていない）。工程の順を決めるのはオーケストレーターであり、
**標準フローの「レビューは最終工程」が、デプロイを含むタスクでどこに入るかは書かれていない。**

---

## I-013 実環境でしか現れない層を実測しに行く工程が無く、ローカルが緑のまま本番が壊れる

- 状態: **未処理**
- 起票: 2026-09-08
- 更新: 2026-09-08
- 種別: `反復`
- 出所: T-014 `[反復]` / T-023 `[反復]` / T-047 `[反復]` `[規約]` / T-048 `[反復]`
- 段の見立て: **未定**

### 観測された事実

**同じ型の手戻りが通算4回起きている**（CSP のホスト許可、T-047 で3回 → T-048 で4回目）。

- 1回目: 裸のホストがワイルドカードにマッチしない罠（`analytics.google.com` / `ad.doubleclick.net`）
- 3回目: 同じホストでもディレクティブが違えば許可が効かない罠（`googleads.g.doubleclick.net` が
  `connect-src` にはあるが `script-src` に無い）
- 4回目: 「ローカル実測は本番の全経路を再現しない」。**本番でしか現れない違反層は、誰かが手で
  実測しに行かないと発見できない**状態が続いている

**同じ形が別領域でも出ている。**

- T-023 — 同じ型の不具合（橙の上の白文字が 4.5:1 未満）が、完了条件に入っていた箇所と入っていなかった
  箇所に分かれて存在し、**後者は実環境確認まで見つからなかった**
- T-014 — 同じワークフローが、性質の違う原因（版廃止 / Referer 不一致）で2度長期停止した。どちらも
  **「リポジトリ外の設定と、リポジトリ内の想定のずれ」**だった

**I-001 の「代理指標」（生成物が正しい ≠ 届いた先が正しい）と同根であるが、I-001 の対応は雛形の
案内文であり、実測しに行く工程を誰が持つかまでは決めていない。**

---

## I-014 ツールの実行許可が、完了条件の検証そのものを阻む

- 状態: **未処理**
- 起票: 2026-09-08
- 更新: 2026-09-08
- 種別: `道具`
- 出所: T-014 `[道具]` / T-058 `[道具]`
- 段の見立て: **未定**（実行許可の設定は配布物の例外であり、このリポジトリで直してよい）

### 観測された事実

- **完了条件にあるコマンド `npm run prices:refresh` が実行許可で拒否され、完了条件2の一部を
  自分では検証できなかった**（T-014）
- **本番 D1 の状態を読むコマンド（`npx wrangler d1 execute --remote` の SELECT）が拒否され、
  コードが本番に反映されているかを確かめられない**（T-058）

**どちらも読み取りである。** 完了条件に書いたコマンドが実行できるかを、起票時に誰も確かめていない。

---

## I-015 割り当てられたスキル・道具が、このリポジトリの技術構成と前提が合わない

- 状態: **未処理**
- 起票: 2026-09-08
- 更新: 2026-09-08
- 種別: `道具`
- 出所: T-031 `[道具]` / T-044 `[道具]` / T-061 `[道具]` / T-070 `[道具]`×2
- 段の見立て: **未定**（5「担当エージェントの定義」。導入時のスキル割り当て `org-init` の対象でもある）

### 観測された事実

- **`ecc:e2e-testing` は Playwright 前提**で、このリポジトリの検証手段（`node:test` + 自作 DOM +
  `node:sqlite`）とは前提が合わず使えなかった（T-070）
- **`ecc:database-migrations` は PostgreSQL / MySQL と ORM 向け**で、Cloudflare D1（SQLite・
  `wrangler d1 execute` で手流し・移行の適用記録を持たない）とは前提が合わなかった（T-070）
- **ウェブ検索の手段（`WebSearch` / `WebFetch`）が設計エージェントの定義に無く**、一次情報の所在を
  GitHub API の総当たりで探した（T-044）
- **X の実データを設計時点で参照する手段が無く**、伝聞の2件だけを根拠に露出モデルを組んだ（T-031）
- **競合・実例のベンチマークを取る手順が組織に無く**、3回目の差し戻しで初めて PO 指示により作られた
  （T-061）

**「割り当てられているのに使えない」と「そもそも無い」が混在している。** 前者は割り当ての誤りで、
後者は不足である。**分けて診る必要がある。**

---

## I-016 完了条件・停止条件が、あり得る解や現実に起きる中間状態を想定していない

- 状態: **統合済み → I-001**
- 適用範囲: **配布物**
- 起票: 2026-09-08
- 更新: 2026-09-08
- 種別: `反復` / `規約`
- 出所: T-044 `[規約]` / T-061 `[反復]`
- 段の見立て: **未定**（2「タスク別ファイルの雛形」の可能性）

### 観測された事実

- **完了条件が構図そのものを構造的に排除しているまま設計が1本仕上がり、丸ごとやり直しになった**
  （T-061）。旧 B-1（4要素すべての左端が 1px 以内で一致）は2列と両立しないが、**それは条件を書いた側も
  設計した側も、実測して初めて分かった。** 着手前の充足確認（4項目）は「完了条件が書かれているか」
  しか見ておらず、**「完了条件が、あり得る解を意図せず排除していないか」を見る欄が無い**
- **停止条件が「骨格そのものが誤りだと判明した場合」と二値で書かれていたが、実際に出たのは
  「骨格は正しいが、その理由付けが誤っている」だった**（T-044）。**止めるか進めるかの判定に、規約からは
  答えが出ない。** 担当が自分で「進めたうえで PO 判断事項として残す」形を選んだ

**I-001（二値で判定できない）とは逆向きである。** どちらの条件も二値ではあった。
**二値であることは、条件が正しいことを保証しない。**

---

## I-017 `## 変更範囲` が「触ってよいファイル」だけで書かれ、他の指示欄と食い違う

- 状態: **未処理**
- 起票: 2026-09-08
- 更新: 2026-09-08
- 種別: `規約`
- 出所: T-009 `[規約]` / T-032 `[規約]` / T-061 `[規約]` / T-070 `[規約]`
- 段の見立て: **未定**（2「雛形」か 3「層2の規約」）

### 観測された事実

| タスク | 何が起きたか |
|---|---|
| T-009 | `config/categories.json` が `## 変更範囲` の表に載っている一方、`## 停止条件` は「変えるなら別タスク」と書いており、**同じファイルについて指示が食い違っていた**（申し送りで解消されたが、変更範囲の表自体は修正されていない） |
| T-061 | `## 変更範囲` に `src/styles/lp.css` が無いが、**完了条件 B-6 は `lp.css` を編集しないと満たせなかった。** 完了条件と変更範囲が正面から食い違っていた |
| T-070 | `## 変更範囲` に `worker/waitlist.js` が無いため、**3つの受け口で同じ検証関数を重複させる形になった。** 範囲の切り方が「触ってよいファイル」だけで示されており、**「既存ファイルへ影響を出さずに共通化する」選択肢が最初から取れない** |
| T-032 | 調査対象の実体がリポジトリ外（`~/.local/bin`）にあり、**そこを読んでよいかが `変更範囲` にも `禁止事項` にも書かれていなかった。** 読み取りのみと判断して進めたが、判断は担当が自分で行った |

**I-005（既存テストの前提を覆すとき S-4 と衝突する）と同じ根から出ている。** あちらは
「範囲に挙がっていないテスト」、こちらは「範囲の切り方そのもの」である。

---

## I-018 受け入れテストの実行順と、完了条件の判定対象が噛み合っていない

- 状態: **未処理**
- 起票: 2026-09-08
- 更新: 2026-09-08
- 種別: `規約`
- 出所: T-010 `[規約]`×2 / T-050 `[規約]` / T-051 `[規約]`×2
- 段の見立て: **未定**

### 観測された事実

- **完了条件が「`npm run build` の出力（`dist/`）に対して機械的に判定できること」と書かれているが、
  受け入れテストは実装前・ビルド前に走る**（T-051）。判定対象をテスト側で決め直すことになり、
  **同じ食い違いをテスト担当と実装担当の両方が独立に踏んだ**（描画結果では通っているのに、
  古い `dist/` のせいで3件落ちる中間状態が出た）
- **受け入れテストを先に書く工程が、既存の回帰テストが同じ挙動の逆を固定していないかを確認する
  手順を持っていない**（T-010。`tests/lp_cta.test.js` と `tests/render.test.js:436-437`）。
  実装まで矛盾が顕在化しなかった
- **決定を覆すタスクで、その決定を固定している既存テストを誰が更新するかが規約に無い**（T-010）。
  実装は `tests/` を触れず、テスト担当は実装前に着手を終えている
- **完了条件が画面の言葉（「要望ボタン」「段階」）で書かれているため、機械が名指しするための目印
  （属性名）をテスト側が先に決めることになる**（T-050）。一方でタスクの `## 判断してよい範囲` は
  DOM 構造を実装の裁量としており、**どちらが目印を決めるのかが規約から読み取れない**

---

## I-019 重大度 Blocker の判定基準が組織の側に無い

- 状態: **未処理**
- 起票: 2026-09-08
- 更新: 2026-09-08
- 種別: `規約`
- 出所: T-051 `[規約]`（レビュー担当）
- 段の見立て: **未定**

### 観測された事実

重大度 Blocker の定義に「セキュリティ上の問題」が含まれるが、**何がそれに当たるかの基準が
組織の側のどこにも書かれていない。** T-051 ではレート制限の不在（未認証で主キーを攻撃者が選べる
D1 書き込み）を Major に置いたが、Blocker とも読める。**判定がレビューのたびに変わる。**

---

## I-020 参照先として渡した成果物が、現行の規約に正面から違反していた

- 状態: **未処理**
- 起票: 2026-09-08
- 更新: 2026-09-08
- 種別: `規約`
- 出所: T-031 `[規約]`
- 段の見立て: **未定**（1「指示文」で閉じる可能性がある）

### 観測された事実

`## 参照すべき成果物` に挙げられた `X_post_strategy.md` と `X_eyecatch_image_method.md` が、
**現行の禁止語規約とブランド表記規約に正面から違反する文例を含んだまま参照先として指定されていた。**

**指示の材料そのものが検査されていない。** 担当エージェントは会話履歴を持たないため、
参照先に書かれていることを正しい前提として扱う。

---

## I-021 指示の出所がタスクファイルと起動メッセージの2箇所に分かれ、ファイルだけを読むと欠ける

- 状態: **未処理**
- 起票: 2026-09-08
- 更新: 2026-09-08
- 種別: `規約` / `道具`
- 出所: T-011 `[規約]` / T-017 `[規約]` / T-050 `[規約]` / T-051 `[道具]`
- 段の見立て: **未定**

### 観測された事実

台帳の規約は**「タスク別ファイルがそのまま作業指示である」**としている。実際には4件とも、
**判断の本文が起動メッセージ側にしか無かった。**

| タスク | 何が起きたか |
|---|---|
| T-011 | `## 判断してよい範囲` と `## 参照すべき成果物` が雛形の案内文のまま空だったが、**同じ内容が起動時のメッセージ側に書かれていた。** 指示の出所が2箇所に分かれている |
| T-017 | T-020 のマージで失効した禁止事項の**正しい読み替え2点が、起動メッセージでのみ渡された。** ファイルだけを読むと失効した禁止事項に従うことになる |
| T-050 | `## 申し送り` の記述が誤っており、**起動時の指示で口頭に打ち消された。** 申し送りは登録後に埋まる唯一の指示欄であるのに、**内容が古くなったときに誰が直すかが決まっていない** |
| T-051 | 起動メッセージが「`docs/tasks/T-051.md` の末尾に足された `## 申し送り` を読め」と指示したが、**その節はファイルに存在しなかった。** PO 判断の本文は起動メッセージ側にしか無く、**セッションが切れていればその判断は失われていた** |

**I-002（上流の完了で下流の指示欄が失効する）は、この症状の一部を既に拾っている。** ただし I-002 の
主題は失効であり、こちらは**失効していなくても指示がファイルに揃っていない**ことである。
**最後の1件（セッションが切れたら失われていた）は、失効とは無関係に起きる。**

---

## I-022 脆いテスト（正規表現・属性の並び・クラス名の完全一致）が、実装の書き方を縛る

- 状態: **未処理**
- 起票: 2026-09-08（**I-004 から分割**。分割前は I-004 の後半として同居していた）
- 更新: 2026-09-08
- 種別: `規約` / `反復`
- 出所: T-010 `[規約]` / T-011（テスト担当）`[規約]`×2 / T-050 `[規約]` / T-051 `[反復]`
- 適用範囲: **このリポジトリのみ**（テストの書き方の問題である）
- 段の見立て: **未定**

### なぜ分割したか

**I-004 は1つのレコードの中で、片方が `効果確認済み`、もう片方が未着手になっていた。**
`適用済み` と表示され続けると、**未着手のこちら側が対処済みとして読み飛ばされる。**
組織点検（2026-09-08）の指摘による。

### 観測された事実

- **LP の作り替えで既存テストが落ちた2件が、どちらも検査の中身ではなく「属性の並び」「CSS の
  クラス名」への結合だった**（T-051。T-050 でも同種 —— `tests/lp_cta.test.js` の送信ボタンの正規表現）。
  **構造を移す種類の変更のたびに、実装が「テストを直すか、実装を検査の書き方に合わせるか」の判断を
  強いられている**
- **変更範囲の外にあるテストが、出力 HTML を正規表現の完全一致で見ており、実装の変更範囲である
  マークアップの選び方をそのテストが縛った**（T-011）。落ちない構造を選ぶ形で回避したため、
  **テスト側で「その回避が妥当か」を独立に確かめる費用が発生した**
- **決定を覆すタスクで、その決定を固定している既存テストを誰が更新するかが規約に無い**（T-010）。
  実装は `tests/` を触れず、テスト担当は実装前に着手を終えている。**誰も更新しない窓が構造的に開いている**

---

## I-023 検査スクリプトが `###` の小見出しとコードブロック内の `#` で節を切る

- 状態: **層1へ差し戻し済み**
- 起票: 2026-09-08
- 更新: 2026-09-08
- 種別: `道具` / `反復`
- 出所: 組織点検（2026-09-08）の改善エージェント `[道具]` `[反復]` — **オーケストレーターが実測で反証済み**
- 適用範囲: **配布物**
- 段の見立て: **7（配布物そのもの）**

### 観測された事実

`.claude/scripts/org-check.py:223` の `section()` が、**`#` で始まる行をすべて見出しの終端として扱う。**

```python
for line in text.splitlines():
    if line.startswith("#"):
        inside = line.strip().lstrip("#").strip() == name
        continue
```

そのため `## 証拠` の直後を `### 完了条件1: …` のような**小見出しで始めると、そこで本文が切られて
「空」と判定される。** コードブロック内の `# tests 337`（`node:test` の標準出力）でも同じことが起きる。

**実測で反証した。** 「状態が『完了』なのに `## 証拠` が空」の警告15件は、**15件すべてが誤りである。**

| 例 | 実際の `## 証拠` |
|---|---|
| T-002 | 22行（`### 完了条件4: 追加キーワードのボリューム調査（達成）` で始まる） |
| T-006 | 133行 |
| T-051 | 252行 |
| T-062 | 94行 |

**誤警報率は台帳の整合性警告 21件中 15件で 71%、`証拠` に限れば 100%。**

**同じ関数を、`完了条件` / `判断してよい範囲` / `変更範囲` / `禁止事項` の未記入検査と、
`## 組織への指摘` の起票漏れ検知（`collect_feedback`）も使っている。**
→ **指摘を小見出しの下に書いたタスクは、起票漏れの検知から丸ごと消える。**

### 二次被害 — 点検の起動が遅れた

検査は毎セッション「組織点検の記録がまだ無い」を出していた。**規約の目安は完了5件だが、実際に
起動されたのは28件時点である。** その1行が、**誤警報15件と同じ画面に並んでいた。**

**「点検を促す仕組みを足す」のではなく、雑音を減らせば足りる。**

### 差し戻し（2026-09-08。PO 回答 Q-012 A案）

**このリポジトリでは直さない。配布元（`agents-org`）へ差し戻す。**
直しても次に配布物を更新した時点で消え、**直した本人以外は直されていたことすら知らない。**

**差し戻す内容:** `section()` を **(a) `##` で始まる行だけを終端とし、(b) ` ``` ` で囲まれた範囲を
見出し判定から除外する**ように直す。関数のコメントへ「小見出し（`###`）とコードブロック内の
`#` 行で節を切らない」を添える。**修正時に、空の節を1つ含むタスクで反証テストを1件書くこと**
——解析が緩くなる方向の変更なので、逆に「本当に空」を見逃す危険がある。

**確認すべき隣接箇所:** `org-decisions.py` が同じ関数を使っていないか。今回は決定ログ索引の生成が
正常に見えるため未確認である。

**配られてくるまで、警告15件は誤警報として無視してよい。**

---

## I-024 `手戻り回数` を誰がいつ加算するかが決まっておらず、停滞検知が一度も発火していない

- 状態: **層1へ差し戻し済み**
- 起票: 2026-09-08
- 更新: 2026-09-08
- 種別: `規約`
- 出所: 組織点検（2026-09-08）の改善エージェント `[規約]`
- 適用範囲: **配布物**
- 段の見立て: **2（雛形）または 3（層2の規約）**

### 観測された事実

**台帳上の手戻り回数の合計は4回（T-044 / T-051 / T-053 / T-070 が各1）。この値は事実に反する。**

- **T-061** は本文に「3回目の差し戻し」「丸ごとやり直し」と書かれているのに `手戻り回数: 0`
- **T-057** も PO 差し戻し3回目で `0`

**停滞検知のサイクル基準は「手戻り回数3以上」で発火する。誰も加算していないため、一度も発火して
いない。** 現在の「サイクル基準 0件」は、**「停滞していない」ではなく「検知が動いていない」**である。

### 原因

`docs/glossary.md:109` は**数える対象**を定義している（レビューからの修正要求／テストからの不具合
レポート／オーケストレーターによる差し戻し）。ところが `.claude/rules/org-task-ledger.md` の
**「誰が何を書くか」の表に、この項目だけが載っていない** —— 状態・担当・依存・優先度・申し送りは
オーケストレーター、決定ログ・作業ログ・証拠は担当、と割り振られているが、**手戻り回数はどちらにも
属していない。**

### なぜ重要か

**効果の照合に使える4指標のうち、2つ（手戻り回数、サイクル基準の停滞）が壊れている。**
次の点検で同じ値（手戻り4回・サイクル停滞0）が出たとき、**それを「改善した」と読んではならない。**

**次サイクルの筆頭に置く。** 今回は診断3件の枠に入らなかった。

---

## I-025 オーケストレーターにトークンが集中している（全体の66.5%）

- 状態: **未処理**
- 起票: 2026-09-08
- 更新: 2026-09-08
- 種別: `ロール`
- 出所: 組織点検（2026-09-08）の改善エージェント `[ロール]`
- 適用範囲: **未定**（診断時に判定する）
- 段の見立て: **未定**

### 観測された事実

担当別の消費で **1,270,313,639 / 1,910,630,513 = 66.5%**。実装役（306M）の **4.1倍**。
うち**タスクIDの付かない「タスク外」だけで 796,584,535 = 41.7%**（返答 4,480回）。

**起票・裁定・タスク分解・PO 応答・退避コミット・実測がすべてこの1体に集まっている。**
**今回の組織点検の結果を台帳へ反映する作業も、同じ1体に載っている。**

### まだ提案に至らない理由

**偏りは明らかだが、対応策を1つに絞れない。** どれが主因かをこの母数では切り分けられない。
**改善エージェントの提案:** 次回は点検の間隔を短くし、**2点間の差分**で見る。

---

## 過去に対処済みのもの

（まだ無い）
