# pergram デザイン要件書 (v1.0)

送付先: **Claude Design**
この1本だけで着手できるように書いてある。他のドキュメントを開く必要はない。

---

## 0. この文書の役割

**要件だけを書く。デザインの答えは書かない。**

| 誰が決めるか | 何を |
|---|---|
| **この文書** | 何を達成すべきか / 画面に何が必ず載るか / 何をしてはいけないか |
| **Claude Design** | 書体・レイアウト・型スケール・余白・動き・構図・情報の並び・トーン |

過去版（[ad-lp.md](./ad-lp.md) v0.3 / v0.4）はピクセル単位まで指定していたため、結果として**正しいが退屈な**ページになった。その反省から、本書は制約と目的だけを渡す。

**🔒 が付いた項目だけは変更不可。** 法規制・ブランド・データの誠実さに関わる。それ以外はすべて提案を歓迎する。既存の配色・書体も「たたき台」であり、要件を満たすなら変えてよい。

---

## 1. プロダクト

**pergram（パーグラム）** — サプリメントを「有効成分1単位あたりの価格」で横並びに比較する日本市場向けサイト。

サプリは剤形・含有量・内容量・価格・塩形態がバラバラで、「同じ成分を同じ量だけ摂るのにいくらかかるか」を比べる手段が実質存在しない。日本には公的なサプリラベルのデータベースもない。pergram はラベル表示の含有量を換算したうえで、単価で並べる。

### 立ち位置 🔒

**効能を提案するツールではなく、事実を計算して並べるツール。**

- 症状や悩みから商品を推薦しない
- 独自の「総合スコア」「おすすめ度」を作らない
- 出すのは、含有量・価格・公的基準値・認証という**検証可能な数値と事実のみ**

これは法務戦略そのものなので崩せない。

### 今回作るもの

**検証用ランディングページ（1枚）。** 公開前にメール登録を集め、どの成分に需要があるかを測る。

本体サービス（成分ランキング・製品詳細）は後続。ただし**同じデザインシステムで作る**ため、LP の設計はそのまま本体に引き継げる形にしてほしい。

---

## 2. 対象ユーザー

**数値を見る習慣がある層。** 価格.com や PCPartPicker を使いこなす人。

- 筋トレ目的でプロテインを**継続購入**している（＝年間コストが効く）
- セール時にまとめ買いする。個人輸入・iHerb を使う層が中核
- 「おすすめ10選」記事を信用していない
- **自分で計算したいが、面倒でやっていない**

痛みは「わからない」ではなく「**面倒くさい**」。
だから LP の仕事は教育ではなく、**「その計算、全製品ぶん終わってます」と見せること**。

---

## 3. このページが達成すべきこと

優先順。①が達成できていれば②③が弱くても合格、逆はない。

| # | 目的 | 判定 |
|---|---|---|
| ① | **「自分がいくら払っているか」に驚かせる** | スクロールせずに驚きが起きるか |
| ② | この計算が**全製品ぶん・毎日・中立に**行われていると伝える | 3つが数字と文言で示されているか |
| ③ | メールアドレスと「見たい成分」を受け取る | フォームまで到達するか |

### 何が「凄さ」なのか 🔒

**「1gあたりで計算する」こと自体は凄くない。** 英語圏には同じことをするサービスが既に複数ある（CompareProteinPrices は700製品超、ProteinMath は毎日更新、SuppsRadar は30成分を g/mg/IU に正規化）。日本語圏にも同種の記事がある。

**凄いのは次の3点だけ。** デザインはこの3つを可視化することに奉仕してほしい。

| 軸 | 競合の欠陥 | 可視化の材料 |
|---|---|---|
| **網羅性** | 「おすすめ10選」形式で、全体の中の最安が分からない | 掲載件数の実数。分布の形 |
| **鮮度** | 執筆時点の価格が残り続ける。プロテインは頻繁にセールする | 更新日時。価格の変動 |
| **中立性** | 上位記事はすべてアフィリエイト記事 | 順位の決まり方の明示。報酬と無関係であること |

---

## 4. 画面に必ず載るもの

### 4.1 製品1件について

| 要素 | 必須 | 備考 |
|---|---|---|
| **製品パッケージ画像** | ◎ | **v0.4 まで欠けていた。比較サービスとして必須** |
| ブランド名・製品名 | ◎ | 実名を出す。伏せると信憑性が消える |
| **主指標（有効成分1単位あたりの価格）** | ◎ | 行内で最も強い要素にする |
| 副指標（含有率・1食あたり価格・内容量など） | ◎ | 主指標より弱く |
| 順位 | ◎ | 順位は意味を持つ |
| 価格の取得日 | ◎ | 鮮度の証拠 |
| 出典（商品ページへのリンク） | ◎ | |
| 販売元 | ○ | |
| データの確からしさ | ○ | 低信頼の行を明示する（§6） |

#### 製品画像の扱い 🔒

- **効能を連想させる画像は禁止だが、パッケージ写真は事実の提示なので可。** 価格.com と同じ扱い
- **画像が存在しない製品が必ずある。** 欠損時の代替表現を設計に含めること。ブランド名の頭文字や無地のプレースホルダなど、**欠損が「壊れている」ように見えない**こと
- 画像の縦横比・背景色・切り抜きの有無はメーカーごとにバラバラ。**揃わない前提**でレイアウトを組むこと
- 画像が主役になって数値が負けないこと。**主役は数値**
- 画像の利用条件は取得元（楽天等）の規約に従う。**要確認事項として残っている**

### 4.2 ページ全体について

| 要素 | 必須 | 目的 |
|---|---|---|
| ロゴ + タグライン | ◎ | §5 |
| 掲載件数の実数 | ◎ | 網羅性の証拠。「たくさん」ではなく「68製品」 |
| 全体の更新日 | ◎ | 鮮度の証拠 |
| **単価の差がどれくらい大きいかの表現** | ◎ | 驚きの源。年額換算など |
| 並べ替え・絞り込みができると分かる表現 | ◎ | ツールであることを示す |
| メール登録フォーム | ◎ | §4.3 |
| 「このサイトがやらないこと」の宣言 | ◎ | 中立性。競合が誰も書いていない差別化 |
| 免責文 | ◎ 🔒 | 常時表示。折りたたまない |
| 数値の出典 | ◎ 🔒 | ラベル表示値であることを明記 |
| OGP 画像 | ◎ | SNS でそのまま読める密度 |

### 4.3 要望の導線（旧「フォーム」）🔒

**2026-09-07（T-051）にメールアドレス先行フォームを段構造へ置き換えた。**
広告の着地点である LP がいきなりメールアドレスという重い対価を求めていたため、
登録が 0 件だった。意思表示を1クリックまで下げ、メールアドレスは後段（任意）に置く。

**段は 要望 → アンケート → メール → 支援。この順を並べ替えない。**
LP と製品一覧で**同じ描画（`src/templates/request.js`）と同じ操作（`src/assets/request.js`）**
を共用する。🔒 片方へ写して2つ持たない — 2つあったから表記が割れた。

| 段 | 目印 | 対価 | 内容 |
|---|---|---|---|
| ① 要望 | `data-request-cta` | 1クリック | 押した時点で意思表示は成立。以降はすべて任意 |
| ② アンケート | `data-request-step="survey"` | 選択 | 見たい成分 / 普段の購入先 / その他の要望（自由記述） |
| ③ メール | `data-request-step="email"` | メールアドレス | 掲載時の通知。ここを飛ばしても次へ進める |
| ④ 支援 | `data-request-step="support"` | 任意 | Codoc。`support` を持たない市場では段ごと出さない |

**②〜④に `required` を付けない。** 要望は①で受け取っている。

| 項目 | 形式 |
|---|---|
| メールアドレス | 任意（③の段） |
| 見たい成分 | 複数選択（プロテイン / クレアチン / EAA・BCAA / HMB / 鉄・亜鉛 / ビタミン / その他） |
| 普段の購入先 | 複数選択（楽天 / Amazon / Yahoo! / iHerb / マイプロテイン / 店舗 / その他） |

**①の押下は GA4（`request_click`）と D1 の両方に残す** 🔒 —
`POST /api/request-signal` が `request_signal` テーブルへ UUID と日時の2列だけを1行入れる。
GA4 が CSP で全滅した実績があり（T-047）、計測が自分の手の中に無いと 0 件の意味が読めない。
重複はブラウザごと（`localStorage` の `pergram.request_signal_id`）に1件へ抑える。
**この行に UUID と日時以外の列を足さない。** 押された場所の区別は GA4 の `location` が持つ。

- **年齢・性別・体調は取らない** 🔒 — 要配慮個人情報であり、取る必要もない
- 購入先は**複数選択**（2026-08-10 に単一選択から変更）。掛け持ちが普通で、
  1つに丸めると提携の優先順位を決めるという本来の目的に使えなくなる。
  保存はカンマ区切り1列のままで、DB の列は増やしていない
- 「その他」は成分チップの1つで、自由記述欄はその**横**に並べる。
  独立した欄にすると、チップ未選択で書かれた要望を成分の希望と判別できない
- 「見たい成分」は**要望ボタンの直後（②の段）**に置く。ここが検証の主目的。
  2026-09-06 まではメールアドレスの直下だったが、そのメールアドレスが後段へ移った
- 送信後は同一ページ内で完了状態に切り替える。別ページに飛ばさない
- 完了文言に感嘆符を使わない

---

## 5. ブランド 🔒

| 項目 | 規則 |
|---|---|
| 表記 | **`pergram`** — 全て小文字。`Pergram` `PerGram` は不可 |
| 読み | パーグラム |
| 由来 | per gram — 1gあたり、というこのプロダクトの唯一の指標 |
| タグライン | **1gあたり、いくら払ってる?**（EN: What are you paying per gram?） |
| 初回接触 | **ロゴとタグラインを必ずセットで出す** |

タグラインを必ず添えるのは、`-gram` が Instagram / Telegram の連想を呼び、単体だと SNS アプリに見えるため。

**ロゴは未確定。** 現行案は「Archivo 800 のワードマーク + ルーラーティック（長短交互の目盛り）」だが、**提案を歓迎する**。要件は次の2つだけ。

- 小文字で組む（単位表記 `mg` `g` `mcg` が小文字だから。名前を単位と同じ字面の世界に置く）
- 禁止ビジュアル（§7）を使わない

---

## 6. 状態の設計

比較サービスは**データが欠けている状態が常態**。ここを設計しないと本番で崩れる。

| 状態 | 要件 |
|---|---|
| 製品画像がない | 代替表現を出す。空白にしない |
| 絞り込み結果が0件 | 「1つ外すと N 件になります」と、ワンタップで外せる導線 |
| 価格の取得に失敗 | 前回価格と取得日を出す。**空欄にしない** |
| データの確からしさが低い | 数値は出すが、根拠が弱いことを明示する |
| 公的な基準値が存在しない成分 | 「定められていません」と明示する。**推定値で埋めない** 🔒 |

**エラー文は謝らない。** 何が起きて、どうすれば直るかだけを書く。

---

## 7. 絶対制約 🔒

薬機法・景表法に直結する。**機能要件より優先し、迷ったらより保守的な方を選ぶ。**

### 使ってはいけない語
```
効く / 効果 / 改善 / 高品質 / おすすめ / 人気No.1 / 安心 / 話題の / 選ばれる / 実感
```

### 使ってよい語
```
単価 / 1gあたり / 含有量 / 含有率 / 元素量 / ラベル表示 / 比較 / 一覧 / 出典 / 更新日 / 安い
```

### 禁止事項

| # | 禁止 | 理由 |
|---|---|---|
| 1 | 成分の働き・体への作用に言及する | 薬機法 |
| 2 | 独自の総合スコア・おすすめ度・加重平均ランキングを作る | 事実提示ではなく推薦になる |
| 3 | 「だからこちらを選ぶべき」と書く | 判断はユーザーに委ねる |
| 4 | 症状・悩みの入力を受け付ける | 「症状→成分→商品」は効能の暗示 |
| 5 | **筋肉・肌・体のシルエット・before/after・人物写真** | 効能訴求と解釈されうる |
| 6 | **葉・植物・カプセル・錠剤・ハート・チェックマーク** | 効能または推薦を連想させる |
| 7 | LP にアフィリエイトリンクを置く | 広告審査でアフィリエイトサイト判定を受ける |
| 8 | ヒーローにダミーデータを置く | ここで嘘をつくと全部が崩れる |

**⑤⑥は「製品パッケージ写真」を含まない。** パッケージは事実の提示なので使ってよい（§4.1）。

### 数値の書き方

- 単位と基準を必ず添える。「¥38」ではなく「**¥38 / タンパク質1g**」
- 前提を必ず併記する。「1年で¥100,740の差」なら「60g/日 × 365日で計算」
- 「実測」と書かない。**ラベル表示値**であることを明記する

---

## 8. 技術的な下限

| 項目 | 要件 |
|---|---|
| **数値の字形** 🔒 | 等幅数字（`tabular-nums`）。桁が揃わない比較表は比較表として機能しない |
| **モバイル主** | 想定流入の大半がスマートフォン。390×844 で成立すること |
| タップ領域 | 44px 以上 |
| 本文サイズ | 16px 未満にしない（iOS のズーム発動を防ぐ） |
| 書体のライセンス | 商用利用可・無料（OFL 等）。**self-host できること**。日本語と英数字の両方が必要 |
| 書体の重み | 読み込むウェイト数を絞る。日本語フォントは重い |
| 色に依存しない | 状態・方向・警告を**色だけで表さない**。記号やテキストを併記する |
| コントラスト | 4.5:1 以上 |
| 動きの抑制 | `prefers-reduced-motion: reduce` を尊重する |
| マークアップ | 順位付きリストは `<ol>`、成分表は `<table>`。`div` で組まない |
| LCP | 2.0 秒以内。広告費を払って流入させるため離脱が直接コストになる |
| JS | フレームワーク不要。静的 HTML + 最小限のスクリプト |

**書体について**: 現行の指定は Archivo / IBM Plex Sans JP / IBM Plex Mono だが、**self-host していないため実装ではシステムフォントにフォールバックしていた**。つまり現物の見た目は指定と無関係。**書体選定は Claude Design に委ねる。** 上記の要件（無料・日本語対応・等幅数字・self-host 可）を満たせば何でもよい。

---

## 9. 配色のたたき台

**変更可。** 要件は「色数を絞ること」と「色だけで意味を担わせないこと」だけ。

| 用途 | 現行値 | 意図 |
|---|---|---|
| 本文・罫線 | `#14161A` | 純黒にしないのはラベル印刷の質感 |
| 背景 | `#FBFAF7` | ラベル紙の白 |
| 補足・出典 | `#6B6F76` | |
| 細罫 | `#DCD9D2` | |
| **強調** | `#1B44C8` | **「今見ている指標」の意味でのみ使う** |
| 認証バッジ | `#1F6F4A` | |
| 警告 | `#B3261E` | 上限量の超過のみ |

**強調色を安売りしないこと。** 全製品の価格を強調色にすると、その色は何も意味しなくなる。色を増やしたくなったら情報設計が間違っている合図。

### 世界観のヒント（拘束しない）

材料は**成分表示パネル**（米国 Supplement Facts / 日本の栄養成分表示）。太罫と細罫で階層を作り、左に名前・右に数値を置く表組みの文法。

これは装飾ではなく立場の宣言で、**「ここはマーケティングの場ではなく、ラベルの数値を読む場だ」**と言っている。この意図が伝わるなら、別のアプローチでもよい。

---

## 10. 参考にした事例

| サービス | 学ぶ点 |
|---|---|
| **Wise**（送金手数料） | ヒーローに実額を直接置く。翻訳の要る文言がなく**計算が語る**。名指しの比較で価格不安をその場の操作に変える |
| **Google Flights**（価格グラフ） | 予測をせず、実在する価格を並べて**分布の形**を見せる。数日ずらすと大きく変わることが「発見」として起きる |
| **価格.com** | 日本ユーザーの期待値の基準。**製品画像 + 絞り込みファセット + 最安値の即時提示** |
| **PCPartPicker** | 単一目的（組む）に全 UI を従属させる |
| **Camelcamelcamel / Keepa** | 価格履歴と「今が買い時か」の即答 |
| **SupplementMath / SuppsRadar**（競合） | 「30回分 vs 2lb」の表記差を剥がす説明の明快さ |

### 採らないもの

- **並列比較マトリクスをデフォルトにしない。** 大規模ユーザビリティテストで、ユーザーは比較対象を選ぶ段階でもスペックを読む段階でも困難を抱えることが分かっている。デフォルトは**単一指標で並んだ1本のリスト**
- 明るい配色・イラスト・グラデーションは、ラベルの質感と衝突する

---

## 11. 使えるデータ

設計時に「この値は出せるか」を確認しなくて済むよう、**実際に存在するフィールド**を挙げる。

### 製品ごと
```
ブランド / 製品名 / 剤形 / フレーバー / 内容量 / 1食のグラム数 / 1容器あたりの回数
有効成分量（1食あたり・換算後） / 販売元 / 価格 / 在庫 / 商品URL / 取得日
製品画像URL（※取得は実装可能。ライセンス要確認）
データの確からしさ（high / medium / low）
```

### 導出できる値
```
有効成分1単位あたりの価格（主指標） / 1食あたり価格 / 目標摂取量あたりの日額
100gあたり含有量 / 含有率 / 内容量 / 全製品の単価分布 / 絞り込み前後の最安差
```

### まだ無い値
```
第三者認証・原産国・添加物などの品質情報（後続フェーズ）
公的な推奨量・上限量（一次資料からの転記が未了）
レビュー評価（取得は可能だが表示は未定）
```

**現時点で実データは未収集。** 掲載件数・金額はすべて仮値であり、デザイン確定後に差し替える。**リアルなダミーで組んでよいが、実装時は実データに置き換わる**前提で。

---

## 12. 計測（実装時の要件）

デザインに影響する範囲だけ。**触られたかどうかを測れる形にしてほしい。**

| 測りたいこと | なぜ |
|---|---|
| デモ的な要素に触れたか | 価値が伝わったかの中間指標 |
| フォームに触れたか / 送信したか | 主 KPI |
| どこまでスクロールしたか | 離脱位置 |

**個人識別情報を計測に送らない。メールアドレスをイベントに含めない。** 🔒

---

## 13. 成果物

| # | もの | 備考 |
|---|---|---|
| 1 | **LP のデザイン**（モバイル・デスクトップ） | 本書 §3 の①が達成できていること |
| 2 | **製品1行のコンポーネント** | 画像あり／なし の両方 |
| 3 | **状態のバリエーション** | §6 の各状態 |
| 4 | **デザイントークン** | 色・型・余白・罫線。本体サービスに引き継げる形で |
| 5 | **ロゴ / ワードマーク** | 現行案の改善または新提案 |
| 6 | **ファビコン** | 文字を入れない |
| 7 | **OGP 画像**（1200×630） | ロゴ + タグライン + 実データの上位数行 |

---

## 14. 判断に迷ったら

1. **法規制の制約（§7）が最優先。** 機能要件より上
2. **主役は数値。** 画像・装飾がそれを超えない
3. **事実だけを書き、優劣を述べない。** 判断はユーザーに渡す
4. **色を増やしたくなったら情報設計を疑う**
5. それ以外は**提案してほしい。** 本書に書いていないことは決めていないという意味

---

## 付録: 未確定事項

| 項目 | 状態 |
|---|---|
| 実データ | 未収集。**LP（`/ja/`）は常に出力される。**20件揃うまで出ないのはヒーローのランキングカードだけ（`build.js` の `MIN_HERO_PRODUCTS`） |
| 製品画像のライセンス | 取得元の規約を確認中。実装側は URL 欠損時の代替表現を用意済み |
| ドメイン | **`pergram.site` を取得済み。2026-08-24 から本番稼働**（当初は `*.workers.dev` 直下だった）。未確定なのは**旧 `pergram.pergram-official.workers.dev` を畳む時期**のほうで、畳むまでは `worker/index.js` が恒久転送する。手順と実測は [ops/deploy.md](../ops/deploy.md) の「決めていないこと」 |
| 書体 | Noto Sans JP / JetBrains Mono に確定。**ただし §8 の self-host は未達**（下記） |
| ロゴ | JetBrains Mono 700 のワードマーク。ルーラーティックは廃止 |

---

## 実装の状態（2026-08-07）

Claude Design の成果物（`LP_mockup/`）を [src/templates/lp/](../../src/templates/lp/) と
[src/styles/lp.css](../../src/styles/lp.css) に移植済み。以下は要件との差分。

### 未達 — 対応が必要

| # | 項目 | 内容 |
|---|---|---|
| 1 | **書体の self-host**（§8） | Noto Sans JP / JetBrains Mono を Google Fonts CDN から読み込んでいる。サブセット化して `src/assets/fonts/` に置き、`tokens.css` の参照を差し替える。LCP 2.0秒（§8）に直結する |
| 2 | ~~**製品パッケージ画像**（§4.1）~~ | **解決済み（2026-08-08）。** 楽天 API の `mediumImageUrls` を収集して `image_url` に保存する。LP のヒーローと製品一覧が別の場所を読んでいた不整合も `product.image_url` に統一。画像の無い製品は従来どおり代替表現。**画像の利用条件の確認は未了** |
| 3 | **OGP 画像**（§13-7） | `scripts/make_ogp.js` は配色のみ新パレットへ更新。構図は旧版のまま。実データ収集後に作り直す |

### モックから変更した点 — 法規制・🔒 制約による

| モックの記述 | 変更後 | 理由 |
|---|---|---|
| 「1gあたりの**真の**価格」 | 「有効成分1gあたりの価格」 | 「真の」は他サービスが偽と示唆する比較表現 |
| 「隠れた**コスパ最強品**が見つかる」 | 「掲載製品を単価順に一列に並べる。最安が先頭に来る」 | §7 禁止②③（優劣の主張） |
| 「「**おすすめ**10選」ばかりで」 | 「「10選」形式の記事ばかりで」 | §7 禁止語 |
| 「アフィリエイト報酬額で順位が**操作されている**」 | 「順位の決まり方が公開されていない」 | 競合への断定を避ける（景表法） |
| フッタ「**効果**・効能の保証や優劣の主張」 | 既存の `disclosure.jp.*` を使用 | §7 禁止語。法的な等価性は維持 |
| ランキングを `div` で組む | `<ol>` + `<li>` | §8 マークアップ |
| ヒーローの「68製品」「ブランドX ¥4.1/g」 | 実データから描画 | §7 禁止⑧。20件未満なら**カードを出さない**（LP 自体は出す） |
| 「中立性」アイコン = 回転四角 + **チェックマーク** | 降順に並んだ3本の棒 | §7 禁止⑥（推薦の連想） |
| SP のフォーム入力 15px | 16px | §8（iOS の自動ズーム） |
| ヘッダにタグラインなし | ヒーロー冒頭と PC ヘッダ・フッタに表示 | §5 🔒（初回接触では必ずセット） |

---

## 製品一覧ページの実装（2026-08-08）

Claude Design の成果物（`Product_page_pc_mock/` — PC / SP 同梱。`.gitignore` 済みで手元にのみ存在）を
[src/templates/products/](../../src/templates/products/) と
[src/styles/products.css](../../src/styles/products.css) に移植した。
URL は既存の `/{locale}/{nutrient}/`。旧 `src/templates/ranking.js` はこれに置き換えて削除した。

### 構成

| ファイル | 役割 |
|---|---|
| `src/templates/products.js` | 合成のみ |
| `src/templates/products/head.js` | ヘッダ（検索 + 要望の導線へのリンク）・見出し・ツールバー・解説・広告表示 |
| `src/templates/request.js` | **要望の導線と段。LP と製品一覧で共用** |
| `src/templates/products/filters.js` | 絞り込み。PC は固定サイドバー、SP は同じ DOM をボトムシートとして出す |
| `src/templates/products/item.js` | 製品1件。**カード表示とリスト表示は同じマークアップ**を CSS のグリッドで組み替える |
| `src/assets/products.js` | 絞り込み・ビュー切替・副指標切替・URL 同期（約 11KB） |

カテゴリ差は `config/categories.json` に追加した `facets` / `explainerKey` /
`unitCostRange` / `priceRange` で吸収する。UI 側に成分名の分岐は無い。

### モックから変更した点 — 法規制・🔒 制約による

| モックの記述 | 変更後 | 理由 |
|---|---|---|
| 並び替え「製品価格が安い順 / 含有率が高い順 / **レビュー評価が高い順**」 | **並び順は単価固定**。セレクトは副指標（含有率 / 1食あたり）の表示切替に変更 | 🔒「並び順を決めるのは常に有効成分1単位あたりの価格」「per serving をソートキーにしない」／ N-03 |
| 「★ 4.6」「**Amazonレビュー 1,204件**」「iHerbレビュー 3,910件」 | **全削除**。確からしさは `confidence`（要確認）と価格取得日で示す | 🔒 N-08（Amazon / iHerb のレビュー本文・星評価の転載） |
| 「1gあたり最安」バッジの**チェックマーク** SVG | 順位番号 + テキスト。`--verified` は主指標の最安1件のみ | §7 禁止⑥（推薦の連想）。色だけで意味を持たせない |
| 「**人気**の栄養素」 | 「対応中の栄養素」 | §7 禁止語の周辺。優劣・推薦の含意を避ける |
| 「市場で最も**選ばれている**ホエイ」 | 削除 | §7 禁止語。`BANNED_WORDS` テストが機械的に落とす |
| 解説文の「乳糖が少ない」「吸収の遅いタイプ」 | **含有率で割るという算術の説明だけ**に限定 | 「事実のみを書き、優劣を述べない」。成分の生理的性質には言及しない |
| ブックマーク（保存した製品） | 実装しない | 検証フェーズでは登録率に効かない。実装する場合も localStorage のみ（🔒 サーバに置かない） |
| SP のボトムタブバー（4タブ） | 置かない。画面内の「絞り込み」ボタンに集約 | リンク先が存在するのは「ランキング」だけで、死んだリンクを3本抱えることになる |
| 製品カテゴリ（WPI/WPC/ソイ…） | `product_attributes` のキーとして表現し、`categories.json` の `facets` から描画 | 🔒 カテゴリ固有テーブルを作らない |
| フッタ「**効果**・効能の保証」 | 既存の `disclosure.jp.*` を使用 | §7 禁止語。法的な等価性は維持 |

### LP からの動線

**ヘッダとヒーローで CTA の主従が違う（2026-09-02 時点）。片方だけを見て「LP の主 CTA は◯◯」と
一般化しないこと。**

| 場所 | 出しているもの | 面 | `data-cta` |
|---|---|---|---|
| LP ヘッダ | 要望の導線（主）とβ版（従）の2本 | `btn--signal` / `btn--quiet` | `header_waitlist` / `header_beta` |
| LP ヒーロー | **β版の1本だけ** | `btn--dark btn--block` | `hero_beta` |
| 製品一覧 | ヘッダの「機能追加リクエスト」（LP の `#waitlist` へ）と、本文の要望ボタン2箇所 | — | `products_request_top` / `products_request_bottom` |

- **ヘッダの主従は T-010（2026-09-02）のまま**。待機リストが主、β版が従。β版は消さずに
  押せる状態のまま、面の強さだけ下げてある。
- **ヒーローは T-020（2026-09-02、PO の指示による見せ方の実験）でβ版1本に絞った。**
  待機リストのボタンと、その真下にあった注記（`lp.ctaNote`）は**要素ごと出力していない。**
  CSS では隠していない（隠すと読み上げには残り、押せない要素が読み上げられるため）。
  ヒーローのβ版だけ塗りのある面（`btn--dark`）にしてヘッダの `btn--quiet` と変えてある。
- 意思表示の受け皿は**ヘッダとページ下部の段構造に残っている。**ヒーローから消えた
  だけで、導線そのものは消えていない。
- 🔒 **文言は LP と製品一覧で同じキー（`request.headerCta` / `request.cta`）を使う**（T-051）。
  以前は製品一覧が「リリース通知を受け取る」、LP が「Waitlist」で、同じ導線を指しているのに
  画面ごとに言葉が違った。**キーを分けると片方だけ直せる状態に戻り、また割れる。**
- 🔒 `data-cta` の綴り（`header_waitlist`）とリンク先（`#waitlist`）は据え置く。T-051 で
  変えたのは**表示ラベルだけ**である。T-047 の直後で、綴りを変えると前後比較ができなくなる。
- ⚠️ **`hero_waitlist` の GA4 イベントは 2026-09-02 以降ゼロになる。計測漏れではない。**
  待機リストの登録数を見るときは `header_waitlist` とフォームの送信を見る。綴りは GA4 の
  履歴と突き合わせるために予約したままにしてあり、**他の要素へ付け替えない。**
- 当初（2026-08-08）はヘッダ・ヒーローとも `btn--quiet` のβ版導線1本ずつで、文言も
  「ベータ版を使ってみる」だった。上の表が現在の実装である。
- 不変条件は [tests/lp_cta.test.js](../../tests/lp_cta.test.js) と
  [tests/render.test.js](../../tests/render.test.js) が両方から固定している。
  **片方だけ直すともう片方で落ちる。**

⚠️ **LP 自体にアフィリエイトリンクは無いが、1クリック先の製品一覧は購入リンクを持つ。**
広告審査でアフィリエイトサイト判定を避ける目的には、この動線は影響しうる。
審査に落ちた場合、最初に外す候補はヘッダ側のベータ導線。

### 確認済み（`npm run preview` + ヘッドレス Chromium）

320 / 390 / 1440px で横スクロールなし・JS エラーなし・本文 16px・
操作要素はすべて 44px 以上。絞り込み → 件数更新 → URL 同期 → 0件表示 → 副指標切替まで動作。

### 未達 — 対応が必要

| # | 項目 | 内容 |
|---|---|---|
| 1 | **絞り込みの枠だけ用意した項目** | 製品タイプ・品質・ECサイト・メーカーが件数 0 で並ぶ。**メーカーだけはデータ投入で有効になるが、製品タイプと品質は有効にならない。** 収集にも取り込みにも該当項目が無く、`product_attributes.json` は誰も書かないため（2026-08-08 確認）。ECサイトは楽天単独なので他2社は 0 のまま。**モックとして意図的にこの状態で置く。商品DBを作る段階で対応する（バックログ）** |
| 2 | **年額換算の差**（§4.2 ◎） | 未実装。最安と最高の年額差を出す枠がまだ無い |
| 3 | **0件時の「1つ外すと N 件」** | 現状は「絞り込みをすべて外す」のみ。どの条件を外せば何件になるかは出していない |

---

## 収集と表示の突き合わせ（2026-08-08）

製品ページが表示しようとしている内容と、収集手順が用意する内容を突き合わせた結果の対応。

### 対応した

| 項目 | 内容 |
|---|---|
| **製品画像** | 参照先が製品一覧 `product.image_url` と LP ヒーロー `row.imageUrl` に割れていたのを前者へ統一。楽天 API の `mediumImageUrls` を収集して保存する。手作業は増えない |
| **含有量の自動読み取り** | 含有量を返す EC API は存在しないため、商品説明文（`itemCaption`）に貼られた栄養成分表示から読み取る（`extractProteinFromCaption`）。「1食(30g)あたり」「100gあたり」「含有率 71%」の3表記と全角に対応。**読めなければ null を返して人間の入力に回す。推測で埋めない** 🔒。食い違う値が読めた行は `label_ambiguous` を立てて必ず人間が見る。**2026-08-09 追記**: 「1食(30g)あたり／100gあたり … たんぱく質 24.0g/80.0g」のような2列表を1行に潰した書き方から片方だけを拾い、含有率 80% を 24% と読む誤りがあったため、1成分に値が2つ並ぶ表記は読まずに `ambiguous` にした。「たんぱく質 20%OFF」の割引率を含有率として読む誤りも塞いだ。併せて全角パーセント（`71％`）を読めるようにした |
| **1食量** | 同じく説明文から読み取る。無いと1食=100g扱いになり副指標「1食あたり」が3〜4倍ずれ、`confidence` が `medium` に落ちて全行に「▲ 要確認」が付く。省いてよい項目ではない |
| **`flavor` の収集をやめた** | 人間が20件ぶん手入力する項目だったが、UI のどこにも表示されず絞り込みにも使われていなかった |
| **アフィリエイト表示を全部外した → 戻した** | 下記 |

### アフィリエイト表示について 🔒

**2026-08-08 の時点では「使っていない」と判断して開示を3箇所とも外した。**
**2026-08-09 にユーザーが「アフィリエイトは利用する」と判断したため、すべて戻した。**

収集時に `RAKUTEN_AFFILIATE_ID` を渡し、楽天が返すアフィリエイト URL を購入リンクとして保存する。
アフィリエイトに参加していない店舗では素の商品 URL のままになる（下書きの `is_affiliate` で分かる）。
**参加店舗が1件でもある以上、開示は常時出す。**

| 場所 | 文言キー | 文言 |
|---|---|---|
| 製品一覧 上部の常時表示 | `products.affiliate` | 各ストアへのリンクにはアフィリエイトリンクを含みます… |
| 製品一覧 フッタ | `disclosure.{jp,us}.affiliate` | 本サイトはアフィリエイト広告を利用しています。 |
| LP フッタ | `disclosure.{jp,us}.affiliate` | 同上 |

リンクの `rel` は `nofollow sponsored noopener`。`sponsored` は報酬付きリンクを表すので付ける。
`disclosure.{jp,us}.affiliatePlanned`（「利用する予定です」）は事実と違うので**削除した**。

**🔒 報酬額を順位に影響させない。** ユーザーの指示は「アフィリエイトアイテムを優先的に表示ではなく、
価格優先で表示する」。並び順を決めるのは常に有効成分1単位あたりの価格で、これは元から変えていない。
加えて、収集時に**報酬率（`affiliateRate`）を下書きに保存しない**ようにした。
持たなければ「報酬の高い順」という並びをそもそも作れない。

目印:
- `tests/collect.test.js`「🔒 報酬率を下書きに残さない。報酬順という並びを作れないようにする」
- `tests/render.test.js`「🔒 広告表示があっても並び順は単価の昇順のまま」
- `tests/render.test.js`「🔒 広告表示は「報酬は順位に影響しない」と明言する」

`lp.foot.about` の「特定商品の推奨や**アフィリエイト誘導を行わない**」は開示文と真っ向から矛盾する
ため書き換えた（「特定商品の推奨を行わない…各ストアへのリンクにはアフィリエイトリンクを含みますが、
順位は価格の計算結果だけで決まります」）。矛盾の再発は
`tests/render.test.js`「🔒 アフィリエイトを利用している以上「誘導を行わない」と書かない」で止まる。

なお §7 禁止⑦「LP にアフィリエイトリンクを置く」は**引き続き有効**。LP に購入リンクは無い
（`tests/render.test.js`「🔒 LP にアフィリエイトリンクを置かない」）。

### 対応しない（バックログ）

| 項目 | 理由 |
|---|---|
| 製品タイプ・品質の絞り込み | 商品DBでカテゴリを設計する段階で扱う。今はモックとして枠だけ置く |
| 複数ECの価格比較 | 名寄せキー（requirements.md Q-07）が未確定。2つ目のソースを足す直前に決める |
| 人間の修正用の管理画面 | 下書きの修正は当面テキストエディタで行う。件数が増えたら検討する |

---

## ヒーローのランキングカードの例外（2026-08-10 / 掲載3件を 2026-09-02 に差し替え）⚠️

**§7 禁止⑧「ヒーローにダミーデータを置く」に対する、期間限定の明示的な例外。**
ユーザー判断で入れた。判断の理由は上位製品のサムネイル画質。

### 何をしたか

`src/build/build.js` の `HERO_PRODUCT_IDS` に3件の製品IDを並べ、ヒーローのカードに出す製品を
単価順の上位3件から手動指定に切り替えた。現在の指定は実際の第4位・第5位・第8位。

**2026-09-02（T-009）に掲載3件を差し替えた。** 差し替え前は実際の第17位・第19位・第20位
（¥9.9〜¥12 / タンパク質1g）で、広告から来た人が最初に見る単価が実データの上位より大きく高かった。
差し替え後は実データとの開きが縮み、カードの並びも単価の昇順になっている。

**下表の単価・袋価格・順位は 2026-08-24 の価格スナップショットに基づく。**
価格更新（T-014）が復旧して数字が動いたら、この表を実データで取り直す。

| カードの表示 | 製品 | 実際の単価 | 実際の順位 | 袋価格 |
|---|---|---|---|---|
| 1 | マイプロテイン ソイプロテイン アイソレート 1kg | ¥4.0 / タンパク質1g | 4位 | ¥3,590 |
| 2 | ツインたんぱく 大豆+ホエイのいいとこ取りプロテイン チョコレート風味 1kg | ¥4.8 / タンパク質1g | 5位 | ¥3,780 |
| 3 | ソイプロテイン 1kg 人工甘味料不使用 大豆プロテイン オールインワン | ¥5.2 / タンパク質1g | 8位 | ¥3,480 |

カード1の製品は在庫なし（`in_stock: false`）だが、**ヒーローに在庫の表示は出さない**（2026-09-02 のユーザー判断）。
製品一覧側の在庫の表示は従来どおり。

### 何が嘘になっているか（把握したうえで残している）

- カードの見出しは「タンパク質 1gあたり最安ランキング（Sample）」のまま
  （`locales/ja.json` の `lp.hero.cardTitle`。900px 未満では `lp.hero.cardTitleShort` の
  「1gあたり最安ランキング（Sample）」になる）。**「（Sample）」は付いているが、
  「最安ランキング」と名乗っていること自体は変わっていない。**
  同じサイトの製品一覧（`/ja/protein/`）には ¥1.9 / g の製品が第1位として載っている
- したがって**行番号 1・2・3 は実際の順位ではない**。景表法の優良誤認に該当しうる
- 製品名・価格・単価・画像は**すべて実データ**。数字そのものを作ってはいない。
  作っているのは並び順だけ

### 何は守られているか

- 製品一覧（`/ja/protein/`）の並び順は有効成分1単位あたりの価格の昇順のまま。触っていない
- N-03（独自の総合スコア・加重平均ランキングを作らない）は維持。スコアは導入していない
- §7 禁止⑦（LP にアフィリエイトリンクを置く）は維持。LP に購入リンクは無い

### 解消の条件

上位製品のサムネイルが差し替わったら `HERO_PRODUCT_IDS` を**空配列に戻す**。
空にすれば単価順の上位3件に戻り、見出しと中身が一致する。
指定中はビルドが毎回 `⚠️ ヒーロー HERO_PRODUCT_IDS で手動指定した N 件を出しています` と出力する。

**より穏当な代替案（この判断より前に検討し、採用しなかったもの）:**
`src/lib/display_overrides.js` の `DISPLAY_FIELDS` に `image_url` を足し、
上位製品の画像だけを差し替える。順位は実データのまま保てる。


---

## 面の体系と字送りの階層（2026-09-08 / T-053）🔒

**PO がプレビューを見て「デザインがバラバラだ」と指摘した。** 主観の問題ではない。
製品一覧の主カラムに縦に積まれる4つの箱が、**角丸3種・背景3種・枠線3種・余白3種**を
持っていた。同じ階層の情報が4通りの見た目を持つと、読み手は「これは別の種類の情報だ」と
誤って受け取る。

この節は**面（サーフェス）の役割を3つに固定し、値の出所を1組のトークンへ寄せる**。
以後、箱を1つ足すたびに角丸と余白を決め直す作業は発生しない。

### 対象範囲

**この節が支配するもの** — 製品一覧と LP の主カラムに積まれる箱
（`.explainer` / `.notice` / `.request-band__inner` / `.request-flow__step`）と、
その中の字送り、要望ボタンの配置、段の開閉で残る器、見出しのフォーカスリング。

**支配しないもの** — 製品カード `.p-item` の内部（列割り・価格の組版）、絞り込みサイドバー、
ヘッダ・フッタ、LP のヒーローと帯（`.waitlist-band`）そのもの。
これらは既存のままで、面の役割の定義だけを共有する。

### 前提・制約

- **`tokens.css` の既存トークンの値を変えない。** `--signal` / `--signal-hover` /
  `--line-control` は WCAG 2.2 AA のコントラスト比を根拠に固定されている
  （根拠は `src/styles/tokens.css` の 🔒 コメント）。この節の設計は**既存の値を1つも
  動かさずに成立する**。動かさないと成立しない案は最初から採っていない
- **新しい色・寸法の「値」を1つも増やしていない。** 増やしたのは**役割の名前**だけで、
  新トークンはすべて既存トークンの別名である
- `.request-*` は **LP と製品一覧で共用**する（`src/templates/request.js` /
  `src/assets/request.js`）。片方だけ良くなる指定を書かない。文脈の差
  （LP は 640px の中央カラム、製品一覧は 900px 以上の広い主カラム）は
  **画面幅と、そのページにしか読み込まれない CSS ファイル**で吸収する
  （`site.css` = 共通 / `lp.css` = LP のみ / `products.css` = 製品一覧のみ）
- ⚠️ **この文書の §9「配色のたたき台」の16進値は実装と食い違っている**（青と緑が書いてあるが
  実装は橙系）。**色の値の唯一の出所は `src/styles/tokens.css` である。**
  §9 を見て配色を決めないこと（T-053 で気付いたが、修正はこのタスクの範囲外）

---

### 1. 面の役割は3つ 🔒

**増やさない。** 4つ目が要ると思ったら、それは情報設計が間違っている合図である。

| 役割 | 名前 | 何を割り当てるか | 見た目 |
|---|---|---|---|
| **A** | **読む面（カード）** | `.explainer` / `.request-flow__step` / `.p-item` | 白い面・細い枠・カードの角丸 |
| **B** | **呼びかけの面（帯）** | `.request-band__inner` | **A と同じ形。色だけが違う**（橙の淡い面と橙の枠） |
| **C** | **注記（面を持たない）** | `.notice` / `.request-form__note` / `.notice__beta` | **箱にしない。** 紙地の上の文字だけ |

**この3つで何が起きるか。**

1. **A と B は形が同じで色だけが違う。** 階層を**幾何ではなく色**で作る。
   幾何（角丸・余白）で階層を作ると、種類が増えるたびに新しい値が必要になり、
   今回の食い違いはそうやって生まれた。色は2つしか無い（`--signal` 系と無彩色）ので、
   色で階層を作るかぎり面の種類は増えようがない
2. **C は箱ではない。** 縦に積まれる箱が4つから2つへ減る。
   広告表示（`.notice`）を灰色の箱から出すのは**弱めるためではない**
   — 箱に入った定型文は「読み飛ばしてよい欄」に見える。本文と同じ地の上に、
   本文と同じ強さ（`--muted-strong`）で置くほうが、常時表示の趣旨に合う。
   ⓘ アイコンと文言はそのまま残す

**面の中に面を入れない。** A の中に A や B を置かない（`.request-flow__step` の中に
カードを作らない）。入れ子の箱は、この規模の画面では必ず階層の誤読を生む。

### 2. 各役割の具体値

**値はすべて `tokens.css` の新しい別名トークンから引く。** コンポーネント側に
角丸・背景・枠線・余白を直書きしない。

#### 2.1 `src/styles/tokens.css` へ足すトークン（追加のみ・既存値は不変）

角丸のブロック（`--radius-pill` の行）の直後に、次のブロックを足す。

```css
  /* --- 面（パネル）— 積まれる箱の見た目はここだけで決める（T-053）--------
     🔒 面の役割は3つ。A 読む面 / B 呼びかけの面 / C 注記（面を持たない）。
        増やさない。値はすべて上の既存トークンの別名で、新しい色も寸法も足していない。
        コンポーネント側に角丸・背景・枠線・余白を直書きしないこと。 */
  --panel-bg: var(--surface);
  --panel-border: 1px solid var(--line);
  --panel-radius: var(--radius);
  --panel-pad: var(--space-4);
  --panel-pad-lg: var(--space-6); /* 900px 以上 */

  /* B（呼びかけの面）。A との違いは色だけである。角丸・余白は A と共有する */
  --panel-call-bg: var(--signal-band);
  --panel-call-border: 1px solid var(--signal-edge);

  /* スティッキーヘッダの高さ。アンカー移動とフォーカス移動の逃げ幅に使う。
     ⚠️ 同じ数値が lp.css（76 / 88px）と products.css（88px）に直書きで
        残っている。移行は T-053 の範囲外 */
  --head-offset: 76px;
  --head-offset-lg: 88px; /* 900px 以上 */
```

#### 2.2 面の値（A / B）

| 項目 | A 読む面 | B 呼びかけの面 |
|---|---|---|
| 背景 | `var(--panel-bg)` | `var(--panel-call-bg)` |
| 枠線 | `var(--panel-border)` | `var(--panel-call-border)` |
| 角丸 | `var(--panel-radius)`（= `--radius` 14px） | **同じ** |
| 内側の余白 | `var(--panel-pad)` / 900px 以上 `var(--panel-pad-lg)` | **同じ** |
| 面と面のあいだ | `var(--space-6)` | **同じ** |

**角丸を 16px（`--radius-lg`）から 14px（`--radius`）へ揃える。**
このページで最も多く出る箱は製品カード `.p-item`（14px）で、`.explainer` も 14px である。
16px を使っていたのは要望の帯と段の2つだけだった。**多数派へ寄せる。**
`--radius-lg`（16px）が残るのは**大きな面の2箇所だけ**である —— モバイルの絞り込みシート
`.filters`（`products.css`）と、LP の「順位が入れ替わる図」`.flip` の 900px 以上
（`lp.css`。900px 未満は 14px）。**新しい箱には使わない。**

> ✅ `.claude/rules/pergram-ui-copy.md` と `src/styles/tokens.css` の角丸の記述は、
> 2026-09-08（T-053 / R-053-3 / R-053-4）に上の2箇所を名指しする形へ直してある。
> 「全画面シート」という書き方は `.flip` を含まないので使わない。

**`.p-item` だけは 900px 以上でも `--space-4` のまま**にする。3列グリッドに並ぶため
1枚あたりが狭く、24px にすると中身の桁組みが崩れる。**意図的な例外であり、
「揃っていない」ではない**（`.p-item` は T-053 の変更範囲外でもある）。

#### 2.3 注記の値（C）

```
背景: なし / 枠線: なし / 角丸: なし / パディング: 0
アイコンと本文の間隔: var(--space-2)
前後の間隔: var(--space-3)（直前の面と同じ話題のとき） / var(--space-6)（話題が変わるとき）
```

#### 2.4 CSS の置き場（実装への指示）

**1つの規則が4つの面の幾何を決める。** `src/styles/site.css` の
「要望の導線と段階」ブロックの先頭に置く。

```css
/* ---- 面の役割（T-053）----------------------------------------------
 * 🔒 積まれる箱の角丸・背景・枠線・余白は**ここだけ**で決める。
 *    個々のコンポーネントで直書きしない。役割は3つ（docs/design/design.md）。
 * ⚠️ .explainer は製品一覧にしか無いが、値の出所を1箇所にするためここに置く。 */
.explainer,
.request-band__inner,
.request-flow__step {
  padding: var(--panel-pad);
  background: var(--panel-bg);
  border: var(--panel-border);
  border-radius: var(--panel-radius);
}

@media (min-width: 900px) {
  .explainer,
  .request-band__inner,
  .request-flow__step {
    padding: var(--panel-pad-lg);
  }
}

/* B は A と形が同じで、色だけが違う */
.request-band__inner {
  background: var(--panel-call-bg);
  border: var(--panel-call-border);
}
```

これに伴って**消すもの**（重複した直書き）:

- `products.css` の `.explainer` から `background` / `border` / `border-radius` / `padding`、
  および 900px 以上の `.explainer { padding: var(--space-6) }`
- `products.css` の `.notice` から `background` / `border-radius` / `padding`
- `site.css` の `.request-band__inner` / `.request-flow__step` の
  `padding` / `background` / `border` / `border-radius`

**完了条件 B-1 / B-2 の判定は、この「消すもの」が消えているかで行える。**

---

### 3. 字送りの階層 🔒

**同じ役割の文字が段ごとに違う値を持っていた**（完了表示だけ 16px・太さ 500・黒、
リード文は段によって `--muted` と `--muted-strong` に割れていた）。次の6段に固定する。

| 段 | 役割 | サイズ | 太さ | 色 | 行送り | どこに使うか |
|---|---|---|---|---|---|---|
| **T1** | 面の見出し・帯の主文 | `--size-md` | 700 | `--ink` | 1.5 | `.request-flow__heading`（アンケート／メール／支援の段の見出し）、**`.request-band__lede`（要望の帯の呼びかけの主文。2026-09-08 / T-061 で T3 から移した）** |
| **T2** | 面のラベル・受領 | `--size-sm` | 700 | `--signal` | 1.6 | `.explainer__label`、`.request-flow__done`、**`.request-band__received`（帯の受領メッセージ。T-061）** |
| **T3** | 本文・リード | `--size-sm` | 400 | `--muted-strong` | 1.8 | `.explainer__body`、`.notice`、`.request-flow__lede`、`.request-flow__thanks`（**`.request-band__lede` は T-061 で T1 へ移した**） |
| **T4** | 条件の注記 | `--size-sm` | 400 | `--muted-weak` | 1.8 | `.request-band__note`（「登録は不要です」）、**`.request-band__micro`（ボタンの直上のマイクロコピー。T-061）** |
| **T5** | 微注記 | `--size-xs` | 400 | `--muted-weak` | 1.7 | `.request-form__note`（**`.notice__beta` は例外。下記**） |
| **T6** | 入力のラベル | `--size-sm` | 700 | `--ink` | 1.6 | `.request-form__legend` |

**🔒 T-061 の追加 —— 帯の主文は T1、ボタンを挟む2つの条件は T4 で揃える。**
要望の帯（`.request-band`）だけは、**面の中に「その面の頭」が要る**。
リード文が周りの注記と同じ 12.5px だと、帯の中で**どれが呼びかけの本体かが決まらない**
（PO 差し戻し3回目の指摘。§4.0）。段の見出しと同じ T1 を当てて、帯の頭であることを
字送りで宣言する。**新しい段（T0 など）は作らない** —— 「面の頭」という役割は
T1 が既に持っている。
そして**マイクロコピー（`.request-band__micro`）と注記（`.request-band__note`）は
同じ T4 にする。** 2つはどちらも「ボタンを押す条件」であり、役割が同じものは
同じ値を持つ（この節の原則そのもの）。**片方だけ弱めない。**

**T3 と T4 は色だけが違う。** サイズを落として区別しない。
「押すのに対価が要らない」という条件は、リード文と同じ大きさで読めなければ意味がない
（🔒 `src/templates/request.js`「注記をボタンから離さない」と同じ理由）。

**T1 は画面幅で変えない。** 段の見出しは常に 18px。段ごとに見出しの大きさが変わらないことが、
完了条件 B-4 が求めているものである。

**🔒 T5 の例外 —— `.notice__beta` はサイズを落とさない**（2026-09-08 / T-053 レビュー R-053-7）。
この文（`products.betaNoData`）は「他ストアの価格は表示例である」ことを画面に明示する
景表法まわりの断り書きで、`.claude/rules/pergram-code-invariants.md` が表示を義務づけている。
**T3 と同じ `--size-sm`（行送りも親の `.notice` と同じ 1.8）に留め、弱さは色
（`--muted-weak`）だけで付ける。** 迷ったらより保守的な方へ倒す（`CLAUDE.md`）。

**T1〜T6 は、サイズ・太さ・色・行送りを CSS 側で明示する。** 継承任せにした段があると、
親の字送りを変えたときにその段だけ黙って追随し、**「同じ役割の文字が段ごとに違う値を持つ」
という今回直した状態へ戻る**（T-053 レビュー R-053-9 で T6 が継承のままだった）。

**見出しと中身のあいだは、段によらず `--space-6`。** リード文（T3）を持つ段では
見出し → 8px → リード文 → 24px → 中身、持たない段（アンケート）では
見出し → 24px → 中身。`.request-flow__heading` の下余白は既定を 24px にし、
リード文が続くときだけ `:has(+ .request-flow__lede)` で 8px へ詰める
（T-053 レビュー R-053-8）。

**変更点（現状 → この階層）**

| 対象 | 現状 | 変更後 |
|---|---|---|
| `.request-flow__done` | 16px / 500 / `--ink` | **T2**（`--size-sm` / 700 / `--signal`） |
| `.request-flow__lede` | `--size-sm` / `--muted` | **T3**（`--muted-strong`） |
| `.notice` | `--size-sm` / `--muted` / 1.85 | **T3**（`--muted-strong` / 1.8） |
| `.explainer__body` | `--size-sm` / `--muted-strong` / 1.8 | **T3**（変更なし） |
| `.request-band__note` | `--size-sm` / `--muted-weak` | **T4**（変更なし） |
| `.request-form__note` | `--size-xs` / `--muted-weak` / 1.7 | **T5**（変更なし） |

`.request-flow__done` を `--signal` にするのは、要望ボタンが押下後に取る色
（`.is-received` の `--signal` の文字）と揃えるためである。**受け取ったことを示す色は
画面内で1つ**にする。

---

### 4. 要望の帯の組版（2026-09-08 / T-061 で全面改訂）🔒

**この節は T-057 が書いた同じ番号の節を置き換える。** T-057 の組版（帯の幅で1列と2列を
切り替え、2列では左に読ませる列・右に CTA 列）は**幅ごとの値としては全て成立していたが、
PO の差し戻しを受けた。** 3回目である（T-053 → T-057 → T-061）。以下はその作り直しで、
**値ではなく要素同士の関係を設計している。**

#### 対象範囲

`.request-band` / `.request-band__inner` と、その中の5要素
（`.request-band__lede` / `.request-band__micro` / `.request-band__received` /
`.request-band__button` / `.request-band__note`）の**構図・字送り・間隔・状態**。
製品一覧と LP の両方。

**カバーしない範囲** — 段（アンケート・メール・支援）の中身と組版、帯の面の値
（角丸・余白・背景。§1 と §2 が持つ）、LP の帯見出し `.waitlist-band__heading`
（`lp.css` / 中央そろえ・21px→30px）、`.explainer` と `.notice` の組版、
GA4 のイベント名と `data-cta` の値。

#### 前提・制約 — 実測値（これが崩れたら本節の設計は無効になる）

Chrome ヘッドレス（旧 `--headless`）＋ CDP、`.preview` の `/ja/protein/` と `/ja/`。
**`document.fonts.ready` を待ってから測定した。** 待たずに測ると文字幅が約 16% 細く出る
（T-053 / T-057 / T-061 で3回とも踏みかけている）。モバイル幅は
`Emulation.setDeviceMetricsOverride`（`mobile: true`）。

| 対象 | 帯の外形 | 中身が使える幅 |
|---|---|---|
| 製品一覧 1440px | 1056px | 1006px |
| 製品一覧 1280px | 936px | 886px |
| 製品一覧 1024px | 680px | 630px |
| 製品一覧 900px | 556px | 506px |
| 製品一覧 768px | 736px | 702px |
| 製品一覧 390px | 358px | 324px |
| 製品一覧 320px | 288px | 254px |
| LP（768px 以上で共通） | 640px | 590px |

**幅は単調ではない。** 768px の帯（736px）は 900px の帯（556px）より**広い**。
900px を境に主カラムの左右が変わるためである。**「画面幅が広い＝帯が広い」と考えて
分岐を引くと必ず外す。**

#### 4.0 なぜ壊れていたのか — 実測による診断 🔒

PO の言葉は「文章とボタンの位置がバラバラ」である。**主観ではない。実測すると、
5要素の横位置を決めている規則が3つあった。**

| 要素 | 横位置を決めていたもの | 1440px での左端 |
|---|---|---|
| リード文 | 2列グリッドの左列（`max-content`） | x = 365 |
| ボタン | 2列グリッドの右列（`--band-cta-max` = 416px） | x = 774.25 |
| 注記 | 「ボタンの直下」＝右列の2行目 | x = 774.25 |

**軸が2本ある**（365 と 774.25。409px 離れている）。さらに `align-items: center` により、
**リード文の上端はボタンの上端より 27.93px 下**にあった（実測 lede y=479.2 / button y=451.27）。
リード文はボタン＋注記の高さに対して**垂直中央に浮いていた**。

**「どれが呼びかけの本体か分からない」の実測上の正体はこの2つである。**
横に2本、縦に浮き1つ。読む人の視線が置く場所が3つできる。

同時に、**LP と製品一覧で軸の作り方そのものが割れていた。**
製品一覧は左そろえ（3要素の左端が一致、中央はバラバラ）、
LP は中央そろえ（3要素の中央が一致、左端は x=203.38 / 176 / 216.5 とバラバラ）。
**同じ部品が、ページによって別の軸で組まれていた。**

**T-053 も T-057 も、直したのは値（角丸・余白・列幅・折り返し）である。**
値は毎回正しくなり、毎回差し戻された。**直っていなかったのは「横位置を決める規則が
いくつあるか」だった。**

#### 4.1 構図 — 一列・一版面・一軸 🔒

**構図は1列。全ての幅で、両ページで、例外なく。**

**軸を作るのは `text-align` ではなく、箱の幅である。** これが本節の中心である。

```
5要素すべてを「同じ幅・同じ位置の箱」にする
  → 中の文字がどちらへ寄っていても、左端も中央も自動的に一致する
```

具体的には、5要素すべてに `width: 100%` と `max-width: var(--band-measure)` を与える。
どの要素も `min(帯の内寸, 416px)` という**同一の幅**を取り、同一の位置に置かれる。

**なぜこれで「軸が1本」になるのか。** 横位置を決める規則が**構造として1つしか存在しない**
からである。列は1本、版面は1つ、そろえは1方向。§4.0 で数えた3つの規則
（列割り・列の上限・直下に付く）は、**書ける場所ごと消えている。**
値を揃えて軸を1本に「する」のではなく、**軸が2本になりうる場所を無くしている。**

**副産物として、B-1 の2つの読み（左端の一致／中央の一致）が同時に満たされる。**
箱が同じ幅で同じ位置なら、左端も中央も右端も一致する。実測（§4.7）で
**7幅すべて・両ページ・両状態で `xSpread = 0` かつ `cxSpread = 0`。**
製品一覧（左そろえ）と LP（中央そろえ）の差は**箱の中の `text-align` に閉じ込められ、
軸の本数に影響しない。**

**🔒 2列に戻さない。** 2列は「軸1本」と**構造的に両立しない** ——
リード文が第1列、ボタンが第2列にある限り、両者の左端は最低でも列間隔ぶん離れる。
`@container` も `@media` も使わない。この 🔒 を破る唯一の合図は
「PO が2列を明示的に指示したとき」であり、そのときは本節ごと書き直すことになる。

#### 4.2 要素の順序と役割 — 一つの頭・一つの行動・二つの条件 🔒

文書順は上から次の5つ。**この順序を入れ替えない**（完了条件 A-3 が
`マイクロコピー < ボタン < 注記` を文字列で検査する）。

| # | 要素 | 役割 | なぜそこにあるか |
|---|---|---|---|
| 1 | `.request-band__lede` | **頭** — 呼びかけの主文 | 帯の中でただ1つ「これは何の話か」を名乗る。押す理由（正式版を出すかどうかが、いま決まる）を1文で置く |
| 2 | `.request-band__micro` | **条件（上）** — 安全 | 「押しても安全」をボタンを読む視線の中で完結させる。匿名であることを言う |
| — | `.request-band__received` | **2 と入れ替わる** — 受領 | 押した後、条件は役目を終える。**同じ枠に**結果を出す（§4.6） |
| 3 | `.request-band__button` | **行動** — ただ1つ | 帯の中で唯一の操作。面としても最も強い |
| 4 | `.request-band__note` | **条件（下）** — 対価 | 「メールアドレスは要らない」。🔒 ボタンから離さない（`src/templates/request.js`） |

**上下の条件は同じ強さで組む（どちらも T4）。** ボタンは**同じ値の2行に上下から挟まれる**。
これが「ボタンは2つの条件に支えられている」という関係の視覚的な形である。
**片方だけ弱めたり大きくしたりしない。** 挟みが崩れると、上のマイクロコピーが
リード文の続きに見え、§4.0 の「断片が3つ」へ戻る。

**上の条件と下の条件は、違うことを言う。**
上＝**安全**（匿名であること）、下＝**対価**（メールアドレスが要らないこと）。
同じことを2回言うと、2行あることの意味が消えて1行の冗長になる。

#### 4.3 字送りの階層 — §3 のどこへ割り当てるか 🔒

**新しい段を作らない。** 5要素はすべて §3 の既存の段へ割り当てる。

| 要素 | 段 | 値 |
|---|---|---|
| `.request-band__lede` | **T1** | `--size-md`（18px）/ 700 / `--ink` / 1.5 |
| `.request-band__micro` | **T4** | `--size-sm`（12.5px）/ 400 / `--muted-weak` / 1.8 |
| `.request-band__note` | **T4** | 同上（**マイクロコピーと同値**） |
| `.request-band__received` | **T2** | `--size-sm` / 700 / `--signal` / 1.8 |
| `.request-band__button` | — | `.btn--signal` のまま。`min-height: var(--tap-target)` |

**リード文を T3 から T1 へ上げるのが、この節で唯一の字送りの変更である。**
根拠は3つ。

1. **帯には「面の頭」が要る。** T1 は段の見出しに使っている段で、役割は「その面の頭」である。
   帯のリード文はまさにそれであり、**新しい役割ではない**
2. **T-057 は同じ変更を「左列を埋めるため」として却下し、判断そのものは申し送りにしていた**
   （T-057 §4.11 最終行）。列が消えた今、問いは「列を埋めるか」ではなく
   「帯の頭は何か」に変わっている。**別の問いなので、別の答えでよい**
3. **`.claude/rules/pergram-ui-copy.md` の「本文 16px 未満にしない」へ1歩近づく。**
   §4.14 が未解決として挙げていた食い違いのうち、**帯の主文の1件だけが解消する**
   （残りは字送りの階層そのものを見直す別タスクの領分。ここでは触らない）

**受領メッセージが T2（`--signal`）なのは、`.request-flow__done` と揃えるためである。**
🔒 §3 の「受け取ったことを示す色は画面内で1つ」。ボタンの `.is-received` が取る色とも揃う。

#### 4.4 間隔の設計 — 段落の切れ目は1つだけ 🔒

```
リード文
   ↓ 24px（--space-4 の下余白 + 行間 8px）… 段落の切れ目。ここだけが広い
マイクロコピー（押下後は受領メッセージ）
   ↓ 8px
ボタン
   ↓ 8px
注記
```

**帯の中で広い間隔は1箇所しかない。** 上が「なぜ」、下が「いま押すもの」である。
**差が意味を作る** —— 24 対 8 の比が、4要素を「1 + 3」の2つの塊に分ける。
等間隔に置くと、5つの独立した行になり、§4.0 の「断片」へ戻る。

**マイクロコピーとボタンの間隔（8px）は、注記とボタンの間隔（8px）**と同じにする
（完了条件 B-5 は「同じか小さい」）。**上下の条件が対称に挟む**という §4.2 の関係を、
間隔でも表す。**片方だけ広げない。**

**実装は `row-gap: var(--space-2)`（8px）を既定にし、リード文にだけ
`margin-block-end: var(--space-4)`（16px）を足して 24px を作る。**
5要素それぞれに margin を配ると、要素を1つ足したときに間隔の規則が崩れる。

#### 4.5 折り返しの制御 — 句読点折り返しを引き継ぐ 🔒

**T-057 §4.3 の指定（`word-break: keep-all` + `overflow-wrap: break-word`）を
そのまま引き継ぐ。** 日本語を「、」「。」の後でしか折り返させないので、
最終行が1〜2文字になることが構造的に起きない。**対で置く。片方だけにしない。**
`text-wrap: balance` / `pretty` は日本語では解決しない（T-057 で実測済み）。

**適用先は T-061 で2つ増える** —— リード文・注記に加えて
**マイクロコピーと受領メッセージ**にも当てる。ボタンのラベルと段の中の文には広げない。

**🔒 帯の本文の1区切り（句読点から句読点まで）は 17 文字以内に収める。**
最も狭い測定幅（320px）で中身が使える幅は **254px**、`--size-sm` は 12.5px なので
1行に入るのは 20 文字。17 文字（212.5px）なら 41px の余裕が残る。
**18 文字を超える区切りを書くと、入らない幅で `overflow-wrap` が語中で折り、
孤立行（完了条件 B-4）が生まれうる。**
リード文は `--size-md`（18px）なので、**1区切り 13 文字以内**（254 / 18 = 14.1）。

| 文言 | 区切りごとの文字数 | 判定 |
|---|---|---|
| リード文 `request.lede` | 12 / 9 | ≤13 ✓ |
| マイクロコピー `request.micro` | 13 / 17 | ≤17 ✓ |
| 注記 `request.note` | 16 / 11 | ≤17 ✓ |
| 受領 `request.receivedMessage` | 11 / 16 | ≤17 ✓ |

**文言を書き換えるときは、この表を引き直すこと。**

#### 4.6 状態 — 未押下 / 受領 / 押し直し 🔒

| 状態 | 見た目 |
|---|---|
| **未押下** | リード文 / マイクロコピー / 橙の面のボタン（`--signal` 地・白文字）/ 注記。受領メッセージは `hidden` |
| **押下直後（受領）** | **マイクロコピーが `hidden` になり、同じ枠に受領メッセージが出る**（T2 / 橙 / 700）。ボタンは `.is-received`（白地・橙の文字・橙の枠）へ退き、ラベルが受領の語に変わる |
| **押し直し** | **何も起きない。** 受領メッセージが既に出ていれば書き換えない |

**🔒 受領メッセージは「マイクロコピーと入れ替える」。別の場所へ足さない。**
理由は2つ。

1. **押した瞬間にボタンが動かない。** 帯の高さが変わらないので、押下点がずれない。
   実測（§4.7）で **7幅すべて・両ページで、未押下と受領の帯の高さが完全に一致**する
2. **視線がすでにその行にある。** マイクロコピーはボタンを押す直前に読まれた行である。
   条件が役目を終えた場所に結果が出るのが、読む順として最も短い

**🔒 押し直しで受領メッセージを書き直さない。** `role="status"` は**中身が変化したときに
読み上げられる**ので、同じ文を書き直すと支援技術が2度読む。実装は
「受領メッセージが既に見えていれば何もしない」で守る。
**あわせて、これは既存の 🔒 と対である** —— 決着の付いた段は開き直さない
（`stepState` が `settled`）ので、**空の器も復活せず `request_survey_view` も二重に飛ばない**
（完了条件 C-3 / T-053 B-5b の回帰）。**T-061 でここに手を入れる必要はない。**

**🔒 受領メッセージの文言は HTML に埋めず、押されたときに JS が書き込む。**
最初から入れて `hidden` を外すだけにすると、`role="status"` の読み上げが飛ぶ
（`.request-flow__done` と同じ理由。`src/assets/request.js` の `finishStep` にある ⚠️ と対）。
**器そのものは初期状態で `hidden`** なので、JS が動かない環境でも帯は壊れない（完了条件 C-5）。

**動きは不透明度だけ、160ms。** `opacity` はコンポジタで動く。位置を動かすと、
上下の要素が押されて §4.4 の間隔が一瞬崩れて見える。
**`prefers-reduced-motion: reduce` の対応を新しく書かない** ——
`src/styles/site.css` の末尾に全称セレクタの打ち消し
（`animation-duration: 0.001ms !important`）が既にあり、これが効く（完了条件 C-4）。
**2つ目の打ち消しを足すと、どちらが効いているか分からなくなる。**

#### 4.7 7幅の実測 — この設計を実ページへ流し込んだ結果

上記の CSS と文言を `.preview` の実ページへ流し込み、7幅・両ページ・両状態で測った値。
`document.fonts.ready` の後に測定。**上下2つの帯は全ての幅で同値だった。**

**軸（完了条件 B-1）**

| 画面幅 | 製品一覧 左端 | `xSpread` | `cxSpread` | LP 左端 | `xSpread` | `cxSpread` |
|---|---|---|---|---|---|---|
| 320 | 33 | **0** | **0** | 33 | **0** | **0** |
| 390 | 33 | **0** | **0** | 33 | **0** | **0** |
| 768 | 33 | **0** | **0** | 176 | **0** | **0** |
| 900 | 345 | **0** | **0** | 242 | **0** | **0** |
| 1024 | 345 | **0** | **0** | 304 | **0** | **0** |
| 1280 | 345 | **0** | **0** | 432 | **0** | **0** |
| 1440 | 365 | **0** | **0** | 512 | **0** | **0** |

`xSpread` は4要素の左端の最大差、`cxSpread` は中央の最大差（px）。
**受領状態でも同じ値である。** 製品一覧の左端は `.explainer` / `.p-item` / `h1` と同じ
ページの軸（320 / 340 + 面の余白）に一致する。

**間隔・寸法・折り返し（B-3 / B-4 / B-5）**

| 画面幅 | 要素の幅 | 間隔 | ボタン高 | リード文 | マイクロ | 注記 | 横溢れ |
|---|---|---|---|---|---|---|---|
| 320 | 254 | 24 / 8 / 8 | 47.88 | 2行（末尾9字）| 2行（17字）| 2行（11字）| **0** |
| 390 | 324 | 24 / 8 / 8 | 47.88 | 2行（9字）| 2行（17字）| 2行（11字）| **0** |
| 768 | 416 | 24 / 8 / 8 | 47.88 | 1行 | 1行 | 1行 | **0** |
| 900 | 416 | 24 / 8 / 8 | 47.88 | 1行 | 1行 | 1行 | **0** |
| 1024 | 416 | 24 / 8 / 8 | 47.88 | 1行 | 1行 | 1行 | **0** |
| 1280 | 416 | 24 / 8 / 8 | 47.88 | 1行 | 1行 | 1行 | **0** |
| 1440 | 416 | 24 / 8 / 8 | 47.88 | 1行 | 1行 | 1行 | **0** |

LP はボタン高が 44px（`.btn` の余白が製品一覧と違う）。いずれも 🔒 44px 以上。
**最終行が1〜2文字になる幅は無い**（最短で9字）。

**押下前後の帯の高さ（C-1 の副次条件 — 押しても動かない）**

| 画面幅 | 製品一覧 未押下 / 受領 | LP 未押下 / 受領 |
|---|---|---|
| 320 / 390 | 265.9 / **265.9** | 262 / **262** |
| 768 | 193.9 / **193.9** | 190 / **190** |
| 900 / 1024 / 1280 / 1440 | 209.9 / **209.9** | 206 / **206** |

**⚠️ 例外が1つある。** 画面幅 **404〜439px**（製品一覧）では、マイクロコピーが2行・
受領メッセージが1行になり、押下で帯が **22.5px 縮む**。マイクロコピー（30字 = 375px）と
受領メッセージ（27字 = 337.5px）の実寸差が、この幅の窓に落ちるためである。
**完了条件の7幅には含まれない。** 閉じたければ2つの文の実寸を揃える（字数を合わせる）。
**閉じるために文言の意味を削るほどのものではない**と判断した。

#### 4.8 縦に伸びること — 何を捨てて何を取ったか 🔒

**1列にしたので、帯は縦に伸びる。** これは 🔒 として記録する
（次に触る担当が「縦長だから2列へ」と戻さないため）。

| 画面幅 | T-057（現行） | T-061（本節） | 差 |
|---|---|---|---|
| 製品一覧 1440 | 128.4（2列）| **209.9** | +81.5 |
| 製品一覧 1280 | 128.4（2列）| **209.9** | +81.5 |
| 製品一覧 1024 | 166.9 | **209.9** | +43.0 |
| 製品一覧 900 | 166.9 | **209.9** | +43.0 |
| 製品一覧 768 | 150.9 | **193.9** | +43.0 |
| 製品一覧 390 / 320 | 195.9 | **265.9** | +70.0 |
| LP 1440 | 163 | **206** | +43.0 |

**+43px の内訳は、増えた要素そのものである** —— マイクロコピー1行（22.5px）＋
間隔（8px）＋リード文が 18px になったぶん（+12.5px）。
**1440 / 1280 の +81.5px のうち 38.5px だけが「2列をやめた代償」**であり、
残りはマイクロコピーと主文の追加ぶんである。

**捨てたもの** — 1440px で帯が横に埋まること。1列では中身の右に 590px の淡橙が残る。
**取ったもの** — 軸が1本であること。

**この2つは両立しない。** 帯を横に埋めるには、短い文と操作を横に並べるしかなく、
並べた瞬間に左端が2箇所へ分かれる。**T-053 と T-057 は横の充填を取り、2回とも
差し戻された。** 3回目に同じ側を取る根拠が無い。

**残る余白は穴ではなく版面の上限である。** `.request-band__inner` は
`.explainer` と同じ幅の面のままで（§1 の「A と B は形が同じで色だけが違う」を守る）、
その中の版面が 416px で止まる。**面を中身の幅まで縮めない** —— 縮めると帯だけが
周りの箱より狭くなり、面の体系が崩れる。

#### 4.9 LP（`/{locale}/`）

**LP 側に新しい指定は要らない。** 既存の `lp.css` の
`justify-items: center` / `text-align: center` がそのまま効き、
§4.1 の「同じ幅の箱」により**左端も中央も一致する**（§4.7 で実測）。

**⚠️ ただし `lp.css` から1行だけ出す必要がある。**
`.waitlist-band .request-band { margin-top: 0; }` は `.request-band` に対する
`margin` の宣言であり、完了条件 B-6（`lp.css` に帯の幾何の宣言が0件）に当たる。
**`site.css` へ移す。** 移し先でも同じセレクタのままでよい（`.waitlist-band` は
製品一覧に存在しないので無害である）。
あわせて `lp.css` 内の `--band-cta-max` に言及するコメントを `--band-measure` へ直す。
**これは `lp.css` を触る唯一の変更であり、宣言を増やす方向ではなく減らす方向である。**

#### 4.10 文言（ja / en）🔒

**変える理由。** 旧 CTA「成分・製品の追加をリクエスト」は**サービス側の都合**
（何を掲載するか）を名乗っており、押す人が得るものが無い。
**押す人の意思表示そのものを名乗る形へ変える**（T-061 決定ログ）。

| キー | ja | en |
|---|---|---|
| `request.lede` | 正式版を出すかどうかを、いま決めています。 | We are deciding whether to build the full version. |
| `request.micro` **新** | 匿名で1件として数えます。個人を特定できる情報は残しません。 | Counted anonymously. We keep nothing that identifies you. |
| `request.cta` | 正式版のリリースを希望する | I want the full version |
| `request.note` | （変更なし）メールアドレスの登録は不要です。押すだけで送信します。 | （変更なし）No email address and no sign-up needed. |
| `request.received`（ボタンのラベル） | ご希望を受け取りました | Thanks, noted |
| `request.receivedMessage` **新** | ありがとうございます！開発の参考にさせていただきます。 | Thank you. This helps us decide what to build next. |
| `request.headerCta` | 正式版を希望する | I want the full version |
| `request.headerCtaShort` | 希望する | I want it |
| `lp.features.bannerText` | 気になる成分・機能がまだ非対応？何を次に載せるかは、リクエストの数で決めています。 | Nutrient you care about not covered yet? Requests decide what we publish next. |

**🔒 「応援」を第1段階（無償）に使わない。** 禁止事項は「第1段階と支援段が別の語を
名乗ること」を求めているが、**有償側は3箇所で「応援」を使っている** ——
`request.supportHeading`（開発を応援する）、`lp.support.buttonText`（📣開発を応援する📣）、
`lp.support.message`（本文中に「開発を応援したい！」）。後ろ2つは Codoc のウィジェットへ渡す
PO 執筆のコピーである。**動かす側を無償にするほうが、触る面が小さく、
課金導線との誤認も起きない。** よって PO 提示の「応援する」ではなく「希望する」を採る。
**有償側（`request.supportHeading` / `lp.support.*`）は1文字も変えない。**

**🔒 上下の条件は違うことを言う**（§4.2）。マイクロコピーは**匿名であること**だけを言い、
注記は**メールアドレスが要らないこと**だけを言う。**どちらかを書き換えるときは、
もう片方と意味が重ならないか必ず確かめる。**

**🔒 「匿名で」「個人を特定できる情報は残しません」は事実である。** 押下が残すのは
`request_signal` の3列（ブラウザが作る UUID v4 / 日時 / どのページか）だけで、
IP・User-Agent・リファラ・成分・自由記述を持たない（`worker/request_signal.js` が正典、
`.claude/rules/pergram-compliance.md`）。**保持する列を増やしたら、この文言も見直す。**

**書かないもの（禁止事項の再掲）**

- **「優先的に対応を進めます」を使わない。** 優先順位を約束する事実がサービス側に無い
- **数を出さない**（押下数・人数・「◯人が応援」）。景表法の「人気」表示（N-03）
- **禁止語**: 効く / 効果 / 改善 / 高品質 / おすすめ / 人気No.1 / 安心 / 話題の / 選ばれる / 実感
- **絵文字を足さない。** PO 提示の候補にあった ✨ は採らない —— 帯の他の文が事実の
  平叙文で、祝祭の記号だけが浮く。手応えは**ボタンの面の反転と橙の受領文**が作る

#### 4.11 CSS の置き場（実装への指示）

| ファイル | 何をするか |
|---|---|
| `src/styles/tokens.css` | `--band-cta-max: 26rem` を **`--band-measure: 26rem` へ改名**（値は同じ）。🔒 コメントを差し替える（切り替え点 56rem との連動は消える）。**他の既存の値は1つも変えない** |
| `src/styles/site.css` | 帯の全定義。**唯一の出所。** `container: request-band / inline-size` と `@container` ブロックを**削除**し、下の断片へ置き換える |
| `src/styles/lp.css` | 3点だけ。(1) `.waitlist-band .request-band { margin-top: 0 }` を **`site.css` へ移す**（§4.9。完了条件 B-6）。(2) 688行の `--band-cta-max` を `--band-measure` へ、参照先 `§4.6` を `§4.11` へ直す。(3) 681行の参照先 `§4.8` を `§4.9` へ直す。**宣言を増やさない** |
| `src/styles/products.css` | **宣言は1つも足さない**（`.request-band*` は現在0件。その状態を保つ）。1735行の 🔒 コメントだけ直す —— 参照先を `§4.1 / §4.11` にし、**「1列と2列の切り替えは帯の幅（@container）で判定する」の一文を消す**（2列そのものが無くなった。残すと次の担当が `@container` を探す） |
| `src/templates/request.js` | 5要素のマークアップ（§4.12） |
| `src/assets/request.js` | `markReceived()` の拡張だけ（§4.12）。段の開閉には触らない |

```css
/* ---- 要望の帯（T-061 / design.md §4）------------------------------
 * 🔒 帯の幾何を決める値は **ここだけ**。products.css / lp.css へ書かない。
 *    この 🔒 は T-051 → T-053 → T-057 と3回破られている。4回目を作らない。
 */
.request-band {
  margin-block-start: var(--space-8);
}
/* 🔒 LP の帯は器そのものが余白（44px / 96px）を持つので、ここでは重ねない。
      **この1行を lp.css へ戻さない**（完了条件 B-6 が margin の宣言を数える）。 */
.waitlist-band .request-band {
  margin-block-start: 0;
}

.request-band__inner {
  display: grid;
  /* 🔒 1列で固定する。2列は「軸が1本」と**構造的に両立しない**（design.md §4.1）。
     リード文が第1列・ボタンが第2列にある限り、左端は列間隔ぶん必ず離れる。
     @container も @media も足さない。 */
  grid-template-columns: minmax(0, 1fr);
  justify-items: stretch;
  /* 🔒 align-items: center を使わない（完了条件 B-2）。リード文がボタンの高さに対して
     垂直中央へ浮くのが、PO の言う「位置がバラバラ」の実測上の正体だった（§4.0）。 */
  align-items: start;
  /* 🔒 既定の間隔は 8px。広いのはリード文の下だけで、そこは margin が作る（§4.4）。
     ここを 16px 等へ上げると「1 + 3」の塊が崩れ、5つの独立した行に戻る。 */
  row-gap: var(--space-2);
  column-gap: 0;
}

/* 🔒 **軸を作るのは text-align ではなく箱の幅である**（design.md §4.1）。
      5要素すべてを同じ幅・同じ位置の箱にすると、中の文字がどちらへ寄っていても
      左端も中央も自動的に一致する。製品一覧（左そろえ）と LP（中央そろえ）の差は
      箱の中に閉じ込められ、軸の本数に影響しない。
      **この5つから1つでも外さないこと。** 外した要素だけが別の幅を持ち、軸が2本になる。 */
.request-band__lede,
.request-band__micro,
.request-band__received,
.request-band__button,
.request-band__note {
  width: 100%;
  max-width: var(--band-measure);
}

/* T1（§3）。帯の中でただ1つの主文。🔒 T3（12.5px）へ戻さない —— 戻すと帯の中で
   「どれが呼びかけの本体か」が決まらなくなる（PO 差し戻し3回目の指摘。§4.0 / §4.3） */
.request-band__lede {
  margin: 0 0 var(--space-4);
  font-size: var(--size-md);
  font-weight: 700;
  line-height: 1.5;
  color: var(--ink);
  /* 🔒 keep-all と overflow-wrap は対で置く（design.md §4.5）。片方だけにしない */
  word-break: keep-all;
  overflow-wrap: break-word;
}

/* T4（§3）。ボタンを上下から挟む2つの条件。🔒 **同じ値にする**（design.md §4.2）。
   片方だけ弱めると、上のマイクロコピーがリード文の続きに見える */
.request-band__micro,
.request-band__note {
  margin: 0;
  font-size: var(--size-sm);
  font-weight: 400;
  line-height: 1.8;
  color: var(--muted-weak);
  word-break: keep-all;
  overflow-wrap: break-word;
}

/* T2（§3）。🔒 受け取ったことを示す色は画面内で1つ（--signal）。
   🔒 マイクロコピーと**入れ替わりで同じ枠に出す**。別の場所へ足すと帯が伸び、
      押した瞬間にボタンが動く（design.md §4.6）。
   🔒 prefers-reduced-motion の打ち消しをここに書かない。site.css 末尾の
      全称セレクタの打ち消しが効く。2つ目を足すとどちらが効いているか分からなくなる */
.request-band__received {
  margin: 0;
  font-size: var(--size-sm);
  font-weight: 700;
  line-height: 1.8;
  color: var(--signal);
  word-break: keep-all;
  overflow-wrap: break-word;
  animation: request-band-receipt 160ms ease-out both;
}
@keyframes request-band-receipt {
  from { opacity: 0; }
  to { opacity: 1; }
}

.request-band__button {
  /* 🔒 タップ領域 44px 以上。幅は上の5要素共通の規則が持つ */
  min-height: var(--tap-target);
}
/* 押した本人へのフィードバック。数は出さず、面を退かせて「受け取った」だけを示す */
.request-band__button.is-received {
  background: var(--surface);
  color: var(--signal);
  border-color: var(--signal);
}
```

```css
/* tokens.css — 差し替えるブロック */
  /* 要望の帯の版面（T-061 / design.md §4.1）。
     🔒 帯の5要素（リード文・マイクロコピー・受領・ボタン・注記）が**共有する箱の幅**である。
        ボタンだけの上限ではない。**5要素の全部に当てることが「軸が1本」の仕掛けそのもの**で、
        1つでも外すとその要素だけ別の幅になり、軸が2本になる。
     🔒 これ以上広げるとボタンが帯そのものより強い面になり「帯 > ボタン」が逆転する。
     ⚠️ 旧名は --band-cta-max（T-057）。2列の CTA 列の上限という意味だったが、
        2列そのものが無くなったので改名した。値は変えていない。 */
  --band-measure: 26rem;
```

#### 4.12 マークアップと挙動（実装への指示）

```html
<!-- src/templates/request.js — requestCta() -->
<section class="request-band">
  <div class="request-band__inner">
    <p class="request-band__lede">{{request.lede}}</p>
    <p class="request-band__micro" data-request-micro>{{request.micro}}</p>
    <!-- 🔒 中身は空で出す。文言は押されたときに JS が書き込む（design.md §4.6）。
         最初から入れて hidden を外すだけだと role="status" の読み上げが飛ぶ。
         🔒 初期状態は hidden。JS が動かない環境でも帯は壊れない（完了条件 C-5） -->
    <p class="request-band__received" role="status" data-request-received
       data-message="{{request.receivedMessage}}" hidden></p>
    <button class="btn btn--signal request-band__button" type="button"
      data-request-cta data-cta="{{location}}"
      aria-controls="request-step-survey" aria-expanded="false"
      data-label-received="{{request.received}}">{{request.cta}}</button>
    <p class="request-band__note">{{request.note}}</p>
  </div>
</section>
```

**🔒 `data-cta` の値（`list-top` 等）と `aria-controls` の値を変えない。**
GA4 の前後比較が切れる（T-058 と同じ理由）。

```js
// src/assets/request.js — markReceived() を広げる。他の関数には触らない
function markReceived(button) {
  const received = button.dataset.labelReceived;
  if (received) button.textContent = received;
  button.classList.add('is-received');
  button.setAttribute('aria-expanded', 'true');

  // 🔒 受領は**マイクロコピーと入れ替える**。別の場所へ足すと帯が伸び、
  //    押した瞬間にボタンが動く（design.md §4.6）
  const band = button.closest('.request-band');
  if (!band) return;
  const slot = band.querySelector('[data-request-received]');
  // 🔒 既に出ていれば何もしない。role="status" は中身が変化したときに読み上げるので、
  //    押し直しで同じ文を書き直すと支援技術が2度読む（完了条件 C-3）
  if (!slot || !slot.hidden) return;
  const micro = band.querySelector('[data-request-micro]');
  if (micro) micro.hidden = true;
  // 🔒 文言はここで書き込む（finishStep の ⚠️ と同じ理由）
  slot.textContent = slot.dataset.message || '';
  slot.hidden = false;
}
```

**段の開閉（`openStep` / `collapseStep` / `finishStep` / `stepState`）には触らない。**
押し直しで空の器が復活しないことも、`request_survey_view` が二重に飛ばないことも、
既存の `settled` の 🔒 が守っている（完了条件 C-3）。

#### 4.13 採用しなかった案

| 案 | なぜ負けたか |
|---|---|
| **2列のまま、左列の中身を上そろえ（`align-items: start`）にする** | 縦の浮きは消えるが**軸は2本のまま**。完了条件 B-1 は左端の一致を求めており、列が2つある限り満たせない。**PO の指摘は「位置がバラバラ」であって「高さがずれている」ではない** |
| **2列にして、リード文も右列へ入れる（左列は空ける）** | 軸は1本になるが、左列が丸ごと死んだ空白になる。T-057 が消したはずの穴が、もっと大きく戻る |
| **リード文だけ帯の外（見出しの位置）へ出す** | 製品一覧には帯の見出しが無い。LP にだけある見出しと二重になり、ページごとに構造が割れる（§4.0 で数えた「ページで軸が違う」を別の形で再発させる） |
| **箱の幅を揃えず、`text-align` だけで軸を作る** | 製品一覧（左）と LP（中央）で**満たす条件が変わる**。B-1 の「混在は不可」の解釈をテストへ持ち込むことになり、判定が主観になる。箱を揃えれば両方が同時に満たされる（§4.1） |
| **面（`.request-band__inner`）を中身の幅まで縮める** | 帯だけが `.explainer` より狭くなり、§1 の「A と B は形が同じで色だけが違う」が崩れる。面の体系は T-053 が直した本体であって、ここで削る理由が無い |
| **`--band-measure` を広げて 1440px の余白を減らす** | 中身の実寸（リード文 336px / マイクロコピー 375px）が短いので、**版面を広げても中身は伸びない。** 伸びるのはボタンだけで、416px を超えると「帯 > ボタン」が逆転する |
| **1440px だけリード文を `--size-lg` へ上げて横を埋める** | 「同じ役割の文字が幅で違う値を持つ」——§3 が T-053 で直した状態そのものへ戻る。埋めるために階層を崩す取引は割に合わない |
| **受領メッセージを注記の下に足す** | 押した瞬間に帯が 22.5px 伸び、**ボタンが下へ動く**。連打・誤タップの原因になる。入れ替えなら高さが変わらない（§4.6・実測済み） |
| **受領をトースト（画面隅の浮き）で出す** | 押下点から遠く、視線が3つ目の場所へ飛ぶ。**この帯の問題がまさにそれだった** |
| **CTA を PO 提示のまま「応援する」にする** | 有償の支援段が3箇所で「応援」を使っており、**課金導線と誤認されうる**（禁止事項）。有償側は Codoc へ渡す PO 執筆のコピーなので、動かす側を無償にした（§4.10） |
| **マイクロコピーで「1タップで終わります」と言う** | 注記の「押すだけで送信します。」と同じことを言う。2行あることの意味が消える（§4.2） |
| **受領メッセージに ✨ を足す** | 帯の他の文が事実の平叙文なので、祝祭の記号だけが浮く。`pergram-ui-copy.md` の「事実のみを書き、優劣を述べない」に寄せる |

#### 4.14 実装時の注意点

- **`--band-cta-max` の参照を残さない。** `site.css` / `lp.css` のコメントにもある。
  改名後に残ると、次に触る担当が存在しないトークンを探す
- **5要素の共通規則から要素を外さない**（§4.11 の 🔒）。ここが軸そのものである
- **`keep-all` を帯の4つの文（リード文・マイクロコピー・受領・注記）以外へ広げない。**
  ボタンのラベルと段の中の文には別の事情がある
- **文言を書き換えたら §4.5 の字数表を引き直す。** 1区切りが 18 文字を超えると、
  320px で語中折りが起き、孤立行が出うる
- **測定は `document.fonts.ready` を待つ。** 待たないと文字幅が約 16% 細く出て、
  「折り返さないはずの幅で折り返さない」と誤判定する。旧 `--headless` と
  絶対パスの `--user-data-dir`、モバイル幅は `Emulation.setDeviceMetricsOverride`
- 帯の面の値（角丸・padding・背景）には触らない。`--panel-*` 経由のまま

#### 4.15 未解決の不確実性

1. **英語（`/en/`）で実測できていない。** `.preview` は `/ja/` しか書き出さない。
   §4.5 の字数の上限は日本語の全角前提であり、英語は空白で折り返すので同じ形では効かない。
   **特に `request.headerCta` の "I want the full version"（23字）は旧
   "Request a feature"（17字）より長く、900〜1024px のヘッダで溢れうる。**
   溢れたときの退避先は "I want it"（`headerCtaShort` と同じ）。テストで確認すること
2. **受領の入れ替えで帯が 22.5px 縮む幅の窓がある**（製品一覧 404〜439px。§4.7）。
   完了条件の7幅には含まれない。閉じるならマイクロコピーと受領メッセージの実寸を揃える
3. **`--size-sm`（12.5px）と「本文 16px 未満にしない」（`pergram-ui-copy.md`）の食い違いは
   残っている。** 本節は帯の主文の1件だけを 18px へ上げた。
   **字送りの階層そのものを見直す別タスクが要る**（T-057 §4.14 から引き継ぎ）
4. **LP の帯見出し `.waitlist-band__heading` の孤立行**（「…決めて／います」）は未対応。
   `lp.css` の領分で、本節の範囲外

#### 4.16 この節で名前を付けたもの

| 語 | 意味 |
|---|---|
| **帯の版面** | 帯の5要素が共有する箱の幅。`--band-measure`（26rem / 416px）で止まる。**帯の面そのものの幅とは別物** |
| **軸** | 帯の要素の横位置がそろう線。左そろえなら左端、中央そろえなら中央。**1本であることが完了条件 B-1** |
| **頭 / 行動 / 条件** | 帯の5要素の役割の呼び分け。頭＝主文（1つ）、行動＝ボタン（1つ）、条件＝ボタンを挟む2つの文（§4.2） |
| **マイクロコピー** | ボタンの直上の条件文（`.request-band__micro`）。**安全**（匿名であること）だけを言う |
| **受領メッセージ** | 押下後にマイクロコピーと**入れ替わって**出る文（`.request-band__received`）。`role="status"` |
| **句読点折り返し** | `word-break: keep-all` + `overflow-wrap: break-word`。日本語を「、」「。」の後でしか折り返させない（T-057 から継承） |
| `--band-measure` | 帯の版面の幅。**旧 `--band-cta-max` の改名。値は 26rem のまま** |

---

### 5. 空の器を消す（完了条件 B-5）

**現象**: アンケートを「回答せずに進む」で飛ばすと、`collapseStep()` が中のフォームを
`hidden` にするだけなので、**枠と余白だけの白いカードが画面に残る**
（1280px の実画面で確認済み）。

**推奨案 — 段そのものを畳む（`src/assets/request.js`、2行）**

```
collapseStep(kind): form.hidden = true;  step.hidden = true;   // 器ごと畳む
finishStep(kind):   collapseStep(kind);  step.hidden = false;  // 完了文言を出すので器は残す
```

- `.request-flow` は `display: grid; gap` なので、`hidden`（= `display: none`）にすれば
  **段の間隔ごと消える**。CSS 側の追加は不要
- 既存テスト「飛ばした段でもフォームは畳まれ、次の段が開く」は
  `[data-request-survey]` の `hidden` と**次の段**の `hidden` を見ており、
  **飛ばした段そのものは見ていない**。テストを書き換えずに通る
- ⚠️ **`openStep()` が無条件に `step.hidden = false` を行うと、畳んだ器が空箱のまま
  開き直す**（要望ボタンを押し直したとき）。設計時は「壊れない」と書いていたが、
  実装後のレビューで**フォームも完了文言も隠れたままの高さ 50px の空箱**が
  復活することが実ブラウザで確認された（T-053 / R-053-1）。段は
  `idle` → `open` → `settled` の状態を持ち、**`settled` からは開き直さない**。
  閲覧イベント（`request_survey_view`）も `idle` から出るときだけ送る

**併せて必要な変更 — 先頭の段の上の余白**

現状 `.request-flow__step:first-child { margin-top: var(--space-8) }` が
「リストと段のあいだ」を作っている。**先頭の段（アンケート）を畳むと、
2番目の段（メール）はこの余白を持たない**（`:first-child` は構造上の指定で、
`display: none` を無視するため）。器の側へ移す。

```css
/* 🔒 器そのものに余白を持たせるのは、見えている段が1つ以上あるときだけ。
      全部閉じている初期状態で空白だけが残るのを避ける（T-050 の 🔒 の言い換え）。 */
.request-flow:has(> .request-flow__step:not([hidden])) {
  margin-top: var(--space-6);
}
.request-flow__step:first-child { margin-top: 0; } /* 旧指定は消す */
```

LP 側（`lp.css`）の `.waitlist-band .request-flow__step:first-child { margin-top: var(--space-6) }`
は、**移さずに削除した**（2026-09-08 / 実装時の判断。T-053 決定ログ
「LP の先頭の段の余白は `:has()` へ移さず、規則ごと削除する」）。移した後の値は
上の `.request-flow:has(...)`（`--space-6`）と**同値**であり、lp.css へ写すと
同じ値の出所が2つになる。lp.css 冒頭の 🔒 が「段そのものは site.css が唯一の出所。
ここへ写して上書きしない」と明記しており、**今回直している「出所が2つある」問題を
直した直後に作ることになる**。LP の実画面（1280px / 390px）で段の上の余白が
従来どおり出ることは確認済み。`:has()` が無い環境では余白が付かないだけで、崩れはしない。

**採らなかった案**

- **CSS だけで消す**（`.request-flow__step:has(> .request-form[hidden]):not(:has(...))`）
  → 二重否定の `:has()` は読めない。次に触る担当が意味を復元できない
- **飛ばしたときにフォームを元へ戻し、段を初期状態へ返す** → 一見きれいだが、
  既存テストが「飛ばしたらフォームは畳まれている」を検査している。
  仕様変更としてテストの書き換えが要り、視覚の修正に混ぜる変更ではない

**未解決（この設計では直さない）**: 段を畳んでも要望ボタンの `aria-expanded` は
`true` のままで、**開いていない領域を開いていると言っている**（WCAG 4.1.2）。
**状態機械の設計として別タスク（T-054）で扱う**。
なお「押し直すと空の段が開く」ほうは T-053 の実装で塞いだ（上の ⚠️）ので、
T-054 が扱うのは `aria-expanded` の値そのものだけである。

---

### 6. フォーカスリングの見せ方（完了条件 B-6）

**現象**: `.request-flow__heading` は `tabindex="-1"` で、段が開いたとき
`src/assets/request.js` がプログラム的にフォーカスを移す。見出しはブロック要素なので、
`:focus-visible` の輪郭が**段の全幅**に出て「壊れた要素」に見える。

**決定 — 可視性は落とさず、輪郭を文字に沿わせる**

```css
/* 🔒 段が開くとここへフォーカスが移る（src/assets/request.js）。
      ブロック要素のままだと段の全幅に枠が出て壊れて見える。
      **輪郭を消さない**（WCAG 2.4.7 / 2.4.11）。文字に沿わせるだけである。 */
.request-flow__heading {
  width: fit-content;
  max-width: 100%;
  scroll-margin-top: var(--head-offset);
}
.request-flow__heading:focus-visible {
  outline: 2px solid var(--signal);
  outline-offset: 4px;
  border-radius: var(--radius-interactive);
}

@media (min-width: 900px) {
  .request-flow__heading { scroll-margin-top: var(--head-offset-lg); }
}
```

- **太さ 2px を維持する。** 色は `--signal`。白い面（`--surface`）に対して 5.18:1 で、
  非テキストコントラストの下限 3:1 を満たす（根拠は `tokens.css` の 🔒 コメント）
- `outline-offset` を 2px から **4px** へ広げる。文字に沿った小さな枠になるので、
  近すぎると字に食い込む
- `border-radius` を付けて、輪郭を他の操作（ボタン・入力欄）と同じ丸みにする
- **`scroll-margin-top` を足す理由**: フォーカス移動と同時に
  `scrollIntoView({ block: 'start' })` が走るため、現状は**見出しがスティッキーヘッダの下に
  潜る**（WCAG 2.4.11 Focus Not Obscured）。B-6 の文言には無いが、同じ操作で起きる
  同じ壊れ方なので併せて直す

**採らなかった案**

- **フォーカスの対象を段の器（`.request-flow__step`）へ移す** → 枠がさらに大きくなる。逆効果
- **輪郭をやめて背景の色替えにする** → 装飾に見えるうえ、面の色を増やすことになる（役割は3つ）

---

### 7. 文言 — 礼を先に置く

**現状**: 要望ボタンを押した直後に出る最初の文が
「回答は任意です。リクエストはすでに受け取りました。」で、**礼を言う前に免責から入っている。**
押した人の行動を否定する語順になっている。

**決定**

| 操作 | キー | 値 |
|---|---|---|
| **追加** | `request.surveyThanks` (ja) | `リクエストありがとうございます。もしよければ任意で詳しくご要望をお聞かせください。` |
| **追加** | `request.surveyThanks` (en) | `Thanks for your request. Tell us more if you like — it is all optional.` |
| **削除** | `request.surveyLede` | ja / en とも**キーごと消す**（描画箇所も消す） |

- 「任意です」は**礼と依頼の中に溶かす**（「もしよければ任意で」）。
  独立した免責の一文として先頭に置かない
- 禁止語（効く / 効果 / 改善 / 高品質 / おすすめ / 人気No.1 / 安心 / 話題の / 選ばれる / 実感）
  を含まない。感嘆符を使わない
- `request.surveyLede` を消しても、アンケートが任意であることは
  「回答せずに進む」ボタンと `request.surveyThanks` の「任意で」が担う

**差し込む位置** — `src/templates/request.js` の `surveyStep()`。
**`<form>` を開いた直後、見出しより前**に置く。

```
<form class="request-form" data-request-survey novalidate>
  <p class="request-flow__thanks">…request.surveyThanks…</p>   ← 追加
  <h2 class="request-flow__heading" tabindex="-1">…request.surveyHeading…</h2>
  <p class="request-flow__lede">…request.surveyLede…</p>        ← この行ごと削除
  <fieldset …>
```

- フォームの中に置くので、送信・スキップでフォームが畳まれると礼の文も一緒に消える。
  **完了状態に礼の文が残らない**
- `.request-flow__thanks` の字送りは **T3**（本文・リード）。
  **新しい字送りの役割を作らない** — 受け取ったことは要望ボタンが
  「リクエストを受け取りました」へ変わることで既に示されており、二重に強調しない

**⚠️ 併せて必要な `src/assets/request.js` の変更（変更範囲の但し書きの拡張が要る）**

段を開くとき、現在は**見出しを画面上端へスクロールさせている**
（`bringIntoView(heading)`）。このままだと、**見出しより前に置いた礼の文が画面の外へ出る。**
完了条件 A-2 の文字列検査は通るが、**PO が求めた「礼を先に読ませる」は実現しない。**

```
heading.focus({ preventScroll: true });   // フォーカスは見出しへ（読み上げの起点は変えない）
bringIntoView(step);                      // 画面へ出すのは段の器（礼の文を含む）
```

`preventScroll` に対応しない環境では無視されて現状の挙動に戻るだけで、壊れない。
`.request-flow__step` にも `scroll-margin-top: var(--head-offset)` を付ける。

**T-053 の変更範囲は `src/assets/request.js` を「空の器が残らないようにする場合のみ」と
書いているため、この1点はオーケストレーターの承認が要る。**

---

### 8. 採用しなかった案（面の体系そのもの）

| 案 | なぜ採らなかったか |
|---|---|
| 4つのセクションの角丸・余白を目視で1つずつ合わせる | **次にセクションが増えたとき、また別の値が直書きされる。** 今回の食い違いはまさにそうやって生まれた。値を揃えても、値の**出所**が揃わなければ再発する |
| 角丸を 16px（`--radius-lg`）へ寄せる | 揃える先としては少数派。このページで最も多く出る箱（製品カード 6枚 + 解説）が 14px であり、2箇所のために8箇所を動かすことになる |
| `.notice` もカード（役割 A）にする | 縦に積まれる箱が3つに増える。広告表示は**本文と同じ強さで常時読める**ことが要件であって、箱に入っていることは要件ではない |
| 面の役割を4つにし、「注記の面」を灰色の箱として残す | 3つに収まるものを4つにすると、次の1つを足す言い訳になる。「役割の数を減らす」がこのタスクの解き方である（T-053 決定ログ） |
| 帯（役割 B）を上下で強弱を変える | **同じ部品が場所によって違って見える**のは、今回直している問題そのもの。位置の区別は `data-cta` が持っており、画面で区別する必要がない |
| 900px 以上でもボタンを中央寄せの積み重ねにする | 左揃えのデータ一覧の中に中央寄せの箱が入ると視線の軸が2本になる。ランディングページの構図をツールへ持ち込まない |
| `.surface` のような基底クラスを作り、テンプレートへ付与する | `.p-item` は T-053 の変更範囲外でクラスを足せない。**トークンで揃えれば、テンプレートを触らずに値の出所を1箇所にできる** |

### 9. 未解決の不確実性

1. **要望ボタンの `aria-expanded` が、段を畳んだ後も `true` のまま残る**（第5節）。
   状態機械の設計として別タスクで扱う
2. **`src/assets/request.js` のスクロール対象の変更**（第7節）が T-053 の変更範囲の
   但し書きを超える。オーケストレーターの承認が要る
3. **この文書の §9「配色のたたき台」の16進値が実装と食い違っている**（青・緑と書いてあるが
   実装は橙系）。修正は T-053 の範囲外
4. `.claude/rules/pergram-ui-copy.md` の「カード 14px / 面 16px」という記述が、
   この整理の後は「カード・パネル 14px / 全画面シート 16px」になる。規約の更新は範囲外

### 10. この設計で名前を付けたもの

| 語 | 意味 |
|---|---|
| **面の役割（A / B / C）** | 縦に積まれる箱の種類。A = 読む面（カード）、B = 呼びかけの面（帯）、C = 注記（面を持たない）。**3つで固定** |
| **パネルトークン**（`--panel-*`） | 面の見た目の唯一の出所。既存トークンの別名であり、新しい値ではない |
| **字送りの階層 T1–T6** | 面の中の文字の役割。見出し / ラベル・受領 / 本文・リード / 条件の注記 / 微注記 / 入力のラベル |
| **`--head-offset`** | スティッキーヘッダの高さ。アンカー移動とフォーカス移動の逃げ幅 |
| **`.request-flow__thanks`** | アンケートの段の冒頭、見出しより前に置く礼と依頼の1段落 |
| **`request.surveyThanks`** | 上の文言のキー |
