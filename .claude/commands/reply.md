---
description: X のリプライ文を作る（140字以内・宣伝ではなく有益な補足）
argument-hint: "[リプライ先の投稿やニュース]"
---

Skill ツールで `x-post` を呼び、その手順に従って返信文を作る。

- モード: reply
- 入力: $ARGUMENTS

相手への敬意を先に置き、宣伝ではなく有益な補足として書く。数字は
`npm run x:facts` の出力からのみ引く。下書きは `npm run x:lint` に通す。
