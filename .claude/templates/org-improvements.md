# 組織改善の台帳

**このファイルは、組織の仕組みそのものについての課題と、その対応を追う台帳である。**
開発する成果物の課題はここには書かない。それはタスク台帳（`docs/task-list-{project-name}.csv` と `docs/tasks/T-XXX.md`）の担当である。

---

## 書いてよいのは誰か

**担当エージェント（設計・実装・レビュー・テスト・ドキュメント）は、このファイルへ書かない。**
自分のタスクファイル `docs/tasks/T-XXX.md` の `## 組織への指摘` へ1行書く。それだけでよい。

理由は、このファイルが全員の共有物だからである。同時に走っている2体が同じファイルへ書けば、**片方の追記が黙って消える**。加えて、全タスクが触るファイルになると、並列に走らせられるタスクが無くなる。

| 誰が | 何を書くか |
| --- | --- |
| **オーケストレーター** | **唯一の書き手。** 担当エージェントから吸い上げた指摘を**1課題1レコードにまとめて起票**する。以後の状態の更新／改善エージェントの診断結果の転記／PO の回答／適用の記録／タスクの外で自分が気づいた課題の起票も行う |
| **改善エージェント** | **書かない。** 診断結果はオーケストレーターへ返す |
| **停滞検知・整合性検査のスクリプト**（`org-check.py`） | **書かない。** タスクファイルに書かれているのに、ここへ起票されていない指摘があれば警告するだけ |

**「まとめる」は判断である。** 3つの別々のタスクから同じ趣旨の指摘が出たとき、1レコードに束ねるか別々の課題として立てるかは、両者の報告を受け取るオーケストレーターにしか決められない。機械に集約させると必ず3レコードになり、優先度の基準（同じ課題が3件以上の別タスクから出ているか）が働かなくなる。

**タスクファイルの `## 組織への指摘` が原文（正典）、このファイルはまとめた作業台である。** タスク別ファイルが正典で CSV が索引、という関係と同じ形になっている。オーケストレーターが起票を怠っても原文は残り、改善エージェントが点検のたびに取りこぼしを拾う。

---

## 状態

| 状態 | 意味 | 次に動かすのは誰か |
| --- | --- | --- |
| `未処理` | 起票されたが、まだ誰も診ていない | 改善エージェント（次の組織点検で） |
| `診断済み` | 原因の推定と対応策が書かれ、優先度がついた | オーケストレーター |
| `承認待ち` | PO確認待ちキューへ載せた（重さが `中` か `重` のもの） | PO |
| `承認済み` | PO が承認した。まだ適用していない | オーケストレーター |
| `層1へ差し戻し済み` | **`適用範囲` が `配布物` のものだけ。** 配布元のリポジトリ（`agents-org`）へ渡した。**このリポジトリでは直さない** | 配布元の側。直った配布物が配られてきて、それを取り込んだら `適用済み` へ |
| `適用済み` | 層2へ反映した。効果はまだ見ていない | 改善エージェント（次の点検で照合） |
| `効果確認済み` | 期待した効果が出たと確認できた。**ここでいったん閉じる** | — |
| `撤回` | **適用した変更を戻した。** 効果が出なかったか、後から害が分かった。**戻した理由と、戻した日を必ず書く** | — |
| `却下` | 採らないと決めた。**理由を必ず書く** | — |
| `見送り` | 今は対処しないが消さない。再燃したら `未処理` へ戻す | — |

---

## 書式

**1課題1ブロック。** 見出しは `### OI-XXX: 一行でわかる課題名`。番号は起票順の通し番号で、**欠番を詰めない**。

**この番号は、適用した後も使う。** 層2の規約やスキルへ変更を反映するとき、変更した箇所の近くに `(OI-007)` と書き添える。**そうしないと、規約の側から「この一文はどの課題から入ったのか」を辿れなくなる。** 辿れないものは、効かないと分かったときにも戻せない。

### `効果確認済み` は終端ではない

**閉じた課題も、後から害が分かることがある。** 規約を1つ足したことで別のところが詰まった、という形で現れる。

そのときは**新しい課題として起票し直さない。** 元の課題の `経過` へ追記し、状態を `撤回` にする。**同じ課題が別の番号で2度立つと、「1度直したはずのことが再燃している」という最も重要な信号が見えなくなる。**

同じ課題が3回以上 `撤回` になったら、**対応策ではなく原因の推定が間違っている。** 原因からやり直す。

`未処理` の時点で埋まっているのは `状態` から `出所` までの5行だけでよい。残りは改善エージェントが診断したときに、オーケストレーターが埋める。

### 欄の一覧 — 診断が済んだら、すべて埋まっていなければならない

| 欄 | いつ埋まるか | 何を書くか |
| --- | --- | --- |
| `状態` | 起票時 | 上の表の10種のどれか |
| `起票日` / `更新日` | 起票時 | `YYYY-MM-DD` の形 |
| `種別` | 起票時 | `規約` / `道具` / `反復` / `ロール` の4つから1つ |
| `出所` | 起票時 | **どのタスクの `## 組織への指摘` から来たか。** この欄だけが起票の証拠であり、セッション開始時に走る検査（`.claude/scripts/org-check.py`）はここしか見ない。本文でタスク番号に触れても起票したことにはならない |
| `優先度` | 診断時 | `高` / `中` / `低` |
| `原因の推定` | 診断時 | なぜそれが起きていると考えるか |
| `対応策` | 診断時 | どのファイルを、どう直すか。**最小の編集で** |
| `適用範囲` | 診断時 | **`配布物` / `このリポジトリのみ`。** 次節で説明する。**空だと検査が警告する** |
| `重さ` | 診断時 | `軽` / `中` / `重` |
| `期待する効果` | 診断時 | **どの指標がどう動けば成功か。** 書けない提案は出さない |
| `悪化しうるもの` | 診断時 | この変更で悪くなる可能性があるもの。「無し」だけで済ませない |
| `経過` | 随時 | 日付つきで1行ずつ足す。**過去の行は消さない** |

### `適用範囲` — 配布元で直すか、このリポジトリで直すか

**判定の問いは1つである。**

> **技術構成の違う別のプロジェクトでも、同じことが起きるか。**

| 値 | 意味 | どこで直すか |
| --- | --- | --- |
| `配布物` | 起きる。原因が組織の設計そのものにある | **配布元のリポジトリ（`agents-org`）。このリポジトリの中では直さない** |
| `このリポジトリのみ` | 起きない。原因が、このリポジトリ固有の事情（技術構成・ゴール・チームの慣習）にある | このリポジトリ。ただし `org-` で始まるファイルは触らない（それは配布物である） |

**`配布物` と判定したものを、このリポジトリの中で直してはならない。** 直しても、次に配布物を更新した時点で上書きされて消える。**これは実際に起きた事故である**——導入先で直された配布物の不具合が、配り直しで2ファイルぶん消えた。

`配布物` と判定したら、次のように進む。

1. 重さは自動的に `重` になる（配布物の変更は全導入先へ波及するため）
2. PO確認待ちキュー（`docs/po-queue.md`）へ載せる。**このリポジトリから配布元へ渡せるのは PO だけである**
3. 状態を `層1へ差し戻し済み` にし、`経過` へ渡した日付を書く
4. 直った配布物が配られてきたら、取り込んで `適用済み` へ進める

**割れることがある。** 中身は導入先固有だが、気づく仕組みは組織の設計、という形で割れる。**そのときは1件に押し込めず、2件に分けて起票する。** 元のレコードの `経過` へ、どの番号へ分けたかを書く。

---

## 組織点検の記録

**組織点検を1回行ったら、ここへ1行足す。** 書式は次のとおり。

```
- YYYY-MM-DD 完了 N件時点 / 起票 N件 / エスカレート N件
```

`完了 N件時点` は、**その点検を行った時点で状態が `完了` になっていたタスクの数**である。
セッション開始時に走る検査（`.claude/scripts/org-check.py`）がこの数を読み、
**「前回の組織点検から N 件完了」の1行を出す。** 次の点検の時期が、
誰も覚えていなくても目に入るようにするための仕掛けである（目安は5件）。

**この行が無いと、検査は「点検の記録がまだ無い」と言い続ける。** 点検を
行ったのに書き忘れると、いつまでも初回点検を促される。

- （まだ1回も行っていない）

---

## 課題

<!-- 以下は書式の見本である。最初の起票時に消してよい

### OI-001: Python のテストの書き方が担当に割り当てられていない

- 状態: 未処理
- 起票日: 2026-09-01
- 更新日: 2026-09-01
- 種別: 道具
- 出所: T-003 / T-007 / T-011 の `## 組織への指摘`（3件）
- 優先度: 高
- 原因の推定: 導入時の初期シーケンスを走らせた時点では、このリポジトリにテストが1本も無く、テストの実行基盤が読み取れなかった
- 対応策: テスト担当の定義の「使うスキル」へ、Python のテストの手順を1行足す
- 適用範囲: このリポジトリのみ
- 重さ: 中
- 期待する効果: テスト作成の工程での滞留日数の中央値が下がる。この種別の指摘が次の点検で0件になる
- 悪化しうるもの: 割り当てを増やすと、セッション開始時に常時読み込まれる一覧が伸びる（概算で数百トークン）
- 経過:
  - 2026-09-01 起票（オーケストレーターが T-003 / T-007 / T-011 の指摘3件を1課題にまとめた）

-->
