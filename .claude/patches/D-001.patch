diff --git a/.claude/agents/org-design.md b/.claude/agents/org-design.md
index 7b29df5..ec2d6be 100644
--- a/.claude/agents/org-design.md
+++ b/.claude/agents/org-design.md
@@ -49,6 +49,22 @@ color: purple
 
 ---
 
+## 往復を減らす — 独立した確認は1往復にまとめる (D-001-5)
+
+**互いに依存しない確認は、1回の応答の中でまとめて呼ぶ。** ツール呼び出しは、1つの応答に複数書けば同時に走る。1つずつ順番に呼ぶと、そのたびに考えて生成する時間が挟まる。**1往復あたり約18秒のうち、約15秒がそれである。ツールの実行速度は主因ではない。削るのは往復の数である。**
+
+| まとめる（結果が互いに影響しない） | まとめない（前の結果で次が決まる） |
+| --- | --- |
+| 複数のファイルを読む | 検索して、見つかったファイルを読む |
+| 別々の記号・文言を検索する | 直して、テストを回す |
+| `git status` と `git log` と対象ファイルの確認 | 実行結果を見て、次に何を調べるか決める |
+
+**実測（T-072）**: 担当5体で実 API 往復 339 回。**そのうち並列で呼ばれたものは 0 だった。** 探す・読む・git 確認だけで約140往復（約42分）を使っている。**その多くは互いに独立しており、まとめられた。**
+
+⚠️ **まとめるために、読む対象を推測で増やさない。** 本当に要るものだけを、まとめて読む。無関係なファイルを10本並べて読むのは、往復は減っても読む量が増えるだけで、差し引きで損をする。
+
+---
+
 ## 入力
 
 作業指示はタスク別ファイル（`docs/tasks/T-XXX.md`）である。設計タスクでは特に以下を確認する。
diff --git a/.claude/agents/org-implementation.md b/.claude/agents/org-implementation.md
index 704dde4..edf7015 100644
--- a/.claude/agents/org-implementation.md
+++ b/.claude/agents/org-implementation.md
@@ -34,6 +34,45 @@ color: green
 
 ---
 
+## 往復を減らす — 独立した確認は1往復にまとめる (D-001-5)
+
+**互いに依存しない確認は、1回の応答の中でまとめて呼ぶ。** ツール呼び出しは、1つの応答に複数書けば同時に走る。1つずつ順番に呼ぶと、そのたびに考えて生成する時間が挟まる。**1往復あたり約18秒のうち、約15秒がそれである。ツールの実行速度は主因ではない。削るのは往復の数である。**
+
+| まとめる（結果が互いに影響しない） | まとめない（前の結果で次が決まる） |
+| --- | --- |
+| 複数のファイルを読む | 検索して、見つかったファイルを読む |
+| 別々の記号・文言を検索する | 直して、テストを回す |
+| `git status` と `git log` と対象ファイルの確認 | 実行結果を見て、次に何を調べるか決める |
+
+**実測（T-072）**: 担当5体で実 API 往復 339 回。**そのうち並列で呼ばれたものは 0 だった。** 探す・読む・git 確認だけで約140往復（約42分）を使っている。**その多くは互いに独立しており、まとめられた。**
+
+⚠️ **まとめるために、読む対象を推測で増やさない。** 本当に要るものだけを、まとめて読む。無関係なファイルを10本並べて読むのは、往復は減っても読む量が増えるだけで、差し引きで損をする。
+
+---
+
+## テストの回し方 — 開発中は関連テストだけ (D-001-2)
+
+**開発中に一式（`npm test`）を回さない。** 触っている領域のテストだけを名指しで回す。
+
+```sh
+node --test tests/waitlist.test.js   # 開発中・反復中はこれ
+npm test                             # 完了報告（判定）の直前に1回だけ
+```
+
+🔒 **一式は、完了報告を書く直前に必ず1回回す。ここは省略できない。** 関連テストだけでは、**自分の変更が無関係のテストを壊したことに気づけない。** 一式を回していない完了報告は、未検証の報告である。
+
+**一式の結果は「全部緑」ではなく「着手時点から増えていない」で判定する。** 同じ作業ブランチで並行している別タスクの未完成分で落ちることがある。増えていたら、増えたファイル名を報告に書く。
+
+### 回す範囲だけでなく、回す回数も減らす
+
+**実測（T-072）**: 実装担当ひとりでテスト実行 **27回・56往復・約17分**。内訳は**実行が 5.5分、考えて生成する時間が 11分**である。**一式が数秒で終わるこの構成では、費用の主因は実行時間ではなく回数である。**
+
+- **1行直すたびに回さない。** 意味のあるまとまりまで書いてから回す
+- 出力を読む前に次の編集を始めない。結局もう一度回すことになる
+- テスト実行と、それと独立した確認（`git status`、別ファイルの参照）は**1往復にまとめる**（上の「往復を減らす」）
+
+---
+
 ## 入力
 
 作業指示はタスク別ファイル（`docs/tasks/T-XXX.md`）である。実装タスクでは特に以下が効く。
diff --git a/.claude/agents/org-review.md b/.claude/agents/org-review.md
index 8749b6a..e4fa616 100644
--- a/.claude/agents/org-review.md
+++ b/.claude/agents/org-review.md
@@ -37,6 +37,22 @@ color: orange
 
 ---
 
+## 往復を減らす — 独立した確認は1往復にまとめる (D-001-5)
+
+**互いに依存しない確認は、1回の応答の中でまとめて呼ぶ。** ツール呼び出しは、1つの応答に複数書けば同時に走る。1つずつ順番に呼ぶと、そのたびに考えて生成する時間が挟まる。**1往復あたり約18秒のうち、約15秒がそれである。ツールの実行速度は主因ではない。削るのは往復の数である。**
+
+| まとめる（結果が互いに影響しない） | まとめない（前の結果で次が決まる） |
+| --- | --- |
+| 複数のファイルを読む | 検索して、見つかったファイルを読む |
+| 別々の記号・文言を検索する | 直して、テストを回す |
+| `git status` と `git log` と対象ファイルの確認 | 実行結果を見て、次に何を調べるか決める |
+
+**実測（T-072）**: 担当5体で実 API 往復 339 回。**そのうち並列で呼ばれたものは 0 だった。** 探す・読む・git 確認だけで約140往復（約42分）を使っている。**その多くは互いに独立しており、まとめられた。**
+
+⚠️ **まとめるために、読む対象を推測で増やさない。** 本当に要るものだけを、まとめて読む。無関係なファイルを10本並べて読むのは、往復は減っても読む量が増えるだけで、差し引きで損をする。
+
+---
+
 ## 入力
 
 作業指示はタスク別ファイル（`docs/tasks/T-XXX.md`）である。レビュータスクでは以下を確認する。
diff --git a/.claude/agents/org-test.md b/.claude/agents/org-test.md
index 29eca38..1d0c228 100644
--- a/.claude/agents/org-test.md
+++ b/.claude/agents/org-test.md
@@ -58,6 +58,45 @@ color: cyan
 
 ---
 
+## 往復を減らす — 独立した確認は1往復にまとめる (D-001-5)
+
+**互いに依存しない確認は、1回の応答の中でまとめて呼ぶ。** ツール呼び出しは、1つの応答に複数書けば同時に走る。1つずつ順番に呼ぶと、そのたびに考えて生成する時間が挟まる。**1往復あたり約18秒のうち、約15秒がそれである。ツールの実行速度は主因ではない。削るのは往復の数である。**
+
+| まとめる（結果が互いに影響しない） | まとめない（前の結果で次が決まる） |
+| --- | --- |
+| 複数のファイルを読む | 検索して、見つかったファイルを読む |
+| 別々の記号・文言を検索する | 直して、テストを回す |
+| `git status` と `git log` と対象ファイルの確認 | 実行結果を見て、次に何を調べるか決める |
+
+**実測（T-072）**: 担当5体で実 API 往復 339 回。**そのうち並列で呼ばれたものは 0 だった。** 探す・読む・git 確認だけで約140往復（約42分）を使っている。**その多くは互いに独立しており、まとめられた。**
+
+⚠️ **まとめるために、読む対象を推測で増やさない。** 本当に要るものだけを、まとめて読む。無関係なファイルを10本並べて読むのは、往復は減っても読む量が増えるだけで、差し引きで損をする。
+
+---
+
+## テストの回し方 — 反復中は対象のテストだけ (D-001-2)
+
+**受け入れテストを書いて赤→緑を確かめている間は、そのテストだけを名指しで回す。** 一式（`npm test`）は、合否の判定を出す直前に1回。**回帰の確認はそこで行う。**
+
+```sh
+node --test tests/waitlist.test.js   # 開発中・反復中はこれ
+npm test                             # 完了報告（判定）の直前に1回だけ
+```
+
+🔒 **一式は、完了報告を書く直前に必ず1回回す。ここは省略できない。** 関連テストだけでは、**自分の変更が無関係のテストを壊したことに気づけない。** 一式を回していない完了報告は、未検証の報告である。
+
+**一式の結果は「全部緑」ではなく「着手時点から増えていない」で判定する。** 同じ作業ブランチで並行している別タスクの未完成分で落ちることがある。増えていたら、増えたファイル名を報告に書く。
+
+### 回す範囲だけでなく、回す回数も減らす
+
+**実測（T-072）**: 実装担当ひとりでテスト実行 **27回・56往復・約17分**。内訳は**実行が 5.5分、考えて生成する時間が 11分**である。**一式が数秒で終わるこの構成では、費用の主因は実行時間ではなく回数である。**
+
+- **1行直すたびに回さない。** 意味のあるまとまりまで書いてから回す
+- 出力を読む前に次の編集を始めない。結局もう一度回すことになる
+- テスト実行と、それと独立した確認（`git status`、別ファイルの参照）は**1往復にまとめる**（上の「往復を減らす」）
+
+---
+
 ## 入力
 
 作業指示はタスク別ファイル（`docs/tasks/T-XXX.md`）である。
diff --git a/.claude/rules/org-task-ledger.md b/.claude/rules/org-task-ledger.md
index c8d7815..cae0b59 100644
--- a/.claude/rules/org-task-ledger.md
+++ b/.claude/rules/org-task-ledger.md
@@ -102,7 +102,7 @@ ID,作成日,更新日,タスク名,状態,優先度,担当,依存タスク,ド
 ## テスト方法       単体／結合／回帰。実環境確認の要否と実施者
 ## 停止条件         このタスク固有のもの。無ければ「共通の停止条件のみ」
 ## 参照すべき成果物 設計成果物、既存コード、過去の決定（docs/decisions-index.md で引ける）
-## 連絡してよい相手 直接やりとりしてよい相手の宛先。無ければ「無し（すべてオーケストレーター経由）」
+## 連絡してよい相手 直接やりとりしてよい相手の宛先。**既定は書く側**（2人目を起こした時点で）
 ## 申し送り         上流タスクの結果から判明した、このタスクの担当が知らないと判断を誤る事実
 ## 成果物           完了時に何ができたか（ファイル・コミット）
 ## ブロッカー       今止めているもの。PO確認待ちなら Q-XXX
@@ -116,17 +116,19 @@ ID,作成日,更新日,タスク名,状態,優先度,担当,依存タスク,ド
 
 **`## 完了条件` / `## 判断してよい範囲` / `## 変更範囲` / `## 禁止事項` の4つは、担当が付いた時点で機械的に検査される。** 未記入のまま割り当てると警告が出る。`## 証拠` は、状態が `完了` のときに検査される。**雛形の案内文（`<!-- ... -->`）が残っているだけの節は、未記入として扱う。**
 
-### `## 連絡してよい相手` は、書かなければ「無し」である
+### `## 連絡してよい相手` の既定は「書く」である (D-001-4)
 
-担当エージェントは、**誰が居るかを自分で一覧する手段を持たない。** つまり宛先を教えられなければ、直接やりとりは成立しない。この欄が空なら、その担当は**すべてオーケストレーター経由で連絡する**。それが既定である。
+担当エージェントは、**誰が居るかを自分で一覧する手段を持たない。** つまり宛先を教えられなければ、直接やりとりは成立しない。この欄が空なら、その担当は**すべてオーケストレーター経由で連絡する** —— 承認済みの経路（レビュー ⇄ 実装、テスト ⇄ 実装）があっても使えず、往復が余分に増える。
+
+**標準フローのタスクでは、2人目の担当を起こした時点で両者の欄を書く。** 「無し（すべてオーケストレーター経由）」と書いてよいのは、直接通信の経路が本当に要らないと判断したときだけである。
 
 書くときは、**相手の宛先をそのまま書く。** 役割名（`org-implementation` など）だけを書いてはならない。**同じ名前が1セッション内で2回使われた時点で、その名前宛ての送信は拒否される**（黙って別のエージェントへ届くのではなく、送信が止まる）。オーケストレーターは役割名をタスクごとに使い回すため、**役割名だけの宛先は2回目の割り当てで必ず壊れる。** 宛先の作り方は `CLAUDE.md` の「エージェント間の直接通信」にある。
 
-**この欄は機械的な検査の対象ではない。** 直接通信の経路が要らないタスクのほうが多く、必須にすると意味の無い記入を強いるためである。
+**この欄は機械的な検査の対象ではない。** 宛先が確定するのは登録時ではなく2人目を起こしたときであり、登録時点の未記入を機械が咎めても意味が無いためである。**検査に出ないぶん、書くのを忘れても誰も言わない。**
 
-### `## 申し送り` は、登録した後で埋まる唯一の指示欄である
+### `## 申し送り` は、登録した後で埋まる指示欄である
 
-他の指示欄（目的・完了条件・判断してよい範囲）は、タスクを登録した時点で書く。**申し送りだけは違う。** 上流のタスクが実際に何を作ったかは、それが終わるまで分からないためである。
+他の指示欄（目的・完了条件・判断してよい範囲）は、タスクを登録した時点で書く。**申し送りと `## 連絡してよい相手` の2つだけは違う。** 上流のタスクが実際に何を作ったかは、それが終わるまで分からないためである。
 
 書くのは**オーケストレーターだけ**。上流タスクの完了報告を受け取るのが彼だけだからである。書く中身は、例えば次のようなもの。
 
diff --git a/.claude/scripts/org-check.py b/.claude/scripts/org-check.py
index dbd88c6..fb7723b 100644
--- a/.claude/scripts/org-check.py
+++ b/.claude/scripts/org-check.py
@@ -94,6 +94,16 @@ REWORK_LIMIT = 3
 # 判定基準を持たないまま動く。人間の目視に任せず機械で捕まえる。
 REQUIRED_SECTIONS = ["完了条件", "判断してよい範囲", "変更範囲", "禁止事項"]
 
+# 担当を付ける時点で埋まっていなければならない節 (D-001-1)。上の4つと違い、
+# **完了したタスクには問わない** —— 終わったタスクの入口を今さら書いても
+# 誰も読まないため、警告にすると純粋な雑音になる。担当エージェントは会話履歴を
+# 引き継がないので、入口が無いと毎回ゼロから探すことになる。
+#
+# ⚠️ この節が「書いてある」ことは、探す時間が短いことを意味しない。実測では
+# 73件中63件に実ファイルパスまで書かれており、140往復した T-072 にも
+# 入口が4本あった。**これは下限を守る検査であって、往復数の対策ではない。**
+REQUIRED_ON_ASSIGN = ["参照すべき成果物"]
+
 # 雛形の案内文（HTMLコメント）と、埋めたつもりの空文字。どちらも「未記入」とみなす。
 HTML_COMMENT = re.compile(r"<!--.*?-->", re.S)
 # 節の区切りになる見出し。`###` 以下の小見出しは節の中身なので、区切りに含めない
@@ -677,6 +687,17 @@ def check(tasks: list[dict], days: int) -> tuple[list, list, list]:
                         f"{label}: 指示が未記入のまま{trigger} → {' / '.join(empty)}"
                         "。レビューとテストが判定基準を持たないまま動く"
                     )
+            # 担当を付ける時点で要る節。完了したタスクには問わない (D-001-1)
+            if state not in ("中止", "完了") and (assigned or state in IN_PROGRESS):
+                empty = [n for n in REQUIRED_ON_ASSIGN
+                         if is_blank(section(t["text"], n))]
+                if empty:
+                    trigger = ("担当が付いている" if assigned
+                               else f"状態が「{state}」になっている")
+                    warn.append(
+                        f"{label}: 入口が未記入のまま{trigger} → {' / '.join(empty)}"
+                        "。担当は会話履歴を引き継がないので、ゼロから探すことになる"
+                    )
             if state == "完了" and is_blank(section(t["text"], "証拠")):
                 warn.append(
                     f"{label}: 状態が「完了」なのに `## 証拠` が空。"
diff --git a/.claude/skills/org-orchestrate/SKILL.md b/.claude/skills/org-orchestrate/SKILL.md
index 9edc5a8..a928d32 100644
--- a/.claude/skills/org-orchestrate/SKILL.md
+++ b/.claude/skills/org-orchestrate/SKILL.md
@@ -80,9 +80,9 @@ CSV索引に行を追加し、`docs/tasks/T-XXX.md` を作る（形式は `.clau
 1. 目的 — このタスクがゴールに対して何を前進させるか
 2. 完了条件 — 何が満たされたら完了か。**二値で判定できる形**
 3. **判断してよい範囲** — どこまで自律的に決めてよいか
-4. 参照すべき成果物 — 先行タスクの設計書、既存コード、**そのタスクの `対象` に効く過去の決定**（下記）
+4. 参照すべき成果物 — 先行タスクの設計書、既存コード、**そのタスクの `対象` に効く過去の決定**（下記）。🔒 **空のまま割り当てない。検査が止める** (D-001-1)
 5. 期待する成果物の形
-6. **連絡してよい相手** — 直接やりとりしてよい相手の宛先。無ければ「無し（すべてオーケストレーター経由）」
+6. **連絡してよい相手** — 直接やりとりしてよい相手の宛先。**既定は書く側**。登録時点では未定でよいが、**2人目を起こす時点で必ず書く**（下記）
 
 ### 割り当てる前に、その領域の過去の決定を引く
 
@@ -96,16 +96,31 @@ python3 .claude/scripts/org-decisions.py    # まず索引を作り直してか
 
 **`状態` の欄が `置き換え済み → T-021` になっている決定は渡さない。** 既に失効している。置き換え先のほうを渡す。
 
+**入口は「パス — そこで何を見るか」の形で書く** (D-001-1)。`src/` 以下の関連コード、では入口にならない。`src/lib/waitlist_fields.js — 自由記述の長さ上限。上限の出所はここだけ`、と書く。
+
+⚠️ **これを書けば探す往復が減る、とは思わないこと。** 実測では、この形の入口が4本揃っていた T-072 でも探す・読むに約140往復かかっている。**入口は下限であって、往復数の対策ではない**（往復数に効くのは対策3と対策5）。ここを速度の対策だと思うと、書いたのに減らないという理由で欄そのものが軽視される。
+
 ### 指示は自己完結でなければならない
 
 各エージェントは**新規の隔離コンテキスト**で始まり、これまでの会話履歴を一切引き継がない。「さきほど決めた方針で」「前回と同様に」は通じない。必要な文脈は、指示の中か、参照先として明示的に含める。
 
 これを外すと、**曖昧な指示を受けたエージェントはエラーを上げずに誤った前提のまま作業を完了させる**。失敗が沈黙するため、手戻りは下流で初めて発覚する。
 
-### 「連絡してよい相手」を書かないと、直接通信は成立しない
+### 「連絡してよい相手」は、既定で書く (D-001-4)
 
 担当エージェントは**誰が居るかを自分で一覧する手段を持たない。** 宛先を教えなければ、承認済みの経路（レビュー → 実装など）があっても送れず、往復はすべてあなたを経由する形に落ちる。**トークンを余分に使うのはこの落ち方である。**
 
+**2人目の担当を起こした時点で、両者のタスクファイルの `## 連絡してよい相手` を書く。** 標準フローでは次の2組が必ず発生する。
+
+| 経路 | いつ書くか | 上限 |
+| --- | --- | --- |
+| レビュー ⇄ 実装 | レビューを起こすとき。実装側にはレビューの宛先を、レビュー側には実装の宛先を | 2往復 |
+| テスト ⇄ 実装 | テストを起こすとき（TDD適用タスクでは実装より先に居る） | 2往復 |
+
+**「無し（すべてオーケストレーター経由）」と書いてよいのは、経路が本当に要らないと判断したときだけである。** 未記入のまま起こさない。**この欄は検査に出ない**（宛先が確定するのは登録時ではないため）。**忘れても誰も言わないので、起こす操作とセットにする。**
+
+⚠️ **差し戻しで担当を起こし直したときも更新する**（下の「差し戻すとき」）。古い宛先が残ると、送信が拒否されるか、居ない相手を待つ。
+
 宛先は**あなたが起動時に付ける名前**である。`Agent` ツールの `name` に、役割名とタスク番号を組み合わせて渡す（`org-implementation-T-007`）。**役割名だけを付けてはならない** —— 同じ名前を1セッション内で2回使うと、その名前宛ての送信は拒否される。あなたは同じ役割のエージェントをタスクごとに起こし直すため、この衝突は必ず起きる。詳しくは `CLAUDE.md` の「エージェント間の直接通信」。
 
 ### 「判断してよい範囲」を毎回書く理由
@@ -114,7 +129,7 @@ python3 .claude/scripts/org-decisions.py    # まず索引を作り直してか
 
 ### 委譲する
 
-Agent ツールで担当エージェントを起動する。**`name` にはタスク番号を含む一意の名前を渡す**（`org-implementation-T-007`）。これが他のエージェントから見た宛先になる。**依存関係が無く、かつ触るファイルが重ならないタスクは同時に走らせる。** 1エージェントが同時に持つのは1タスクまで。
+Agent ツールで担当エージェントを起動する。**`name` にはタスク番号を含む一意の名前を渡す**（`org-implementation-T-007`）。これが他のエージェントから見た宛先であり、**差し戻すときに呼び戻す宛先でもある**（下の「差し戻すとき」）。**依存関係が無く、かつ触るファイルが重ならないタスクは同時に走らせる。** 1エージェントが同時に持つのは1タスクまで。
 
 **作業ブランチを用意するのはあなたである。** 既定ブランチ（`main` 等）の上で担当エージェントを走らせない。まとまり（機能・マイルストーン）ごとに `org/<まとまりの名前>` を切り、その上で走らせる。**ブランチを切り替えてよいのもあなただけ**——他のエージェントが作業している最中に切り替えると、その足元が変わる。
 
@@ -184,7 +199,25 @@ python3 .claude/scripts/org-tokens.py --task T-007
 
 ### 差し戻すとき
 
-タスクの `手戻り回数` を**加算する**。数えるのは: レビューの修正要求、テストの不具合レポート、あなたによる担当の戻し。数えないのは: 実装が自分で気づいて直したもの、Minor 指摘への対応。
+**同じエージェントを呼び戻す。枝番を付けて起こし直さない。** (D-001-3)
+
+差し戻しは `SendMessage` で、**そのタスクで既に使っている宛先へそのまま送る**（`org-implementation-T-007`）。**相手が動いている必要はない**——終了したエージェントは、メッセージが届いた時点で再開される（`CLAUDE.md` の「エージェント間の直接通信」）。
+
+**`Agent` で `org-implementation-T-007-2` のような枝番を起こさない。** 新しいエージェントは会話履歴を持たないため、そのタスクで既に読んだファイル・既に下した判断・既に試して駄目だった手を、**ゼロから積み直す。** 差し戻し1回が、1タスクぶんの立ち上げ費用になる。
+
+**実測（T-072）**: 担当の起動が9回。うちテストの2回目は単独で **21.8M トークン / 133往復**で、その相当部分が「前任が何をしたか」の再取得だった。**呼び戻していれば起動は6〜7回で済んでいる。**
+
+| 起こし直してよい場合 | なぜ |
+| --- | --- |
+| 前任のコンテキストが尽きた（応答が返らない、明らかに文脈を失っている） | 呼び戻しても積み直しになる |
+| 差し戻し先の役が変わる（実装 → 設計 など） | そもそも別の役である |
+| セッションをまたいだ | 前のセッションのエージェントは居ない。宛先が存在しない |
+
+**起こし直すときは、タスクの `## 申し送り` へ「前任が何をどこまでやったか」を書く。** 書かなければ、新任は必ず積み直す。**枝番を付けた宛先は、それ自体が「前任が居る」ことを他の担当から隠す**——レビューが実装へ送る宛先が古いままになる。起こし直したら `## 連絡してよい相手` も更新する。
+
+⚠️ **役割名だけの宛先を使い回して起こし直すと、送信そのものが拒否される。** 誤配より安全な倒れ方だが、気づかなければ往復が成立しない。宛先は起動時にタスク番号を含めて一意にしておく。
+
+**`手戻り回数` を加算する。** 数えるのは: レビューの修正要求、テストの不具合レポート、あなたによる担当の戻し。数えないのは: 実装が自分で気づいて直したもの、Minor 指摘への対応。
 
 ---
 
diff --git a/.claude/templates/T-XXX.md b/.claude/templates/T-XXX.md
index e74c0a6..0859faa 100644
--- a/.claude/templates/T-XXX.md
+++ b/.claude/templates/T-XXX.md
@@ -59,20 +59,50 @@
 <!-- このタスク固有の停止条件があれば、上を書き換える -->
 
 ## 参照すべき成果物
-<!-- 設計成果物、既存コード、過去の決定（docs/decisions-index.md で引ける）。
-     実在するパスだけを書く -->
+<!-- 🔒 担当を付ける前に埋める。空のまま割り当てると検査が止める (D-001-1)。
+
+     設計成果物、既存コード、過去の決定（docs/decisions-index.md で引ける）。
+     実在するパスだけを書く。担当は会話履歴を引き継がないので、ここに
+     書かれていないものは、その担当にとって存在しない。
+
+     1本ずつ「パス — そこで何を見るか」の形で書く。
+
+       悪い: src/ 以下の関連コード
+       良い: src/lib/waitlist_fields.js — 自由記述の長さ上限。上限の出所はここだけ
+       良い: docs/tasks/T-072.md の決定ログ — 匿名の行を引き取る理由
+
+     ⚠️ 入口を書けば探す往復が短くなる、という欄ではない。実測では、
+     この形の入口が4本揃っていたタスクでも140往復している。これは
+     下限を守るための欄である -->
 
 ## 連絡してよい相手
-無し（すべてオーケストレーター経由）
-<!-- 直接やりとりしてよい相手が居るなら、その宛先をそのまま書く。
-     宛先は起動時に付けた名前であり、タスクごとに一意でなければならない
-     （例: org-implementation-T-007）。役割名だけを書くと、同じ名前が
-     使い回された時点で送信が拒否される -->
+未定（2人目の担当を起こす時点でオーケストレーターが書く）
+<!-- 既定は「書く」である (D-001-4)。標準フローのタスクでは、少なくとも
+     次の2組が発生する。
+
+       レビュー ⇄ 実装 … 修正要求と修正結果（2往復まで）
+       テスト   ⇄ 実装 … 不具合レポートと修正（2往復まで）
+
+     ここが空だと、この往復は全部オーケストレーターを経由する形に落ちる。
+     担当エージェントは誰が居るかを一覧する手段を持たないため、
+     書かれていない宛先は、その担当にとって存在しない。
+
+     「無し（すべてオーケストレーター経由）」と書いてよいのは、直接通信の
+     経路が本当に要らないと判断したときだけ。空欄のまま放置しない。
+
+     宛先は起動時に付けた名前をそのまま書く。タスクごとに一意でなければ
+     ならない（例: org-implementation-T-007）。役割名だけを書くと、同じ
+     名前が使い回された時点で送信が拒否される。
+
+     書く時点は登録時ではなく、2人目を起こしたときである。1人目しか
+     居ない時点では、相手が存在しない。差し戻しで担当を起こし直したら、
+     ここも更新する -->
 
 ## 申し送り
 無し
 <!-- 上流タスクの完了報告から、この担当が知らないと判断を誤る事実を、
-     オーケストレーターが転記する。登録した後で埋まる唯一の指示欄 -->
+     オーケストレーターが転記する。「連絡してよい相手」と並んで、
+     登録した後で埋まる指示欄である -->
 
 ## 成果物
 <!-- 完了時に何ができたか。ファイルパスとコミット -->
