{
  "_comment": "requirements.md §4.2 — カテゴリ差はこの JSON で吸収する。カテゴリ固有テーブルを作らない。新カテゴリの追加はここに1ブロック足すだけで済む状態を保つこと。facets は product_attributes のキーをグループにまとめたもので、UI 側はこの配列を回して描画するだけにする（コンポーネントに成分名の分岐を書かない）。",

  "protein": {
    "primaryMetric": "cost_per_nutrient_unit",
    "secondaryMetrics": ["content_ratio", "cost_per_serving"],
    "displayUnit": "g",
    "targetIntake": { "default": 60, "min": 10, "max": 200, "step": 5 },
    "explainerKey": "products.explainer.protein",
    "facets": [
      {
        "id": "product_type",
        "style": "check",
        "keys": ["whey_wpi", "whey_wpc", "soy", "casein", "pea", "rice"]
      },
      {
        "id": "quality",
        "style": "chip",
        "keys": [
          "organic_cert",
          "gmp_cert",
          "third_party_cert",
          "no_artificial_sweetener",
          "grass_fed",
          "vegan"
        ]
      }
    ],
    "unitCostRange": { "min": 3, "max": 12, "step": 0.5 },
    "priceRange": { "min": 0, "max": 20000, "step": 500 }
  },
  "creatine": {
    "primaryMetric": "cost_per_nutrient_unit",
    "secondaryMetrics": ["content_ratio", "cost_per_serving"],
    "displayUnit": "g",
    "targetIntake": { "default": 5, "min": 1, "max": 25, "step": 1 },
    "explainerKey": null,
    "facets": [
      {
        "id": "quality",
        "style": "chip",
        "keys": ["gmp_cert", "third_party_cert", "no_artificial_sweetener", "vegan"]
      }
    ],
    "unitCostRange": { "min": 5, "max": 60, "step": 1 },
    "priceRange": { "min": 0, "max": 12000, "step": 500 }
  },
  "zinc": {
    "primaryMetric": "cost_per_nutrient_unit",
    "secondaryMetrics": ["ul_percentage", "cost_per_serving"],
    "displayUnit": "mg",
    "targetIntake": { "default": 10, "min": 1, "max": 40, "step": 1 },
    "explainerKey": null,
    "facets": [
      {
        "id": "quality",
        "style": "chip",
        "keys": ["gmp_cert", "third_party_cert", "vegan"]
      }
    ],
    "unitCostRange": { "min": 0.5, "max": 10, "step": 0.5 },
    "priceRange": { "min": 0, "max": 8000, "step": 250 }
  }
}
